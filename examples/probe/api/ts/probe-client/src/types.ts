// Code generated by nightseam. DO NOT EDIT.
import { createValidator, withDeclaration, DuplexError, type AnyFamily, type FamilyBinding, type TypeBinding, type Slots, type TypeExpression, type WireFamily } from "@nightseam/runtime";
export type { AnyFamily, FamilyBinding, TypeBinding, Slots, TypeExpression };
import type { ValueAdapter, ValueContext } from "@nightseam/runtime";
import { LiveOwner } from "@nightseam/live";
import type { WireModelContext } from "@nightseam/runtime";
/** One message of the nightseam.duplex/1 profile: the members the peer acts on, and nothing else. */
export interface Envelope {
  /** The profile's version, 1. */
  "version": number;
  /** request, response, event or cancel. */
  "kind": string;
  /** What correlates a response or a cancel with its request. */
  "id"?: string;
  /** The method a request names. */
  "method"?: string;
  /** A request's parameters. */
  "params"?: unknown;
  /** A response's result. */
  "result"?: unknown;
  /** A response's error. */
  "error"?: unknown;
  /** The event an event frame names. */
  "event"?: string;
  /** An event's data. */
  "data"?: unknown;
  /** The W3C Trace Context of the frame. */
  "traceparent"?: string;
  /** The vendor state of that trace. */
  "tracestate"?: string;
  /** What a request or an event carries about the call, delivered to the handler beside the payload. */
  "meta"?: Record<string, string>;
}
/** A reference to a channel on the connection that carries the message holding it. */
export interface Handle {
  /** The channel's id on that connection. */
  "channel": number;
}
/** Told about a payload the server saw. The client implements it and hands it over; the server calls it back across the seam. */
/** A value of it is one implementation, called across the seam; each is its own binding, with its own lifetime. */
export type Notice = (request: Payload, options?: { signal?: AbortSignal; owner?: LiveOwner }) => Promise<void>;
/** What a probe carries: some text, and how often it has been seen. */
export interface Payload {
  /** The text of the payload. */
  "text": string;
  /** How often the payload has been seen. */
  "count": number;
}
/** Asks the server to stop sending notices. Releasing the reference to it is not this, and cancelling the call that returned it is neither. */
/** A value of it is one implementation, called across the seam; each is its own binding, with its own lifetime. */
export type Stop = (options?: { signal?: AbortSignal; owner?: LiveOwner }) => Promise<void>;
/** What watch answers: a record of callables the client calls back, held apart from the call that returned it. */
export interface Subscription {
  /** Ends the subscription. */
  "stop": Stop;
}
/** A callable supplied as an argument, beside ordinary data. */
export interface Watch {
  /** What to call this subscription; data, and carried as data. */
  "label": string;
  /** The interface the server calls back. */
  "watcher": Watcher;
}
/** An interface, which is a record of callable members; each member is its own binding, with its own lifetime. */
export interface Watcher {
  /** Called once for every payload the server sees while the subscription lasts. */
  "notice": Notice;
}
/** The family: its name and the wire types a slot of it draws on. */
export interface Family { readonly name: "probe"; Envelope: Envelope; Handle: Handle; Notice: Notice; Payload: Payload; Stop: Stop; Subscription: Subscription; Watch: Watch; Watcher: Watcher }
/** The declaration a reference to Notice carries. It is nominal: a reference is usable exactly where this callable is expected. */
export const contractNotice = "probe/Notice";
/** Makes a binding of a local Notice and answers the reference that names it. */
export function exportNotice(owner: LiveOwner, value: Notice): unknown {
  return owner.exportValue((owner) => {
    const parent = owner;
    const reference = owner.export(contractNotice, wireDigest, async (request, options) => {
      const owner = parent.child();
      const context = { ...options, owner };
      validateWire("Payload", request);
      const argument = request as Payload;
      await value(argument, context);
      return undefined;
    });
    return reference.toJSON();
  });
}
/** A Notice that calls the binding a reference names. */
export function importNotice(owner: LiveOwner, raw: unknown): Notice {
  const invoke = owner.import(owner.scope.decode(raw), contractNotice, wireDigest);
  const scope = owner.scope;
  return async (request: Payload, options?: { signal?: AbortSignal; owner?: LiveOwner }) => {
    const owner = options?.owner?.scope === scope ? options.owner : scope.owner();
    await invoke((() => { const converted = request; validateWire("Payload", converted); return converted; })(), options);
    return;
  };
}
/** The declaration a reference to Stop carries. It is nominal: a reference is usable exactly where this callable is expected. */
export const contractStop = "probe/Stop";
/** Makes a binding of a local Stop and answers the reference that names it. */
export function exportStop(owner: LiveOwner, value: Stop): unknown {
  return owner.exportValue((owner) => {
    const parent = owner;
    const reference = owner.export(contractStop, wireDigest, async (request, options) => {
      const owner = parent.child();
      const context = { ...options, owner };
      await value(context);
      return undefined;
    });
    return reference.toJSON();
  });
}
/** A Stop that calls the binding a reference names. */
export function importStop(owner: LiveOwner, raw: unknown): Stop {
  const invoke = owner.import(owner.scope.decode(raw), contractStop, wireDigest);
  const scope = owner.scope;
  return async (options?: { signal?: AbortSignal; owner?: LiveOwner }) => {
    const owner = options?.owner?.scope === scope ? options.owner : scope.owner();
    await invoke(undefined, options);
    return;
  };
}
/** Writes Subscription as it travels: each callable in it becomes a binding of the owner, and the reference that names it takes its place. */
export function exportSubscription(owner: LiveOwner, value: Subscription): unknown {
  return owner.exportValue((owner) => {
    const out: Record<string, unknown> = {};
    out["stop"] = exportStop(owner, (value["stop"]) as Stop);
    return out;
  });
}
/** Reads Subscription as it arrived: each reference in it becomes a typed proxy of the binding it names, so a handler is given native values. */
export function importSubscription(owner: LiveOwner, raw: unknown): Subscription {
  return owner.importValue((owner) => {
    const wire = raw as Record<string, unknown>;
    const out: Record<string, unknown> = {};
    out["stop"] = importStop(owner, wire["stop"]);
    return out as unknown as Subscription;
  });
}
/** Writes Watch as it travels: each callable in it becomes a binding of the owner, and the reference that names it takes its place. */
export function exportWatch(owner: LiveOwner, value: Watch): unknown {
  return owner.exportValue((owner) => {
    const out: Record<string, unknown> = {};
    out["label"] = value["label"];
    out["watcher"] = exportWatcher(owner, (value["watcher"]) as Watcher);
    return out;
  });
}
/** Reads Watch as it arrived: each reference in it becomes a typed proxy of the binding it names, so a handler is given native values. */
export function importWatch(owner: LiveOwner, raw: unknown): Watch {
  return owner.importValue((owner) => {
    const wire = raw as Record<string, unknown>;
    const out: Record<string, unknown> = {};
    out["label"] = wire["label"];
    out["watcher"] = importWatcher(owner, wire["watcher"]);
    return out as unknown as Watch;
  });
}
/** Writes Watcher as it travels: each callable in it becomes a binding of the owner, and the reference that names it takes its place. */
export function exportWatcher(owner: LiveOwner, value: Watcher): unknown {
  return owner.exportValue((owner) => {
    const out: Record<string, unknown> = {};
    out["notice"] = exportNotice(owner, (value["notice"]) as Notice);
    return out;
  });
}
/** Reads Watcher as it arrived: each reference in it becomes a typed proxy of the binding it names, so a handler is given native values. */
export function importWatcher(owner: LiveOwner, raw: unknown): Watcher {
  return owner.importValue((owner) => {
    const wire = raw as Record<string, unknown>;
    const out: Record<string, unknown> = {};
    out["notice"] = importNotice(owner, wire["notice"]);
    return out as unknown as Watcher;
  });
}
export function adapterNotice(): ValueAdapter<Notice> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Notice", validate: validateWire, slots };
  const needsContext = true;
  return { binding, needsContext,
    export(context: unknown, value: Notice): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      const convert = (owner: LiveOwner): unknown => {
        const converted = exportNotice(owner!, value);
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return context.exportValue(convert);
    },
    import(context: unknown, raw: unknown): Notice {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: LiveOwner): Notice => {
        const converted = importNotice(owner!, raw);
        return converted;
      };
      return context.importValue(convert);
    },
  };
}
export function adapterPayload(): ValueAdapter<Payload> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Payload", validate: validateWire, slots };
  const needsContext = false;
  return { binding, needsContext,
    export(context: unknown, value: Payload): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = value;
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Payload {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Payload => {
        const converted = raw as Payload;
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterStop(): ValueAdapter<Stop> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Stop", validate: validateWire, slots };
  const needsContext = true;
  return { binding, needsContext,
    export(context: unknown, value: Stop): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      const convert = (owner: LiveOwner): unknown => {
        const converted = exportStop(owner!, value);
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return context.exportValue(convert);
    },
    import(context: unknown, raw: unknown): Stop {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: LiveOwner): Stop => {
        const converted = importStop(owner!, raw);
        return converted;
      };
      return context.importValue(convert);
    },
  };
}
export function adapterSubscription(): ValueAdapter<Subscription> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Subscription", validate: validateWire, slots };
  const needsContext = true;
  return { binding, needsContext,
    export(context: unknown, value: Subscription): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      const convert = (owner: LiveOwner): unknown => {
        const converted = exportSubscription(owner!, value);
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return context.exportValue(convert);
    },
    import(context: unknown, raw: unknown): Subscription {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: LiveOwner): Subscription => {
        const converted = importSubscription(owner!, raw);
        return converted;
      };
      return context.importValue(convert);
    },
  };
}
export function adapterWatch(): ValueAdapter<Watch> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Watch", validate: validateWire, slots };
  const needsContext = true;
  return { binding, needsContext,
    export(context: unknown, value: Watch): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      const convert = (owner: LiveOwner): unknown => {
        const converted = exportWatch(owner!, value);
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return context.exportValue(convert);
    },
    import(context: unknown, raw: unknown): Watch {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: LiveOwner): Watch => {
        const converted = importWatch(owner!, raw);
        return converted;
      };
      return context.importValue(convert);
    },
  };
}
export function adapterWatcher(): ValueAdapter<Watcher> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Watcher", validate: validateWire, slots };
  const needsContext = true;
  return { binding, needsContext,
    export(context: unknown, value: Watcher): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      const convert = (owner: LiveOwner): unknown => {
        const converted = exportWatcher(owner!, value);
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return context.exportValue(convert);
    },
    import(context: unknown, raw: unknown): Watcher {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: LiveOwner): Watcher => {
        const converted = importWatcher(owner!, raw);
        return converted;
      };
      return context.importValue(convert);
    },
  };
}
export interface ServerMethods {
  echo(params: Payload, context?: WireModelContext): Payload | Promise<Payload>;
  watch(params: Watch, context?: WireModelContext & { valueContext: unknown }): Subscription | Promise<Subscription>;
}
export interface ServerEvents {
}
export interface Server { methods: ServerMethods; events: ServerEvents; }
export interface ClientMethods {
  reverse(params: Payload, context?: WireModelContext): Payload | Promise<Payload>;
}
export interface ClientEvents {
  changed(data: Payload, context?: WireModelContext): void | Promise<void>;
}
export interface Client { methods: ClientMethods; events: ClientEvents; }
export type ServerModel = (remote: Client) => Server;
export type ClientModel = (remote: Server) => Client;

const contractTypes = {"types":{"Envelope":{"kind":"record","fields":[{"name":"version","type":"integer","required":true},{"name":"kind","type":"string","required":true},{"name":"id","type":"string","required":false},{"name":"method","type":"string","required":false},{"name":"params","type":"json","required":false},{"name":"result","type":"json","required":false},{"name":"error","type":"json","required":false},{"name":"event","type":"string","required":false},{"name":"data","type":"json","required":false},{"name":"traceparent","type":"string","required":false},{"name":"tracestate","type":"string","required":false},{"name":"meta","type":{"map":"string"},"required":false}]},"Handle":{"kind":"record","fields":[{"name":"channel","type":"integer","required":true}]},"Notice":{"kind":"callable","contract":"probe/Notice","request":"Payload"},"Payload":{"kind":"record","fields":[{"name":"text","type":"string","required":true},{"name":"count","type":"integer","required":true}]},"Stop":{"kind":"callable","contract":"probe/Stop"},"Subscription":{"kind":"record","fields":[{"name":"stop","type":"Stop","required":true}]},"Watch":{"kind":"record","fields":[{"name":"label","type":"string","required":true},{"name":"watcher","type":"Watcher","required":true}]},"Watcher":{"kind":"record","fields":[{"name":"notice","type":"Notice","required":true}]}}} as unknown as WireFamily;
/** Canonical language-neutral declaration bytes, the input to wireDigest. */
export const wireDeclaration = "{\"definitions\":{\"probe\":{\"client\":{\"ref\":\"probe/$client\"},\"errors\":[\"denied\"],\"kind\":\"family\",\"parameters\":[],\"server\":{\"ref\":\"probe/$server\"},\"types\":{\"Notice\":{\"ref\":\"probe/Notice\"},\"Payload\":{\"ref\":\"probe/Payload\"},\"Stop\":{\"ref\":\"probe/Stop\"},\"Subscription\":{\"ref\":\"probe/Subscription\"},\"Watch\":{\"ref\":\"probe/Watch\"},\"Watcher\":{\"ref\":\"probe/Watcher\"}}},\"probe/$client\":{\"direction\":\"client\",\"events\":{},\"extends\":[],\"kind\":\"side\",\"methods\":{\"reverse\":{\"errors\":[],\"request\":{\"ref\":\"probe/Payload\"},\"result\":{\"ref\":\"probe/Payload\"}}},\"parameters\":[]},\"probe/$server\":{\"direction\":\"server\",\"events\":{\"changed\":{\"ref\":\"probe/Payload\"}},\"extends\":[],\"kind\":\"side\",\"methods\":{\"echo\":{\"errors\":[],\"request\":{\"ref\":\"probe/Payload\"},\"result\":{\"ref\":\"probe/Payload\"}},\"watch\":{\"errors\":[\"denied\"],\"request\":{\"ref\":\"probe/Watch\"},\"result\":{\"ref\":\"probe/Subscription\"}}},\"parameters\":[]},\"probe/Notice\":{\"errors\":[],\"kind\":\"callable\",\"parameters\":[],\"request\":{\"ref\":\"probe/Payload\"},\"result\":{\"empty\":true}},\"probe/Payload\":{\"fields\":[{\"name\":\"text\",\"nullable\":false,\"required\":true,\"type\":{\"primitive\":\"string\"},\"unique\":false},{\"name\":\"count\",\"nullable\":false,\"required\":true,\"type\":{\"primitive\":\"integer\"},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]},\"probe/Stop\":{\"errors\":[],\"kind\":\"callable\",\"parameters\":[],\"request\":{\"empty\":true},\"result\":{\"empty\":true}},\"probe/Subscription\":{\"fields\":[{\"name\":\"stop\",\"nullable\":false,\"required\":true,\"type\":{\"ref\":\"probe/Stop\"},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]},\"probe/Watch\":{\"fields\":[{\"name\":\"label\",\"nullable\":false,\"required\":true,\"type\":{\"primitive\":\"string\"},\"unique\":false},{\"name\":\"watcher\",\"nullable\":false,\"required\":true,\"type\":{\"ref\":\"probe/Watcher\"},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]},\"probe/Watcher\":{\"fields\":[{\"name\":\"notice\",\"nullable\":false,\"required\":true,\"type\":{\"ref\":\"probe/Notice\"},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]}},\"root\":{\"ref\":\"probe\"},\"version\":1}";
/** SHA-256 of the canonical full declaration's exact UTF-8 bytes. */
export const wireDigest = "9c7d14529719d97fbe4bb64aeb8b4e50dd6a8b2acb81c82ae34212d34a3c7cac";
/** Runtime validation applies equally to calls, replies, reverse calls and events; what fills a slot of a parameter is validated by the binding of the family that fills it. */
export const validateWire = withDeclaration(createValidator(contractTypes, wireDigest, {  }), wireDeclaration);
/** This family bound: its name and its validator, to fill a family slot in another family's client. */
export const family = { name: "probe", validate: validateWire } as const;
