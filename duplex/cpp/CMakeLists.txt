add_library(nightseam_duplex src/pipe.cpp src/ws.cpp)
add_library(Nightseam::duplex ALIAS nightseam_duplex)
target_include_directories(nightseam_duplex PUBLIC include)
target_link_libraries(nightseam_duplex PUBLIC Threads::Threads)
target_link_libraries(nightseam_duplex PRIVATE Boost::asio Boost::beast)
target_compile_features(nightseam_duplex PUBLIC cxx_std_20)
if(WIN32)
  target_compile_definitions(nightseam_duplex PRIVATE _WIN32_WINNT=0x0A00)
  target_link_libraries(nightseam_duplex PRIVATE ws2_32 mswsock)
endif()

if(BUILD_TESTING)
  add_executable(nightseam_duplex_tests tests/seam.cpp)
  target_link_libraries(nightseam_duplex_tests PRIVATE Nightseam::duplex)
  add_test(NAME cpp.duplex COMMAND nightseam_duplex_tests)
  set_tests_properties(cpp.duplex PROPERTIES TIMEOUT 20)
  add_executable(nightseam_websocket_tests tests/websocket.cpp)
  target_link_libraries(nightseam_websocket_tests PRIVATE Nightseam::duplex)
  add_test(NAME cpp.websocket COMMAND nightseam_websocket_tests)
  set_tests_properties(cpp.websocket PROPERTIES TIMEOUT 30)
endif()
