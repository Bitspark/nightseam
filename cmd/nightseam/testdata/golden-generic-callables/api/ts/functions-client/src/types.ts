// Code generated by nightseam. DO NOT EDIT.
import { createValidator, withDeclaration, jsonAdapter, familyTypeAdapter, callableIdentity, DuplexError, type DeclarationIdentity, type AnyFamily, type FamilyBinding, type TypeBinding, type Slots, type TypeExpression, type WireFamily } from "@nightseam/runtime";
export type { AnyFamily, FamilyBinding, TypeBinding, Slots, TypeExpression };
import type { ValueAdapter, ValueContext } from "@nightseam/runtime";
import { LiveOwner } from "@nightseam/live";
import type { WireModelContext } from "@nightseam/runtime";
export interface Bundle<T = unknown> {
  "borrowed": T;
  "fresh": Array<T>;
}
/** One message of the nightseam.duplex/1 profile: the members the peer acts on, and nothing else. */
export interface Envelope {
  /** The profile's version, 1. */
  "version": number;
  /** request, response, event or cancel. */
  "kind": string;
  /** What correlates a response or a cancel with its request. */
  "id"?: string;
  /** The method a request names. */
  "method"?: string;
  /** A request's parameters. */
  "params"?: unknown;
  /** A response's result. */
  "result"?: unknown;
  /** A response's error. */
  "error"?: unknown;
  /** The event an event frame names. */
  "event"?: string;
  /** An event's data. */
  "data"?: unknown;
  /** The W3C Trace Context of the frame. */
  "traceparent"?: string;
  /** The vendor state of that trace. */
  "tracestate"?: string;
  /** What a request or an event carries about the call, delivered to the handler beside the payload. */
  "meta"?: Record<string, string>;
}
/** A value of it is one implementation, called across the seam; each is its own binding, with its own lifetime. */
export type Function<A = unknown, B = unknown> = (request: A, options?: { signal?: AbortSignal; owner?: LiveOwner }) => Promise<B>;
/** A reference to a channel on the connection that carries the message holding it. */
export interface Handle {
  /** The channel's id on that connection. */
  "channel": number;
}
export type IntFunction = Function<number, number>;
/** A value of it is one implementation, called across the seam; each is its own binding, with its own lifetime. */
export type Other<A = unknown, B = unknown> = (request: A, options?: { signal?: AbortSignal; owner?: LiveOwner }) => Promise<B>;
/** The family: its name and the wire types a slot of it draws on. */
export interface Family { readonly name: "functions"; Envelope: Envelope; Handle: Handle; IntFunction: IntFunction }
/** Conversion only: use the value adapter or validate the entire wire value inside the same operation batch. */
/** Writes Bundle using the supplied conversion for each type argument. */
export function exportBundleUnchecked<T = unknown>(value: Bundle<T>, convert_T_: (value: T) => unknown): unknown {
  const out: Record<string, unknown> = {};
  out["borrowed"] = convert_T_((value["borrowed"]) as T);
  out["fresh"] = (value["fresh"] as unknown[]).map((item) => convert_T_((item) as T));
  return out;
}
/** Reads Bundle using the supplied conversion for each type argument. */
/** Conversion only: validation belongs to the value adapter or its caller. */
export function importBundleUnchecked<T = unknown>(raw: unknown, convert_T_: (value: unknown) => T): Bundle<T> {
  const wire = raw as Record<string, unknown>;
  const out: Record<string, unknown> = {};
  out["borrowed"] = convert_T_(wire["borrowed"]);
  out["fresh"] = (wire["fresh"] as unknown[]).map((item) => convert_T_(item));
  return out as unknown as Bundle<T>;
}
/** Canonical identity of this closed nominal callable application. */
export function contractFunction<A = unknown, B = unknown>(slot_A: ValueAdapter<A>, slot_B: ValueAdapter<B>): DeclarationIdentity {
  if (typeof slot_A.export !== 'function' || typeof slot_A.import !== 'function') throw new Error('missing complete value interpretation for A');
  if (typeof slot_B.export !== 'function' || typeof slot_B.import !== 'function') throw new Error('missing complete value interpretation for B');
  const slots: Slots = { "A": slot_A.binding, "B": slot_B.binding };
  return callableIdentity({ type: "Function", validate: validateWire, slots });
}
/** Exports an implementation with complete recipes retained for its later invocations. */
export function exportFunction<A = unknown, B = unknown>(owner: LiveOwner, value: Function<A, B>, slot_A: ValueAdapter<A>, slot_B: ValueAdapter<B>): unknown {
  if (typeof slot_A.export !== 'function' || typeof slot_A.import !== 'function') throw new Error('missing complete value interpretation for A');
  if (typeof slot_B.export !== 'function' || typeof slot_B.import !== 'function') throw new Error('missing complete value interpretation for B');
  const slots: Slots = { "A": slot_A.binding, "B": slot_B.binding };
  const identity = callableIdentity({ type: "Function", validate: validateWire, slots });
  if (typeof value !== 'function') throw new TypeError('callable implementation must be a function');
  return owner.exportValue((owner) => {
    const parent = owner;
    const reference = owner.export(identity.path, identity.digest ?? '', async (request, options) => {
      const owner = parent.child();
      const context = Object.assign(Object.create(options ?? null), { owner });
      validateWire("A", request, '$', slots);
      const argument = (slot_A.needsContext ? owner!.importValue((owner) => slot_A.import(owner, request)) : slot_A.import(owner, request));
      const result = await value(argument, context);
      return (slot_B.needsContext ? owner!.exportValue((owner) => { const converted = slot_B.export(owner, (result) as B); validateWire("B", converted, '$', slots); return converted; }) : (() => { const converted = slot_B.export(owner, (result) as B); validateWire("B", converted, '$', slots); return converted; })());
    });
    return reference.toJSON();
  });
}
/** Imports a callable whose conversions use the owner of each invocation. */
export function importFunction<A = unknown, B = unknown>(owner: LiveOwner, raw: unknown, slot_A: ValueAdapter<A>, slot_B: ValueAdapter<B>): Function<A, B> {
  if (typeof slot_A.export !== 'function' || typeof slot_A.import !== 'function') throw new Error('missing complete value interpretation for A');
  if (typeof slot_B.export !== 'function' || typeof slot_B.import !== 'function') throw new Error('missing complete value interpretation for B');
  const slots: Slots = { "A": slot_A.binding, "B": slot_B.binding };
  const identity = callableIdentity({ type: "Function", validate: validateWire, slots });
  const invoke = owner.import(owner.scope.decode(raw), identity.path, identity.digest ?? '');
  const scope = owner.scope;
  return async (request: A, options?: { signal?: AbortSignal; owner?: LiveOwner }) => {
    const owner = options?.owner?.scope === scope ? options.owner : scope.owner();
    const result = await (slot_A.needsContext ? owner!.publishValue(owner => (slot_A.needsContext ? owner!.exportValue((owner) => { const converted = slot_A.export(owner, (request) as A); validateWire("A", converted, '$', slots); return converted; }) : (() => { const converted = slot_A.export(owner, (request) as A); validateWire("A", converted, '$', slots); return converted; })()), sent => invoke(sent, options)) : invoke((slot_A.needsContext ? owner!.exportValue((owner) => { const converted = slot_A.export(owner, (request) as A); validateWire("A", converted, '$', slots); return converted; }) : (() => { const converted = slot_A.export(owner, (request) as A); validateWire("A", converted, '$', slots); return converted; })()), options));
    validateWire("B", result, '$', slots);
    return (slot_B.needsContext ? owner!.importValue((owner) => slot_B.import(owner, result)) : slot_B.import(owner, result));
  };
}
/** Canonical identity of this closed nominal callable application. */
export function contractIntFunction(): DeclarationIdentity {
  const slots: Slots = {  };
  return callableIdentity({ type: "IntFunction", validate: validateWire, slots });
}
/** Exports an implementation with complete recipes retained for its later invocations. */
export function exportIntFunction(owner: LiveOwner, value: IntFunction): unknown {
  const slots: Slots = {  };
  const identity = callableIdentity({ type: "IntFunction", validate: validateWire, slots });
  if (typeof value !== 'function') throw new TypeError('callable implementation must be a function');
  return owner.exportValue((owner) => {
    const parent = owner;
    const reference = owner.export(identity.path, identity.digest ?? '', async (request, options) => {
      const owner = parent.child();
      const context = Object.assign(Object.create(options ?? null), { owner });
      validateWire("integer", request, '$', slots);
      const argument = request as number;
      const result = await value(argument, context);
      return (() => { const converted = result; validateWire("integer", converted, '$', slots); return converted; })();
    });
    return reference.toJSON();
  });
}
/** Imports a callable whose conversions use the owner of each invocation. */
export function importIntFunction(owner: LiveOwner, raw: unknown): IntFunction {
  const slots: Slots = {  };
  const identity = callableIdentity({ type: "IntFunction", validate: validateWire, slots });
  const invoke = owner.import(owner.scope.decode(raw), identity.path, identity.digest ?? '');
  const scope = owner.scope;
  return async (request: number, options?: { signal?: AbortSignal; owner?: LiveOwner }) => {
    const owner = options?.owner?.scope === scope ? options.owner : scope.owner();
    const result = await invoke((() => { const converted = request; validateWire("integer", converted, '$', slots); return converted; })(), options);
    validateWire("integer", result, '$', slots);
    return result as number;
  };
}
/** Canonical identity of this closed nominal callable application. */
export function contractOther<A = unknown, B = unknown>(slot_A: ValueAdapter<A>, slot_B: ValueAdapter<B>): DeclarationIdentity {
  if (typeof slot_A.export !== 'function' || typeof slot_A.import !== 'function') throw new Error('missing complete value interpretation for A');
  if (typeof slot_B.export !== 'function' || typeof slot_B.import !== 'function') throw new Error('missing complete value interpretation for B');
  const slots: Slots = { "A": slot_A.binding, "B": slot_B.binding };
  return callableIdentity({ type: "Other", validate: validateWire, slots });
}
/** Exports an implementation with complete recipes retained for its later invocations. */
export function exportOther<A = unknown, B = unknown>(owner: LiveOwner, value: Other<A, B>, slot_A: ValueAdapter<A>, slot_B: ValueAdapter<B>): unknown {
  if (typeof slot_A.export !== 'function' || typeof slot_A.import !== 'function') throw new Error('missing complete value interpretation for A');
  if (typeof slot_B.export !== 'function' || typeof slot_B.import !== 'function') throw new Error('missing complete value interpretation for B');
  const slots: Slots = { "A": slot_A.binding, "B": slot_B.binding };
  const identity = callableIdentity({ type: "Other", validate: validateWire, slots });
  if (typeof value !== 'function') throw new TypeError('callable implementation must be a function');
  return owner.exportValue((owner) => {
    const parent = owner;
    const reference = owner.export(identity.path, identity.digest ?? '', async (request, options) => {
      const owner = parent.child();
      const context = Object.assign(Object.create(options ?? null), { owner });
      validateWire("A", request, '$', slots);
      const argument = (slot_A.needsContext ? owner!.importValue((owner) => slot_A.import(owner, request)) : slot_A.import(owner, request));
      const result = await value(argument, context);
      return (slot_B.needsContext ? owner!.exportValue((owner) => { const converted = slot_B.export(owner, (result) as B); validateWire("B", converted, '$', slots); return converted; }) : (() => { const converted = slot_B.export(owner, (result) as B); validateWire("B", converted, '$', slots); return converted; })());
    });
    return reference.toJSON();
  });
}
/** Imports a callable whose conversions use the owner of each invocation. */
export function importOther<A = unknown, B = unknown>(owner: LiveOwner, raw: unknown, slot_A: ValueAdapter<A>, slot_B: ValueAdapter<B>): Other<A, B> {
  if (typeof slot_A.export !== 'function' || typeof slot_A.import !== 'function') throw new Error('missing complete value interpretation for A');
  if (typeof slot_B.export !== 'function' || typeof slot_B.import !== 'function') throw new Error('missing complete value interpretation for B');
  const slots: Slots = { "A": slot_A.binding, "B": slot_B.binding };
  const identity = callableIdentity({ type: "Other", validate: validateWire, slots });
  const invoke = owner.import(owner.scope.decode(raw), identity.path, identity.digest ?? '');
  const scope = owner.scope;
  return async (request: A, options?: { signal?: AbortSignal; owner?: LiveOwner }) => {
    const owner = options?.owner?.scope === scope ? options.owner : scope.owner();
    const result = await (slot_A.needsContext ? owner!.publishValue(owner => (slot_A.needsContext ? owner!.exportValue((owner) => { const converted = slot_A.export(owner, (request) as A); validateWire("A", converted, '$', slots); return converted; }) : (() => { const converted = slot_A.export(owner, (request) as A); validateWire("A", converted, '$', slots); return converted; })()), sent => invoke(sent, options)) : invoke((slot_A.needsContext ? owner!.exportValue((owner) => { const converted = slot_A.export(owner, (request) as A); validateWire("A", converted, '$', slots); return converted; }) : (() => { const converted = slot_A.export(owner, (request) as A); validateWire("A", converted, '$', slots); return converted; })()), options));
    validateWire("B", result, '$', slots);
    return (slot_B.needsContext ? owner!.importValue((owner) => slot_B.import(owner, result)) : slot_B.import(owner, result));
  };
}
export function adapterBundle<T = unknown>(slot_T: ValueAdapter<T>): ValueAdapter<Bundle<T>> {
  if (typeof slot_T.export !== 'function' || typeof slot_T.import !== 'function') throw new Error('missing complete value interpretation for T');
  const slots: Slots = { "T": slot_T.binding };
  const binding: TypeBinding = { type: "Bundle", validate: validateWire, slots };
  const needsContext = slot_T.needsContext;
  return { binding, needsContext,
    export(context: unknown, value: Bundle<T>): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = exportBundleUnchecked<T>(value, (value: T) => slot_T.export(owner, value));
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Bundle<T> {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Bundle<T> => {
        const converted = importBundleUnchecked<T>(raw, (value: unknown) => slot_T.import(owner, value));
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterFunction<A = unknown, B = unknown>(slot_A: ValueAdapter<A>, slot_B: ValueAdapter<B>): ValueAdapter<Function<A, B>> {
  if (typeof slot_A.export !== 'function' || typeof slot_A.import !== 'function') throw new Error('missing complete value interpretation for A');
  if (typeof slot_B.export !== 'function' || typeof slot_B.import !== 'function') throw new Error('missing complete value interpretation for B');
  const slots: Slots = { "A": slot_A.binding, "B": slot_B.binding };
  const binding: TypeBinding = { type: "Function", validate: validateWire, slots };
  const needsContext = true || slot_A.needsContext || slot_B.needsContext;
  return { binding, needsContext,
    export(context: unknown, value: Function<A, B>): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      const convert = (owner: LiveOwner): unknown => {
        const converted = exportFunction<A, B>(owner!, value, slot_A, slot_B);
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return context.exportValue(convert);
    },
    import(context: unknown, raw: unknown): Function<A, B> {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: LiveOwner): Function<A, B> => {
        const converted = importFunction<A, B>(owner!, raw, slot_A, slot_B);
        return converted;
      };
      return context.importValue(convert);
    },
  };
}
export function adapterIntFunction(): ValueAdapter<IntFunction> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "IntFunction", validate: validateWire, slots };
  const needsContext = true;
  return { binding, needsContext,
    export(context: unknown, value: IntFunction): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      const convert = (owner: LiveOwner): unknown => {
        const converted = exportIntFunction(owner!, value);
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return context.exportValue(convert);
    },
    import(context: unknown, raw: unknown): IntFunction {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: LiveOwner): IntFunction => {
        const converted = importIntFunction(owner!, raw);
        return converted;
      };
      return context.importValue(convert);
    },
  };
}
export function adapterOther<A = unknown, B = unknown>(slot_A: ValueAdapter<A>, slot_B: ValueAdapter<B>): ValueAdapter<Other<A, B>> {
  if (typeof slot_A.export !== 'function' || typeof slot_A.import !== 'function') throw new Error('missing complete value interpretation for A');
  if (typeof slot_B.export !== 'function' || typeof slot_B.import !== 'function') throw new Error('missing complete value interpretation for B');
  const slots: Slots = { "A": slot_A.binding, "B": slot_B.binding };
  const binding: TypeBinding = { type: "Other", validate: validateWire, slots };
  const needsContext = true || slot_A.needsContext || slot_B.needsContext;
  return { binding, needsContext,
    export(context: unknown, value: Other<A, B>): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      const convert = (owner: LiveOwner): unknown => {
        const converted = exportOther<A, B>(owner!, value, slot_A, slot_B);
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return context.exportValue(convert);
    },
    import(context: unknown, raw: unknown): Other<A, B> {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: LiveOwner): Other<A, B> => {
        const converted = importOther<A, B>(owner!, raw, slot_A, slot_B);
        return converted;
      };
      return context.importValue(convert);
    },
  };
}
export interface ServerMethods {
}
export interface ServerEvents {
}
export interface Server { methods: ServerMethods; events: ServerEvents; }
export interface ClientMethods {
}
export interface ClientEvents {
}
export interface Client { methods: ClientMethods; events: ClientEvents; }
export type ServerModel = (remote: Client) => Server;
export type ClientModel = (remote: Server) => Client;

const contractTypes = {"types":{"Bundle":{"kind":"record","fields":[{"name":"borrowed","type":"T","required":true},{"name":"fresh","type":{"array":"T"},"required":true}],"parameters":[{"name":"T"}]},"Envelope":{"kind":"record","fields":[{"name":"version","type":"integer","required":true},{"name":"kind","type":"string","required":true},{"name":"id","type":"string","required":false},{"name":"method","type":"string","required":false},{"name":"params","type":"json","required":false},{"name":"result","type":"json","required":false},{"name":"error","type":"json","required":false},{"name":"event","type":"string","required":false},{"name":"data","type":"json","required":false},{"name":"traceparent","type":"string","required":false},{"name":"tracestate","type":"string","required":false},{"name":"meta","type":{"map":"string"},"required":false}]},"Function":{"kind":"callable","parameters":[{"name":"A"},{"name":"B"}],"contract":"functions/Function","request":"A","result":"B"},"Handle":{"kind":"record","fields":[{"name":"channel","type":"integer","required":true}]},"IntFunction":{"kind":"alias","type":{"apply":"Function","with":{"A":"integer","B":"integer"}}},"Other":{"kind":"callable","parameters":[{"name":"A"},{"name":"B"}],"contract":"functions/Other","request":"A","result":"B"}}} as unknown as WireFamily;
/** Canonical language-neutral declaration bytes, the input to wireDigest. */
export const wireDeclaration = "{\"definitions\":{\"functions\":{\"client\":{\"ref\":\"functions/$client\"},\"errors\":[],\"kind\":\"family\",\"parameters\":[],\"server\":{\"ref\":\"functions/$server\"},\"types\":{\"Bundle\":{\"ref\":\"functions/Bundle\"},\"Function\":{\"ref\":\"functions/Function\"},\"IntFunction\":{\"apply\":\"functions/Function\",\"arguments\":[{\"primitive\":\"integer\"},{\"primitive\":\"integer\"}]},\"Other\":{\"ref\":\"functions/Other\"}}},\"functions/$client\":{\"direction\":\"client\",\"events\":{},\"extends\":[],\"kind\":\"side\",\"methods\":{},\"parameters\":[]},\"functions/$server\":{\"direction\":\"server\",\"events\":{},\"extends\":[],\"kind\":\"side\",\"methods\":{},\"parameters\":[]},\"functions/Bundle\":{\"fields\":[{\"name\":\"borrowed\",\"nullable\":false,\"required\":true,\"type\":{\"parameter\":\"functions/Bundle/T\"},\"unique\":false},{\"name\":\"fresh\",\"nullable\":false,\"required\":true,\"type\":{\"array\":{\"parameter\":\"functions/Bundle/T\"}},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[{\"name\":\"T\",\"of\":\"\"}]},\"functions/Function\":{\"errors\":[],\"kind\":\"callable\",\"parameters\":[{\"name\":\"A\",\"of\":\"\"},{\"name\":\"B\",\"of\":\"\"}],\"request\":{\"parameter\":\"functions/Function/A\"},\"result\":{\"parameter\":\"functions/Function/B\"}},\"functions/Other\":{\"errors\":[],\"kind\":\"callable\",\"parameters\":[{\"name\":\"A\",\"of\":\"\"},{\"name\":\"B\",\"of\":\"\"}],\"request\":{\"parameter\":\"functions/Other/A\"},\"result\":{\"parameter\":\"functions/Other/B\"}}},\"root\":{\"ref\":\"functions\"},\"version\":1}";
/** SHA-256 of the canonical full declaration's exact UTF-8 bytes. */
export const wireDigest = "59099fcd895f74759f264a4776087b78b35125c5baa45484d17e0f4fe1aba298";
/** Runtime validation applies equally to calls, replies, reverse calls and events; what fills a slot of a parameter is validated by the binding of the family that fills it. */
export const validateWire = withDeclaration(createValidator(contractTypes, wireDigest, {  }), wireDeclaration);
/** This family's declaration and reusable interpretations, with no operation context or owner. */
export const family = { name: "functions", validate: validateWire, types: { "Envelope": jsonAdapter<Envelope>({ type: "Envelope", validate: validateWire }), "Handle": jsonAdapter<Handle>({ type: "Handle", validate: validateWire }), "IntFunction": adapterIntFunction() } } as const;
