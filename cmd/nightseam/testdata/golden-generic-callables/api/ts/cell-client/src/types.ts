// Code generated by nightseam. DO NOT EDIT.
import { createValidator, withDeclaration, jsonAdapter, familyTypeAdapter, callableIdentity, DuplexError, type DeclarationIdentity, type AnyFamily, type FamilyBinding, type TypeBinding, type Slots, type TypeExpression, type WireFamily } from "@nightseam/runtime";
export type { AnyFamily, FamilyBinding, TypeBinding, Slots, TypeExpression };
import type { ValueAdapter, ValueContext } from "@nightseam/runtime";
import type { WireModelContext } from "@nightseam/runtime";
/** One message of the nightseam.duplex/1 profile: the members the peer acts on, and nothing else. */
export interface Envelope {
  /** The profile's version, 1. */
  "version": number;
  /** request, response, event or cancel. */
  "kind": string;
  /** What correlates a response or a cancel with its request. */
  "id"?: string;
  /** The method a request names. */
  "method"?: string;
  /** A request's parameters. */
  "params"?: unknown;
  /** A response's result. */
  "result"?: unknown;
  /** A response's error. */
  "error"?: unknown;
  /** The event an event frame names. */
  "event"?: string;
  /** An event's data. */
  "data"?: unknown;
  /** The W3C Trace Context of the frame. */
  "traceparent"?: string;
  /** The vendor state of that trace. */
  "tracestate"?: string;
  /** What a request or an event carries about the call, delivered to the handler beside the payload. */
  "meta"?: Record<string, string>;
}
/** A reference to a channel on the connection that carries the message holding it. */
export interface Handle {
  /** The channel's id on that connection. */
  "channel": number;
}
export interface Put<T = unknown> {
  "value": T;
}
/** The family: its name and the wire types a slot of it draws on. */
export interface Family { readonly name: "cell"; Envelope: Envelope; Handle: Handle }
/** Conversion only: use the value adapter or validate the entire wire value inside the same operation batch. */
/** Writes Put using the supplied conversion for each type argument. */
export function exportPutUnchecked<T = unknown>(value: Put<T>, convert_T_: (value: T) => unknown): unknown {
  const out: Record<string, unknown> = {};
  out["value"] = convert_T_((value["value"]) as T);
  return out;
}
/** Reads Put using the supplied conversion for each type argument. */
/** Conversion only: validation belongs to the value adapter or its caller. */
export function importPutUnchecked<T = unknown>(raw: unknown, convert_T_: (value: unknown) => T): Put<T> {
  const wire = raw as Record<string, unknown>;
  const out: Record<string, unknown> = {};
  out["value"] = convert_T_(wire["value"]);
  return out as unknown as Put<T>;
}
export function adapterPut<T = unknown>(slot_T: ValueAdapter<T>): ValueAdapter<Put<T>> {
  if (typeof slot_T.export !== 'function' || typeof slot_T.import !== 'function') throw new Error('missing complete value interpretation for T');
  const slots: Slots = { "T": slot_T.binding };
  const binding: TypeBinding = { type: "Put", validate: validateWire, slots };
  const needsContext = slot_T.needsContext;
  return { binding, needsContext,
    export(context: unknown, value: Put<T>): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = exportPutUnchecked<T>(value, (value: T) => slot_T.export(owner, value));
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Put<T> {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Put<T> => {
        const converted = importPutUnchecked<T>(raw, (value: unknown) => slot_T.import(owner, value));
        return converted;
      };
      return convert(context);
    },
  };
}
export interface ServerMethods<T = unknown> {
  get(params: Record<string, never>, context?: ValueContext<T, WireModelContext>): T | Promise<T>;
  replace(params: Put<T>, context?: ValueContext<Put<T> | number, WireModelContext>): number | Promise<number>;
}
export interface ServerEvents<T = unknown> {
}
export interface Server<T = unknown> { methods: ServerMethods<T>; events: ServerEvents<T>; }
export interface ClientMethods<T = unknown> {
}
export interface ClientEvents<T = unknown> {
}
export interface Client<T = unknown> { methods: ClientMethods<T>; events: ClientEvents<T>; }
export type ServerModel<T = unknown> = (remote: Client<T>) => Server<T>;
export type ClientModel<T = unknown> = (remote: Server<T>) => Client<T>;

const contractTypes = {"types":{"Envelope":{"kind":"record","fields":[{"name":"version","type":"integer","required":true},{"name":"kind","type":"string","required":true},{"name":"id","type":"string","required":false},{"name":"method","type":"string","required":false},{"name":"params","type":"json","required":false},{"name":"result","type":"json","required":false},{"name":"error","type":"json","required":false},{"name":"event","type":"string","required":false},{"name":"data","type":"json","required":false},{"name":"traceparent","type":"string","required":false},{"name":"tracestate","type":"string","required":false},{"name":"meta","type":{"map":"string"},"required":false}]},"Handle":{"kind":"record","fields":[{"name":"channel","type":"integer","required":true}]},"Put":{"kind":"record","fields":[{"name":"value","type":"T","required":true}]}},"parameters":[{"name":"T"}]} as unknown as WireFamily;
/** Canonical language-neutral declaration bytes, the input to wireDigest. */
export const wireDeclaration = "{\"definitions\":{\"cell\":{\"client\":{\"ref\":\"cell/$client\"},\"errors\":[],\"kind\":\"family\",\"parameters\":[{\"name\":\"T\",\"of\":\"\"}],\"server\":{\"ref\":\"cell/$server\"},\"types\":{\"Put\":{\"ref\":\"cell/Put\"}}},\"cell/$client\":{\"direction\":\"client\",\"events\":{},\"extends\":[],\"kind\":\"side\",\"methods\":{},\"parameters\":[{\"name\":\"T\",\"of\":\"\"}]},\"cell/$server\":{\"direction\":\"server\",\"events\":{},\"extends\":[],\"kind\":\"side\",\"methods\":{\"get\":{\"errors\":[],\"request\":{\"empty\":true},\"result\":{\"parameter\":\"cell/T\"}},\"replace\":{\"errors\":[],\"request\":{\"ref\":\"cell/Put\"},\"result\":{\"primitive\":\"integer\"}}},\"parameters\":[{\"name\":\"T\",\"of\":\"\"}]},\"cell/Put\":{\"captures\":[{\"name\":\"T\",\"of\":\"\"}],\"fields\":[{\"name\":\"value\",\"nullable\":false,\"required\":true,\"type\":{\"parameter\":\"cell/T\"},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]}},\"root\":{\"ref\":\"cell\"},\"version\":1}";
/** SHA-256 of the canonical full declaration's exact UTF-8 bytes. */
export const wireDigest = "e8a1d3e8b39b271c101343b450a9538b3b1c914ed48d9cd5fa79fb18b13a9b4a";
/** Runtime validation applies equally to calls, replies, reverse calls and events; what fills a slot of a parameter is validated by the binding of the family that fills it. */
export const validateWire = withDeclaration(createValidator(contractTypes, wireDigest, {  }), wireDeclaration);
/** This family's declaration and reusable interpretations, with no operation context or owner. */
export const family = { name: "cell", validate: validateWire, types: { "Envelope": jsonAdapter<Envelope>({ type: "Envelope", validate: validateWire }), "Handle": jsonAdapter<Handle>({ type: "Handle", validate: validateWire }) } } as const;
