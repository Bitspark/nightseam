// Code generated by nightseam. DO NOT EDIT.
package holderbinding

import (
	context "context"
	json "encoding/json"
	protocol "example.test/generated/api/go/holder-protocol"
	fmt "fmt"
	duplex "github.com/Bitspark/nightseam/duplex/go"
	runtime "github.com/Bitspark/nightseam/runtime/go"
	http "net/http"
)

// Remote provides typed calls back to the connected client.
type Remote[SEnvelope, SPayload any] struct{ Peer *runtime.Peer }
type Handler[SEnvelope, SPayload any] interface {
	Hold(ctx context.Context, remote *Remote[SEnvelope, SPayload], params protocol.Held[SEnvelope, SPayload]) (SPayload, error)
}

// install registers the family's methods on the options a peer is made with.
func install[SEnvelope, SPayload any](handler Handler[SEnvelope, SPayload], options *runtime.Options) error {
	if handler == nil {
		return fmt.Errorf("handler is required")
	}
	handlers := map[string]runtime.Handler{}
	for name, existing := range options.Handlers {
		handlers[name] = existing
	}
	if _, exists := handlers["hold"]; exists {
		return fmt.Errorf("duplicate handler %s", "hold")
	}
	handlers["hold"] = func(ctx context.Context, peer *runtime.Peer, raw json.RawMessage) (any, error) {
		var params protocol.Held[SEnvelope, SPayload]
		if err := protocol.ValidateExpressionRaw(protocol.TypeExpression("\"Held\""), raw); err != nil {
			return nil, &runtime.PublicError{Code: "invalid_params", Message: err.Error()}
		}
		if err := json.Unmarshal(raw, &params); err != nil {
			return nil, &runtime.PublicError{Code: "invalid_params", Message: err.Error()}
		}
		result, err := handler.Hold(ctx, &Remote[SEnvelope, SPayload]{Peer: peer}, params)
		if err != nil {
			return nil, err
		}
		if err = protocol.ValidateValue(protocol.TypeExpression("\"S.Payload\""), result); err != nil {
			return nil, err
		}
		return result, nil
	}
	options.Handlers = handlers
	return nil
}

// NewHandler serves the family at a WebSocket endpoint; it requires explicit authentication and origin policy through options.
func NewHandler[SEnvelope runtime.Of[STag], SPayload runtime.Of[STag], STag any](handler Handler[SEnvelope, SPayload], options runtime.ServerOptions) (http.Handler, error) {
	if err := install[SEnvelope, SPayload](handler, &options.Options); err != nil {
		return nil, err
	}
	return runtime.NewHandler(options)
}

// Serve serves the family over a connection of the seam — a tunnel channel, a pipe, an accepted socket — as the server side of it; the peer is the caller's to close.
func Serve[SEnvelope runtime.Of[STag], SPayload runtime.Of[STag], STag any](ctx context.Context, conn duplex.Conn, options runtime.Options, handler Handler[SEnvelope, SPayload]) (*runtime.Peer, error) {
	if err := install[SEnvelope, SPayload](handler, &options); err != nil {
		return nil, err
	}
	return runtime.NewPeer(ctx, conn, runtime.ServerRole, options)
}
