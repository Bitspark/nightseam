// Code generated by nightseam. DO NOT EDIT.
import { createValidator, withDeclaration, jsonAdapter, familyTypeAdapter, callableIdentity, DuplexError, type DeclarationIdentity, type AnyFamily, type FamilyBinding, type TypeBinding, type Slots, type TypeExpression, type WireFamily } from "@nightseam/runtime";
export type { AnyFamily, FamilyBinding, TypeBinding, Slots, TypeExpression };
import type * as worker from "@example/worker-client";
import { validateWire as validate_worker } from "@example/worker-client";
import * as live_worker from "@example/worker-client";
import type { ValueAdapter, ValueContext } from "@nightseam/runtime";
import { LiveOwner } from "@nightseam/live";
import type { WireModelContext } from "@nightseam/runtime";
/** One message of the nightseam.duplex/1 profile: the members the peer acts on, and nothing else. */
export interface Envelope {
  /** The profile's version, 1. */
  "version": number;
  /** request, response, event or cancel. */
  "kind": string;
  /** What correlates a response or a cancel with its request. */
  "id"?: string;
  /** The method a request names. */
  "method"?: string;
  /** A request's parameters. */
  "params"?: unknown;
  /** A response's result. */
  "result"?: unknown;
  /** A response's error. */
  "error"?: unknown;
  /** The event an event frame names. */
  "event"?: string;
  /** An event's data. */
  "data"?: unknown;
  /** The W3C Trace Context of the frame. */
  "traceparent"?: string;
  /** The vendor state of that trace. */
  "tracestate"?: string;
  /** What a request or an event carries about the call, delivered to the handler beside the payload. */
  "meta"?: Record<string, string>;
}
/** A reference to a channel on the connection that carries the message holding it. */
export interface Handle {
  /** The channel's id on that connection. */
  "channel": number;
}
export interface RelieveRequest {
  "shift": Shift;
  "sink": worker.ProgressSink;
}
/** Self-contained data, declared where data belongs. */
export interface Shift {
  "name": string;
}
/** A live value drawn from an imported family: the callable is worker's declaration, and the reference carries worker's contract wherever it travels. */
export interface Watch {
  "shift": Shift;
  "sink": worker.ProgressSink;
  "spares"?: Array<worker.ProgressSink>;
}
/** The family: its name and the wire types a slot of it draws on. */
export interface Family { readonly name: "supervisor"; Envelope: Envelope; Handle: Handle; RelieveRequest: RelieveRequest; Shift: Shift; Watch: Watch }
/** Conversion only: use the value adapter or validate the entire wire value inside the same operation batch. */
/** Writes RelieveRequest as it travels: each callable in it becomes a binding of the owner, and the reference that names it takes its place. */
export function exportRelieveRequestUnchecked(owner: LiveOwner, value: RelieveRequest): unknown {
  return owner.exportValue((owner) => {
    const out: Record<string, unknown> = {};
    out["shift"] = value["shift"];
    out["sink"] = live_worker.exportProgressSinkUnchecked(owner, (value["sink"]) as worker.ProgressSink);
    return out;
  });
}
/** Reads RelieveRequest as it arrived: each reference in it becomes a typed proxy of the binding it names, so a handler is given native values. */
/** Conversion only: validation belongs to the value adapter or its caller. */
export function importRelieveRequestUnchecked(owner: LiveOwner, raw: unknown): RelieveRequest {
  return owner.importValue((owner) => {
    const wire = raw as Record<string, unknown>;
    const out: Record<string, unknown> = {};
    out["shift"] = wire["shift"];
    out["sink"] = live_worker.importProgressSinkUnchecked(owner, wire["sink"]);
    return out as unknown as RelieveRequest;
  });
}
/** Conversion only: use the value adapter or validate the entire wire value inside the same operation batch. */
/** Writes Watch as it travels: each callable in it becomes a binding of the owner, and the reference that names it takes its place. */
export function exportWatchUnchecked(owner: LiveOwner, value: Watch): unknown {
  return owner.exportValue((owner) => {
    const out: Record<string, unknown> = {};
    out["shift"] = value["shift"];
    out["sink"] = live_worker.exportProgressSinkUnchecked(owner, (value["sink"]) as worker.ProgressSink);
    if (value["spares"] !== undefined) out["spares"] = (value["spares"] as unknown[]).map((item) => live_worker.exportProgressSinkUnchecked(owner, (item) as worker.ProgressSink));
    return out;
  });
}
/** Reads Watch as it arrived: each reference in it becomes a typed proxy of the binding it names, so a handler is given native values. */
/** Conversion only: validation belongs to the value adapter or its caller. */
export function importWatchUnchecked(owner: LiveOwner, raw: unknown): Watch {
  return owner.importValue((owner) => {
    const wire = raw as Record<string, unknown>;
    const out: Record<string, unknown> = {};
    out["shift"] = wire["shift"];
    out["sink"] = live_worker.importProgressSinkUnchecked(owner, wire["sink"]);
    if (wire["spares"] !== undefined) out["spares"] = (wire["spares"] as unknown[]).map((item) => live_worker.importProgressSinkUnchecked(owner, item));
    return out as unknown as Watch;
  });
}
export function adapterRelieveRequest(): ValueAdapter<RelieveRequest> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "RelieveRequest", validate: validateWire, slots };
  const needsContext = true;
  return { binding, needsContext,
    export(context: unknown, value: RelieveRequest): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      const convert = (owner: LiveOwner): unknown => {
        const converted = exportRelieveRequestUnchecked(owner!, value);
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return context.exportValue(convert);
    },
    import(context: unknown, raw: unknown): RelieveRequest {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: LiveOwner): RelieveRequest => {
        const converted = importRelieveRequestUnchecked(owner!, raw);
        return converted;
      };
      return context.importValue(convert);
    },
  };
}
export function adapterShift(): ValueAdapter<Shift> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Shift", validate: validateWire, slots };
  const needsContext = false;
  return { binding, needsContext,
    export(context: unknown, value: Shift): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = value;
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Shift {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Shift => {
        const converted = raw as Shift;
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterWatch(): ValueAdapter<Watch> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Watch", validate: validateWire, slots };
  const needsContext = true;
  return { binding, needsContext,
    export(context: unknown, value: Watch): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      const convert = (owner: LiveOwner): unknown => {
        const converted = exportWatchUnchecked(owner!, value);
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return context.exportValue(convert);
    },
    import(context: unknown, raw: unknown): Watch {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: LiveOwner): Watch => {
        const converted = importWatchUnchecked(owner!, raw);
        return converted;
      };
      return context.importValue(convert);
    },
  };
}
export interface ServerMethods {
  shift(params: Shift, context?: WireModelContext): string | Promise<string>;
  relieve(params: RelieveRequest, context?: WireModelContext & { valueContext: unknown }): Shift | Promise<Shift>;
  watch(params: Watch, context?: WireModelContext & { valueContext: unknown }): worker.Job | Promise<worker.Job>;
}
export interface ServerEvents {
}
export interface Server { methods: ServerMethods; events: ServerEvents; }
export interface ClientMethods {
}
export interface ClientEvents {
}
export interface Client { methods: ClientMethods; events: ClientEvents; }
export type ServerModel = (remote: Client) => Server;
export type ClientModel = (remote: Server) => Client;

const contractTypes = {"types":{"Envelope":{"kind":"record","fields":[{"name":"version","type":"integer","required":true},{"name":"kind","type":"string","required":true},{"name":"id","type":"string","required":false},{"name":"method","type":"string","required":false},{"name":"params","type":"json","required":false},{"name":"result","type":"json","required":false},{"name":"error","type":"json","required":false},{"name":"event","type":"string","required":false},{"name":"data","type":"json","required":false},{"name":"traceparent","type":"string","required":false},{"name":"tracestate","type":"string","required":false},{"name":"meta","type":{"map":"string"},"required":false}]},"Handle":{"kind":"record","fields":[{"name":"channel","type":"integer","required":true}]},"Shift":{"kind":"record","fields":[{"name":"name","type":"string","required":true}]},"Watch":{"kind":"record","fields":[{"name":"shift","type":"Shift","required":true},{"name":"sink","type":"worker.ProgressSink","required":true},{"name":"spares","type":{"array":"worker.ProgressSink"},"required":false}]}}} as unknown as WireFamily;
/** Canonical language-neutral declaration bytes, the input to wireDigest. */
export const wireDeclaration = "{\"definitions\":{\"supervisor\":{\"client\":{\"ref\":\"supervisor/$client\"},\"errors\":[\"refused\"],\"kind\":\"family\",\"parameters\":[],\"server\":{\"ref\":\"supervisor/$server\"},\"types\":{\"Shift\":{\"ref\":\"supervisor/Shift\"},\"Watch\":{\"ref\":\"supervisor/Watch\"}}},\"supervisor/$client\":{\"direction\":\"client\",\"events\":{},\"extends\":[],\"kind\":\"side\",\"methods\":{},\"parameters\":[]},\"supervisor/$server\":{\"direction\":\"server\",\"events\":{},\"extends\":[],\"kind\":\"side\",\"methods\":{\"relieve\":{\"errors\":[],\"request\":{\"fields\":[{\"name\":\"shift\",\"nullable\":false,\"required\":true,\"type\":{\"ref\":\"supervisor/Shift\"},\"unique\":false},{\"name\":\"sink\",\"nullable\":false,\"required\":true,\"type\":{\"ref\":\"worker/ProgressSink\"},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]},\"result\":{\"ref\":\"supervisor/Shift\"}},\"shift\":{\"errors\":[],\"request\":{\"ref\":\"supervisor/Shift\"},\"result\":{\"primitive\":\"string\"}},\"watch\":{\"errors\":[\"refused\"],\"request\":{\"ref\":\"supervisor/Watch\"},\"result\":{\"ref\":\"worker/Job\"}}},\"parameters\":[]},\"supervisor/Shift\":{\"fields\":[{\"name\":\"name\",\"nullable\":false,\"required\":true,\"type\":{\"primitive\":\"string\"},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]},\"supervisor/Watch\":{\"fields\":[{\"name\":\"shift\",\"nullable\":false,\"required\":true,\"type\":{\"ref\":\"supervisor/Shift\"},\"unique\":false},{\"name\":\"sink\",\"nullable\":false,\"required\":true,\"type\":{\"ref\":\"worker/ProgressSink\"},\"unique\":false},{\"name\":\"spares\",\"nullable\":false,\"required\":false,\"type\":{\"array\":{\"ref\":\"worker/ProgressSink\"}},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]},\"worker/Cancel\":{\"errors\":[],\"kind\":\"callable\",\"parameters\":[],\"request\":{\"empty\":true},\"result\":{\"empty\":true}},\"worker/Job\":{\"fields\":[{\"name\":\"ticket\",\"nullable\":false,\"required\":true,\"type\":{\"entity\":{\"ref\":\"worker/Ticket\"}},\"unique\":false},{\"name\":\"cancel\",\"nullable\":false,\"required\":true,\"type\":{\"ref\":\"worker/Cancel\"},\"unique\":false},{\"name\":\"rename\",\"nullable\":false,\"required\":false,\"type\":{\"ref\":\"worker/Rename\"},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]},\"worker/ProgressSink\":{\"fields\":[{\"name\":\"report\",\"nullable\":false,\"required\":true,\"type\":{\"ref\":\"worker/Report\"},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]},\"worker/Rename\":{\"errors\":[\"gone\"],\"kind\":\"callable\",\"parameters\":[],\"request\":{\"ref\":\"worker/Ticket\"},\"result\":{\"ref\":\"worker/Ticket\"}},\"worker/Report\":{\"errors\":[],\"kind\":\"callable\",\"parameters\":[],\"request\":{\"primitive\":\"integer\"},\"result\":{\"empty\":true}},\"worker/Ticket\":{\"fields\":[{\"name\":\"id\",\"nullable\":false,\"required\":true,\"type\":{\"primitive\":\"string\"},\"unique\":false},{\"name\":\"label\",\"nullable\":false,\"required\":true,\"type\":{\"primitive\":\"string\"},\"unique\":false}],\"key\":\"id\",\"kind\":\"entity\",\"open\":false,\"parameters\":[]}},\"root\":{\"ref\":\"supervisor\"},\"version\":1}";
/** SHA-256 of the canonical full declaration's exact UTF-8 bytes. */
export const wireDigest = "9419cf3bf490b4a0676bed8f60a6d183cbfd338b4be778fbcf30117d4322acfa";
/** Runtime validation applies equally to calls, replies, reverse calls and events; what fills a slot of a parameter is validated by the binding of the family that fills it. */
export const validateWire = withDeclaration(createValidator(contractTypes, wireDigest, { "worker": validate_worker }), wireDeclaration);
/** This family's declaration and reusable interpretations, with no operation context or owner. */
export const family = { name: "supervisor", validate: validateWire, types: { "Envelope": jsonAdapter<Envelope>({ type: "Envelope", validate: validateWire }), "Handle": jsonAdapter<Handle>({ type: "Handle", validate: validateWire }), "RelieveRequest": adapterRelieveRequest(), "Shift": adapterShift(), "Watch": adapterWatch() } } as const;
