// Code generated by nightseam. DO NOT EDIT.
import { createValidator, withDeclaration, jsonAdapter, familyTypeAdapter, DuplexError, type AnyFamily, type FamilyBinding, type TypeBinding, type Slots, type TypeExpression, type WireFamily } from "@nightseam/runtime";
export type { AnyFamily, FamilyBinding, TypeBinding, Slots, TypeExpression };
import type * as probe from "@example/probe-client";
import { validateWire as validate_probe } from "@example/probe-client";
import type { ValueAdapter, ValueContext } from "@nightseam/runtime";
import type { WireModelContext } from "@nightseam/runtime";
export interface Both<S extends AnyFamily = AnyFamily, T extends AnyFamily = AnyFamily> {
  "frame": Frame<S>;
  "echoes": Array<Echo<T>>;
}
export interface Echo<T extends AnyFamily = AnyFamily> {
  "heard": T["Envelope"];
}
/** One message of the nightseam.duplex/1 profile: the members the peer acts on, and nothing else. */
export interface Envelope {
  /** The profile's version, 1. */
  "version": number;
  /** request, response, event or cancel. */
  "kind": string;
  /** What correlates a response or a cancel with its request. */
  "id"?: string;
  /** The method a request names. */
  "method"?: string;
  /** A request's parameters. */
  "params"?: unknown;
  /** A response's result. */
  "result"?: unknown;
  /** A response's error. */
  "error"?: unknown;
  /** The event an event frame names. */
  "event"?: string;
  /** An event's data. */
  "data"?: unknown;
  /** The W3C Trace Context of the frame. */
  "traceparent"?: string;
  /** The vendor state of that trace. */
  "tracestate"?: string;
  /** What a request or an event carries about the call, delivered to the handler beside the payload. */
  "meta"?: Record<string, string>;
}
export interface Frame<S extends AnyFamily = AnyFamily> {
  "message": S["Envelope"];
  "back": S["Handle"];
}
/** A reference to a channel on the connection that carries the message holding it. */
export interface Handle {
  /** The channel's id on that connection. */
  "channel": number;
}
export interface Named {
  "held": probe.Envelope;
}
/** The family: its name and the wire types a slot of it draws on. */
export interface Family { readonly name: "pair"; Envelope: Envelope; Handle: Handle; Named: Named }
/** Conversion only: use the value adapter or validate the entire wire value inside the same operation batch. */
/** Writes Both using the supplied conversion for each type argument. */
export function exportBothUnchecked<S extends AnyFamily = AnyFamily, T extends AnyFamily = AnyFamily>(value: Both<S, T>, convert_S_Envelope: (value: S["Envelope"]) => unknown, convert_S_Handle: (value: S["Handle"]) => unknown, convert_T_Envelope: (value: T["Envelope"]) => unknown): unknown {
  const out: Record<string, unknown> = {};
  out["frame"] = exportFrameUnchecked<S>((value["frame"]) as Frame<S>, (input: S["Envelope"]): unknown => convert_S_Envelope((input) as S["Envelope"]), (input: S["Handle"]): unknown => convert_S_Handle((input) as S["Handle"]));
  out["echoes"] = (value["echoes"] as unknown[]).map((item) => exportEchoUnchecked<T>((item) as Echo<T>, (input: T["Envelope"]): unknown => convert_T_Envelope((input) as T["Envelope"])));
  return out;
}
/** Reads Both using the supplied conversion for each type argument. */
/** Conversion only: validation belongs to the value adapter or its caller. */
export function importBothUnchecked<S extends AnyFamily = AnyFamily, T extends AnyFamily = AnyFamily>(raw: unknown, convert_S_Envelope: (value: unknown) => S["Envelope"], convert_S_Handle: (value: unknown) => S["Handle"], convert_T_Envelope: (value: unknown) => T["Envelope"]): Both<S, T> {
  const wire = raw as Record<string, unknown>;
  const out: Record<string, unknown> = {};
  out["frame"] = importFrameUnchecked<S>(wire["frame"], (input: unknown): S["Envelope"] => (convert_S_Envelope(input)) as S["Envelope"], (input: unknown): S["Handle"] => (convert_S_Handle(input)) as S["Handle"]);
  out["echoes"] = (wire["echoes"] as unknown[]).map((item) => importEchoUnchecked<T>(item, (input: unknown): T["Envelope"] => (convert_T_Envelope(input)) as T["Envelope"]));
  return out as unknown as Both<S, T>;
}
/** Conversion only: use the value adapter or validate the entire wire value inside the same operation batch. */
/** Writes Echo using the supplied conversion for each type argument. */
export function exportEchoUnchecked<T extends AnyFamily = AnyFamily>(value: Echo<T>, convert_T_Envelope: (value: T["Envelope"]) => unknown): unknown {
  const out: Record<string, unknown> = {};
  out["heard"] = convert_T_Envelope((value["heard"]) as T["Envelope"]);
  return out;
}
/** Reads Echo using the supplied conversion for each type argument. */
/** Conversion only: validation belongs to the value adapter or its caller. */
export function importEchoUnchecked<T extends AnyFamily = AnyFamily>(raw: unknown, convert_T_Envelope: (value: unknown) => T["Envelope"]): Echo<T> {
  const wire = raw as Record<string, unknown>;
  const out: Record<string, unknown> = {};
  out["heard"] = convert_T_Envelope(wire["heard"]);
  return out as unknown as Echo<T>;
}
/** Conversion only: use the value adapter or validate the entire wire value inside the same operation batch. */
/** Writes Frame using the supplied conversion for each type argument. */
export function exportFrameUnchecked<S extends AnyFamily = AnyFamily>(value: Frame<S>, convert_S_Envelope: (value: S["Envelope"]) => unknown, convert_S_Handle: (value: S["Handle"]) => unknown): unknown {
  const out: Record<string, unknown> = {};
  out["message"] = convert_S_Envelope((value["message"]) as S["Envelope"]);
  out["back"] = convert_S_Handle((value["back"]) as S["Handle"]);
  return out;
}
/** Reads Frame using the supplied conversion for each type argument. */
/** Conversion only: validation belongs to the value adapter or its caller. */
export function importFrameUnchecked<S extends AnyFamily = AnyFamily>(raw: unknown, convert_S_Envelope: (value: unknown) => S["Envelope"], convert_S_Handle: (value: unknown) => S["Handle"]): Frame<S> {
  const wire = raw as Record<string, unknown>;
  const out: Record<string, unknown> = {};
  out["message"] = convert_S_Envelope(wire["message"]);
  out["back"] = convert_S_Handle(wire["back"]);
  return out as unknown as Frame<S>;
}
export function adapterBoth<S extends AnyFamily = AnyFamily, T extends AnyFamily = AnyFamily>(slot_S: FamilyBinding<S, "Envelope" | "Handle">, slot_T: FamilyBinding<T, "Envelope">): ValueAdapter<Both<S, T>> {
  const type_S_Envelope = familyTypeAdapter(slot_S, "Envelope");
  const type_S_Handle = familyTypeAdapter(slot_S, "Handle");
  const type_T_Envelope = familyTypeAdapter(slot_T, "Envelope");
  const slots: Slots = { "S": slot_S, "T": slot_T };
  const binding: TypeBinding = { type: "Both", validate: validateWire, slots };
  const needsContext = type_S_Envelope.needsContext || type_S_Handle.needsContext || type_T_Envelope.needsContext;
  return { binding, needsContext,
    export(context: unknown, value: Both<S, T>): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = exportBothUnchecked<S, T>(value, (value: S["Envelope"]) => type_S_Envelope.export(owner, value), (value: S["Handle"]) => type_S_Handle.export(owner, value), (value: T["Envelope"]) => type_T_Envelope.export(owner, value));
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Both<S, T> {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Both<S, T> => {
        const converted = importBothUnchecked<S, T>(raw, (value: unknown) => type_S_Envelope.import(owner, value), (value: unknown) => type_S_Handle.import(owner, value), (value: unknown) => type_T_Envelope.import(owner, value));
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterEcho<T extends AnyFamily = AnyFamily>(slot_T: FamilyBinding<T, "Envelope">): ValueAdapter<Echo<T>> {
  const type_T_Envelope = familyTypeAdapter(slot_T, "Envelope");
  const slots: Slots = { "T": slot_T };
  const binding: TypeBinding = { type: "Echo", validate: validateWire, slots };
  const needsContext = type_T_Envelope.needsContext;
  return { binding, needsContext,
    export(context: unknown, value: Echo<T>): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = exportEchoUnchecked<T>(value, (value: T["Envelope"]) => type_T_Envelope.export(owner, value));
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Echo<T> {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Echo<T> => {
        const converted = importEchoUnchecked<T>(raw, (value: unknown) => type_T_Envelope.import(owner, value));
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterFrame<S extends AnyFamily = AnyFamily>(slot_S: FamilyBinding<S, "Envelope" | "Handle">): ValueAdapter<Frame<S>> {
  const type_S_Envelope = familyTypeAdapter(slot_S, "Envelope");
  const type_S_Handle = familyTypeAdapter(slot_S, "Handle");
  const slots: Slots = { "S": slot_S };
  const binding: TypeBinding = { type: "Frame", validate: validateWire, slots };
  const needsContext = type_S_Envelope.needsContext || type_S_Handle.needsContext;
  return { binding, needsContext,
    export(context: unknown, value: Frame<S>): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = exportFrameUnchecked<S>(value, (value: S["Envelope"]) => type_S_Envelope.export(owner, value), (value: S["Handle"]) => type_S_Handle.export(owner, value));
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Frame<S> {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Frame<S> => {
        const converted = importFrameUnchecked<S>(raw, (value: unknown) => type_S_Envelope.import(owner, value), (value: unknown) => type_S_Handle.import(owner, value));
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterNamed(): ValueAdapter<Named> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Named", validate: validateWire, slots };
  const needsContext = false;
  return { binding, needsContext,
    export(context: unknown, value: Named): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = value;
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Named {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Named => {
        const converted = raw as Named;
        return converted;
      };
      return convert(context);
    },
  };
}
export interface ServerMethods<S extends AnyFamily = AnyFamily, T extends AnyFamily = AnyFamily> {
  named(params: Named, context?: WireModelContext): Named | Promise<Named>;
  relay(params: T["Envelope"], context?: ValueContext<T["Envelope"] | Both<S, T>, WireModelContext>): Both<S, T> | Promise<Both<S, T>>;
}
export interface ServerEvents<S extends AnyFamily = AnyFamily, T extends AnyFamily = AnyFamily> {
}
export interface Server<S extends AnyFamily = AnyFamily, T extends AnyFamily = AnyFamily> { methods: ServerMethods<S, T>; events: ServerEvents<S, T>; }
export interface ClientMethods<S extends AnyFamily = AnyFamily, T extends AnyFamily = AnyFamily> {
}
export interface ClientEvents<S extends AnyFamily = AnyFamily, T extends AnyFamily = AnyFamily> {
  echoed(data: Echo<T>, context?: ValueContext<Echo<T>, WireModelContext>): void | Promise<void>;
}
export interface Client<S extends AnyFamily = AnyFamily, T extends AnyFamily = AnyFamily> { methods: ClientMethods<S, T>; events: ClientEvents<S, T>; }
export type ServerModel<S extends AnyFamily = AnyFamily, T extends AnyFamily = AnyFamily> = (remote: Client<S, T>) => Server<S, T>;
export type ClientModel<S extends AnyFamily = AnyFamily, T extends AnyFamily = AnyFamily> = (remote: Server<S, T>) => Client<S, T>;

const contractTypes = {"types":{"Both":{"kind":"record","fields":[{"name":"frame","type":"Frame","required":true},{"name":"echoes","type":{"array":"Echo"},"required":true}]},"Echo":{"kind":"record","fields":[{"name":"heard","type":"T.Envelope","required":true}]},"Envelope":{"kind":"record","fields":[{"name":"version","type":"integer","required":true},{"name":"kind","type":"string","required":true},{"name":"id","type":"string","required":false},{"name":"method","type":"string","required":false},{"name":"params","type":"json","required":false},{"name":"result","type":"json","required":false},{"name":"error","type":"json","required":false},{"name":"event","type":"string","required":false},{"name":"data","type":"json","required":false},{"name":"traceparent","type":"string","required":false},{"name":"tracestate","type":"string","required":false},{"name":"meta","type":{"map":"string"},"required":false}]},"Frame":{"kind":"record","fields":[{"name":"message","type":"S.Envelope","required":true},{"name":"back","type":"S.Handle","required":true}]},"Handle":{"kind":"record","fields":[{"name":"channel","type":"integer","required":true}]},"Named":{"kind":"record","fields":[{"name":"held","type":"probe.Envelope","required":true}]}},"parameters":[{"name":"S","of":"protocol"},{"name":"T","of":"protocol"}]} as unknown as WireFamily;
/** Canonical language-neutral declaration bytes, the input to wireDigest. */
export const wireDeclaration = "{\"definitions\":{\"pair\":{\"client\":{\"ref\":\"pair/$client\"},\"errors\":[],\"kind\":\"family\",\"parameters\":[{\"name\":\"S\",\"of\":\"protocol\"},{\"name\":\"T\",\"of\":\"protocol\"}],\"server\":{\"ref\":\"pair/$server\"},\"types\":{\"Both\":{\"ref\":\"pair/Both\"},\"Echo\":{\"ref\":\"pair/Echo\"},\"Frame\":{\"ref\":\"pair/Frame\"},\"Named\":{\"ref\":\"pair/Named\"}}},\"pair/$client\":{\"direction\":\"client\",\"events\":{},\"extends\":[],\"kind\":\"side\",\"methods\":{},\"parameters\":[{\"name\":\"S\",\"of\":\"protocol\"},{\"name\":\"T\",\"of\":\"protocol\"}]},\"pair/$server\":{\"direction\":\"server\",\"events\":{\"echoed\":{\"ref\":\"pair/Echo\"}},\"extends\":[],\"kind\":\"side\",\"methods\":{\"named\":{\"errors\":[],\"request\":{\"ref\":\"pair/Named\"},\"result\":{\"ref\":\"pair/Named\"}},\"relay\":{\"errors\":[],\"request\":{\"draw\":{\"parameter\":\"pair/T\"},\"name\":\"Envelope\"},\"result\":{\"ref\":\"pair/Both\"}}},\"parameters\":[{\"name\":\"S\",\"of\":\"protocol\"},{\"name\":\"T\",\"of\":\"protocol\"}]},\"pair/Both\":{\"captures\":[{\"name\":\"S\",\"of\":\"protocol\"},{\"name\":\"T\",\"of\":\"protocol\"}],\"fields\":[{\"name\":\"frame\",\"nullable\":false,\"required\":true,\"type\":{\"ref\":\"pair/Frame\"},\"unique\":false},{\"name\":\"echoes\",\"nullable\":false,\"required\":true,\"type\":{\"array\":{\"ref\":\"pair/Echo\"}},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]},\"pair/Echo\":{\"captures\":[{\"name\":\"T\",\"of\":\"protocol\"}],\"fields\":[{\"name\":\"heard\",\"nullable\":false,\"required\":true,\"type\":{\"draw\":{\"parameter\":\"pair/T\"},\"name\":\"Envelope\"},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]},\"pair/Frame\":{\"captures\":[{\"name\":\"S\",\"of\":\"protocol\"}],\"fields\":[{\"name\":\"message\",\"nullable\":false,\"required\":true,\"type\":{\"draw\":{\"parameter\":\"pair/S\"},\"name\":\"Envelope\"},\"unique\":false},{\"name\":\"back\",\"nullable\":false,\"required\":true,\"type\":{\"draw\":{\"parameter\":\"pair/S\"},\"name\":\"Handle\"},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]},\"pair/Named\":{\"fields\":[{\"name\":\"held\",\"nullable\":false,\"required\":true,\"type\":{\"family\":{\"ref\":\"probe\"},\"projection\":\"Envelope\"},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]},\"probe\":{\"client\":{\"ref\":\"probe/$client\"},\"errors\":[\"denied\"],\"kind\":\"family\",\"parameters\":[],\"server\":{\"ref\":\"probe/$server\"},\"types\":{\"Base\":{\"ref\":\"probe/Base\"},\"OpenRecord\":{\"ref\":\"probe/OpenRecord\"},\"Payload\":{\"ref\":\"probe/Payload\"},\"Payloads\":{\"array\":{\"ref\":\"probe/Payload\"}},\"Status\":{\"ref\":\"probe/Status\"}}},\"probe/$client\":{\"direction\":\"client\",\"events\":{},\"extends\":[],\"kind\":\"side\",\"methods\":{\"reverse\":{\"errors\":[],\"request\":{\"ref\":\"probe/Payload\"},\"result\":{\"ref\":\"probe/Payload\"}}},\"parameters\":[]},\"probe/$server\":{\"direction\":\"server\",\"events\":{\"changed\":{\"ref\":\"probe/Payload\"}},\"extends\":[],\"kind\":\"side\",\"methods\":{\"echo\":{\"errors\":[],\"request\":{\"ref\":\"probe/Payload\"},\"result\":{\"ref\":\"probe/Payload\"}},\"no_args\":{\"errors\":[],\"request\":{\"empty\":true},\"result\":{\"primitive\":\"string\"}}},\"parameters\":[]},\"probe/Base\":{\"fields\":[{\"name\":\"text\",\"nullable\":false,\"required\":true,\"type\":{\"primitive\":\"string\"},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]},\"probe/OpenRecord\":{\"fields\":[{\"name\":\"id\",\"nullable\":false,\"required\":true,\"type\":{\"primitive\":\"string\"},\"unique\":false},{\"name\":\"note\",\"nullable\":false,\"required\":false,\"type\":{\"primitive\":\"string\"},\"unique\":false}],\"kind\":\"record\",\"open\":true,\"parameters\":[]},\"probe/Payload\":{\"extends\":[{\"ref\":\"probe/Base\"}],\"fields\":[{\"name\":\"count\",\"nullable\":false,\"required\":true,\"type\":{\"primitive\":\"integer\"},\"unique\":false},{\"name\":\"note\",\"nullable\":true,\"required\":false,\"type\":{\"primitive\":\"string\"},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]},\"probe/Status\":{\"kind\":\"enum\",\"parameters\":[],\"values\":[\"context.example\",\"done\",\"ready\"]}},\"root\":{\"ref\":\"pair\"},\"version\":1}";
/** SHA-256 of the canonical full declaration's exact UTF-8 bytes. */
export const wireDigest = "4558079f5d30e99b47c58112f17b4212f52354c9228cc308c00fd06bc39357e1";
/** Runtime validation applies equally to calls, replies, reverse calls and events; what fills a slot of a parameter is validated by the binding of the family that fills it. */
export const validateWire = withDeclaration(createValidator(contractTypes, wireDigest, { "probe": validate_probe }), wireDeclaration);
/** This family's declaration and reusable interpretations, with no operation context or owner. */
export const family = { name: "pair", validate: validateWire, types: { "Envelope": jsonAdapter<Envelope>({ type: "Envelope", validate: validateWire }), "Handle": jsonAdapter<Handle>({ type: "Handle", validate: validateWire }), "Named": adapterNamed() } } as const;
