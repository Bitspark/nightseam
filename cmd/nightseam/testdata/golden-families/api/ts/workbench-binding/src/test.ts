// Code generated by nightseam. DO NOT EDIT.
import { DuplexPeer, DuplexError, wirePair, forwardWire, createDispatcher, type AdapterContext, type WireModelContext, type ValueAdapter, type ValueContext } from "@nightseam/runtime";
import { mount, pipe as framePipe, type Endpoint } from '@nightseam/duplex';
import { toWire, prepareFromWire } from './index.ts';
import type * as Protocol from "@example/workbench-client/types";
import type { AnyFamily, FamilyBinding } from "@example/workbench-client/types";

export type Presentation = (wire: Endpoint) => { wire: Endpoint; close(): void } | Promise<{ wire: Endpoint; close(): void }>;
export interface Options {
 context?: AdapterContext;
 remoteContext?: AdapterContext;
 presentation?: Presentation;
 inputs?: Readonly<Record<string, unknown>>;
 equal?: (method: string, direct: unknown, roundTrip: unknown) => void | Promise<void>;
 callContext?: WireModelContext & { valueContext?: unknown };
}
function once(action: () => void): () => void { let closed=false; return () => { if (!closed) { closed=true; action(); } }; }
function hasMethod(facet:unknown,name:string):boolean{for(let current=facet;current!=null&&current!==Object.prototype;current=Object.getPrototypeOf(current)){if(Object.prototype.hasOwnProperty.call(current,name))return typeof (facet as Record<string,unknown>)[name]==='function';}return false;}
export const local: Presentation = wire => ({wire,close(){}});
export const mounted: Presentation = wire => {
 const root=mount(new Map([['family',wire]]));
 try { const dispatcher=createDispatcher(root); return {wire:dispatcher.select(['family']),close:once(()=>{try{dispatcher.close(1000,'');}finally{root.close(1000,'');}})}; }
 catch(error){root.close(1000,'');throw error;}
};
export const forwarded: Presentation = wire => {
 const [left,right]=wirePair();
 try { const detach=forwardWire(right,wire); return {wire:left,close:once(()=>{detach();left.close(1000,'');})}; }
 catch(error){left.close(1000,'');throw error;}
};
export const pipe: Presentation = async wire => {
 const [left,right]=framePipe(); let detach=()=>{};
 const server=new DuplexPeer({role:'server',prepare(peer){detach=forwardWire(peer.wire(),wire);}});
 const client=new DuplexPeer({role:'client'});
 const close=once(()=>{detach();client.close();server.close();left.close(1000,'');right.close(1000,'');});
 try {await Promise.all([server.attach(right),client.attach(left)]);return {wire:client.wire(),close};}
 catch(error){close();throw error;}
};
type Outcome = {ok:true;value:unknown}|{ok:false;error:unknown};
async function outcome(call:()=>unknown):Promise<Outcome>{try{return {ok:true,value:await call()};}catch(error){return {ok:false,error};}}
function canonical(value:unknown):string {
 if(value===undefined)return 'undefined';
 if(typeof value==='function'||typeof value==='symbol'||typeof value==='bigint')throw new Error('result needs an equal observer');
 if(value===null||typeof value!=='object')return JSON.stringify(value);
 if(Array.isArray(value))return '['+value.map(canonical).join(',')+']';
 return '{'+Object.keys(value).sort().map(key=>JSON.stringify(key)+':'+canonical((value as Record<string,unknown>)[key])).join(',')+'}';
}
async function compare(method:string,expected:Outcome,actual:Outcome,equal:Options['equal']):Promise<void>{
 if(!expected.ok || !actual.ok){
  const code=(error:unknown)=>error instanceof DuplexError?error.code:'internal';
  if(!expected.ok&&!actual.ok&&code(expected.error)===code(actual.error)){
   if(expected.error instanceof DuplexError && actual.error instanceof DuplexError && (expected.error.message!==actual.error.message || canonical(expected.error.data)!==canonical(actual.error.data)))throw new Error(method+': public error changed');
   return;
  }
  throw new Error(method+': direct and round-trip outcomes differ');
 }
 if(equal){await equal(method,expected.value,actual.value);return;}
 if(canonical(expected.value)!==canonical(actual.value))throw new Error(method+': direct and round-trip results differ');
}

export async function pair(model: Protocol.ServerModel, options: Options): Promise<{ model: Protocol.ServerModel; close(): void }> {
  const wire = toWire(model, options.context ?? {});
  let close = once(() => wire.close(1000, ''));
  try {
  const view = await (options.presentation ?? pipe)(wire);
  const rootClose = close; close = once(() => { try { view.close(); } finally { rootClose(); } });
  const prepared = prepareFromWire(view.wire, options.remoteContext ?? {});
  const viewClose = close; close = once(() => { try { prepared.close(); } finally { viewClose(); } });
  return { model: await prepared.complete(options.callContext), close };
  } catch (error) { close(); throw error; }
}
const examples: Readonly<Record<string, { raw?: string; reason?: string }>> = {
"CancelWorkItemParams": { raw: "{\"id\":\"‹id›\",\"expected_version\":0,\"reason\":\"‹reason›\"}", reason: "" },
"CreateProjectParams": { raw: "{\"key\":\"‹key›\",\"name\":\"‹name›\",\"description\":\"‹description›\"}", reason: "" },
"CreateWorkItemParams": { raw: "{\"project\":\"‹project›\",\"title\":\"‹title›\",\"draft\":{\"objective\":\"‹objective›\",\"criteria\":[{\"id\":\"‹id›\",\"description\":\"‹description›\"}],\"constraints\":[\"‹constraints›\"],\"dependencies\":[\"‹dependencies›\"]}}", reason: "" },
"Criterion": { raw: "{\"id\":\"‹id›\",\"description\":\"‹description›\"}", reason: "" },
"Event": { raw: "{\"sequence\":0,\"type\":\"‹type›\",\"project_id\":\"‹project_id›\",\"work_item_id\":\"‹work_item_id›\",\"actor_id\":\"‹actor_id›\",\"data\":{},\"created_at\":\"2026-01-01T00:00:00Z\"}", reason: "" },
"GetWorkItemParams": { raw: "{\"id\":\"‹id›\"}", reason: "" },
"ListEventsParams": { raw: "{\"after\":0,\"project\":\"‹project›\",\"limit\":0}", reason: "" },
"ListProjectsParams": { raw: "{}", reason: "" },
"ListWorkItemsParams": { raw: "{\"project\":\"‹project›\",\"state\":\"‹state›\"}", reason: "" },
"Project": { raw: "{\"id\":\"‹id›\",\"owner_id\":\"‹owner_id›\",\"version\":0,\"key\":\"‹key›\",\"name\":\"‹name›\",\"description\":\"‹description›\",\"created_at\":\"2026-01-01T00:00:00Z\",\"updated_at\":\"2026-01-01T00:00:00Z\"}", reason: "" },
"PublishSpecificationParams": { raw: "{\"id\":\"‹id›\",\"expected_version\":0}", reason: "" },
"ReopenWorkItemParams": { raw: "{\"id\":\"‹id›\",\"expected_version\":0,\"reason\":\"‹reason›\"}", reason: "" },
"SetDependenciesParams": { raw: "{\"id\":\"‹id›\",\"expected_version\":0,\"dependencies\":[\"‹dependencies›\"]}", reason: "" },
"SetStepsParams": { raw: "{\"id\":\"‹id›\",\"expected_version\":0,\"steps\":[{\"id\":\"‹id›\",\"objective\":\"‹objective›\",\"state\":\"‹state›\",\"result_note\":\"‹result_note›\",\"change_reason\":\"‹change_reason›\"}],\"reason\":\"‹reason›\"}", reason: "" },
"Specification": { raw: "{\"objective\":\"‹objective›\",\"criteria\":[{\"id\":\"‹id›\",\"description\":\"‹description›\"}],\"constraints\":[\"‹constraints›\"],\"dependencies\":[\"‹dependencies›\"],\"id\":\"‹id›\",\"revision\":0,\"published_at\":\"2026-01-01T00:00:00Z\"}", reason: "" },
"SpecificationDraft": { raw: "{\"objective\":\"‹objective›\",\"criteria\":[{\"id\":\"‹id›\",\"description\":\"‹description›\"}],\"constraints\":[\"‹constraints›\"],\"dependencies\":[\"‹dependencies›\"]}", reason: "" },
"Step": { raw: "{\"id\":\"‹id›\",\"objective\":\"‹objective›\",\"state\":\"‹state›\",\"result_note\":\"‹result_note›\",\"change_reason\":\"‹change_reason›\"}", reason: "" },
"SubscribeParams": { raw: "{\"after\":0,\"project\":\"‹project›\"}", reason: "" },
"SubscribeResult": { raw: "{\"subscribed\":true}", reason: "" },
"UpdateProjectParams": { raw: "{\"id\":\"‹id›\",\"expected_version\":0,\"key\":\"‹key›\",\"name\":\"‹name›\",\"description\":\"‹description›\"}", reason: "" },
"UpdateWorkItemParams": { raw: "{\"id\":\"‹id›\",\"expected_version\":0,\"title\":\"‹title›\",\"draft\":{\"objective\":\"‹objective›\",\"criteria\":[{\"id\":\"‹id›\",\"description\":\"‹description›\"}],\"constraints\":[\"‹constraints›\"],\"dependencies\":[\"‹dependencies›\"]}}", reason: "" },
"User": { raw: "{\"id\":\"‹id›\",\"email\":\"‹email›\",\"name\":\"‹name›\"}", reason: "" },
"WorkItem": { raw: "{\"id\":\"‹id›\",\"project_id\":\"‹project_id›\",\"key\":\"‹key›\",\"title\":\"‹title›\",\"state\":\"‹state›\",\"version\":0,\"draft\":{\"objective\":\"‹objective›\",\"criteria\":[{\"id\":\"‹id›\",\"description\":\"‹description›\"}],\"constraints\":[\"‹constraints›\"],\"dependencies\":[\"‹dependencies›\"]},\"specifications\":[{\"objective\":\"‹objective›\",\"criteria\":[{\"id\":\"‹id›\",\"description\":\"‹description›\"}],\"constraints\":[\"‹constraints›\"],\"dependencies\":[\"‹dependencies›\"],\"id\":\"‹id›\",\"revision\":0,\"published_at\":\"2026-01-01T00:00:00Z\"}],\"current_specification_id\":\"‹current_specification_id›\",\"plan_specification_id\":\"‹plan_specification_id›\",\"steps\":[{\"id\":\"‹id›\",\"objective\":\"‹objective›\",\"state\":\"‹state›\",\"result_note\":\"‹result_note›\",\"change_reason\":\"‹change_reason›\"}],\"dependencies\":[\"‹dependencies›\"],\"readiness\":\"‹readiness›\",\"created_at\":\"2026-01-01T00:00:00Z\",\"updated_at\":\"2026-01-01T00:00:00Z\"}", reason: "" },
};
/** A fresh documented data witness, validated by the caller's exact value adapter. */
export function example<T>(name: string, adapter: ValueAdapter<T>): T { const value = Object.prototype.hasOwnProperty.call(examples,name) ? examples[name] : undefined; if (!value) throw new Error('unknown example '+name); if (value.reason || adapter.needsContext) throw new Error('example '+name+' unavailable: '+(value.reason || 'an acquiring adapter needs a native witness')); return adapter.import(undefined, JSON.parse(value.raw!)); }
/** Exercise every method on two fresh equivalent models; missing evidence is an error. */
export async function smoke(model: Protocol.ServerModel, opposite: Protocol.Client, options: Options): Promise<void> {
  const bindings = {};
  const inputs = new Map<string,unknown>(); const seen = new Map<string,number>(); let inputError: unknown;
  const observed: Protocol.ServerModel = remote => { const value=model(remote);
  if(!hasMethod(value?.methods,"listEvents"))throw new Error("model method events.list is required");
  if(!hasMethod(value?.methods,"me"))throw new Error("model method me is required");
  if(!hasMethod(value?.methods,"createProject"))throw new Error("model method projects.create is required");
  if(!hasMethod(value?.methods,"listProjects"))throw new Error("model method projects.list is required");
  if(!hasMethod(value?.methods,"updateProject"))throw new Error("model method projects.update is required");
  if(!hasMethod(value?.methods,"subscribe"))throw new Error("model method subscribe is required");
  if(!hasMethod(value?.methods,"cancelWorkItem"))throw new Error("model method work.cancel is required");
  if(!hasMethod(value?.methods,"createWorkItem"))throw new Error("model method work.create is required");
  if(!hasMethod(value?.methods,"setDependencies"))throw new Error("model method work.dependencies is required");
  if(!hasMethod(value?.methods,"getWorkItem"))throw new Error("model method work.get is required");
  if(!hasMethod(value?.methods,"listWorkItems"))throw new Error("model method work.list is required");
  if(!hasMethod(value?.methods,"publishSpecification"))throw new Error("model method work.publish is required");
  if(!hasMethod(value?.methods,"reopenWorkItem"))throw new Error("model method work.reopen is required");
  if(!hasMethod(value?.methods,"setSteps"))throw new Error("model method work.steps is required");
  if(!hasMethod(value?.methods,"updateWorkItem"))throw new Error("model method work.update is required");
  return { ...value, methods: {
  async listEvents(input,context) { const key="events.list"; seen.set(key,(seen.get(key)??0)+1); const tracked=inputs.has(key),expected=inputs.get(key);inputs.delete(key); if(tracked)try { await compare("events.list.request",{ok:true,value:expected},{ok:true,value:input},options.equal); } catch(error){inputError=error;throw error;} return value.methods.listEvents(input,context); },
  async me(input,context) { const key="me"; seen.set(key,(seen.get(key)??0)+1); const tracked=inputs.has(key),expected=inputs.get(key);inputs.delete(key); if(tracked)try { await compare("me.request",{ok:true,value:expected},{ok:true,value:input},options.equal); } catch(error){inputError=error;throw error;} return value.methods.me(input,context); },
  async createProject(input,context) { const key="projects.create"; seen.set(key,(seen.get(key)??0)+1); const tracked=inputs.has(key),expected=inputs.get(key);inputs.delete(key); if(tracked)try { await compare("projects.create.request",{ok:true,value:expected},{ok:true,value:input},options.equal); } catch(error){inputError=error;throw error;} return value.methods.createProject(input,context); },
  async listProjects(input,context) { const key="projects.list"; seen.set(key,(seen.get(key)??0)+1); const tracked=inputs.has(key),expected=inputs.get(key);inputs.delete(key); if(tracked)try { await compare("projects.list.request",{ok:true,value:expected},{ok:true,value:input},options.equal); } catch(error){inputError=error;throw error;} return value.methods.listProjects(input,context); },
  async updateProject(input,context) { const key="projects.update"; seen.set(key,(seen.get(key)??0)+1); const tracked=inputs.has(key),expected=inputs.get(key);inputs.delete(key); if(tracked)try { await compare("projects.update.request",{ok:true,value:expected},{ok:true,value:input},options.equal); } catch(error){inputError=error;throw error;} return value.methods.updateProject(input,context); },
  async subscribe(input,context) { const key="subscribe"; seen.set(key,(seen.get(key)??0)+1); const tracked=inputs.has(key),expected=inputs.get(key);inputs.delete(key); if(tracked)try { await compare("subscribe.request",{ok:true,value:expected},{ok:true,value:input},options.equal); } catch(error){inputError=error;throw error;} return value.methods.subscribe(input,context); },
  async cancelWorkItem(input,context) { const key="work.cancel"; seen.set(key,(seen.get(key)??0)+1); const tracked=inputs.has(key),expected=inputs.get(key);inputs.delete(key); if(tracked)try { await compare("work.cancel.request",{ok:true,value:expected},{ok:true,value:input},options.equal); } catch(error){inputError=error;throw error;} return value.methods.cancelWorkItem(input,context); },
  async createWorkItem(input,context) { const key="work.create"; seen.set(key,(seen.get(key)??0)+1); const tracked=inputs.has(key),expected=inputs.get(key);inputs.delete(key); if(tracked)try { await compare("work.create.request",{ok:true,value:expected},{ok:true,value:input},options.equal); } catch(error){inputError=error;throw error;} return value.methods.createWorkItem(input,context); },
  async setDependencies(input,context) { const key="work.dependencies"; seen.set(key,(seen.get(key)??0)+1); const tracked=inputs.has(key),expected=inputs.get(key);inputs.delete(key); if(tracked)try { await compare("work.dependencies.request",{ok:true,value:expected},{ok:true,value:input},options.equal); } catch(error){inputError=error;throw error;} return value.methods.setDependencies(input,context); },
  async getWorkItem(input,context) { const key="work.get"; seen.set(key,(seen.get(key)??0)+1); const tracked=inputs.has(key),expected=inputs.get(key);inputs.delete(key); if(tracked)try { await compare("work.get.request",{ok:true,value:expected},{ok:true,value:input},options.equal); } catch(error){inputError=error;throw error;} return value.methods.getWorkItem(input,context); },
  async listWorkItems(input,context) { const key="work.list"; seen.set(key,(seen.get(key)??0)+1); const tracked=inputs.has(key),expected=inputs.get(key);inputs.delete(key); if(tracked)try { await compare("work.list.request",{ok:true,value:expected},{ok:true,value:input},options.equal); } catch(error){inputError=error;throw error;} return value.methods.listWorkItems(input,context); },
  async publishSpecification(input,context) { const key="work.publish"; seen.set(key,(seen.get(key)??0)+1); const tracked=inputs.has(key),expected=inputs.get(key);inputs.delete(key); if(tracked)try { await compare("work.publish.request",{ok:true,value:expected},{ok:true,value:input},options.equal); } catch(error){inputError=error;throw error;} return value.methods.publishSpecification(input,context); },
  async reopenWorkItem(input,context) { const key="work.reopen"; seen.set(key,(seen.get(key)??0)+1); const tracked=inputs.has(key),expected=inputs.get(key);inputs.delete(key); if(tracked)try { await compare("work.reopen.request",{ok:true,value:expected},{ok:true,value:input},options.equal); } catch(error){inputError=error;throw error;} return value.methods.reopenWorkItem(input,context); },
  async setSteps(input,context) { const key="work.steps"; seen.set(key,(seen.get(key)??0)+1); const tracked=inputs.has(key),expected=inputs.get(key);inputs.delete(key); if(tracked)try { await compare("work.steps.request",{ok:true,value:expected},{ok:true,value:input},options.equal); } catch(error){inputError=error;throw error;} return value.methods.setSteps(input,context); },
  async updateWorkItem(input,context) { const key="work.update"; seen.set(key,(seen.get(key)??0)+1); const tracked=inputs.has(key),expected=inputs.get(key);inputs.delete(key); if(tracked)try { await compare("work.update.request",{ok:true,value:expected},{ok:true,value:input},options.equal); } catch(error){inputError=error;throw error;} return value.methods.updateWorkItem(input,context); },
  } }; };
  const prepared = await pair(observed, options);
  try {
  const remote = prepared.model(opposite); const direct = model(opposite);
  {
  let input: unknown;
  if (options.inputs && Object.prototype.hasOwnProperty.call(options.inputs,"events.list")) { input=options.inputs["events.list"]; } else {
  if (false) throw new Error("input events.list needs a caller-supplied native value");
  input = JSON.parse("{\"after\":0,\"project\":\"‹project›\",\"limit\":0}");
  }
  inputs.set("events.list",input);
  const before=seen.get("events.list")??0;
  const actual = await outcome(() => remote.methods.listEvents(input as Protocol.ListEventsParams, options.callContext as WireModelContext));
  if(inputError!==undefined)throw inputError;
  if((seen.get("events.list")??0)<=before)throw new Error("events.list: model was not reached");
  const expected = await outcome(() => direct.methods.listEvents(input as Protocol.ListEventsParams, options.callContext as WireModelContext));
  await compare("events.list", expected, actual, options.equal);
  }
  {
  let input: unknown;
  if (options.inputs && Object.prototype.hasOwnProperty.call(options.inputs,"me")) { input=options.inputs["me"]; } else {
  input = JSON.parse("{}");
  }
  inputs.set("me",input);
  const before=seen.get("me")??0;
  const actual = await outcome(() => remote.methods.me(input as Record<string, never>, options.callContext as WireModelContext));
  if(inputError!==undefined)throw inputError;
  if((seen.get("me")??0)<=before)throw new Error("me: model was not reached");
  const expected = await outcome(() => direct.methods.me(input as Record<string, never>, options.callContext as WireModelContext));
  await compare("me", expected, actual, options.equal);
  }
  {
  let input: unknown;
  if (options.inputs && Object.prototype.hasOwnProperty.call(options.inputs,"projects.create")) { input=options.inputs["projects.create"]; } else {
  if (false) throw new Error("input projects.create needs a caller-supplied native value");
  input = JSON.parse("{\"key\":\"‹key›\",\"name\":\"‹name›\",\"description\":\"‹description›\"}");
  }
  inputs.set("projects.create",input);
  const before=seen.get("projects.create")??0;
  const actual = await outcome(() => remote.methods.createProject(input as Protocol.CreateProjectParams, options.callContext as WireModelContext));
  if(inputError!==undefined)throw inputError;
  if((seen.get("projects.create")??0)<=before)throw new Error("projects.create: model was not reached");
  const expected = await outcome(() => direct.methods.createProject(input as Protocol.CreateProjectParams, options.callContext as WireModelContext));
  await compare("projects.create", expected, actual, options.equal);
  }
  {
  let input: unknown;
  if (options.inputs && Object.prototype.hasOwnProperty.call(options.inputs,"projects.list")) { input=options.inputs["projects.list"]; } else {
  if (false) throw new Error("input projects.list needs a caller-supplied native value");
  input = JSON.parse("{}");
  }
  inputs.set("projects.list",input);
  const before=seen.get("projects.list")??0;
  const actual = await outcome(() => remote.methods.listProjects(input as Protocol.ListProjectsParams, options.callContext as WireModelContext));
  if(inputError!==undefined)throw inputError;
  if((seen.get("projects.list")??0)<=before)throw new Error("projects.list: model was not reached");
  const expected = await outcome(() => direct.methods.listProjects(input as Protocol.ListProjectsParams, options.callContext as WireModelContext));
  await compare("projects.list", expected, actual, options.equal);
  }
  {
  let input: unknown;
  if (options.inputs && Object.prototype.hasOwnProperty.call(options.inputs,"projects.update")) { input=options.inputs["projects.update"]; } else {
  if (false) throw new Error("input projects.update needs a caller-supplied native value");
  input = JSON.parse("{\"id\":\"‹id›\",\"expected_version\":0,\"key\":\"‹key›\",\"name\":\"‹name›\",\"description\":\"‹description›\"}");
  }
  inputs.set("projects.update",input);
  const before=seen.get("projects.update")??0;
  const actual = await outcome(() => remote.methods.updateProject(input as Protocol.UpdateProjectParams, options.callContext as WireModelContext));
  if(inputError!==undefined)throw inputError;
  if((seen.get("projects.update")??0)<=before)throw new Error("projects.update: model was not reached");
  const expected = await outcome(() => direct.methods.updateProject(input as Protocol.UpdateProjectParams, options.callContext as WireModelContext));
  await compare("projects.update", expected, actual, options.equal);
  }
  {
  let input: unknown;
  if (options.inputs && Object.prototype.hasOwnProperty.call(options.inputs,"subscribe")) { input=options.inputs["subscribe"]; } else {
  if (false) throw new Error("input subscribe needs a caller-supplied native value");
  input = JSON.parse("{\"after\":0,\"project\":\"‹project›\"}");
  }
  inputs.set("subscribe",input);
  const before=seen.get("subscribe")??0;
  const actual = await outcome(() => remote.methods.subscribe(input as Protocol.SubscribeParams, options.callContext as WireModelContext));
  if(inputError!==undefined)throw inputError;
  if((seen.get("subscribe")??0)<=before)throw new Error("subscribe: model was not reached");
  const expected = await outcome(() => direct.methods.subscribe(input as Protocol.SubscribeParams, options.callContext as WireModelContext));
  await compare("subscribe", expected, actual, options.equal);
  }
  {
  let input: unknown;
  if (options.inputs && Object.prototype.hasOwnProperty.call(options.inputs,"work.cancel")) { input=options.inputs["work.cancel"]; } else {
  if (false) throw new Error("input work.cancel needs a caller-supplied native value");
  input = JSON.parse("{\"id\":\"‹id›\",\"expected_version\":0,\"reason\":\"‹reason›\"}");
  }
  inputs.set("work.cancel",input);
  const before=seen.get("work.cancel")??0;
  const actual = await outcome(() => remote.methods.cancelWorkItem(input as Protocol.CancelWorkItemParams, options.callContext as WireModelContext));
  if(inputError!==undefined)throw inputError;
  if((seen.get("work.cancel")??0)<=before)throw new Error("work.cancel: model was not reached");
  const expected = await outcome(() => direct.methods.cancelWorkItem(input as Protocol.CancelWorkItemParams, options.callContext as WireModelContext));
  await compare("work.cancel", expected, actual, options.equal);
  }
  {
  let input: unknown;
  if (options.inputs && Object.prototype.hasOwnProperty.call(options.inputs,"work.create")) { input=options.inputs["work.create"]; } else {
  if (false) throw new Error("input work.create needs a caller-supplied native value");
  input = JSON.parse("{\"project\":\"‹project›\",\"title\":\"‹title›\",\"draft\":{\"objective\":\"‹objective›\",\"criteria\":[{\"id\":\"‹id›\",\"description\":\"‹description›\"}],\"constraints\":[\"‹constraints›\"],\"dependencies\":[\"‹dependencies›\"]}}");
  }
  inputs.set("work.create",input);
  const before=seen.get("work.create")??0;
  const actual = await outcome(() => remote.methods.createWorkItem(input as Protocol.CreateWorkItemParams, options.callContext as WireModelContext));
  if(inputError!==undefined)throw inputError;
  if((seen.get("work.create")??0)<=before)throw new Error("work.create: model was not reached");
  const expected = await outcome(() => direct.methods.createWorkItem(input as Protocol.CreateWorkItemParams, options.callContext as WireModelContext));
  await compare("work.create", expected, actual, options.equal);
  }
  {
  let input: unknown;
  if (options.inputs && Object.prototype.hasOwnProperty.call(options.inputs,"work.dependencies")) { input=options.inputs["work.dependencies"]; } else {
  if (false) throw new Error("input work.dependencies needs a caller-supplied native value");
  input = JSON.parse("{\"id\":\"‹id›\",\"expected_version\":0,\"dependencies\":[\"‹dependencies›\"]}");
  }
  inputs.set("work.dependencies",input);
  const before=seen.get("work.dependencies")??0;
  const actual = await outcome(() => remote.methods.setDependencies(input as Protocol.SetDependenciesParams, options.callContext as WireModelContext));
  if(inputError!==undefined)throw inputError;
  if((seen.get("work.dependencies")??0)<=before)throw new Error("work.dependencies: model was not reached");
  const expected = await outcome(() => direct.methods.setDependencies(input as Protocol.SetDependenciesParams, options.callContext as WireModelContext));
  await compare("work.dependencies", expected, actual, options.equal);
  }
  {
  let input: unknown;
  if (options.inputs && Object.prototype.hasOwnProperty.call(options.inputs,"work.get")) { input=options.inputs["work.get"]; } else {
  if (false) throw new Error("input work.get needs a caller-supplied native value");
  input = JSON.parse("{\"id\":\"‹id›\"}");
  }
  inputs.set("work.get",input);
  const before=seen.get("work.get")??0;
  const actual = await outcome(() => remote.methods.getWorkItem(input as Protocol.GetWorkItemParams, options.callContext as WireModelContext));
  if(inputError!==undefined)throw inputError;
  if((seen.get("work.get")??0)<=before)throw new Error("work.get: model was not reached");
  const expected = await outcome(() => direct.methods.getWorkItem(input as Protocol.GetWorkItemParams, options.callContext as WireModelContext));
  await compare("work.get", expected, actual, options.equal);
  }
  {
  let input: unknown;
  if (options.inputs && Object.prototype.hasOwnProperty.call(options.inputs,"work.list")) { input=options.inputs["work.list"]; } else {
  if (false) throw new Error("input work.list needs a caller-supplied native value");
  input = JSON.parse("{\"project\":\"‹project›\",\"state\":\"‹state›\"}");
  }
  inputs.set("work.list",input);
  const before=seen.get("work.list")??0;
  const actual = await outcome(() => remote.methods.listWorkItems(input as Protocol.ListWorkItemsParams, options.callContext as WireModelContext));
  if(inputError!==undefined)throw inputError;
  if((seen.get("work.list")??0)<=before)throw new Error("work.list: model was not reached");
  const expected = await outcome(() => direct.methods.listWorkItems(input as Protocol.ListWorkItemsParams, options.callContext as WireModelContext));
  await compare("work.list", expected, actual, options.equal);
  }
  {
  let input: unknown;
  if (options.inputs && Object.prototype.hasOwnProperty.call(options.inputs,"work.publish")) { input=options.inputs["work.publish"]; } else {
  if (false) throw new Error("input work.publish needs a caller-supplied native value");
  input = JSON.parse("{\"id\":\"‹id›\",\"expected_version\":0}");
  }
  inputs.set("work.publish",input);
  const before=seen.get("work.publish")??0;
  const actual = await outcome(() => remote.methods.publishSpecification(input as Protocol.PublishSpecificationParams, options.callContext as WireModelContext));
  if(inputError!==undefined)throw inputError;
  if((seen.get("work.publish")??0)<=before)throw new Error("work.publish: model was not reached");
  const expected = await outcome(() => direct.methods.publishSpecification(input as Protocol.PublishSpecificationParams, options.callContext as WireModelContext));
  await compare("work.publish", expected, actual, options.equal);
  }
  {
  let input: unknown;
  if (options.inputs && Object.prototype.hasOwnProperty.call(options.inputs,"work.reopen")) { input=options.inputs["work.reopen"]; } else {
  if (false) throw new Error("input work.reopen needs a caller-supplied native value");
  input = JSON.parse("{\"id\":\"‹id›\",\"expected_version\":0,\"reason\":\"‹reason›\"}");
  }
  inputs.set("work.reopen",input);
  const before=seen.get("work.reopen")??0;
  const actual = await outcome(() => remote.methods.reopenWorkItem(input as Protocol.ReopenWorkItemParams, options.callContext as WireModelContext));
  if(inputError!==undefined)throw inputError;
  if((seen.get("work.reopen")??0)<=before)throw new Error("work.reopen: model was not reached");
  const expected = await outcome(() => direct.methods.reopenWorkItem(input as Protocol.ReopenWorkItemParams, options.callContext as WireModelContext));
  await compare("work.reopen", expected, actual, options.equal);
  }
  {
  let input: unknown;
  if (options.inputs && Object.prototype.hasOwnProperty.call(options.inputs,"work.steps")) { input=options.inputs["work.steps"]; } else {
  if (false) throw new Error("input work.steps needs a caller-supplied native value");
  input = JSON.parse("{\"id\":\"‹id›\",\"expected_version\":0,\"steps\":[{\"id\":\"‹id›\",\"objective\":\"‹objective›\",\"state\":\"‹state›\",\"result_note\":\"‹result_note›\",\"change_reason\":\"‹change_reason›\"}],\"reason\":\"‹reason›\"}");
  }
  inputs.set("work.steps",input);
  const before=seen.get("work.steps")??0;
  const actual = await outcome(() => remote.methods.setSteps(input as Protocol.SetStepsParams, options.callContext as WireModelContext));
  if(inputError!==undefined)throw inputError;
  if((seen.get("work.steps")??0)<=before)throw new Error("work.steps: model was not reached");
  const expected = await outcome(() => direct.methods.setSteps(input as Protocol.SetStepsParams, options.callContext as WireModelContext));
  await compare("work.steps", expected, actual, options.equal);
  }
  {
  let input: unknown;
  if (options.inputs && Object.prototype.hasOwnProperty.call(options.inputs,"work.update")) { input=options.inputs["work.update"]; } else {
  if (false) throw new Error("input work.update needs a caller-supplied native value");
  input = JSON.parse("{\"id\":\"‹id›\",\"expected_version\":0,\"title\":\"‹title›\",\"draft\":{\"objective\":\"‹objective›\",\"criteria\":[{\"id\":\"‹id›\",\"description\":\"‹description›\"}],\"constraints\":[\"‹constraints›\"],\"dependencies\":[\"‹dependencies›\"]}}");
  }
  inputs.set("work.update",input);
  const before=seen.get("work.update")??0;
  const actual = await outcome(() => remote.methods.updateWorkItem(input as Protocol.UpdateWorkItemParams, options.callContext as WireModelContext));
  if(inputError!==undefined)throw inputError;
  if((seen.get("work.update")??0)<=before)throw new Error("work.update: model was not reached");
  const expected = await outcome(() => direct.methods.updateWorkItem(input as Protocol.UpdateWorkItemParams, options.callContext as WireModelContext));
  await compare("work.update", expected, actual, options.equal);
  }
  } finally { prepared.close(); }
}
