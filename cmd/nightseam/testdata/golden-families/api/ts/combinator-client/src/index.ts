// Code generated by nightseam. DO NOT EDIT.
import { DuplexError, callWire, emitWire, registerWire, wirePair, type WireModelContext } from "@nightseam/runtime";
import { encodePath, type Wire } from '@nightseam/duplex';
import type { AdapterContext, ValueAdapter, ValueContext } from "@nightseam/runtime";
import type { LiveOwner } from "@nightseam/live";
import { validateWire } from './types.ts';
import type * as Protocol from './types.ts';
import type { AnyFamily, FamilyBinding, Slots } from './types.ts';
import * as conversion from './types.ts';
import * as live_boxes from "@example/boxes-client";
import type * as boxes from "@example/boxes-client";
export * from './types.ts';
export { DuplexError };
export type { AdapterContext };
function makeAdapter(context: AdapterContext) {
  const observer = context.options?.observer;
  const propagator = context.options?.propagator;
  const requestTimeoutMs = context.options?.requestTimeoutMs;
  const bindings = {  };
  const slots: Slots = {  };
  const environment = context.valueEnvironment;
  if ((true) && !environment) throw new DuplexError('scope_closed', 'model adaptation requires an explicit value environment');
  function hasModelHandler(facet: object, name: string): boolean {
    for (let current = facet; current !== null && current !== Object.prototype; current = Object.getPrototypeOf(current)) {
      if (Object.prototype.hasOwnProperty.call(current, name)) return typeof (facet as Record<string, unknown>)[name] === 'function';
    }
    return false;
  }
  function proxyServer(wire: Wire): Protocol.Server {
    return {
      methods: {
        async name(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs ?? requestTimeoutMs, meta: context?.outgoingMeta, observer, propagator, family: "combinator" };
          validateWire({ empty: true }, params);
          const result = await callWire<string>(wire, ["name"], params, options);
          validateWire("string", result);
          return result;
        },
        async pack(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs ?? requestTimeoutMs, meta: context?.outgoingMeta, observer, propagator, family: "combinator" };
          const owner = (true || true) ? environment!.select((context as {valueContext?: unknown} | undefined)?.valueContext) : undefined;
          const result = await environment!.publish(owner, owner => environment!.export(owner, (owner) => { const converted = live_boxes.exportBox<Protocol.Unary>((params) as boxes.Box<Protocol.Unary>, (input: Protocol.Unary): unknown => conversion.exportUnary(owner as LiveOwner, (input) as Protocol.Unary)); validateWire({"apply":"boxes.Box","with":{"T":"Unary"}}, converted); return converted; }), sent => callWire(wire, ["pack"], sent, options));
          validateWire({"apply":"boxes.Batch","with":{"T":{"apply":"Bundle","with":{"T":"Count"}}}}, result);
          return environment!.import(owner, (owner) => live_boxes.importBatch<Protocol.Bundle<Protocol.Count>>(result, (input: unknown): Protocol.Bundle<Protocol.Count> => (conversion.importBundle<Protocol.Count>(owner as LiveOwner, input, (owner: LiveOwner, input: unknown): Protocol.Count => (input) as Protocol.Count)) as Protocol.Bundle<Protocol.Count>));
        },
        async toolkit(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs ?? requestTimeoutMs, meta: context?.outgoingMeta, observer, propagator, family: "combinator" };
          const owner = (true) ? environment!.select((context as {valueContext?: unknown} | undefined)?.valueContext) : undefined;
          const result = await callWire(wire, ["toolkit"], (() => { const converted = params; validateWire({"kind":"record","fields":[{"name":"seed","type":"Count","required":true}]}, converted); return converted; })(), options);
          validateWire("Toolkit", result);
          return environment!.import(owner, (owner) => conversion.importToolkit(owner as LiveOwner, result));
        },
      },
      events: {
      },
    };
  }
  function bindServer(wire: Wire, implementation: Protocol.Server): void {
    if (!implementation?.methods || !implementation.events) throw new Error('model methods and events are required');
    if (!hasModelHandler(implementation.methods, "name")) throw new Error("handler for name is required");
    if (!hasModelHandler(implementation.methods, "pack")) throw new Error("handler for pack is required");
    if (!hasModelHandler(implementation.methods, "toolkit")) throw new Error("handler for toolkit is required");
    const detach: Array<() => void> = [];
    try {
      detach.push(registerWire(wire, ["name"], {
        observer, family: "combinator",
        request: async (raw, context) => {
          try { validateWire({ empty: true }, raw); } catch (error) { throw new DuplexError('invalid_params', String(error)); }
          const params = raw;
          const result = await implementation.methods.name(params as Record<string, never>, context);
          return (() => { const converted = result; validateWire("string", converted); return converted; })();
        },
      }));
      detach.push(registerWire(wire, ["pack"], {
        observer, family: "combinator",
        request: async (raw, context) => {
          try { validateWire({"apply":"boxes.Box","with":{"T":"Unary"}}, raw); } catch (error) { throw new DuplexError('invalid_params', String(error)); }
          const owner = (true || true) ? environment!.child(undefined) : undefined; const ownedContext = Object.create(context) as WireModelContext & { valueContext: unknown }; if (owner !== undefined) Object.defineProperty(ownedContext, 'valueContext', { value: owner, enumerable: true });
          const params = environment!.import(owner, (owner) => live_boxes.importBox<Protocol.Unary>(raw, (input: unknown): Protocol.Unary => (conversion.importUnary(owner as LiveOwner, input)) as Protocol.Unary));
          const result = await implementation.methods.pack(params as boxes.Box<Protocol.Unary>, ownedContext);
          return environment!.export(owner, (owner) => { const converted = live_boxes.exportBatch<Protocol.Bundle<Protocol.Count>>((result) as boxes.Batch<Protocol.Bundle<Protocol.Count>>, (input: Protocol.Bundle<Protocol.Count>): unknown => conversion.exportBundle<Protocol.Count>(owner as LiveOwner, (input) as Protocol.Bundle<Protocol.Count>, (owner: LiveOwner, input: Protocol.Count): unknown => input)); validateWire({"apply":"boxes.Batch","with":{"T":{"apply":"Bundle","with":{"T":"Count"}}}}, converted); return converted; });
        },
      }));
      detach.push(registerWire(wire, ["toolkit"], {
        observer, family: "combinator",
        request: async (raw, context) => {
          try { validateWire({"kind":"record","fields":[{"name":"seed","type":"Count","required":true}]}, raw); } catch (error) { throw new DuplexError('invalid_params', String(error)); }
          const owner = (true) ? environment!.child(undefined) : undefined; const ownedContext = Object.create(context) as WireModelContext & { valueContext: unknown }; if (owner !== undefined) Object.defineProperty(ownedContext, 'valueContext', { value: owner, enumerable: true });
          const params = raw as Protocol.ToolkitRequest;
          const result = await implementation.methods.toolkit(params as Protocol.ToolkitRequest, ownedContext);
          return environment!.export(owner, (owner) => { const converted = conversion.exportToolkit(owner as LiveOwner, (result) as Protocol.Toolkit); validateWire("Toolkit", converted); return converted; });
        },
      }));
    } catch (error) { for (const remove of detach.reverse()) remove(); throw error; }
  }
  function proxyClient(wire: Wire): Protocol.Client {
    return {
      methods: {
      },
      events: {
      },
    };
  }
  function bindClient(wire: Wire, implementation: Protocol.Client): void {
    if (!implementation?.methods || !implementation.events) throw new Error('model methods and events are required');
    const detach: Array<() => void> = [];
    try {
    } catch (error) { for (const remove of detach.reverse()) remove(); throw error; }
  }
  return { proxyServer, proxyClient, bindServer, bindClient };
}
/** Exposes one model session at a new local wire origin. */
export function toWire(model: Protocol.ClientModel, context: AdapterContext): Wire {
  const adapter = makeAdapter(context);
  const [access, binding] = wirePair({ ...context.options, families: { ...context.options?.families, [encodePath(["name"])]: "combinator", [encodePath(["pack"])]: "combinator", [encodePath(["toolkit"])]: "combinator" } });
  try { const implementation = model(adapter.proxyServer(binding)); adapter.bindClient(binding, implementation); return access; } catch (error) { access.close(); throw error; }
}
/** Interprets a wire as the same model factory; the resulting session binds once. */
export async function fromWire(wire: Wire, context: AdapterContext): Promise<Protocol.ClientModel> {
  const adapter = makeAdapter(context);
  let bound = false;
  return remote => {
    if (bound) throw new DuplexError('already_bound', 'model already bound');
    bound = true;
    adapter.bindServer(wire, remote);
    return adapter.proxyClient(wire);
  };
}
export const errors = { refused: "refused" } as const;
export type ErrorCode = (typeof errors)[keyof typeof errors];
