// Code generated by nightseam. DO NOT EDIT.
import { createValidator, DuplexError, type AnyFamily, type FamilyBinding, type TypeBinding, type Slots, type TypeExpression, type WireFamily } from "@nightseam/runtime";
export type { AnyFamily, FamilyBinding, TypeBinding, Slots, TypeExpression };
import type * as boxes from "@example/boxes-client";
import { validateWire as validate_boxes } from "@example/boxes-client";
import * as live_boxes from "@example/boxes-client";
import type { ValueAdapter, ValueContext } from "@nightseam/runtime";
import { LiveOwner } from "@nightseam/live";
import type { WireModelContext } from "@nightseam/runtime";
export interface Bundle<T = unknown> {
  "metadata": BundleMetadata<T>;
  "run": Unary;
}
export interface BundleMetadata<T = unknown> {
  "seed": T;
}
/** What a unary callable takes and answers. */
export type Count = number;
/** One message of the nightseam.duplex/1 profile: the members the peer acts on, and nothing else. */
export interface Envelope {
  /** The profile's version, 1. */
  "version": number;
  /** request, response, event or cancel. */
  "kind": string;
  /** What correlates a response or a cancel with its request. */
  "id"?: string;
  /** The method a request names. */
  "method"?: string;
  /** A request's parameters. */
  "params"?: unknown;
  /** A response's result. */
  "result"?: unknown;
  /** A response's error. */
  "error"?: unknown;
  /** The event an event frame names. */
  "event"?: string;
  /** An event's data. */
  "data"?: unknown;
  /** The W3C Trace Context of the frame. */
  "traceparent"?: string;
  /** The vendor state of that trace. */
  "tracestate"?: string;
  /** What a request or an event carries about the call, delivered to the handler beside the payload. */
  "meta"?: Record<string, string>;
}
/** Higher order both ways: it takes a callable and answers one. */
/** A value of it is one implementation, called across the seam; each is its own binding, with its own lifetime. */
export type Factory = (request: Unary, options?: { signal?: AbortSignal; owner?: LiveOwner }) => Promise<Unary>;
/** A reference to a channel on the connection that carries the message holding it. */
export interface Handle {
  /** The channel's id on that connection. */
  "channel": number;
}
/** Higher order in the result alone. */
/** A value of it is one implementation, called across the seam; each is its own binding, with its own lifetime. */
export type Producer = (options?: { signal?: AbortSignal; owner?: LiveOwner }) => Promise<Unary>;
/** Higher order in the argument alone. */
/** A value of it is one implementation, called across the seam; each is its own binding, with its own lifetime. */
export type Sink = (request: Unary, options?: { signal?: AbortSignal; owner?: LiveOwner }) => Promise<void>;
/** The three carried together, so one call hands over a whole higher-order surface. */
export interface Toolkit {
  "twice": Factory;
  "identity": Producer;
  "apply": Sink;
}
export interface ToolkitRequest {
  "seed": Count;
}
/** A function of one number. */
/** A value of it is one implementation, called across the seam; each is its own binding, with its own lifetime. */
export type Unary = (request: Count, options?: { signal?: AbortSignal; owner?: LiveOwner }) => Promise<Count>;
/** The family: its name and the wire types a slot of it draws on. */
export interface Family { readonly name: "combinator"; Count: Count; Envelope: Envelope; Factory: Factory; Handle: Handle; Producer: Producer; Sink: Sink; Toolkit: Toolkit; ToolkitRequest: ToolkitRequest; Unary: Unary }
/** Writes Bundle using the supplied conversion for each type argument. */
export function exportBundle<T = unknown>(owner: LiveOwner, value: Bundle<T>, convert_T_: (owner: LiveOwner, value: T) => unknown): unknown {
  return owner.exportValue((owner) => {
    const out: Record<string, unknown> = {};
    out["metadata"] = exportBundleMetadata<T>((value["metadata"]) as BundleMetadata<T>, (input: T): unknown => convert_T_(owner, (input) as T));
    out["run"] = exportUnary(owner, (value["run"]) as Unary);
    return out;
  });
}
/** Reads Bundle using the supplied conversion for each type argument. */
export function importBundle<T = unknown>(owner: LiveOwner, raw: unknown, convert_T_: (owner: LiveOwner, value: unknown) => T): Bundle<T> {
  return owner.importValue((owner) => {
    const wire = raw as Record<string, unknown>;
    const out: Record<string, unknown> = {};
    out["metadata"] = importBundleMetadata<T>(wire["metadata"], (input: unknown): T => (convert_T_(owner, input)) as T);
    out["run"] = importUnary(owner, wire["run"]);
    return out as unknown as Bundle<T>;
  });
}
/** Writes BundleMetadata using the supplied conversion for each type argument. */
export function exportBundleMetadata<T = unknown>(value: BundleMetadata<T>, convert_T_: (value: T) => unknown): unknown {
  const out: Record<string, unknown> = {};
  out["seed"] = convert_T_((value["seed"]) as T);
  return out;
}
/** Reads BundleMetadata using the supplied conversion for each type argument. */
export function importBundleMetadata<T = unknown>(raw: unknown, convert_T_: (value: unknown) => T): BundleMetadata<T> {
  const wire = raw as Record<string, unknown>;
  const out: Record<string, unknown> = {};
  out["seed"] = convert_T_(wire["seed"]);
  return out as unknown as BundleMetadata<T>;
}
/** The declaration a reference to Factory carries. It is nominal: a reference is usable exactly where this callable is expected. */
export const contractFactory = "combinator/Factory";
/** Makes a binding of a local Factory and answers the reference that names it. */
export function exportFactory(owner: LiveOwner, value: Factory): unknown {
  return owner.exportValue((owner) => {
    const parent = owner;
    const reference = owner.export(contractFactory, wireDigest, async (request, options) => {
      const owner = parent.child();
      const context = { ...options, owner };
      validateWire("Unary", request);
      const argument = owner!.importValue((owner) => importUnary(owner, request));
      const result = await value(argument, context);
      return owner!.exportValue((owner) => { const converted = exportUnary(owner, (result) as Unary); validateWire("Unary", converted); return converted; });
    });
    return reference.toJSON();
  });
}
/** A Factory that calls the binding a reference names. */
export function importFactory(owner: LiveOwner, raw: unknown): Factory {
  const invoke = owner.import(owner.scope.decode(raw), contractFactory, wireDigest);
  const scope = owner.scope;
  return async (request: Unary, options?: { signal?: AbortSignal; owner?: LiveOwner }) => {
    const owner = options?.owner?.scope === scope ? options.owner : scope.owner();
    const result = await owner!.publishValue(owner => owner!.exportValue((owner) => { const converted = exportUnary(owner, (request) as Unary); validateWire("Unary", converted); return converted; }), sent => invoke(sent, options));
    validateWire("Unary", result);
    return owner!.importValue((owner) => importUnary(owner, result));
  };
}
/** The declaration a reference to Producer carries. It is nominal: a reference is usable exactly where this callable is expected. */
export const contractProducer = "combinator/Producer";
/** Makes a binding of a local Producer and answers the reference that names it. */
export function exportProducer(owner: LiveOwner, value: Producer): unknown {
  return owner.exportValue((owner) => {
    const parent = owner;
    const reference = owner.export(contractProducer, wireDigest, async (request, options) => {
      const owner = parent.child();
      const context = { ...options, owner };
      const result = await value(context);
      return owner!.exportValue((owner) => { const converted = exportUnary(owner, (result) as Unary); validateWire("Unary", converted); return converted; });
    });
    return reference.toJSON();
  });
}
/** A Producer that calls the binding a reference names. */
export function importProducer(owner: LiveOwner, raw: unknown): Producer {
  const invoke = owner.import(owner.scope.decode(raw), contractProducer, wireDigest);
  const scope = owner.scope;
  return async (options?: { signal?: AbortSignal; owner?: LiveOwner }) => {
    const owner = options?.owner?.scope === scope ? options.owner : scope.owner();
    const result = await invoke(undefined, options);
    validateWire("Unary", result);
    return owner!.importValue((owner) => importUnary(owner, result));
  };
}
/** The declaration a reference to Sink carries. It is nominal: a reference is usable exactly where this callable is expected. */
export const contractSink = "combinator/Sink";
/** Makes a binding of a local Sink and answers the reference that names it. */
export function exportSink(owner: LiveOwner, value: Sink): unknown {
  return owner.exportValue((owner) => {
    const parent = owner;
    const reference = owner.export(contractSink, wireDigest, async (request, options) => {
      const owner = parent.child();
      const context = { ...options, owner };
      validateWire("Unary", request);
      const argument = owner!.importValue((owner) => importUnary(owner, request));
      await value(argument, context);
      return undefined;
    });
    return reference.toJSON();
  });
}
/** A Sink that calls the binding a reference names. */
export function importSink(owner: LiveOwner, raw: unknown): Sink {
  const invoke = owner.import(owner.scope.decode(raw), contractSink, wireDigest);
  const scope = owner.scope;
  return async (request: Unary, options?: { signal?: AbortSignal; owner?: LiveOwner }) => {
    const owner = options?.owner?.scope === scope ? options.owner : scope.owner();
    await owner!.publishValue(owner => owner!.exportValue((owner) => { const converted = exportUnary(owner, (request) as Unary); validateWire("Unary", converted); return converted; }), sent => invoke(sent, options));
    return;
  };
}
/** Writes Toolkit as it travels: each callable in it becomes a binding of the owner, and the reference that names it takes its place. */
export function exportToolkit(owner: LiveOwner, value: Toolkit): unknown {
  return owner.exportValue((owner) => {
    const out: Record<string, unknown> = {};
    out["twice"] = exportFactory(owner, (value["twice"]) as Factory);
    out["identity"] = exportProducer(owner, (value["identity"]) as Producer);
    out["apply"] = exportSink(owner, (value["apply"]) as Sink);
    return out;
  });
}
/** Reads Toolkit as it arrived: each reference in it becomes a typed proxy of the binding it names, so a handler is given native values. */
export function importToolkit(owner: LiveOwner, raw: unknown): Toolkit {
  return owner.importValue((owner) => {
    const wire = raw as Record<string, unknown>;
    const out: Record<string, unknown> = {};
    out["twice"] = importFactory(owner, wire["twice"]);
    out["identity"] = importProducer(owner, wire["identity"]);
    out["apply"] = importSink(owner, wire["apply"]);
    return out as unknown as Toolkit;
  });
}
/** The declaration a reference to Unary carries. It is nominal: a reference is usable exactly where this callable is expected. */
export const contractUnary = "combinator/Unary";
/** Makes a binding of a local Unary and answers the reference that names it. */
export function exportUnary(owner: LiveOwner, value: Unary): unknown {
  return owner.exportValue((owner) => {
    const parent = owner;
    const reference = owner.export(contractUnary, wireDigest, async (request, options) => {
      const owner = parent.child();
      const context = { ...options, owner };
      validateWire("Count", request);
      const argument = request as Count;
      const result = await value(argument, context);
      return (() => { const converted = result; validateWire("Count", converted); return converted; })();
    });
    return reference.toJSON();
  });
}
/** A Unary that calls the binding a reference names. */
export function importUnary(owner: LiveOwner, raw: unknown): Unary {
  const invoke = owner.import(owner.scope.decode(raw), contractUnary, wireDigest);
  const scope = owner.scope;
  return async (request: Count, options?: { signal?: AbortSignal; owner?: LiveOwner }) => {
    const owner = options?.owner?.scope === scope ? options.owner : scope.owner();
    const result = await invoke((() => { const converted = request; validateWire("Count", converted); return converted; })(), options);
    validateWire("Count", result);
    return result as Count;
  };
}
export function adapterBundle<T = unknown>(slot_T: ValueAdapter<T>): ValueAdapter<Bundle<T>> {
  const slots: Slots = { "T": slot_T.binding };
  const binding: TypeBinding = { type: "Bundle", validate: validateWire, slots };
  const needsContext = true || slot_T.needsContext;
  return { binding, needsContext,
    export(context: unknown, value: Bundle<T>): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      const convert = (owner: LiveOwner): unknown => {
        const converted = exportBundle<T>(owner!, value, (owner: LiveOwner, value: T) => slot_T.export(owner, value));
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return context.exportValue(convert);
    },
    import(context: unknown, raw: unknown): Bundle<T> {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: LiveOwner): Bundle<T> => {
        const converted = importBundle<T>(owner!, raw, (owner: LiveOwner, value: unknown) => slot_T.import(owner, value));
        return converted;
      };
      return context.importValue(convert);
    },
  };
}
export function adapterBundleMetadata<T = unknown>(slot_T: ValueAdapter<T>): ValueAdapter<BundleMetadata<T>> {
  const slots: Slots = { "T": slot_T.binding };
  const binding: TypeBinding = { type: "BundleMetadata", validate: validateWire, slots };
  const needsContext = slot_T.needsContext;
  return { binding, needsContext,
    export(context: unknown, value: BundleMetadata<T>): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = exportBundleMetadata<T>(value, (value: T) => slot_T.export(owner, value));
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): BundleMetadata<T> {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): BundleMetadata<T> => {
        const converted = importBundleMetadata<T>(raw, (value: unknown) => slot_T.import(owner, value));
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterCount(): ValueAdapter<Count> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Count", validate: validateWire, slots };
  const needsContext = false;
  return { binding, needsContext,
    export(context: unknown, value: Count): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = value;
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Count {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Count => {
        const converted = raw as Count;
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterFactory(): ValueAdapter<Factory> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Factory", validate: validateWire, slots };
  const needsContext = true;
  return { binding, needsContext,
    export(context: unknown, value: Factory): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      const convert = (owner: LiveOwner): unknown => {
        const converted = exportFactory(owner!, value);
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return context.exportValue(convert);
    },
    import(context: unknown, raw: unknown): Factory {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: LiveOwner): Factory => {
        const converted = importFactory(owner!, raw);
        return converted;
      };
      return context.importValue(convert);
    },
  };
}
export function adapterProducer(): ValueAdapter<Producer> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Producer", validate: validateWire, slots };
  const needsContext = true;
  return { binding, needsContext,
    export(context: unknown, value: Producer): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      const convert = (owner: LiveOwner): unknown => {
        const converted = exportProducer(owner!, value);
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return context.exportValue(convert);
    },
    import(context: unknown, raw: unknown): Producer {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: LiveOwner): Producer => {
        const converted = importProducer(owner!, raw);
        return converted;
      };
      return context.importValue(convert);
    },
  };
}
export function adapterSink(): ValueAdapter<Sink> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Sink", validate: validateWire, slots };
  const needsContext = true;
  return { binding, needsContext,
    export(context: unknown, value: Sink): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      const convert = (owner: LiveOwner): unknown => {
        const converted = exportSink(owner!, value);
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return context.exportValue(convert);
    },
    import(context: unknown, raw: unknown): Sink {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: LiveOwner): Sink => {
        const converted = importSink(owner!, raw);
        return converted;
      };
      return context.importValue(convert);
    },
  };
}
export function adapterToolkit(): ValueAdapter<Toolkit> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Toolkit", validate: validateWire, slots };
  const needsContext = true;
  return { binding, needsContext,
    export(context: unknown, value: Toolkit): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      const convert = (owner: LiveOwner): unknown => {
        const converted = exportToolkit(owner!, value);
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return context.exportValue(convert);
    },
    import(context: unknown, raw: unknown): Toolkit {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: LiveOwner): Toolkit => {
        const converted = importToolkit(owner!, raw);
        return converted;
      };
      return context.importValue(convert);
    },
  };
}
export function adapterToolkitRequest(): ValueAdapter<ToolkitRequest> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "ToolkitRequest", validate: validateWire, slots };
  const needsContext = false;
  return { binding, needsContext,
    export(context: unknown, value: ToolkitRequest): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = value;
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): ToolkitRequest {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): ToolkitRequest => {
        const converted = raw as ToolkitRequest;
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterUnary(): ValueAdapter<Unary> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Unary", validate: validateWire, slots };
  const needsContext = true;
  return { binding, needsContext,
    export(context: unknown, value: Unary): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      const convert = (owner: LiveOwner): unknown => {
        const converted = exportUnary(owner!, value);
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return context.exportValue(convert);
    },
    import(context: unknown, raw: unknown): Unary {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: LiveOwner): Unary => {
        const converted = importUnary(owner!, raw);
        return converted;
      };
      return context.importValue(convert);
    },
  };
}
export interface ServerMethods {
  name(params: Record<string, never>, context?: WireModelContext): string | Promise<string>;
  pack(params: boxes.Box<Unary>, context?: WireModelContext & { valueContext: unknown }): boxes.Batch<Bundle<Count>> | Promise<boxes.Batch<Bundle<Count>>>;
  toolkit(params: ToolkitRequest, context?: WireModelContext & { valueContext: unknown }): Toolkit | Promise<Toolkit>;
}
export interface ServerEvents {
}
export interface Server { methods: ServerMethods; events: ServerEvents; }
export interface ClientMethods {
}
export interface ClientEvents {
}
export interface Client { methods: ClientMethods; events: ClientEvents; }
export type ServerModel = (remote: Client) => Server;
export type ClientModel = (remote: Server) => Client;

const contractTypes = {"types":{"Bundle":{"kind":"record","fields":[{"name":"metadata","type":{"kind":"record","fields":[{"name":"seed","type":"T","required":true}]},"required":true},{"name":"run","type":"Unary","required":true}],"parameters":[{"name":"T"}]},"Count":{"kind":"alias","type":"integer"},"Envelope":{"kind":"record","fields":[{"name":"version","type":"integer","required":true},{"name":"kind","type":"string","required":true},{"name":"id","type":"string","required":false},{"name":"method","type":"string","required":false},{"name":"params","type":"json","required":false},{"name":"result","type":"json","required":false},{"name":"error","type":"json","required":false},{"name":"event","type":"string","required":false},{"name":"data","type":"json","required":false},{"name":"traceparent","type":"string","required":false},{"name":"tracestate","type":"string","required":false},{"name":"meta","type":{"map":"string"},"required":false}]},"Factory":{"kind":"callable","contract":"combinator/Factory","request":"Unary","result":"Unary"},"Handle":{"kind":"record","fields":[{"name":"channel","type":"integer","required":true}]},"Producer":{"kind":"callable","contract":"combinator/Producer","result":"Unary"},"Sink":{"kind":"callable","contract":"combinator/Sink","request":"Unary"},"Toolkit":{"kind":"record","fields":[{"name":"twice","type":"Factory","required":true},{"name":"identity","type":"Producer","required":true},{"name":"apply","type":"Sink","required":true}]},"Unary":{"kind":"callable","contract":"combinator/Unary","request":"Count","result":"Count"}}} as unknown as WireFamily;
/** The SHA-256 digest of the family's exact wire description. */
export const wireDigest = "be0900855e894625d265fa3b6fb6fc2e0b750a0223394996ca7a21f475beb9c7";
/** Runtime validation applies equally to calls, replies, reverse calls and events; what fills a slot of a parameter is validated by the binding of the family that fills it. */
export const validateWire = createValidator(contractTypes, wireDigest, { "boxes": validate_boxes });
/** This family bound: its name and its validator, to fill a family slot in another family's client. */
export const family = { name: "combinator", validate: validateWire } as const;
