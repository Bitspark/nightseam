// Code generated by nightseam. DO NOT EDIT.
import { DuplexError, callWire, emitWire, registerWire, wirePair, declarationDigest, familyTypeAdapter, validateDrawnType, jsonAdapter, identityHandler, IDENTITY_METHOD, prepareIdentity, type WireModelContext, type WireCallOptions } from "@nightseam/runtime";
import { encodePath, type Wire } from '@nightseam/duplex';
import type { AdapterContext, ValueAdapter, ValueContext } from "@nightseam/runtime";
import { validateWire } from './types.ts';
import type * as Protocol from './types.ts';
import type { AnyFamily, FamilyBinding, Slots } from './types.ts';
import * as conversion from './types.ts';
export * from './types.ts';
export { DuplexError };
export type { AdapterContext };
function makeAdapter(context: AdapterContext) {
  const options = { ...context.options };
  const observer = options.observer;
  const propagator = options.propagator;
  const requestTimeoutMs = options.requestTimeoutMs;
  const bindings = {  };
  const slots: Slots = {  };
  const identity = { path: "workbench", digest: declarationDigest(validateWire, slots) };
  function hasModelHandler(facet: object, name: string): boolean {
    for (let current = facet; current !== null && current !== Object.prototype; current = Object.getPrototypeOf(current)) {
      if (Object.prototype.hasOwnProperty.call(current, name)) return typeof (facet as Record<string, unknown>)[name] === 'function';
    }
    return false;
  }
  function proxyServer(wire: Wire): Protocol.Server {
    return {
      methods: {
        async listEvents(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs ?? requestTimeoutMs, meta: context?.outgoingMeta, observer, propagator, family: "workbench" };
          validateWire("ListEventsParams", params);
          const result = await callWire<Array<Protocol.Event>>(wire, ["events.list"], params, options);
          validateWire({"array":"Event"}, result);
          return result;
        },
        async me(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs ?? requestTimeoutMs, meta: context?.outgoingMeta, observer, propagator, family: "workbench" };
          validateWire({ empty: true }, params);
          const result = await callWire<Protocol.User>(wire, ["me"], params, options);
          validateWire("User", result);
          return result;
        },
        async createProject(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs ?? requestTimeoutMs, meta: context?.outgoingMeta, observer, propagator, family: "workbench" };
          validateWire("CreateProjectParams", params);
          const result = await callWire<Protocol.Project>(wire, ["projects.create"], params, options);
          validateWire("Project", result);
          return result;
        },
        async listProjects(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs ?? requestTimeoutMs, meta: context?.outgoingMeta, observer, propagator, family: "workbench" };
          validateWire("ListProjectsParams", params);
          const result = await callWire<Array<Protocol.Project>>(wire, ["projects.list"], params, options);
          validateWire({"array":"Project"}, result);
          return result;
        },
        async updateProject(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs ?? requestTimeoutMs, meta: context?.outgoingMeta, observer, propagator, family: "workbench" };
          validateWire("UpdateProjectParams", params);
          const result = await callWire<Protocol.Project>(wire, ["projects.update"], params, options);
          validateWire("Project", result);
          return result;
        },
        async subscribe(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs ?? requestTimeoutMs, meta: context?.outgoingMeta, observer, propagator, family: "workbench" };
          validateWire("SubscribeParams", params);
          const result = await callWire<Protocol.SubscribeResult>(wire, ["subscribe"], params, options);
          validateWire("SubscribeResult", result);
          return result;
        },
        async cancelWorkItem(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs ?? requestTimeoutMs, meta: context?.outgoingMeta, observer, propagator, family: "workbench" };
          validateWire("CancelWorkItemParams", params);
          const result = await callWire<Protocol.WorkItem>(wire, ["work.cancel"], params, options);
          validateWire("WorkItem", result);
          return result;
        },
        async createWorkItem(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs ?? requestTimeoutMs, meta: context?.outgoingMeta, observer, propagator, family: "workbench" };
          validateWire("CreateWorkItemParams", params);
          const result = await callWire<Protocol.WorkItem>(wire, ["work.create"], params, options);
          validateWire("WorkItem", result);
          return result;
        },
        async setDependencies(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs ?? requestTimeoutMs, meta: context?.outgoingMeta, observer, propagator, family: "workbench" };
          validateWire("SetDependenciesParams", params);
          const result = await callWire<Protocol.WorkItem>(wire, ["work.dependencies"], params, options);
          validateWire("WorkItem", result);
          return result;
        },
        async getWorkItem(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs ?? requestTimeoutMs, meta: context?.outgoingMeta, observer, propagator, family: "workbench" };
          validateWire("GetWorkItemParams", params);
          const result = await callWire<Protocol.WorkItem>(wire, ["work.get"], params, options);
          validateWire("WorkItem", result);
          return result;
        },
        async listWorkItems(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs ?? requestTimeoutMs, meta: context?.outgoingMeta, observer, propagator, family: "workbench" };
          validateWire("ListWorkItemsParams", params);
          const result = await callWire<Array<Protocol.WorkItem>>(wire, ["work.list"], params, options);
          validateWire({"array":"WorkItem"}, result);
          return result;
        },
        async publishSpecification(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs ?? requestTimeoutMs, meta: context?.outgoingMeta, observer, propagator, family: "workbench" };
          validateWire("PublishSpecificationParams", params);
          const result = await callWire<Protocol.WorkItem>(wire, ["work.publish"], params, options);
          validateWire("WorkItem", result);
          return result;
        },
        async reopenWorkItem(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs ?? requestTimeoutMs, meta: context?.outgoingMeta, observer, propagator, family: "workbench" };
          validateWire("ReopenWorkItemParams", params);
          const result = await callWire<Protocol.WorkItem>(wire, ["work.reopen"], params, options);
          validateWire("WorkItem", result);
          return result;
        },
        async setSteps(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs ?? requestTimeoutMs, meta: context?.outgoingMeta, observer, propagator, family: "workbench" };
          validateWire("SetStepsParams", params);
          const result = await callWire<Protocol.WorkItem>(wire, ["work.steps"], params, options);
          validateWire("WorkItem", result);
          return result;
        },
        async updateWorkItem(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs ?? requestTimeoutMs, meta: context?.outgoingMeta, observer, propagator, family: "workbench" };
          validateWire("UpdateWorkItemParams", params);
          const result = await callWire<Protocol.WorkItem>(wire, ["work.update"], params, options);
          validateWire("WorkItem", result);
          return result;
        },
      },
      events: {
      },
    };
  }
  function validateServer(implementation: Protocol.Server): void {
    if (!implementation?.methods || !implementation.events) throw new Error('model methods and events are required');
    if (!hasModelHandler(implementation.methods, "listEvents")) throw new Error("handler for events.list is required");
    if (!hasModelHandler(implementation.methods, "me")) throw new Error("handler for me is required");
    if (!hasModelHandler(implementation.methods, "createProject")) throw new Error("handler for projects.create is required");
    if (!hasModelHandler(implementation.methods, "listProjects")) throw new Error("handler for projects.list is required");
    if (!hasModelHandler(implementation.methods, "updateProject")) throw new Error("handler for projects.update is required");
    if (!hasModelHandler(implementation.methods, "subscribe")) throw new Error("handler for subscribe is required");
    if (!hasModelHandler(implementation.methods, "cancelWorkItem")) throw new Error("handler for work.cancel is required");
    if (!hasModelHandler(implementation.methods, "createWorkItem")) throw new Error("handler for work.create is required");
    if (!hasModelHandler(implementation.methods, "setDependencies")) throw new Error("handler for work.dependencies is required");
    if (!hasModelHandler(implementation.methods, "getWorkItem")) throw new Error("handler for work.get is required");
    if (!hasModelHandler(implementation.methods, "listWorkItems")) throw new Error("handler for work.list is required");
    if (!hasModelHandler(implementation.methods, "publishSpecification")) throw new Error("handler for work.publish is required");
    if (!hasModelHandler(implementation.methods, "reopenWorkItem")) throw new Error("handler for work.reopen is required");
    if (!hasModelHandler(implementation.methods, "setSteps")) throw new Error("handler for work.steps is required");
    if (!hasModelHandler(implementation.methods, "updateWorkItem")) throw new Error("handler for work.update is required");
  }
  function bindServer(wire: Wire, implementation: () => Protocol.Server): () => void {
    const detach: Array<() => void> = [];
    try {
      detach.push(registerWire(wire, ["events.list"], {
        observer, family: "workbench",
        request: async (raw, context) => {
          const target = implementation();
          try { validateWire("ListEventsParams", raw); } catch (error) { if (error instanceof DuplexError && error.code === 'contract_mismatch') throw error; throw new DuplexError('invalid_params', String(error)); }
          const params = raw as Protocol.ListEventsParams;
          const result = await target.methods.listEvents(params as Protocol.ListEventsParams, context);
          return (() => { const converted = result; validateWire({"array":"Event"}, converted); return converted; })();
        },
      }));
      detach.push(registerWire(wire, ["me"], {
        observer, family: "workbench",
        request: async (raw, context) => {
          const target = implementation();
          try { validateWire({ empty: true }, raw); } catch (error) { if (error instanceof DuplexError && error.code === 'contract_mismatch') throw error; throw new DuplexError('invalid_params', String(error)); }
          const params = raw;
          const result = await target.methods.me(params as Record<string, never>, context);
          return (() => { const converted = result; validateWire("User", converted); return converted; })();
        },
      }));
      detach.push(registerWire(wire, ["projects.create"], {
        observer, family: "workbench",
        request: async (raw, context) => {
          const target = implementation();
          try { validateWire("CreateProjectParams", raw); } catch (error) { if (error instanceof DuplexError && error.code === 'contract_mismatch') throw error; throw new DuplexError('invalid_params', String(error)); }
          const params = raw as Protocol.CreateProjectParams;
          const result = await target.methods.createProject(params as Protocol.CreateProjectParams, context);
          return (() => { const converted = result; validateWire("Project", converted); return converted; })();
        },
      }));
      detach.push(registerWire(wire, ["projects.list"], {
        observer, family: "workbench",
        request: async (raw, context) => {
          const target = implementation();
          try { validateWire("ListProjectsParams", raw); } catch (error) { if (error instanceof DuplexError && error.code === 'contract_mismatch') throw error; throw new DuplexError('invalid_params', String(error)); }
          const params = raw as Protocol.ListProjectsParams;
          const result = await target.methods.listProjects(params as Protocol.ListProjectsParams, context);
          return (() => { const converted = result; validateWire({"array":"Project"}, converted); return converted; })();
        },
      }));
      detach.push(registerWire(wire, ["projects.update"], {
        observer, family: "workbench",
        request: async (raw, context) => {
          const target = implementation();
          try { validateWire("UpdateProjectParams", raw); } catch (error) { if (error instanceof DuplexError && error.code === 'contract_mismatch') throw error; throw new DuplexError('invalid_params', String(error)); }
          const params = raw as Protocol.UpdateProjectParams;
          const result = await target.methods.updateProject(params as Protocol.UpdateProjectParams, context);
          return (() => { const converted = result; validateWire("Project", converted); return converted; })();
        },
      }));
      detach.push(registerWire(wire, ["subscribe"], {
        observer, family: "workbench",
        request: async (raw, context) => {
          const target = implementation();
          try { validateWire("SubscribeParams", raw); } catch (error) { if (error instanceof DuplexError && error.code === 'contract_mismatch') throw error; throw new DuplexError('invalid_params', String(error)); }
          const params = raw as Protocol.SubscribeParams;
          const result = await target.methods.subscribe(params as Protocol.SubscribeParams, context);
          return (() => { const converted = result; validateWire("SubscribeResult", converted); return converted; })();
        },
      }));
      detach.push(registerWire(wire, ["work.cancel"], {
        observer, family: "workbench",
        request: async (raw, context) => {
          const target = implementation();
          try { validateWire("CancelWorkItemParams", raw); } catch (error) { if (error instanceof DuplexError && error.code === 'contract_mismatch') throw error; throw new DuplexError('invalid_params', String(error)); }
          const params = raw as Protocol.CancelWorkItemParams;
          const result = await target.methods.cancelWorkItem(params as Protocol.CancelWorkItemParams, context);
          return (() => { const converted = result; validateWire("WorkItem", converted); return converted; })();
        },
      }));
      detach.push(registerWire(wire, ["work.create"], {
        observer, family: "workbench",
        request: async (raw, context) => {
          const target = implementation();
          try { validateWire("CreateWorkItemParams", raw); } catch (error) { if (error instanceof DuplexError && error.code === 'contract_mismatch') throw error; throw new DuplexError('invalid_params', String(error)); }
          const params = raw as Protocol.CreateWorkItemParams;
          const result = await target.methods.createWorkItem(params as Protocol.CreateWorkItemParams, context);
          return (() => { const converted = result; validateWire("WorkItem", converted); return converted; })();
        },
      }));
      detach.push(registerWire(wire, ["work.dependencies"], {
        observer, family: "workbench",
        request: async (raw, context) => {
          const target = implementation();
          try { validateWire("SetDependenciesParams", raw); } catch (error) { if (error instanceof DuplexError && error.code === 'contract_mismatch') throw error; throw new DuplexError('invalid_params', String(error)); }
          const params = raw as Protocol.SetDependenciesParams;
          const result = await target.methods.setDependencies(params as Protocol.SetDependenciesParams, context);
          return (() => { const converted = result; validateWire("WorkItem", converted); return converted; })();
        },
      }));
      detach.push(registerWire(wire, ["work.get"], {
        observer, family: "workbench",
        request: async (raw, context) => {
          const target = implementation();
          try { validateWire("GetWorkItemParams", raw); } catch (error) { if (error instanceof DuplexError && error.code === 'contract_mismatch') throw error; throw new DuplexError('invalid_params', String(error)); }
          const params = raw as Protocol.GetWorkItemParams;
          const result = await target.methods.getWorkItem(params as Protocol.GetWorkItemParams, context);
          return (() => { const converted = result; validateWire("WorkItem", converted); return converted; })();
        },
      }));
      detach.push(registerWire(wire, ["work.list"], {
        observer, family: "workbench",
        request: async (raw, context) => {
          const target = implementation();
          try { validateWire("ListWorkItemsParams", raw); } catch (error) { if (error instanceof DuplexError && error.code === 'contract_mismatch') throw error; throw new DuplexError('invalid_params', String(error)); }
          const params = raw as Protocol.ListWorkItemsParams;
          const result = await target.methods.listWorkItems(params as Protocol.ListWorkItemsParams, context);
          return (() => { const converted = result; validateWire({"array":"WorkItem"}, converted); return converted; })();
        },
      }));
      detach.push(registerWire(wire, ["work.publish"], {
        observer, family: "workbench",
        request: async (raw, context) => {
          const target = implementation();
          try { validateWire("PublishSpecificationParams", raw); } catch (error) { if (error instanceof DuplexError && error.code === 'contract_mismatch') throw error; throw new DuplexError('invalid_params', String(error)); }
          const params = raw as Protocol.PublishSpecificationParams;
          const result = await target.methods.publishSpecification(params as Protocol.PublishSpecificationParams, context);
          return (() => { const converted = result; validateWire("WorkItem", converted); return converted; })();
        },
      }));
      detach.push(registerWire(wire, ["work.reopen"], {
        observer, family: "workbench",
        request: async (raw, context) => {
          const target = implementation();
          try { validateWire("ReopenWorkItemParams", raw); } catch (error) { if (error instanceof DuplexError && error.code === 'contract_mismatch') throw error; throw new DuplexError('invalid_params', String(error)); }
          const params = raw as Protocol.ReopenWorkItemParams;
          const result = await target.methods.reopenWorkItem(params as Protocol.ReopenWorkItemParams, context);
          return (() => { const converted = result; validateWire("WorkItem", converted); return converted; })();
        },
      }));
      detach.push(registerWire(wire, ["work.steps"], {
        observer, family: "workbench",
        request: async (raw, context) => {
          const target = implementation();
          try { validateWire("SetStepsParams", raw); } catch (error) { if (error instanceof DuplexError && error.code === 'contract_mismatch') throw error; throw new DuplexError('invalid_params', String(error)); }
          const params = raw as Protocol.SetStepsParams;
          const result = await target.methods.setSteps(params as Protocol.SetStepsParams, context);
          return (() => { const converted = result; validateWire("WorkItem", converted); return converted; })();
        },
      }));
      detach.push(registerWire(wire, ["work.update"], {
        observer, family: "workbench",
        request: async (raw, context) => {
          const target = implementation();
          try { validateWire("UpdateWorkItemParams", raw); } catch (error) { if (error instanceof DuplexError && error.code === 'contract_mismatch') throw error; throw new DuplexError('invalid_params', String(error)); }
          const params = raw as Protocol.UpdateWorkItemParams;
          const result = await target.methods.updateWorkItem(params as Protocol.UpdateWorkItemParams, context);
          return (() => { const converted = result; validateWire("WorkItem", converted); return converted; })();
        },
      }));
    } catch (error) { for (const remove of detach.reverse()) remove(); throw error; }
    return () => { for (const remove of detach.reverse()) remove(); };
  }
  function proxyClient(wire: Wire): Protocol.Client {
    return {
      methods: {
      },
      events: {
        async changed(data, context) {
          const options = { context, meta: context?.outgoingMeta, observer, propagator, family: "workbench" };
          validateWire("Event", data);
          emitWire(wire, ["workbench.changed"], data, options);
        },
      },
    };
  }
  function validateClient(implementation: Protocol.Client): void {
    if (!implementation?.methods || !implementation.events) throw new Error('model methods and events are required');
    if (!hasModelHandler(implementation.events, "changed")) throw new Error("event handler for workbench.changed is required");
  }
  function bindClient(wire: Wire, implementation: () => Protocol.Client): () => void {
    const detach: Array<() => void> = [];
    try {
      detach.push(registerWire(wire, ["workbench.changed"], {
        observer, family: "workbench",
        event: async (raw, context) => {
          const target = implementation();
          try { validateWire("Event", raw); } catch (error) { wire.close(); throw error; }
          await target.events.changed(raw as Protocol.Event, context);
        },
      }));
    } catch (error) { for (const remove of detach.reverse()) remove(); throw error; }
    return () => { for (const remove of detach.reverse()) remove(); };
  }
  return { options, identity, proxyServer, proxyClient, validateServer, validateClient, bindServer, bindClient };
}
/** Exposes one model session at a new local wire origin. */
export function toWire(model: Protocol.ClientModel, context: AdapterContext): Wire {
  const adapter = makeAdapter(context);
  const [access, binding] = wirePair({ ...adapter.options, families: { ...adapter.options.families, [encodePath(["events.list"])]: "workbench", [encodePath(["me"])]: "workbench", [encodePath(["projects.create"])]: "workbench", [encodePath(["projects.list"])]: "workbench", [encodePath(["projects.update"])]: "workbench", [encodePath(["subscribe"])]: "workbench", [encodePath(["work.cancel"])]: "workbench", [encodePath(["work.create"])]: "workbench", [encodePath(["work.dependencies"])]: "workbench", [encodePath(["work.get"])]: "workbench", [encodePath(["work.list"])]: "workbench", [encodePath(["work.publish"])]: "workbench", [encodePath(["work.reopen"])]: "workbench", [encodePath(["work.steps"])]: "workbench", [encodePath(["work.update"])]: "workbench", [encodePath(["workbench.changed"])]: "workbench" } });
  try {
    registerWire(binding, [IDENTITY_METHOD], { request: identityHandler(adapter.identity) });
    const implementation = model(adapter.proxyServer(binding));
    adapter.validateClient(implementation);
    adapter.bindClient(binding, () => implementation);
    return access;
  } catch (error) { access.close(); throw error; }
}
/** Registers receivers before reading begins; complete checks identity before returning the one-use model factory. Close detaches this interpretation without closing its carrier. */
export function prepareFromWire(wire: Wire, context: AdapterContext): { complete(options?: WireCallOptions): Promise<Protocol.ClientModel>; close(): void } {
  const adapter = makeAdapter(context);
  const gate = prepareIdentity(wire, adapter.identity, adapter.options);
  let implementation: Protocol.Server | undefined;
  let completed = false, bound = false, closed = false;
  const detach: Array<() => void> = [];
  const cleanup = () => { if (closed) return; closed = true; implementation = undefined; gate.close(); for (const remove of detach.reverse()) remove(); };
  try {
    detach.push(adapter.bindServer(gate.wire, () => implementation!));
  } catch (error) { cleanup(); throw error; }
  return {
    async complete(options?: WireCallOptions): Promise<Protocol.ClientModel> {
      if (closed) throw new DuplexError('scope_closed', 'model interpretation is closed');
      if (completed) throw new DuplexError('already_bound', 'model interpretation already completed');
      completed = true;
      try { await gate.check(options); } catch (error) { cleanup(); throw error; }
      return remote => {
        if (closed) throw new DuplexError('scope_closed', 'model interpretation is closed');
        if (bound) throw new DuplexError('already_bound', 'model already bound');
        bound = true;
        try {
          adapter.validateServer(remote);
          implementation = remote;
          gate.ready();
          return adapter.proxyClient(gate.wire);
        } catch (error) { cleanup(); throw error; }
      };
    },
    close: cleanup,
  };
}
/** Checks and interprets a wire as the same model factory; the resulting session binds once. */
export async function fromWire(wire: Wire, context: AdapterContext): Promise<Protocol.ClientModel> {
  const preparation = prepareFromWire(wire, context);
  try { return await preparation.complete(); } catch (error) { preparation.close(); throw error; }
}
export const errors = { canceled: "canceled", conflict: "conflict", deadlineExceeded: "deadline_exceeded", internal: "internal", invalidArgument: "invalid_argument", invalidParams: "invalid_params", invalidRequest: "invalid_request", methodNotFound: "method_not_found", notFound: "not_found", unauthenticated: "unauthenticated" } as const;
export type ErrorCode = (typeof errors)[keyof typeof errors];
