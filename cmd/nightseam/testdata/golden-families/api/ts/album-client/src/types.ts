// Code generated by nightseam. DO NOT EDIT.
import { createValidator, withDeclaration, DuplexError, type AnyFamily, type FamilyBinding, type TypeBinding, type Slots, type TypeExpression, type WireFamily } from "@nightseam/runtime";
export type { AnyFamily, FamilyBinding, TypeBinding, Slots, TypeExpression };
import type * as carrier from "@example/carrier-client";
import { validateWire as validate_carrier } from "@example/carrier-client";
import type * as probe from "@example/probe-client";
import { validateWire as validate_probe } from "@example/probe-client";
import * as live_carrier from "@example/carrier-client";
import type { ValueAdapter, ValueContext } from "@nightseam/runtime";
import type { WireModelContext } from "@nightseam/runtime";
export interface Borrowed<B extends AnyFamily = AnyFamily> {
  "frame": carrier.Frame<B>;
}
export interface Both<A extends AnyFamily = AnyFamily, B extends AnyFamily = AnyFamily> {
  "mine": Mine<A>;
  "borrowed": Borrowed<B>;
  "fixed": Fixed;
}
/** One message of the nightseam.duplex/1 profile: the members the peer acts on, and nothing else. */
export interface Envelope {
  /** The profile's version, 1. */
  "version": number;
  /** request, response, event or cancel. */
  "kind": string;
  /** What correlates a response or a cancel with its request. */
  "id"?: string;
  /** The method a request names. */
  "method"?: string;
  /** A request's parameters. */
  "params"?: unknown;
  /** A response's result. */
  "result"?: unknown;
  /** A response's error. */
  "error"?: unknown;
  /** The event an event frame names. */
  "event"?: string;
  /** An event's data. */
  "data"?: unknown;
  /** The W3C Trace Context of the frame. */
  "traceparent"?: string;
  /** The vendor state of that trace. */
  "tracestate"?: string;
  /** What a request or an event carries about the call, delivered to the handler beside the payload. */
  "meta"?: Record<string, string>;
}
export interface Fixed {
  "frame": carrier.Frame<probe.Family>;
}
/** A reference to a channel on the connection that carries the message holding it. */
export interface Handle {
  /** The channel's id on that connection. */
  "channel": number;
}
export interface Mine<A extends AnyFamily = AnyFamily> {
  "held": A["Envelope"];
}
/** The family: its name and the wire types a slot of it draws on. */
export interface Family { readonly name: "album"; Envelope: Envelope; Fixed: Fixed; Handle: Handle }
/** Writes Borrowed using the supplied conversion for each type argument. */
export function exportBorrowed<B extends AnyFamily = AnyFamily>(value: Borrowed<B>, convert_B_Envelope: (value: B["Envelope"]) => unknown): unknown {
  const out: Record<string, unknown> = {};
  out["frame"] = live_carrier.exportFrame<B>((value["frame"]) as carrier.Frame<B>, (input: B["Envelope"]): unknown => convert_B_Envelope((input) as B["Envelope"]));
  return out;
}
/** Reads Borrowed using the supplied conversion for each type argument. */
export function importBorrowed<B extends AnyFamily = AnyFamily>(raw: unknown, convert_B_Envelope: (value: unknown) => B["Envelope"]): Borrowed<B> {
  const wire = raw as Record<string, unknown>;
  const out: Record<string, unknown> = {};
  out["frame"] = live_carrier.importFrame<B>(wire["frame"], (input: unknown): B["Envelope"] => (convert_B_Envelope(input)) as B["Envelope"]);
  return out as unknown as Borrowed<B>;
}
/** Writes Both using the supplied conversion for each type argument. */
export function exportBoth<A extends AnyFamily = AnyFamily, B extends AnyFamily = AnyFamily>(value: Both<A, B>, convert_A_Envelope: (value: A["Envelope"]) => unknown, convert_B_Envelope: (value: B["Envelope"]) => unknown): unknown {
  const out: Record<string, unknown> = {};
  out["mine"] = exportMine<A>((value["mine"]) as Mine<A>, (input: A["Envelope"]): unknown => convert_A_Envelope((input) as A["Envelope"]));
  out["borrowed"] = exportBorrowed<B>((value["borrowed"]) as Borrowed<B>, (input: B["Envelope"]): unknown => convert_B_Envelope((input) as B["Envelope"]));
  out["fixed"] = value["fixed"];
  return out;
}
/** Reads Both using the supplied conversion for each type argument. */
export function importBoth<A extends AnyFamily = AnyFamily, B extends AnyFamily = AnyFamily>(raw: unknown, convert_A_Envelope: (value: unknown) => A["Envelope"], convert_B_Envelope: (value: unknown) => B["Envelope"]): Both<A, B> {
  const wire = raw as Record<string, unknown>;
  const out: Record<string, unknown> = {};
  out["mine"] = importMine<A>(wire["mine"], (input: unknown): A["Envelope"] => (convert_A_Envelope(input)) as A["Envelope"]);
  out["borrowed"] = importBorrowed<B>(wire["borrowed"], (input: unknown): B["Envelope"] => (convert_B_Envelope(input)) as B["Envelope"]);
  out["fixed"] = wire["fixed"];
  return out as unknown as Both<A, B>;
}
/** Writes Mine using the supplied conversion for each type argument. */
export function exportMine<A extends AnyFamily = AnyFamily>(value: Mine<A>, convert_A_Envelope: (value: A["Envelope"]) => unknown): unknown {
  const out: Record<string, unknown> = {};
  out["held"] = convert_A_Envelope((value["held"]) as A["Envelope"]);
  return out;
}
/** Reads Mine using the supplied conversion for each type argument. */
export function importMine<A extends AnyFamily = AnyFamily>(raw: unknown, convert_A_Envelope: (value: unknown) => A["Envelope"]): Mine<A> {
  const wire = raw as Record<string, unknown>;
  const out: Record<string, unknown> = {};
  out["held"] = convert_A_Envelope(wire["held"]);
  return out as unknown as Mine<A>;
}
export function adapterBorrowed<B extends AnyFamily = AnyFamily>(slot_B: FamilyBinding<B>): ValueAdapter<Borrowed<B>> {
  const slots: Slots = { "B": slot_B };
  const binding: TypeBinding = { type: "Borrowed", validate: validateWire, slots };
  const needsContext = false;
  return { binding, needsContext,
    export(context: unknown, value: Borrowed<B>): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = exportBorrowed<B>(value, (value: B["Envelope"]) => value);
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Borrowed<B> {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Borrowed<B> => {
        const converted = importBorrowed<B>(raw, (value: unknown) => value as B["Envelope"]);
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterBoth<A extends AnyFamily = AnyFamily, B extends AnyFamily = AnyFamily>(slot_A: FamilyBinding<A>, slot_B: FamilyBinding<B>): ValueAdapter<Both<A, B>> {
  const slots: Slots = { "A": slot_A, "B": slot_B };
  const binding: TypeBinding = { type: "Both", validate: validateWire, slots };
  const needsContext = false;
  return { binding, needsContext,
    export(context: unknown, value: Both<A, B>): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = exportBoth<A, B>(value, (value: A["Envelope"]) => value, (value: B["Envelope"]) => value);
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Both<A, B> {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Both<A, B> => {
        const converted = importBoth<A, B>(raw, (value: unknown) => value as A["Envelope"], (value: unknown) => value as B["Envelope"]);
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterFixed(): ValueAdapter<Fixed> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Fixed", validate: validateWire, slots };
  const needsContext = false;
  return { binding, needsContext,
    export(context: unknown, value: Fixed): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = value;
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Fixed {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Fixed => {
        const converted = raw as Fixed;
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterMine<A extends AnyFamily = AnyFamily>(slot_A: FamilyBinding<A>): ValueAdapter<Mine<A>> {
  const slots: Slots = { "A": slot_A };
  const binding: TypeBinding = { type: "Mine", validate: validateWire, slots };
  const needsContext = false;
  return { binding, needsContext,
    export(context: unknown, value: Mine<A>): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = exportMine<A>(value, (value: A["Envelope"]) => value);
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Mine<A> {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Mine<A> => {
        const converted = importMine<A>(raw, (value: unknown) => value as A["Envelope"]);
        return converted;
      };
      return convert(context);
    },
  };
}
export interface ServerMethods<A extends AnyFamily = AnyFamily, B extends AnyFamily = AnyFamily> {
  look(params: Mine<A>, context?: WireModelContext): Both<A, B> | Promise<Both<A, B>>;
}
export interface ServerEvents<A extends AnyFamily = AnyFamily, B extends AnyFamily = AnyFamily> {
}
export interface Server<A extends AnyFamily = AnyFamily, B extends AnyFamily = AnyFamily> { methods: ServerMethods<A, B>; events: ServerEvents<A, B>; }
export interface ClientMethods<A extends AnyFamily = AnyFamily, B extends AnyFamily = AnyFamily> {
}
export interface ClientEvents<A extends AnyFamily = AnyFamily, B extends AnyFamily = AnyFamily> {
}
export interface Client<A extends AnyFamily = AnyFamily, B extends AnyFamily = AnyFamily> { methods: ClientMethods<A, B>; events: ClientEvents<A, B>; }
export type ServerModel<A extends AnyFamily = AnyFamily, B extends AnyFamily = AnyFamily> = (remote: Client<A, B>) => Server<A, B>;
export type ClientModel<A extends AnyFamily = AnyFamily, B extends AnyFamily = AnyFamily> = (remote: Server<A, B>) => Client<A, B>;

const contractTypes = {"types":{"Borrowed":{"kind":"record","fields":[{"name":"frame","type":{"apply":"carrier.Frame","with":{"S":"B"}},"required":true}]},"Both":{"kind":"record","fields":[{"name":"mine","type":"Mine","required":true},{"name":"borrowed","type":"Borrowed","required":true},{"name":"fixed","type":"Fixed","required":true}]},"Envelope":{"kind":"record","fields":[{"name":"version","type":"integer","required":true},{"name":"kind","type":"string","required":true},{"name":"id","type":"string","required":false},{"name":"method","type":"string","required":false},{"name":"params","type":"json","required":false},{"name":"result","type":"json","required":false},{"name":"error","type":"json","required":false},{"name":"event","type":"string","required":false},{"name":"data","type":"json","required":false},{"name":"traceparent","type":"string","required":false},{"name":"tracestate","type":"string","required":false},{"name":"meta","type":{"map":"string"},"required":false}]},"Fixed":{"kind":"record","fields":[{"name":"frame","type":{"apply":"carrier.Frame","with":{"S":"probe"}},"required":true}]},"Handle":{"kind":"record","fields":[{"name":"channel","type":"integer","required":true}]},"Mine":{"kind":"record","fields":[{"name":"held","type":"A.Envelope","required":true}]}},"parameters":[{"name":"A","of":"protocol"},{"name":"B","of":"protocol"}]} as unknown as WireFamily;
/** Canonical language-neutral declaration bytes, the input to wireDigest. */
export const wireDeclaration = "{\"definitions\":{\"album\":{\"client\":{\"ref\":\"album/$client\"},\"errors\":[],\"kind\":\"family\",\"parameters\":[{\"name\":\"A\",\"of\":\"protocol\"},{\"name\":\"B\",\"of\":\"protocol\"}],\"server\":{\"ref\":\"album/$server\"},\"types\":{\"Borrowed\":{\"ref\":\"album/Borrowed\"},\"Both\":{\"ref\":\"album/Both\"},\"Fixed\":{\"ref\":\"album/Fixed\"},\"Mine\":{\"ref\":\"album/Mine\"}}},\"album/$client\":{\"direction\":\"client\",\"events\":{},\"extends\":[],\"kind\":\"side\",\"methods\":{},\"parameters\":[{\"name\":\"A\",\"of\":\"protocol\"},{\"name\":\"B\",\"of\":\"protocol\"}]},\"album/$server\":{\"direction\":\"server\",\"events\":{},\"extends\":[],\"kind\":\"side\",\"methods\":{\"look\":{\"errors\":[],\"request\":{\"ref\":\"album/Mine\"},\"result\":{\"ref\":\"album/Both\"}}},\"parameters\":[{\"name\":\"A\",\"of\":\"protocol\"},{\"name\":\"B\",\"of\":\"protocol\"}]},\"album/Borrowed\":{\"captures\":[{\"name\":\"B\",\"of\":\"protocol\"}],\"fields\":[{\"name\":\"frame\",\"nullable\":false,\"required\":true,\"type\":{\"apply\":\"carrier/Frame\",\"arguments\":[{\"parameter\":\"album/B\"}]},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]},\"album/Both\":{\"captures\":[{\"name\":\"A\",\"of\":\"protocol\"},{\"name\":\"B\",\"of\":\"protocol\"}],\"fields\":[{\"name\":\"mine\",\"nullable\":false,\"required\":true,\"type\":{\"ref\":\"album/Mine\"},\"unique\":false},{\"name\":\"borrowed\",\"nullable\":false,\"required\":true,\"type\":{\"ref\":\"album/Borrowed\"},\"unique\":false},{\"name\":\"fixed\",\"nullable\":false,\"required\":true,\"type\":{\"ref\":\"album/Fixed\"},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]},\"album/Fixed\":{\"fields\":[{\"name\":\"frame\",\"nullable\":false,\"required\":true,\"type\":{\"apply\":\"carrier/Frame\",\"arguments\":[{\"ref\":\"probe\"}]},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]},\"album/Mine\":{\"captures\":[{\"name\":\"A\",\"of\":\"protocol\"}],\"fields\":[{\"name\":\"held\",\"nullable\":false,\"required\":true,\"type\":{\"draw\":{\"parameter\":\"album/A\"},\"name\":\"Envelope\"},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]},\"carrier/Frame\":{\"captures\":[{\"name\":\"S\",\"of\":\"protocol\"}],\"fields\":[{\"name\":\"sequence\",\"nullable\":false,\"required\":true,\"type\":{\"primitive\":\"integer\"},\"unique\":false},{\"name\":\"message\",\"nullable\":false,\"required\":true,\"type\":{\"draw\":{\"parameter\":\"carrier/S\"},\"name\":\"Envelope\"},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]},\"probe\":{\"client\":{\"ref\":\"probe/$client\"},\"errors\":[\"denied\"],\"kind\":\"family\",\"parameters\":[],\"server\":{\"ref\":\"probe/$server\"},\"types\":{\"Base\":{\"ref\":\"probe/Base\"},\"OpenRecord\":{\"ref\":\"probe/OpenRecord\"},\"Payload\":{\"ref\":\"probe/Payload\"},\"Payloads\":{\"array\":{\"ref\":\"probe/Payload\"}},\"Status\":{\"ref\":\"probe/Status\"}}},\"probe/$client\":{\"direction\":\"client\",\"events\":{},\"extends\":[],\"kind\":\"side\",\"methods\":{\"reverse\":{\"errors\":[],\"request\":{\"ref\":\"probe/Payload\"},\"result\":{\"ref\":\"probe/Payload\"}}},\"parameters\":[]},\"probe/$server\":{\"direction\":\"server\",\"events\":{\"changed\":{\"ref\":\"probe/Payload\"}},\"extends\":[],\"kind\":\"side\",\"methods\":{\"echo\":{\"errors\":[],\"request\":{\"ref\":\"probe/Payload\"},\"result\":{\"ref\":\"probe/Payload\"}},\"no_args\":{\"errors\":[],\"request\":{\"empty\":true},\"result\":{\"primitive\":\"string\"}}},\"parameters\":[]},\"probe/Base\":{\"fields\":[{\"name\":\"text\",\"nullable\":false,\"required\":true,\"type\":{\"primitive\":\"string\"},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]},\"probe/OpenRecord\":{\"fields\":[{\"name\":\"id\",\"nullable\":false,\"required\":true,\"type\":{\"primitive\":\"string\"},\"unique\":false},{\"name\":\"note\",\"nullable\":false,\"required\":false,\"type\":{\"primitive\":\"string\"},\"unique\":false}],\"kind\":\"record\",\"open\":true,\"parameters\":[]},\"probe/Payload\":{\"extends\":[{\"ref\":\"probe/Base\"}],\"fields\":[{\"name\":\"count\",\"nullable\":false,\"required\":true,\"type\":{\"primitive\":\"integer\"},\"unique\":false},{\"name\":\"note\",\"nullable\":true,\"required\":false,\"type\":{\"primitive\":\"string\"},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]},\"probe/Status\":{\"kind\":\"enum\",\"parameters\":[],\"values\":[\"context.example\",\"done\",\"ready\"]}},\"root\":{\"ref\":\"album\"},\"version\":1}";
/** SHA-256 of the canonical full declaration's exact UTF-8 bytes. */
export const wireDigest = "31193ffdbacccfc0a5df5ede7d7740c5af9a6107e2bb73d4da7a4b153a894b01";
/** Runtime validation applies equally to calls, replies, reverse calls and events; what fills a slot of a parameter is validated by the binding of the family that fills it. */
export const validateWire = withDeclaration(createValidator(contractTypes, wireDigest, { "carrier": validate_carrier, "probe": validate_probe }), wireDeclaration);
/** This family bound: its name and its validator, to fill a family slot in another family's client. */
export const family = { name: "album", validate: validateWire } as const;
