// Code generated by nightseam. DO NOT EDIT.
import { createValidator, withDeclaration, jsonAdapter, familyTypeAdapter, DuplexError, type AnyFamily, type FamilyBinding, type TypeBinding, type Slots, type TypeExpression, type WireFamily } from "@nightseam/runtime";
export type { AnyFamily, FamilyBinding, TypeBinding, Slots, TypeExpression };
import type { ValueAdapter, ValueContext } from "@nightseam/runtime";
export type Batch<T = unknown> = Page<Result<Box<Record<string, T | null>>>>;
export interface Box<T = unknown> {
  "item": T;
}
export type Choice<T = unknown> = { "kind": "none" } | { "kind": "some"; "value": T };
export interface Page<T = unknown> {
  "items": Array<T>;
  "next"?: T | null;
  [key: string]: unknown;
}
export type Result<T = unknown> = { "kind": "none" } | { "kind": "some"; "value": T } | { "kind": "error"; "value": string };
/** The family: its name and the wire types a slot of it draws on. */
export interface Family { readonly name: "boxes";  }
/** Conversion only: use the value adapter or validate the entire wire value inside the same operation batch. */
/** Writes Batch using the supplied conversion for each type argument. */
export function exportBatchUnchecked<T = unknown>(value: Batch<T>, convert_T_: (value: T) => unknown): unknown {
  return exportPageUnchecked<Result<Box<Record<string, T | null>>>>((value) as Page<Result<Box<Record<string, T | null>>>>, (input: Result<Box<Record<string, T | null>>>): unknown => exportResultUnchecked<Box<Record<string, T | null>>>((input) as Result<Box<Record<string, T | null>>>, (input: Box<Record<string, T | null>>): unknown => exportBoxUnchecked<Record<string, T | null>>((input) as Box<Record<string, T | null>>, (input: Record<string, T | null>): unknown => Object.fromEntries(Object.entries(input as Record<string, unknown>).map(([key, item]) => [key, (item === null ? null : convert_T_((item) as T))])))));
}
/** Reads Batch using the supplied conversion for each type argument. */
/** Conversion only: validation belongs to the value adapter or its caller. */
export function importBatchUnchecked<T = unknown>(raw: unknown, convert_T_: (value: unknown) => T): Batch<T> {
  return importPageUnchecked<Result<Box<Record<string, T | null>>>>(raw, (input: unknown): Result<Box<Record<string, T | null>>> => (importResultUnchecked<Box<Record<string, T | null>>>(input, (input: unknown): Box<Record<string, T | null>> => (importBoxUnchecked<Record<string, T | null>>(input, (input: unknown): Record<string, T | null> => (Object.fromEntries(Object.entries(input as Record<string, unknown>).map(([key, item]) => [key, (item === null ? null : convert_T_(item))]))) as Record<string, T | null>)) as Box<Record<string, T | null>>)) as Result<Box<Record<string, T | null>>>);
}
/** Conversion only: use the value adapter or validate the entire wire value inside the same operation batch. */
/** Writes Box using the supplied conversion for each type argument. */
export function exportBoxUnchecked<T = unknown>(value: Box<T>, convert_T_: (value: T) => unknown): unknown {
  const out: Record<string, unknown> = {};
  out["item"] = convert_T_((value["item"]) as T);
  return out;
}
/** Reads Box using the supplied conversion for each type argument. */
/** Conversion only: validation belongs to the value adapter or its caller. */
export function importBoxUnchecked<T = unknown>(raw: unknown, convert_T_: (value: unknown) => T): Box<T> {
  const wire = raw as Record<string, unknown>;
  const out: Record<string, unknown> = {};
  out["item"] = convert_T_(wire["item"]);
  return out as unknown as Box<T>;
}
/** Conversion only: use the value adapter or validate the entire wire value inside the same operation batch. */
/** Writes Choice using the supplied conversion for each type argument. */
export function exportChoiceUnchecked<T = unknown>(value: Choice<T>, convert_T_: (value: T) => unknown): unknown {
  const held = value as Record<string, unknown>;
  switch (held["kind"]) {
    case "none": return { "kind": "none" };
    case "some": return { "kind": "some", "value": convert_T_((held["value"]) as T) };
  }
  throw new Error("Choice: unknown variant " + String(held["kind"]));
}
/** Reads Choice using the supplied conversion for each type argument. */
/** Conversion only: validation belongs to the value adapter or its caller. */
export function importChoiceUnchecked<T = unknown>(raw: unknown, convert_T_: (value: unknown) => T): Choice<T> {
  const held = raw as Record<string, unknown>;
  switch (held["kind"]) {
    case "none": return { "kind": "none" } as unknown as Choice<T>;
    case "some": return { "kind": "some", "value": convert_T_(held["value"]) } as unknown as Choice<T>;
  }
  throw new Error("Choice: unknown variant " + String(held["kind"]));
}
/** Conversion only: use the value adapter or validate the entire wire value inside the same operation batch. */
/** Writes Page using the supplied conversion for each type argument. */
export function exportPageUnchecked<T = unknown>(value: Page<T>, convert_T_: (value: T) => unknown): unknown {
  const out: Record<string, unknown> = { ...value };
  out["items"] = (value["items"] as unknown[]).map((item) => convert_T_((item) as T));
  if (value["next"] !== undefined) out["next"] = (value["next"] === null ? null : convert_T_((value["next"]) as T));
  return out;
}
/** Reads Page using the supplied conversion for each type argument. */
/** Conversion only: validation belongs to the value adapter or its caller. */
export function importPageUnchecked<T = unknown>(raw: unknown, convert_T_: (value: unknown) => T): Page<T> {
  const wire = raw as Record<string, unknown>;
  const out: Record<string, unknown> = { ...wire };
  out["items"] = (wire["items"] as unknown[]).map((item) => convert_T_(item));
  if (wire["next"] !== undefined) out["next"] = (wire["next"] === null ? null : convert_T_(wire["next"]));
  return out as unknown as Page<T>;
}
/** Conversion only: use the value adapter or validate the entire wire value inside the same operation batch. */
/** Writes Result using the supplied conversion for each type argument. */
export function exportResultUnchecked<T = unknown>(value: Result<T>, convert_T_: (value: T) => unknown): unknown {
  const held = value as Record<string, unknown>;
  switch (held["kind"]) {
    case "none": return { "kind": "none" };
    case "some": return { "kind": "some", "value": convert_T_((held["value"]) as T) };
    case "error": return { "kind": "error", "value": held["value"] };
  }
  throw new Error("Result: unknown variant " + String(held["kind"]));
}
/** Reads Result using the supplied conversion for each type argument. */
/** Conversion only: validation belongs to the value adapter or its caller. */
export function importResultUnchecked<T = unknown>(raw: unknown, convert_T_: (value: unknown) => T): Result<T> {
  const held = raw as Record<string, unknown>;
  switch (held["kind"]) {
    case "none": return { "kind": "none" } as unknown as Result<T>;
    case "some": return { "kind": "some", "value": convert_T_(held["value"]) } as unknown as Result<T>;
    case "error": return { "kind": "error", "value": held["value"] } as unknown as Result<T>;
  }
  throw new Error("Result: unknown variant " + String(held["kind"]));
}
export function adapterBatch<T = unknown>(slot_T: ValueAdapter<T>): ValueAdapter<Batch<T>> {
  const slots: Slots = { "T": slot_T.binding };
  const binding: TypeBinding = { type: "Batch", validate: validateWire, slots };
  const needsContext = slot_T.needsContext;
  return { binding, needsContext,
    export(context: unknown, value: Batch<T>): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = exportBatchUnchecked<T>(value, (value: T) => slot_T.export(owner, value));
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Batch<T> {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Batch<T> => {
        const converted = importBatchUnchecked<T>(raw, (value: unknown) => slot_T.import(owner, value));
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterBox<T = unknown>(slot_T: ValueAdapter<T>): ValueAdapter<Box<T>> {
  const slots: Slots = { "T": slot_T.binding };
  const binding: TypeBinding = { type: "Box", validate: validateWire, slots };
  const needsContext = slot_T.needsContext;
  return { binding, needsContext,
    export(context: unknown, value: Box<T>): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = exportBoxUnchecked<T>(value, (value: T) => slot_T.export(owner, value));
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Box<T> {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Box<T> => {
        const converted = importBoxUnchecked<T>(raw, (value: unknown) => slot_T.import(owner, value));
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterChoice<T = unknown>(slot_T: ValueAdapter<T>): ValueAdapter<Choice<T>> {
  const slots: Slots = { "T": slot_T.binding };
  const binding: TypeBinding = { type: "Choice", validate: validateWire, slots };
  const needsContext = slot_T.needsContext;
  return { binding, needsContext,
    export(context: unknown, value: Choice<T>): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = exportChoiceUnchecked<T>(value, (value: T) => slot_T.export(owner, value));
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Choice<T> {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Choice<T> => {
        const converted = importChoiceUnchecked<T>(raw, (value: unknown) => slot_T.import(owner, value));
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterPage<T = unknown>(slot_T: ValueAdapter<T>): ValueAdapter<Page<T>> {
  const slots: Slots = { "T": slot_T.binding };
  const binding: TypeBinding = { type: "Page", validate: validateWire, slots };
  const needsContext = slot_T.needsContext;
  return { binding, needsContext,
    export(context: unknown, value: Page<T>): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = exportPageUnchecked<T>(value, (value: T) => slot_T.export(owner, value));
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Page<T> {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Page<T> => {
        const converted = importPageUnchecked<T>(raw, (value: unknown) => slot_T.import(owner, value));
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterResult<T = unknown>(slot_T: ValueAdapter<T>): ValueAdapter<Result<T>> {
  const slots: Slots = { "T": slot_T.binding };
  const binding: TypeBinding = { type: "Result", validate: validateWire, slots };
  const needsContext = slot_T.needsContext;
  return { binding, needsContext,
    export(context: unknown, value: Result<T>): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = exportResultUnchecked<T>(value, (value: T) => slot_T.export(owner, value));
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Result<T> {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Result<T> => {
        const converted = importResultUnchecked<T>(raw, (value: unknown) => slot_T.import(owner, value));
        return converted;
      };
      return convert(context);
    },
  };
}

const contractTypes = {"types":{"Batch":{"kind":"alias","type":{"apply":"Page","with":{"T":{"apply":"Result","with":{"T":{"apply":"Box","with":{"T":{"map":{"nullable":"T"}}}}}}}},"parameters":[{"name":"T"}]},"Box":{"kind":"record","fields":[{"name":"item","type":"T","required":true}],"parameters":[{"name":"T"}]},"Choice":{"kind":"union","parameters":[{"name":"T"}],"tag":"kind","variants":{"none":{"empty":true},"some":"T"}},"Page":{"kind":"record","fields":[{"name":"items","type":{"array":"T"},"required":true},{"name":"next","type":{"nullable":"T"},"required":false}],"open":true,"parameters":[{"name":"T"}]},"Result":{"kind":"union","extends":[{"apply":"Choice","with":{"T":"T"}}],"parameters":[{"name":"T"}],"tag":"kind","variants":{"error":"string"}}}} as unknown as WireFamily;
/** Canonical language-neutral declaration bytes, the input to wireDigest. */
export const wireDeclaration = "{\"definitions\":{\"boxes\":{\"client\":{\"ref\":\"boxes/$client\"},\"errors\":[],\"kind\":\"family\",\"parameters\":[],\"server\":{\"ref\":\"boxes/$server\"},\"types\":{\"Batch\":{\"ref\":\"boxes/Batch\"},\"Box\":{\"ref\":\"boxes/Box\"},\"Choice\":{\"ref\":\"boxes/Choice\"},\"Page\":{\"ref\":\"boxes/Page\"},\"Result\":{\"ref\":\"boxes/Result\"}}},\"boxes/$client\":{\"direction\":\"client\",\"events\":{},\"extends\":[],\"kind\":\"side\",\"methods\":{},\"parameters\":[]},\"boxes/$server\":{\"direction\":\"server\",\"events\":{},\"extends\":[],\"kind\":\"side\",\"methods\":{},\"parameters\":[]},\"boxes/Batch\":{\"kind\":\"alias\",\"parameters\":[{\"name\":\"T\",\"of\":\"\"}],\"type\":{\"apply\":\"boxes/Page\",\"arguments\":[{\"apply\":\"boxes/Result\",\"arguments\":[{\"apply\":\"boxes/Box\",\"arguments\":[{\"map\":{\"nullable\":{\"parameter\":\"boxes/Batch/T\"}}}]}]}]}},\"boxes/Box\":{\"fields\":[{\"name\":\"item\",\"nullable\":false,\"required\":true,\"type\":{\"parameter\":\"boxes/Box/T\"},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[{\"name\":\"T\",\"of\":\"\"}]},\"boxes/Choice\":{\"kind\":\"union\",\"parameters\":[{\"name\":\"T\",\"of\":\"\"}],\"tag\":\"kind\",\"value\":\"value\",\"variants\":{\"none\":{\"empty\":true},\"some\":{\"parameter\":\"boxes/Choice/T\"}}},\"boxes/Page\":{\"fields\":[{\"name\":\"items\",\"nullable\":false,\"required\":true,\"type\":{\"array\":{\"parameter\":\"boxes/Page/T\"}},\"unique\":false},{\"name\":\"next\",\"nullable\":false,\"required\":false,\"type\":{\"nullable\":{\"parameter\":\"boxes/Page/T\"}},\"unique\":false}],\"kind\":\"record\",\"open\":true,\"parameters\":[{\"name\":\"T\",\"of\":\"\"}]},\"boxes/Result\":{\"extends\":[{\"apply\":\"boxes/Choice\",\"arguments\":[{\"parameter\":\"boxes/Result/T\"}]}],\"kind\":\"union\",\"parameters\":[{\"name\":\"T\",\"of\":\"\"}],\"tag\":\"kind\",\"value\":\"value\",\"variants\":{\"error\":{\"primitive\":\"string\"}}}},\"root\":{\"ref\":\"boxes\"},\"version\":1}";
/** SHA-256 of the canonical full declaration's exact UTF-8 bytes. */
export const wireDigest = "0c0d4d75964e3d41b382c9554c92ea1e3eec0de30b4f384c64e59924cfb98cdc";
/** Runtime validation applies equally to calls, replies, reverse calls and events; what fills a slot of a parameter is validated by the binding of the family that fills it. */
export const validateWire = withDeclaration(createValidator(contractTypes, wireDigest, {  }), wireDeclaration);
/** This family's declaration and reusable interpretations, with no operation context or owner. */
export const family = { name: "boxes", validate: validateWire, types: {  } } as const;
