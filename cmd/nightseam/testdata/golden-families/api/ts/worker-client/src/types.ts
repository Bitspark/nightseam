// Code generated by nightseam. DO NOT EDIT.
import { createValidator, DuplexError, type AnyFamily, type FamilyBinding, type TypeBinding, type Slots, type TypeExpression, type WireFamily } from "@nightseam/runtime";
export type { AnyFamily, FamilyBinding, TypeBinding, Slots, TypeExpression };
import type { ValueAdapter, ValueContext } from "@nightseam/runtime";
import { LiveOwner } from "@nightseam/live";
import type { WireModelContext } from "@nightseam/runtime";
/** Asks the job to stop. Releasing a reference to it is not this, and cancelling the call that returned it is neither. */
/** A value of it is one implementation, called across the seam; each is its own binding, with its own lifetime. */
export type Cancel = (options?: { signal?: AbortSignal; owner?: LiveOwner }) => Promise<void>;
/** One message of the nightseam.duplex/1 profile: the members the peer acts on, and nothing else. */
export interface Envelope {
  /** The profile's version, 1. */
  "version": number;
  /** request, response, event or cancel. */
  "kind": string;
  /** What correlates a response or a cancel with its request. */
  "id"?: string;
  /** The method a request names. */
  "method"?: string;
  /** A request's parameters. */
  "params"?: unknown;
  /** A response's result. */
  "result"?: unknown;
  /** A response's error. */
  "error"?: unknown;
  /** The event an event frame names. */
  "event"?: string;
  /** An event's data. */
  "data"?: unknown;
  /** The W3C Trace Context of the frame. */
  "traceparent"?: string;
  /** The vendor state of that trace. */
  "tracestate"?: string;
  /** What a request or an event carries about the call, delivered to the handler beside the payload. */
  "meta"?: Record<string, string>;
}
/** A reference to a channel on the connection that carries the message holding it. */
export interface Handle {
  /** The channel's id on that connection. */
  "channel": number;
}
/** What start answers: a record whose members are separately released. */
export interface Job {
  "ticket": string;
  "cancel": Cancel;
  "rename"?: Rename;
}
/** Callables in a sum: one arm carries them and one carries nothing. */
export type Outcome = { "state": "finished" } | { "state": "running"; "value": Job };
/** How far along a job is, from 0 to 100. */
export type Percent = number;
/** An interface, which is a record of callable members; each member is its own binding. */
export interface ProgressSink {
  "report": Report;
}
/** Renames the job and answers what it is now called. */
/** A value of it is one implementation, called across the seam; each is its own binding, with its own lifetime. */
export type Rename = (request: Ticket, options?: { signal?: AbortSignal; owner?: LiveOwner }) => Promise<Ticket>;
/** Told how far along the job is. */
/** A value of it is one implementation, called across the seam; each is its own binding, with its own lifetime. */
export type Report = (request: Percent, options?: { signal?: AbortSignal; owner?: LiveOwner }) => Promise<void>;
/** Callables in a container. */
export type Sinks = Record<string, ProgressSink>;
/** A callable supplied as an argument, beside data. */
export interface Start {
  "ticket": Ticket;
  "progress": ProgressSink;
  "watchers"?: Watchers;
}
export interface Supervise {
  "sinks": Sinks;
}
/** A job as the application records it; its key is data and identifies a row, never a binding. */
export interface Ticket {
  "id": string;
  "label": string;
}
/** Callables in a list, each of which may be null. */
export type Watchers = Array<Report | null>;
/** The family: its name and the wire types a slot of it draws on. */
export interface Family { readonly name: "worker"; Cancel: Cancel; Envelope: Envelope; Handle: Handle; Job: Job; Outcome: Outcome; Percent: Percent; ProgressSink: ProgressSink; Rename: Rename; Report: Report; Sinks: Sinks; Start: Start; Supervise: Supervise; Ticket: Ticket; Watchers: Watchers }
/** The declaration a reference to Cancel carries. It is nominal: a reference is usable exactly where this callable is expected. */
export const contractCancel = "worker/Cancel";
/** Makes a binding of a local Cancel and answers the reference that names it. */
export function exportCancel(owner: LiveOwner, value: Cancel): unknown {
  return owner.exportValue((owner) => {
    const parent = owner;
    const reference = owner.export(contractCancel, wireDigest, async (request, options) => {
      const owner = parent.child();
      const context = { ...options, owner };
      await value(context);
      return undefined;
    });
    return reference.toJSON();
  });
}
/** A Cancel that calls the binding a reference names. */
export function importCancel(owner: LiveOwner, raw: unknown): Cancel {
  const invoke = owner.import(owner.scope.decode(raw), contractCancel, wireDigest);
  const scope = owner.scope;
  return async (options?: { signal?: AbortSignal; owner?: LiveOwner }) => {
    const owner = options?.owner?.scope === scope ? options.owner : scope.owner();
    await invoke(undefined, options);
    return;
  };
}
/** Writes Job as it travels: each callable in it becomes a binding of the owner, and the reference that names it takes its place. */
export function exportJob(owner: LiveOwner, value: Job): unknown {
  return owner.exportValue((owner) => {
    const out: Record<string, unknown> = {};
    out["ticket"] = value["ticket"];
    out["cancel"] = exportCancel(owner, (value["cancel"]) as Cancel);
    if (value["rename"] !== undefined) out["rename"] = exportRename(owner, (value["rename"]) as Rename);
    return out;
  });
}
/** Reads Job as it arrived: each reference in it becomes a typed proxy of the binding it names, so a handler is given native values. */
export function importJob(owner: LiveOwner, raw: unknown): Job {
  return owner.importValue((owner) => {
    const wire = raw as Record<string, unknown>;
    const out: Record<string, unknown> = {};
    out["ticket"] = wire["ticket"];
    out["cancel"] = importCancel(owner, wire["cancel"]);
    if (wire["rename"] !== undefined) out["rename"] = importRename(owner, wire["rename"]);
    return out as unknown as Job;
  });
}
/** Writes Outcome as it travels: each callable in it becomes a binding of the owner, and the reference that names it takes its place. */
export function exportOutcome(owner: LiveOwner, value: Outcome): unknown {
  return owner.exportValue((owner) => {
    const held = value as Record<string, unknown>;
    switch (held["state"]) {
      case "finished": return { "state": "finished" };
      case "running": return { "state": "running", "value": exportJob(owner, (held["value"]) as Job) };
    }
    throw new Error("Outcome: unknown variant " + String(held["state"]));
  });
}
/** Reads Outcome as it arrived: each reference in it becomes a typed proxy of the binding it names, so a handler is given native values. */
export function importOutcome(owner: LiveOwner, raw: unknown): Outcome {
  return owner.importValue((owner) => {
    const held = raw as Record<string, unknown>;
    switch (held["state"]) {
      case "finished": return { "state": "finished" } as unknown as Outcome;
      case "running": return { "state": "running", "value": importJob(owner, held["value"]) } as unknown as Outcome;
    }
    throw new Error("Outcome: unknown variant " + String(held["state"]));
  });
}
/** Writes ProgressSink as it travels: each callable in it becomes a binding of the owner, and the reference that names it takes its place. */
export function exportProgressSink(owner: LiveOwner, value: ProgressSink): unknown {
  return owner.exportValue((owner) => {
    const out: Record<string, unknown> = {};
    out["report"] = exportReport(owner, (value["report"]) as Report);
    return out;
  });
}
/** Reads ProgressSink as it arrived: each reference in it becomes a typed proxy of the binding it names, so a handler is given native values. */
export function importProgressSink(owner: LiveOwner, raw: unknown): ProgressSink {
  return owner.importValue((owner) => {
    const wire = raw as Record<string, unknown>;
    const out: Record<string, unknown> = {};
    out["report"] = importReport(owner, wire["report"]);
    return out as unknown as ProgressSink;
  });
}
/** The declaration a reference to Rename carries. It is nominal: a reference is usable exactly where this callable is expected. */
export const contractRename = "worker/Rename";
/** Makes a binding of a local Rename and answers the reference that names it. */
export function exportRename(owner: LiveOwner, value: Rename): unknown {
  return owner.exportValue((owner) => {
    const parent = owner;
    const reference = owner.export(contractRename, wireDigest, async (request, options) => {
      const owner = parent.child();
      const context = { ...options, owner };
      validateWire("Ticket", request);
      const argument = request as Ticket;
      const result = await value(argument, context);
      return (() => { const converted = result; validateWire("Ticket", converted); return converted; })();
    });
    return reference.toJSON();
  });
}
/** A Rename that calls the binding a reference names. */
export function importRename(owner: LiveOwner, raw: unknown): Rename {
  const invoke = owner.import(owner.scope.decode(raw), contractRename, wireDigest);
  const scope = owner.scope;
  return async (request: Ticket, options?: { signal?: AbortSignal; owner?: LiveOwner }) => {
    const owner = options?.owner?.scope === scope ? options.owner : scope.owner();
    const result = await invoke((() => { const converted = request; validateWire("Ticket", converted); return converted; })(), options);
    validateWire("Ticket", result);
    return result as Ticket;
  };
}
/** The declaration a reference to Report carries. It is nominal: a reference is usable exactly where this callable is expected. */
export const contractReport = "worker/Report";
/** Makes a binding of a local Report and answers the reference that names it. */
export function exportReport(owner: LiveOwner, value: Report): unknown {
  return owner.exportValue((owner) => {
    const parent = owner;
    const reference = owner.export(contractReport, wireDigest, async (request, options) => {
      const owner = parent.child();
      const context = { ...options, owner };
      validateWire("Percent", request);
      const argument = request as Percent;
      await value(argument, context);
      return undefined;
    });
    return reference.toJSON();
  });
}
/** A Report that calls the binding a reference names. */
export function importReport(owner: LiveOwner, raw: unknown): Report {
  const invoke = owner.import(owner.scope.decode(raw), contractReport, wireDigest);
  const scope = owner.scope;
  return async (request: Percent, options?: { signal?: AbortSignal; owner?: LiveOwner }) => {
    const owner = options?.owner?.scope === scope ? options.owner : scope.owner();
    await invoke((() => { const converted = request; validateWire("Percent", converted); return converted; })(), options);
    return;
  };
}
/** Writes Sinks as it travels: each callable in it becomes a binding of the owner, and the reference that names it takes its place. */
export function exportSinks(owner: LiveOwner, value: Sinks): unknown {
  return owner.exportValue((owner) => {
    return Object.fromEntries(Object.entries(value as Record<string, unknown>).map(([key, item]) => [key, exportProgressSink(owner, (item) as ProgressSink)]));
  });
}
/** Reads Sinks as it arrived: each reference in it becomes a typed proxy of the binding it names, so a handler is given native values. */
export function importSinks(owner: LiveOwner, raw: unknown): Sinks {
  return owner.importValue((owner) => {
    return Object.fromEntries(Object.entries(raw as Record<string, unknown>).map(([key, item]) => [key, importProgressSink(owner, item)]));
  });
}
/** Writes Start as it travels: each callable in it becomes a binding of the owner, and the reference that names it takes its place. */
export function exportStart(owner: LiveOwner, value: Start): unknown {
  return owner.exportValue((owner) => {
    const out: Record<string, unknown> = {};
    out["ticket"] = value["ticket"];
    out["progress"] = exportProgressSink(owner, (value["progress"]) as ProgressSink);
    if (value["watchers"] !== undefined) out["watchers"] = exportWatchers(owner, (value["watchers"]) as Watchers);
    return out;
  });
}
/** Reads Start as it arrived: each reference in it becomes a typed proxy of the binding it names, so a handler is given native values. */
export function importStart(owner: LiveOwner, raw: unknown): Start {
  return owner.importValue((owner) => {
    const wire = raw as Record<string, unknown>;
    const out: Record<string, unknown> = {};
    out["ticket"] = wire["ticket"];
    out["progress"] = importProgressSink(owner, wire["progress"]);
    if (wire["watchers"] !== undefined) out["watchers"] = importWatchers(owner, wire["watchers"]);
    return out as unknown as Start;
  });
}
/** Writes Supervise as it travels: each callable in it becomes a binding of the owner, and the reference that names it takes its place. */
export function exportSupervise(owner: LiveOwner, value: Supervise): unknown {
  return owner.exportValue((owner) => {
    const out: Record<string, unknown> = {};
    out["sinks"] = exportSinks(owner, (value["sinks"]) as Sinks);
    return out;
  });
}
/** Reads Supervise as it arrived: each reference in it becomes a typed proxy of the binding it names, so a handler is given native values. */
export function importSupervise(owner: LiveOwner, raw: unknown): Supervise {
  return owner.importValue((owner) => {
    const wire = raw as Record<string, unknown>;
    const out: Record<string, unknown> = {};
    out["sinks"] = importSinks(owner, wire["sinks"]);
    return out as unknown as Supervise;
  });
}
/** Writes Watchers as it travels: each callable in it becomes a binding of the owner, and the reference that names it takes its place. */
export function exportWatchers(owner: LiveOwner, value: Watchers): unknown {
  return owner.exportValue((owner) => {
    return (value as unknown[]).map((item) => (item === null ? null : exportReport(owner, (item) as Report)));
  });
}
/** Reads Watchers as it arrived: each reference in it becomes a typed proxy of the binding it names, so a handler is given native values. */
export function importWatchers(owner: LiveOwner, raw: unknown): Watchers {
  return owner.importValue((owner) => {
    return (raw as unknown[]).map((item) => (item === null ? null : importReport(owner, item)));
  });
}
export function adapterCancel(): ValueAdapter<Cancel> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Cancel", validate: validateWire, slots };
  const needsContext = true;
  return { binding, needsContext,
    export(context: unknown, value: Cancel): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      const convert = (owner: LiveOwner): unknown => {
        const converted = exportCancel(owner!, value);
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return context.exportValue(convert);
    },
    import(context: unknown, raw: unknown): Cancel {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: LiveOwner): Cancel => {
        const converted = importCancel(owner!, raw);
        return converted;
      };
      return context.importValue(convert);
    },
  };
}
export function adapterJob(): ValueAdapter<Job> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Job", validate: validateWire, slots };
  const needsContext = true;
  return { binding, needsContext,
    export(context: unknown, value: Job): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      const convert = (owner: LiveOwner): unknown => {
        const converted = exportJob(owner!, value);
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return context.exportValue(convert);
    },
    import(context: unknown, raw: unknown): Job {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: LiveOwner): Job => {
        const converted = importJob(owner!, raw);
        return converted;
      };
      return context.importValue(convert);
    },
  };
}
export function adapterOutcome(): ValueAdapter<Outcome> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Outcome", validate: validateWire, slots };
  const needsContext = true;
  return { binding, needsContext,
    export(context: unknown, value: Outcome): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      const convert = (owner: LiveOwner): unknown => {
        const converted = exportOutcome(owner!, value);
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return context.exportValue(convert);
    },
    import(context: unknown, raw: unknown): Outcome {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: LiveOwner): Outcome => {
        const converted = importOutcome(owner!, raw);
        return converted;
      };
      return context.importValue(convert);
    },
  };
}
export function adapterPercent(): ValueAdapter<Percent> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Percent", validate: validateWire, slots };
  const needsContext = false;
  return { binding, needsContext,
    export(context: unknown, value: Percent): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = value;
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Percent {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Percent => {
        const converted = raw as Percent;
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterProgressSink(): ValueAdapter<ProgressSink> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "ProgressSink", validate: validateWire, slots };
  const needsContext = true;
  return { binding, needsContext,
    export(context: unknown, value: ProgressSink): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      const convert = (owner: LiveOwner): unknown => {
        const converted = exportProgressSink(owner!, value);
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return context.exportValue(convert);
    },
    import(context: unknown, raw: unknown): ProgressSink {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: LiveOwner): ProgressSink => {
        const converted = importProgressSink(owner!, raw);
        return converted;
      };
      return context.importValue(convert);
    },
  };
}
export function adapterRename(): ValueAdapter<Rename> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Rename", validate: validateWire, slots };
  const needsContext = true;
  return { binding, needsContext,
    export(context: unknown, value: Rename): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      const convert = (owner: LiveOwner): unknown => {
        const converted = exportRename(owner!, value);
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return context.exportValue(convert);
    },
    import(context: unknown, raw: unknown): Rename {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: LiveOwner): Rename => {
        const converted = importRename(owner!, raw);
        return converted;
      };
      return context.importValue(convert);
    },
  };
}
export function adapterReport(): ValueAdapter<Report> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Report", validate: validateWire, slots };
  const needsContext = true;
  return { binding, needsContext,
    export(context: unknown, value: Report): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      const convert = (owner: LiveOwner): unknown => {
        const converted = exportReport(owner!, value);
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return context.exportValue(convert);
    },
    import(context: unknown, raw: unknown): Report {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: LiveOwner): Report => {
        const converted = importReport(owner!, raw);
        return converted;
      };
      return context.importValue(convert);
    },
  };
}
export function adapterSinks(): ValueAdapter<Sinks> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Sinks", validate: validateWire, slots };
  const needsContext = true;
  return { binding, needsContext,
    export(context: unknown, value: Sinks): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      const convert = (owner: LiveOwner): unknown => {
        const converted = exportSinks(owner!, value);
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return context.exportValue(convert);
    },
    import(context: unknown, raw: unknown): Sinks {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: LiveOwner): Sinks => {
        const converted = importSinks(owner!, raw);
        return converted;
      };
      return context.importValue(convert);
    },
  };
}
export function adapterStart(): ValueAdapter<Start> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Start", validate: validateWire, slots };
  const needsContext = true;
  return { binding, needsContext,
    export(context: unknown, value: Start): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      const convert = (owner: LiveOwner): unknown => {
        const converted = exportStart(owner!, value);
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return context.exportValue(convert);
    },
    import(context: unknown, raw: unknown): Start {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: LiveOwner): Start => {
        const converted = importStart(owner!, raw);
        return converted;
      };
      return context.importValue(convert);
    },
  };
}
export function adapterSupervise(): ValueAdapter<Supervise> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Supervise", validate: validateWire, slots };
  const needsContext = true;
  return { binding, needsContext,
    export(context: unknown, value: Supervise): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      const convert = (owner: LiveOwner): unknown => {
        const converted = exportSupervise(owner!, value);
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return context.exportValue(convert);
    },
    import(context: unknown, raw: unknown): Supervise {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: LiveOwner): Supervise => {
        const converted = importSupervise(owner!, raw);
        return converted;
      };
      return context.importValue(convert);
    },
  };
}
export function adapterTicket(): ValueAdapter<Ticket> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Ticket", validate: validateWire, slots };
  const needsContext = false;
  return { binding, needsContext,
    export(context: unknown, value: Ticket): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = value;
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Ticket {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Ticket => {
        const converted = raw as Ticket;
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterWatchers(): ValueAdapter<Watchers> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Watchers", validate: validateWire, slots };
  const needsContext = true;
  return { binding, needsContext,
    export(context: unknown, value: Watchers): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      const convert = (owner: LiveOwner): unknown => {
        const converted = exportWatchers(owner!, value);
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return context.exportValue(convert);
    },
    import(context: unknown, raw: unknown): Watchers {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: LiveOwner): Watchers => {
        const converted = importWatchers(owner!, raw);
        return converted;
      };
      return context.importValue(convert);
    },
  };
}
export interface ServerMethods {
  describe(params: Ticket, context?: WireModelContext): string | Promise<string>;
  start(params: Start, context?: WireModelContext & { valueContext: unknown }): Job | Promise<Job>;
}
export interface ServerEvents {
}
export interface Server { methods: ServerMethods; events: ServerEvents; }
export interface ClientMethods {
  supervise(params: Supervise, context?: WireModelContext & { valueContext: unknown }): Outcome | Promise<Outcome>;
}
export interface ClientEvents {
  settled(data: Outcome, context?: WireModelContext & { valueContext: unknown }): void | Promise<void>;
}
export interface Client { methods: ClientMethods; events: ClientEvents; }
export type ServerModel = (remote: Client) => Server;
export type ClientModel = (remote: Server) => Client;

const contractTypes = {"types":{"Cancel":{"kind":"callable","contract":"worker/Cancel"},"Envelope":{"kind":"record","fields":[{"name":"version","type":"integer","required":true},{"name":"kind","type":"string","required":true},{"name":"id","type":"string","required":false},{"name":"method","type":"string","required":false},{"name":"params","type":"json","required":false},{"name":"result","type":"json","required":false},{"name":"error","type":"json","required":false},{"name":"event","type":"string","required":false},{"name":"data","type":"json","required":false},{"name":"traceparent","type":"string","required":false},{"name":"tracestate","type":"string","required":false},{"name":"meta","type":{"map":"string"},"required":false}]},"Handle":{"kind":"record","fields":[{"name":"channel","type":"integer","required":true}]},"Job":{"kind":"record","fields":[{"name":"ticket","type":{"ref":"Ticket"},"required":true},{"name":"cancel","type":"Cancel","required":true},{"name":"rename","type":"Rename","required":false}]},"Outcome":{"kind":"union","tag":"state","variants":{"finished":{"empty":true},"running":"Job"}},"Percent":{"kind":"alias","type":"integer"},"ProgressSink":{"kind":"record","fields":[{"name":"report","type":"Report","required":true}]},"Rename":{"kind":"callable","contract":"worker/Rename","request":"Ticket","result":"Ticket","errors":["gone"]},"Report":{"kind":"callable","contract":"worker/Report","request":"Percent"},"Sinks":{"kind":"alias","type":{"map":"ProgressSink"}},"Start":{"kind":"record","fields":[{"name":"ticket","type":"Ticket","required":true},{"name":"progress","type":"ProgressSink","required":true},{"name":"watchers","type":"Watchers","required":false}]},"Supervise":{"kind":"record","fields":[{"name":"sinks","type":"Sinks","required":true}]},"Ticket":{"kind":"entity","key":"id","fields":[{"name":"id","type":"string","required":true},{"name":"label","type":"string","required":true}]},"Watchers":{"kind":"alias","type":{"array":{"nullable":"Report"}}}}} as unknown as WireFamily;
/** The SHA-256 digest of the family's exact wire description. */
export const wireDigest = "4d47db78adbf50b36bc70d8a951b78cf41185f5f11a9854f572e7ecb8f17f077";
/** Runtime validation applies equally to calls, replies, reverse calls and events; what fills a slot of a parameter is validated by the binding of the family that fills it. */
export const validateWire = createValidator(contractTypes, wireDigest, {  });
/** This family bound: its name and its validator, to fill a family slot in another family's client. */
export const family = { name: "worker", validate: validateWire } as const;
