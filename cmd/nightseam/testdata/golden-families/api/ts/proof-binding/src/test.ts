// Code generated by nightseam. DO NOT EDIT.
import { DuplexPeer, DuplexError, wirePair, forwardWire, type AdapterContext, type WireModelContext, type ValueAdapter, type ValueContext } from "@nightseam/runtime";
import { at, mount, pipe as framePipe, type Wire } from '@nightseam/duplex';
import { toWire, prepareFromWire } from './index.ts';
import type * as Protocol from "@example/proof-client/types";
import type { AnyFamily, FamilyBinding } from "@example/proof-client/types";
import type * as probe from "@example/probe-client";

export type Presentation = (wire: Wire) => { wire: Wire; close(): void } | Promise<{ wire: Wire; close(): void }>;
export interface Options {
 context?: AdapterContext;
 remoteContext?: AdapterContext;
 presentation?: Presentation;
 inputs?: Readonly<Record<string, unknown>>;
 equal?: (method: string, direct: unknown, roundTrip: unknown) => void | Promise<void>;
 callContext?: WireModelContext & { valueContext?: unknown };
}
function once(action: () => void): () => void { let closed=false; return () => { if (!closed) { closed=true; action(); } }; }
function hasMethod(facet:unknown,name:string):boolean{for(let current=facet;current!=null&&current!==Object.prototype;current=Object.getPrototypeOf(current)){if(Object.prototype.hasOwnProperty.call(current,name))return typeof (facet as Record<string,unknown>)[name]==='function';}return false;}
export const local: Presentation = wire => ({wire,close(){}});
export const mounted: Presentation = wire => {const root=mount(new Map([['family',wire]]));return {wire:at(root,['family']),close:once(()=>root.close(1000,''))};};
export const forwarded: Presentation = wire => {
 const [left,right]=wirePair();
 try { const detach=forwardWire(right,wire); return {wire:left,close:once(()=>{detach();left.close(1000,'');})}; }
 catch(error){left.close(1000,'');throw error;}
};
export const pipe: Presentation = async wire => {
 const [left,right]=framePipe(); let detach=()=>{};
 const server=new DuplexPeer({role:'server',prepare(peer){detach=forwardWire(peer.wire(),wire);}});
 const client=new DuplexPeer({role:'client'});
 const close=once(()=>{detach();client.close();server.close();left.close(1000,'');right.close(1000,'');});
 try {await Promise.all([server.attach(right),client.attach(left)]);return {wire:client.wire(),close};}
 catch(error){close();throw error;}
};
type Outcome = {ok:true;value:unknown}|{ok:false;error:unknown};
async function outcome(call:()=>unknown):Promise<Outcome>{try{return {ok:true,value:await call()};}catch(error){return {ok:false,error};}}
function canonical(value:unknown):string {
 if(value===undefined)return 'undefined';
 if(typeof value==='function'||typeof value==='symbol'||typeof value==='bigint')throw new Error('result needs an equal observer');
 if(value===null||typeof value!=='object')return JSON.stringify(value);
 if(Array.isArray(value))return '['+value.map(canonical).join(',')+']';
 return '{'+Object.keys(value).sort().map(key=>JSON.stringify(key)+':'+canonical((value as Record<string,unknown>)[key])).join(',')+'}';
}
async function compare(method:string,expected:Outcome,actual:Outcome,equal:Options['equal']):Promise<void>{
 if(!expected.ok || !actual.ok){
  const code=(error:unknown)=>error instanceof DuplexError?error.code:'internal';
  if(!expected.ok&&!actual.ok&&code(expected.error)===code(actual.error)){
   if(expected.error instanceof DuplexError && actual.error instanceof DuplexError && (expected.error.message!==actual.error.message || canonical(expected.error.data)!==canonical(actual.error.data)))throw new Error(method+': public error changed');
   return;
  }
  throw new Error(method+': direct and round-trip outcomes differ');
 }
 if(equal){await equal(method,expected.value,actual.value);return;}
 if(canonical(expected.value)!==canonical(actual.value))throw new Error(method+': direct and round-trip results differ');
}

export async function pair<S extends AnyFamily = AnyFamily, Item = unknown>(model: Protocol.ServerModel<S, Item>, options: Options, binding_s: FamilyBinding<S>, binding_item: ValueAdapter<Item>): Promise<{ model: Protocol.ServerModel<S, Item>; close(): void }> {
  const wire = toWire<S, Item>(model, options.context ?? {}, binding_s, binding_item);
  let close = once(() => wire.close(1000, ''));
  try {
  const view = await (options.presentation ?? pipe)(wire);
  const rootClose = close; close = once(() => { try { view.close(); } finally { rootClose(); } });
  const prepared = prepareFromWire<S, Item>(view.wire, options.remoteContext ?? {}, binding_s, binding_item);
  const viewClose = close; close = once(() => { try { prepared.close(); } finally { viewClose(); } });
  return { model: await prepared.complete(options.callContext), close };
  } catch (error) { close(); throw error; }
}
const examples: Readonly<Record<string, { raw?: string; reason?: string }>> = {
"Carried": { raw: "{\"message\":{\"version\":0,\"kind\":\"‹kind›\",\"id\":\"‹id›\",\"method\":\"‹method›\",\"params\":{},\"result\":{},\"error\":{},\"event\":\"‹event›\",\"data\":{},\"traceparent\":\"‹traceparent›\",\"tracestate\":\"‹tracestate›\",\"meta\":{\"‹key›\":\"‹meta›\"}},\"back\":{\"channel\":0},\"page\":{\"items\":[\"‹items›\"],\"next\":\"‹next›\"}}", reason: "" },
"Option": { raw: "{\"kind\":\"none\",\"value\":{}}", reason: "" },
"OptionNone": { raw: "{}", reason: "" },
"Page": { raw: "{\"items\":[\"‹items›\"],\"next\":\"‹next›\"}", reason: "" },
"Part": { raw: "{\"type\":\"count\",\"value\":0}", reason: "" },
"PartImage": { raw: "{\"url\":\"https://a/\",\"alt\":\"‹alt›\"}", reason: "" },
"Parts": { raw: "{\"items\":[{\"type\":\"count\",\"value\":0}],\"next\":\"‹next›\"}", reason: "" },
"PartsRequest": { raw: "{\"after\":\"‹after›\"}", reason: "" },
"Result": { raw: "{\"kind\":\"err\",\"value\":\"‹err›\"}", reason: "" },
"RichPart": { raw: "{\"type\":\"count\",\"value\":0}", reason: "" },
"RichPartTable": { raw: "{\"rows\":[\"‹rows›\"]}", reason: "" },
"TextPart": { raw: "{\"type\":\"text\",\"body\":\"‹body›\"}", reason: "" },
};
/** A fresh documented data witness, validated by the caller's exact value adapter. */
export function example<T>(name: string, adapter: ValueAdapter<T>): T { const value = Object.prototype.hasOwnProperty.call(examples,name) ? examples[name] : undefined; if (!value) throw new Error('unknown example '+name); if (value.reason || adapter.needsContext) throw new Error('example '+name+' unavailable: '+(value.reason || 'an acquiring adapter needs a native witness')); return adapter.import(undefined, JSON.parse(value.raw!)); }
/** Exercise every method on two fresh equivalent models; missing evidence is an error. */
export async function smoke<S extends AnyFamily = AnyFamily, Item = unknown>(model: Protocol.ServerModel<S, Item>, opposite: Protocol.Client<S, Item>, options: Options, binding_s: FamilyBinding<S>, binding_item: ValueAdapter<Item>): Promise<void> {
  const bindings = {s: binding_s, item: binding_item};
  const inputs = new Map<string,unknown>(); const seen = new Map<string,number>(); let inputError: unknown;
  const observed: Protocol.ServerModel<S, Item> = remote => { const value=model(remote);
  if(!hasMethod(value?.methods,"echo"))throw new Error("model method echo is required");
  if(!hasMethod(value?.methods,"noArgs"))throw new Error("model method no_args is required");
  if(!hasMethod(value?.methods,"classify"))throw new Error("model method classify is required");
  if(!hasMethod(value?.methods,"classifyRich"))throw new Error("model method classify_rich is required");
  if(!hasMethod(value?.methods,"parts"))throw new Error("model method parts is required");
  if(!hasMethod(value?.methods,"relay"))throw new Error("model method relay is required");
  return { ...value, methods: {
  async echo(input,context) { const key="echo"; seen.set(key,(seen.get(key)??0)+1); const tracked=inputs.has(key),expected=inputs.get(key);inputs.delete(key); if(tracked)try { await compare("echo.request",{ok:true,value:expected},{ok:true,value:input},options.equal); } catch(error){inputError=error;throw error;} return value.methods.echo(input,context); },
  async noArgs(input,context) { const key="no_args"; seen.set(key,(seen.get(key)??0)+1); const tracked=inputs.has(key),expected=inputs.get(key);inputs.delete(key); if(tracked)try { await compare("no_args.request",{ok:true,value:expected},{ok:true,value:input},options.equal); } catch(error){inputError=error;throw error;} return value.methods.noArgs(input,context); },
  async classify(input,context) { const key="classify"; seen.set(key,(seen.get(key)??0)+1); const tracked=inputs.has(key),expected=inputs.get(key);inputs.delete(key); if(tracked)try { await compare("classify.request",{ok:true,value:expected},{ok:true,value:input},options.equal); } catch(error){inputError=error;throw error;} return value.methods.classify(input,context); },
  async classifyRich(input,context) { const key="classify_rich"; seen.set(key,(seen.get(key)??0)+1); const tracked=inputs.has(key),expected=inputs.get(key);inputs.delete(key); if(tracked)try { await compare("classify_rich.request",{ok:true,value:expected},{ok:true,value:input},options.equal); } catch(error){inputError=error;throw error;} return value.methods.classifyRich(input,context); },
  async parts(input,context) { const key="parts"; seen.set(key,(seen.get(key)??0)+1); const tracked=inputs.has(key),expected=inputs.get(key);inputs.delete(key); if(tracked)try { await compare("parts.request",{ok:true,value:expected},{ok:true,value:input},options.equal); } catch(error){inputError=error;throw error;} return value.methods.parts(input,context); },
  async relay(input,context) { const key="relay"; seen.set(key,(seen.get(key)??0)+1); const tracked=inputs.has(key),expected=inputs.get(key);inputs.delete(key); if(tracked)try { await compare("relay.request",{ok:true,value:expected},{ok:true,value:input},options.equal); } catch(error){inputError=error;throw error;} return value.methods.relay(input,context); },
  } }; };
  const prepared = await pair<S, Item>(observed, options, binding_s, binding_item);
  try {
  const remote = prepared.model(opposite); const direct = model(opposite);
  {
  let input: unknown;
  if (options.inputs && Object.prototype.hasOwnProperty.call(options.inputs,"echo")) { input=options.inputs["echo"]; } else {
  if (false) throw new Error("input echo needs a caller-supplied native value");
  input = JSON.parse("{\"text\":\"‹text›\",\"count\":0,\"note\":\"‹note›\"}");
  }
  inputs.set("echo",input);
  const before=seen.get("echo")??0;
  const actual = await outcome(() => remote.methods.echo(input as probe.Payload, options.callContext as WireModelContext));
  if(inputError!==undefined)throw inputError;
  if((seen.get("echo")??0)<=before)throw new Error("echo: model was not reached");
  const expected = await outcome(() => direct.methods.echo(input as probe.Payload, options.callContext as WireModelContext));
  await compare("echo", expected, actual, options.equal);
  }
  {
  let input: unknown;
  if (options.inputs && Object.prototype.hasOwnProperty.call(options.inputs,"no_args")) { input=options.inputs["no_args"]; } else {
  input = JSON.parse("{}");
  }
  inputs.set("no_args",input);
  const before=seen.get("no_args")??0;
  const actual = await outcome(() => remote.methods.noArgs(input as Record<string, never>, options.callContext as WireModelContext));
  if(inputError!==undefined)throw inputError;
  if((seen.get("no_args")??0)<=before)throw new Error("no_args: model was not reached");
  const expected = await outcome(() => direct.methods.noArgs(input as Record<string, never>, options.callContext as WireModelContext));
  await compare("no_args", expected, actual, options.equal);
  }
  {
  let input: unknown;
  if (options.inputs && Object.prototype.hasOwnProperty.call(options.inputs,"classify")) { input=options.inputs["classify"]; } else {
  if (false) throw new Error("input classify needs a caller-supplied native value");
  input = JSON.parse("{\"type\":\"count\",\"value\":0}");
  }
  inputs.set("classify",input);
  const before=seen.get("classify")??0;
  const actual = await outcome(() => remote.methods.classify(input as Protocol.Part, options.callContext as WireModelContext));
  if(inputError!==undefined)throw inputError;
  if((seen.get("classify")??0)<=before)throw new Error("classify: model was not reached");
  const expected = await outcome(() => direct.methods.classify(input as Protocol.Part, options.callContext as WireModelContext));
  await compare("classify", expected, actual, options.equal);
  }
  {
  let input: unknown;
  if (options.inputs && Object.prototype.hasOwnProperty.call(options.inputs,"classify_rich")) { input=options.inputs["classify_rich"]; } else {
  if (false) throw new Error("input classify_rich needs a caller-supplied native value");
  input = JSON.parse("{\"type\":\"count\",\"value\":0}");
  }
  inputs.set("classify_rich",input);
  const before=seen.get("classify_rich")??0;
  const actual = await outcome(() => remote.methods.classifyRich(input as Protocol.RichPart, options.callContext as WireModelContext));
  if(inputError!==undefined)throw inputError;
  if((seen.get("classify_rich")??0)<=before)throw new Error("classify_rich: model was not reached");
  const expected = await outcome(() => direct.methods.classifyRich(input as Protocol.RichPart, options.callContext as WireModelContext));
  await compare("classify_rich", expected, actual, options.equal);
  }
  {
  let input: unknown;
  if (options.inputs && Object.prototype.hasOwnProperty.call(options.inputs,"parts")) { input=options.inputs["parts"]; } else {
  if (false) throw new Error("input parts needs a caller-supplied native value");
  input = JSON.parse("{\"after\":\"‹after›\"}");
  }
  inputs.set("parts",input);
  const before=seen.get("parts")??0;
  const actual = await outcome(() => remote.methods.parts(input as Protocol.PartsRequest, options.callContext as WireModelContext));
  if(inputError!==undefined)throw inputError;
  if((seen.get("parts")??0)<=before)throw new Error("parts: model was not reached");
  const expected = await outcome(() => direct.methods.parts(input as Protocol.PartsRequest, options.callContext as WireModelContext));
  await compare("parts", expected, actual, options.equal);
  }
  {
  let input: unknown;
  if (options.inputs && Object.prototype.hasOwnProperty.call(options.inputs,"relay")) { input=options.inputs["relay"]; } else {
  if ((bindings.item.needsContext)) throw new Error("input relay needs a caller-supplied native value");
  input = JSON.parse("{\"message\":{\"version\":0,\"kind\":\"‹kind›\",\"id\":\"‹id›\",\"method\":\"‹method›\",\"params\":{},\"result\":{},\"error\":{},\"event\":\"‹event›\",\"data\":{},\"traceparent\":\"‹traceparent›\",\"tracestate\":\"‹tracestate›\",\"meta\":{\"‹key›\":\"‹meta›\"}},\"back\":{\"channel\":0},\"page\":{\"items\":[\"‹items›\"],\"next\":\"‹next›\"}}");
  }
  if (!options.equal && ((bindings.item.needsContext))) throw new Error("relay: requires an equal observer for live values");
  inputs.set("relay",input);
  const before=seen.get("relay")??0;
  const actual = await outcome(() => remote.methods.relay(input as Protocol.Carried<S, Item>, options.callContext as ValueContext<Protocol.Carried<S, Item> | Protocol.Option<Protocol.Envelope>, WireModelContext>));
  if(inputError!==undefined)throw inputError;
  if((seen.get("relay")??0)<=before)throw new Error("relay: model was not reached");
  const expected = await outcome(() => direct.methods.relay(input as Protocol.Carried<S, Item>, options.callContext as ValueContext<Protocol.Carried<S, Item> | Protocol.Option<Protocol.Envelope>, WireModelContext>));
  await compare("relay", expected, actual, options.equal);
  }
  } finally { prepared.close(); }
}
