// Code generated by nightseam. DO NOT EDIT.
import { DuplexError, callWire, emitWire, registerWire, wirePair, type WireModelContext } from "@nightseam/runtime";
import { encodePath, type Wire } from '@nightseam/duplex';
import type { AdapterContext, ValueAdapter, ValueContext } from "@nightseam/runtime";
import type { LiveOwner } from "@nightseam/live";
import { validateWire } from "@example/supervisor-client/types";
import type * as Protocol from "@example/supervisor-client/types";
import type { AnyFamily, FamilyBinding, Slots } from "@example/supervisor-client/types";
import * as conversion from "@example/supervisor-client/types";
import * as live_worker from "@example/worker-client";
import type * as worker from "@example/worker-client";
export * from "@example/supervisor-client/types";
export { DuplexError };
export type { AdapterContext };
function makeAdapter(context: AdapterContext) {
  const observer = context.options?.observer;
  const propagator = context.options?.propagator;
  const requestTimeoutMs = context.options?.requestTimeoutMs;
  const bindings = {  };
  const slots: Slots = {  };
  const environment = context.valueEnvironment;
  if ((true) && !environment) throw new DuplexError('scope_closed', 'model adaptation requires an explicit value environment');
  function hasModelHandler(facet: object, name: string): boolean {
    for (let current = facet; current !== null && current !== Object.prototype; current = Object.getPrototypeOf(current)) {
      if (Object.prototype.hasOwnProperty.call(current, name)) return typeof (facet as Record<string, unknown>)[name] === 'function';
    }
    return false;
  }
  function proxyServer(wire: Wire): Protocol.Server {
    return {
      methods: {
        async shift(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs ?? requestTimeoutMs, meta: context?.outgoingMeta, observer, propagator, family: "supervisor" };
          validateWire("Shift", params);
          const result = await callWire<string>(wire, ["shift"], params, options);
          validateWire("string", result);
          return result;
        },
        async relieve(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs ?? requestTimeoutMs, meta: context?.outgoingMeta, observer, propagator, family: "supervisor" };
          const owner = (true) ? environment!.select((context as {valueContext?: unknown} | undefined)?.valueContext) : undefined;
          const result = await environment!.publish(owner, owner => environment!.export(owner, (owner) => { const converted = conversion.exportRelieveRequest(owner as LiveOwner, (params) as Protocol.RelieveRequest); validateWire({"kind":"record","fields":[{"name":"shift","type":"Shift","required":true},{"name":"sink","type":"worker.ProgressSink","required":true}]}, converted); return converted; }), sent => callWire(wire, ["relieve"], sent, options));
          validateWire("Shift", result);
          return result as Protocol.Shift;
        },
        async watch(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs ?? requestTimeoutMs, meta: context?.outgoingMeta, observer, propagator, family: "supervisor" };
          const owner = (true || true) ? environment!.select((context as {valueContext?: unknown} | undefined)?.valueContext) : undefined;
          const result = await environment!.publish(owner, owner => environment!.export(owner, (owner) => { const converted = conversion.exportWatch(owner as LiveOwner, (params) as Protocol.Watch); validateWire("Watch", converted); return converted; }), sent => callWire(wire, ["watch"], sent, options));
          validateWire("worker.Job", result);
          return environment!.import(owner, (owner) => live_worker.importJob(owner as LiveOwner, result));
        },
      },
      events: {
      },
    };
  }
  function bindServer(wire: Wire, implementation: Protocol.Server): void {
    if (!implementation?.methods || !implementation.events) throw new Error('model methods and events are required');
    if (!hasModelHandler(implementation.methods, "shift")) throw new Error("handler for shift is required");
    if (!hasModelHandler(implementation.methods, "relieve")) throw new Error("handler for relieve is required");
    if (!hasModelHandler(implementation.methods, "watch")) throw new Error("handler for watch is required");
    const detach: Array<() => void> = [];
    try {
      detach.push(registerWire(wire, ["shift"], {
        observer, family: "supervisor",
        request: async (raw, context) => {
          try { validateWire("Shift", raw); } catch (error) { throw new DuplexError('invalid_params', String(error)); }
          const params = raw as Protocol.Shift;
          const result = await implementation.methods.shift(params as Protocol.Shift, context);
          return (() => { const converted = result; validateWire("string", converted); return converted; })();
        },
      }));
      detach.push(registerWire(wire, ["relieve"], {
        observer, family: "supervisor",
        request: async (raw, context) => {
          try { validateWire({"kind":"record","fields":[{"name":"shift","type":"Shift","required":true},{"name":"sink","type":"worker.ProgressSink","required":true}]}, raw); } catch (error) { throw new DuplexError('invalid_params', String(error)); }
          const owner = (true) ? environment!.child(undefined) : undefined; const ownedContext = Object.create(context) as WireModelContext & { valueContext: unknown }; if (owner !== undefined) Object.defineProperty(ownedContext, 'valueContext', { value: owner, enumerable: true });
          const params = environment!.import(owner, (owner) => conversion.importRelieveRequest(owner as LiveOwner, raw));
          const result = await implementation.methods.relieve(params as Protocol.RelieveRequest, ownedContext);
          return (() => { const converted = result; validateWire("Shift", converted); return converted; })();
        },
      }));
      detach.push(registerWire(wire, ["watch"], {
        observer, family: "supervisor",
        request: async (raw, context) => {
          try { validateWire("Watch", raw); } catch (error) { throw new DuplexError('invalid_params', String(error)); }
          const owner = (true || true) ? environment!.child(undefined) : undefined; const ownedContext = Object.create(context) as WireModelContext & { valueContext: unknown }; if (owner !== undefined) Object.defineProperty(ownedContext, 'valueContext', { value: owner, enumerable: true });
          const params = environment!.import(owner, (owner) => conversion.importWatch(owner as LiveOwner, raw));
          const result = await implementation.methods.watch(params as Protocol.Watch, ownedContext);
          return environment!.export(owner, (owner) => { const converted = live_worker.exportJob(owner as LiveOwner, (result) as worker.Job); validateWire("worker.Job", converted); return converted; });
        },
      }));
    } catch (error) { for (const remove of detach.reverse()) remove(); throw error; }
  }
  function proxyClient(wire: Wire): Protocol.Client {
    return {
      methods: {
      },
      events: {
      },
    };
  }
  function bindClient(wire: Wire, implementation: Protocol.Client): void {
    if (!implementation?.methods || !implementation.events) throw new Error('model methods and events are required');
    const detach: Array<() => void> = [];
    try {
    } catch (error) { for (const remove of detach.reverse()) remove(); throw error; }
  }
  return { proxyServer, proxyClient, bindServer, bindClient };
}
/** Exposes one model session at a new local wire origin. */
export function toWire(model: Protocol.ServerModel, context: AdapterContext): Wire {
  const adapter = makeAdapter(context);
  const [access, binding] = wirePair({ ...context.options, families: { ...context.options?.families, [encodePath(["shift"])]: "supervisor", [encodePath(["relieve"])]: "supervisor", [encodePath(["watch"])]: "supervisor" } });
  try { const implementation = model(adapter.proxyClient(binding)); adapter.bindServer(binding, implementation); return access; } catch (error) { access.close(); throw error; }
}
/** Interprets a wire as the same model factory; the resulting session binds once. */
export async function fromWire(wire: Wire, context: AdapterContext): Promise<Protocol.ServerModel> {
  const adapter = makeAdapter(context);
  let bound = false;
  return remote => {
    if (bound) throw new DuplexError('already_bound', 'model already bound');
    bound = true;
    adapter.bindClient(wire, remote);
    return adapter.proxyServer(wire);
  };
}
export const errors = { refused: "refused" } as const;
export type ErrorCode = (typeof errors)[keyof typeof errors];
