// Code generated by nightseam. DO NOT EDIT.
import { DuplexPeer, DuplexError, type PeerOptions, type CallOptions, type EmitOptions, type RequestContext, type EventContext, type FrameConnection, type WebSocketLike } from "@nightseam/runtime";
import { validateWire } from "@example/supervisor-client/types";
import type * as Protocol from "@example/supervisor-client/types";
import { liveOver, scopeOf, type LiveOwner } from "@nightseam/live";
import * as conversion from "@example/supervisor-client/types";
import * as live_worker from "@example/worker-client";
import type * as worker from "@example/worker-client";
export * from "@example/supervisor-client/types";
export { DuplexError };
/** Typed handlers for the declaration's server side. */
export interface Handler {
  shift(params: Protocol.Shift, remote: Remote, context: RequestContext): string | Promise<string>;
  relieve(params: Protocol.RelieveRequest, remote: Remote, context: RequestContext & { owner: LiveOwner }): Protocol.Shift | Promise<Protocol.Shift>;
  watch(params: Protocol.Watch, remote: Remote, context: RequestContext & { owner: LiveOwner }): worker.Job | Promise<worker.Job>;
}
/** Client-originated event listeners installed before the connection reads its first frame. */
export interface Events {
}
/** Typed reverse calls and events for one connected client. */
export class Remote {
  readonly peer: DuplexPeer;
  constructor(peer: DuplexPeer) {
    this.peer = peer;
  }
  close(): void { this.peer.close(); }
}
/** Installs the typed binding before a host-configured peer starts reading. An existing live scope is preserved. */
export function install(peer: DuplexPeer, handler: Handler, events: Events = {}): Remote {
  if (!handler) throw new Error('handler is required');
  if (typeof handler.shift !== 'function') throw new Error("handler for shift is required");
  if (typeof handler.relieve !== 'function') throw new Error("handler for relieve is required");
  if (typeof handler.watch !== 'function') throw new Error("handler for watch is required");
  const remote = new Remote(peer);
  if (!scopeOf(peer)) liveOver(peer, {});
  peer.handle("shift", async (params, context) => { try { validateWire("Shift", params); } catch(error) { if (error instanceof DuplexError && error.code === 'contract_mismatch') throw error; throw new DuplexError('invalid_params', String(error)); } const result = await handler.shift(params as Protocol.Shift, remote, context); validateWire("string", result); return result; });
  peer.handle("relieve", async (raw, context) => { const scope = scopeOf(remote.peer); if (!scope) throw new DuplexError('scope_closed', 'the connection carries no live scope'); const owner = scope.owner().child(); const ownedContext = { ...context, owner }; try { validateWire({"kind":"record","fields":[{"name":"shift","type":"Shift","required":true},{"name":"sink","type":"worker.ProgressSink","required":true}]}, raw); } catch(error) { if (error instanceof DuplexError && error.code === 'contract_mismatch') throw error; throw new DuplexError('invalid_params', String(error)); } const params = owner.importValue((owner) => conversion.importRelieveRequest(owner, raw)); const result = await handler.relieve(params as Protocol.RelieveRequest, remote, ownedContext); return (() => { const converted = result; validateWire("Shift", converted); return converted; })(); });
  peer.handle("watch", async (raw, context) => { const scope = scopeOf(remote.peer); if (!scope) throw new DuplexError('scope_closed', 'the connection carries no live scope'); const owner = scope.owner().child(); const ownedContext = { ...context, owner }; try { validateWire("Watch", raw); } catch(error) { if (error instanceof DuplexError && error.code === 'contract_mismatch') throw error; throw new DuplexError('invalid_params', String(error)); } const params = owner.importValue((owner) => conversion.importWatch(owner, raw)); const result = await handler.watch(params as Protocol.Watch, remote, ownedContext); return owner.exportValue((owner) => { const converted = live_worker.exportJob(owner, (result) as worker.Job); validateWire("worker.Job", converted); return converted; }); });
  return remote;
}
/** Serves an externally authenticated accepted connection. The returned peer is the caller's to close. */
export async function serve(connection: FrameConnection | WebSocketLike, options: PeerOptions, handler: Handler, events: Events = {}): Promise<DuplexPeer> {
  const peer = new DuplexPeer({ ...{ ...options, families: { ...options.families, "shift": "supervisor", "relieve": "supervisor", "watch": "supervisor" } }, role: 'server' });
  try { install(peer, handler, events); await peer.attach(connection); return peer; } catch (error) { peer.close(); throw error; }
}
