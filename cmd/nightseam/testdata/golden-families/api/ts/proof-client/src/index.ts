// Code generated by nightseam. DO NOT EDIT.
import { DuplexError, callWire, emitWire, registerWire, wirePair, declarationDigest, familyTypeAdapter, jsonAdapter, validateDrawnType, identityHandler, IDENTITY_METHOD, prepareIdentity, type WireModelContext, type WireCallOptions } from "@nightseam/runtime";
import { encodePath, type Wire } from '@nightseam/duplex';
import type { AdapterContext, ValueAdapter, ValueContext } from "@nightseam/runtime";
import { validateWire } from './types.ts';
import type * as Protocol from './types.ts';
import type { AnyFamily, FamilyBinding, Slots } from './types.ts';
import * as conversion from './types.ts';
import type * as probe from "@example/probe-client";
export * from './types.ts';
export { DuplexError };
export type { AdapterContext };
function makeAdapter<S extends AnyFamily = AnyFamily, Item = unknown>(context: AdapterContext, s: FamilyBinding<S, "Envelope" | "Handle">, item: ValueAdapter<Item>) {
  const options = { ...context.options };
  const observer = options.observer;
  const propagator = options.propagator;
  const requestTimeoutMs = options.requestTimeoutMs;
  const bindings = { s, item };
  const slots: Slots = { "S": s, "Item": item.binding };
  familyTypeAdapter(s, "Envelope");
  familyTypeAdapter(s, "Handle");
  if (typeof item.export !== 'function' || typeof item.import !== 'function') throw new Error('missing complete value interpretation for Item');
  const identity = { path: "proof", digest: declarationDigest(validateWire, slots) };
  const environment = context.valueEnvironment;
  if ((s.types["Envelope"].needsContext || s.types["Handle"].needsContext || item.needsContext) && !environment) throw new DuplexError('scope_closed', 'model adaptation requires an explicit value environment');
  function hasModelHandler(facet: object, name: string): boolean {
    for (let current = facet; current !== null && current !== Object.prototype; current = Object.getPrototypeOf(current)) {
      if (Object.prototype.hasOwnProperty.call(current, name)) return typeof (facet as Record<string, unknown>)[name] === 'function';
    }
    return false;
  }
  function proxyServer(wire: Wire): Protocol.Server<S, Item> {
    return {
      methods: {
        async echo(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs ?? requestTimeoutMs, meta: context?.outgoingMeta, observer, propagator, family: "proof" };
          validateWire("probe.Payload", params, '$', slots);
          const result = await callWire<probe.Payload>(wire, ["echo"], params, options);
          validateWire("probe.Payload", result, '$', slots);
          return result;
        },
        async noArgs(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs ?? requestTimeoutMs, meta: context?.outgoingMeta, observer, propagator, family: "proof" };
          validateWire({ empty: true }, params, '$', slots);
          const result = await callWire<string>(wire, ["no_args"], params, options);
          validateWire("string", result, '$', slots);
          return result;
        },
        async classify(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs ?? requestTimeoutMs, meta: context?.outgoingMeta, observer, propagator, family: "proof" };
          validateWire("Part", params, '$', slots);
          const result = await callWire<string>(wire, ["classify"], params, options);
          validateWire("string", result, '$', slots);
          return result;
        },
        async classifyRich(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs ?? requestTimeoutMs, meta: context?.outgoingMeta, observer, propagator, family: "proof" };
          validateWire("RichPart", params, '$', slots);
          const result = await callWire<string>(wire, ["classify_rich"], params, options);
          validateWire("string", result, '$', slots);
          return result;
        },
        async parts(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs ?? requestTimeoutMs, meta: context?.outgoingMeta, observer, propagator, family: "proof" };
          validateWire({"kind":"record","fields":[{"name":"after","type":{"nullable":"string"},"required":false}]}, params, '$', slots);
          const result = await callWire<Protocol.Result<Protocol.Parts, string>>(wire, ["parts"], params, options);
          validateWire({"apply":"Result","with":{"E":"string","T":"Parts"}}, result, '$', slots);
          return result;
        },
        async relay(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs ?? requestTimeoutMs, meta: context?.outgoingMeta, observer, propagator, family: "proof" };
          const owner = ((bindings.s.types["Envelope"].needsContext || bindings.s.types["Handle"].needsContext || bindings.item.needsContext)) ? environment!.select((context as {valueContext?: unknown} | undefined)?.valueContext) : undefined;
          const result = await ((bindings.s.types["Envelope"].needsContext || bindings.s.types["Handle"].needsContext || bindings.item.needsContext) ? environment!.publish(owner, owner => ((bindings.s.types["Envelope"].needsContext || bindings.s.types["Handle"].needsContext || bindings.item.needsContext) ? environment!.export(owner, (owner) => { const converted = conversion.exportCarriedUnchecked<S, Item>((params) as Protocol.Carried<S, Item>, (input: S["Envelope"]): unknown => bindings.s.types["Envelope"].export(owner, (input) as S["Envelope"]), (input: S["Handle"]): unknown => bindings.s.types["Handle"].export(owner, (input) as S["Handle"]), (input: Item): unknown => bindings.item.export(owner, (input) as Item)); validateWire("Carried", converted, '$', slots); return converted; }) : (() => { const converted = conversion.exportCarriedUnchecked<S, Item>((params) as Protocol.Carried<S, Item>, (input: S["Envelope"]): unknown => bindings.s.types["Envelope"].export(owner, (input) as S["Envelope"]), (input: S["Handle"]): unknown => bindings.s.types["Handle"].export(owner, (input) as S["Handle"]), (input: Item): unknown => bindings.item.export(owner, (input) as Item)); validateWire("Carried", converted, '$', slots); return converted; })()), sent => callWire(wire, ["relay"], sent, options)) : callWire(wire, ["relay"], ((bindings.s.types["Envelope"].needsContext || bindings.s.types["Handle"].needsContext || bindings.item.needsContext) ? environment!.export(owner, (owner) => { const converted = conversion.exportCarriedUnchecked<S, Item>((params) as Protocol.Carried<S, Item>, (input: S["Envelope"]): unknown => bindings.s.types["Envelope"].export(owner, (input) as S["Envelope"]), (input: S["Handle"]): unknown => bindings.s.types["Handle"].export(owner, (input) as S["Handle"]), (input: Item): unknown => bindings.item.export(owner, (input) as Item)); validateWire("Carried", converted, '$', slots); return converted; }) : (() => { const converted = conversion.exportCarriedUnchecked<S, Item>((params) as Protocol.Carried<S, Item>, (input: S["Envelope"]): unknown => bindings.s.types["Envelope"].export(owner, (input) as S["Envelope"]), (input: S["Handle"]): unknown => bindings.s.types["Handle"].export(owner, (input) as S["Handle"]), (input: Item): unknown => bindings.item.export(owner, (input) as Item)); validateWire("Carried", converted, '$', slots); return converted; })()), options));
          validateWire({"apply":"Option","with":{"T":"Envelope"}}, result, '$', slots);
          return result as Protocol.Option<Protocol.Envelope>;
        },
      },
      events: {
      },
    };
  }
  function validateServer(implementation: Protocol.Server<S, Item>): void {
    if (!implementation?.methods || !implementation.events) throw new Error('model methods and events are required');
    if (!hasModelHandler(implementation.methods, "echo")) throw new Error("handler for echo is required");
    if (!hasModelHandler(implementation.methods, "noArgs")) throw new Error("handler for no_args is required");
    if (!hasModelHandler(implementation.methods, "classify")) throw new Error("handler for classify is required");
    if (!hasModelHandler(implementation.methods, "classifyRich")) throw new Error("handler for classify_rich is required");
    if (!hasModelHandler(implementation.methods, "parts")) throw new Error("handler for parts is required");
    if (!hasModelHandler(implementation.methods, "relay")) throw new Error("handler for relay is required");
  }
  function bindServer(wire: Wire, implementation: () => Protocol.Server<S, Item>): () => void {
    const detach: Array<() => void> = [];
    try {
      detach.push(registerWire(wire, ["echo"], {
        observer, family: "proof",
        request: async (raw, context) => {
          const target = implementation();
          try { validateWire("probe.Payload", raw, '$', slots); } catch (error) { if (error instanceof DuplexError && error.code === 'contract_mismatch') throw error; throw new DuplexError('invalid_params', String(error)); }
          const params = raw as probe.Payload;
          const result = await target.methods.echo(params as probe.Payload, context);
          return (() => { const converted = result; validateWire("probe.Payload", converted, '$', slots); return converted; })();
        },
      }));
      detach.push(registerWire(wire, ["no_args"], {
        observer, family: "proof",
        request: async (raw, context) => {
          const target = implementation();
          try { validateWire({ empty: true }, raw, '$', slots); } catch (error) { if (error instanceof DuplexError && error.code === 'contract_mismatch') throw error; throw new DuplexError('invalid_params', String(error)); }
          const params = raw;
          const result = await target.methods.noArgs(params as Record<string, never>, context);
          return (() => { const converted = result; validateWire("string", converted, '$', slots); return converted; })();
        },
      }));
      detach.push(registerWire(wire, ["classify"], {
        observer, family: "proof",
        request: async (raw, context) => {
          const target = implementation();
          try { validateWire("Part", raw, '$', slots); } catch (error) { if (error instanceof DuplexError && error.code === 'contract_mismatch') throw error; throw new DuplexError('invalid_params', String(error)); }
          const params = raw as Protocol.Part;
          const result = await target.methods.classify(params as Protocol.Part, context);
          return (() => { const converted = result; validateWire("string", converted, '$', slots); return converted; })();
        },
      }));
      detach.push(registerWire(wire, ["classify_rich"], {
        observer, family: "proof",
        request: async (raw, context) => {
          const target = implementation();
          try { validateWire("RichPart", raw, '$', slots); } catch (error) { if (error instanceof DuplexError && error.code === 'contract_mismatch') throw error; throw new DuplexError('invalid_params', String(error)); }
          const params = raw as Protocol.RichPart;
          const result = await target.methods.classifyRich(params as Protocol.RichPart, context);
          return (() => { const converted = result; validateWire("string", converted, '$', slots); return converted; })();
        },
      }));
      detach.push(registerWire(wire, ["parts"], {
        observer, family: "proof",
        request: async (raw, context) => {
          const target = implementation();
          try { validateWire({"kind":"record","fields":[{"name":"after","type":{"nullable":"string"},"required":false}]}, raw, '$', slots); } catch (error) { if (error instanceof DuplexError && error.code === 'contract_mismatch') throw error; throw new DuplexError('invalid_params', String(error)); }
          const params = raw as Protocol.PartsRequest;
          const result = await target.methods.parts(params as Protocol.PartsRequest, context);
          return (() => { const converted = result; validateWire({"apply":"Result","with":{"E":"string","T":"Parts"}}, converted, '$', slots); return converted; })();
        },
      }));
      detach.push(registerWire(wire, ["relay"], {
        observer, family: "proof",
        request: async (raw, context) => {
          const target = implementation();
          try { validateWire("Carried", raw, '$', slots); } catch (error) { if (error instanceof DuplexError && error.code === 'contract_mismatch') throw error; throw new DuplexError('invalid_params', String(error)); }
          const owner = ((bindings.s.types["Envelope"].needsContext || bindings.s.types["Handle"].needsContext || bindings.item.needsContext)) ? environment!.child(undefined) : undefined; const ownedContext = Object.create(context) as ValueContext<Protocol.Carried<S, Item> | Protocol.Option<Protocol.Envelope>, WireModelContext>; if (owner !== undefined) Object.defineProperty(ownedContext, 'valueContext', { value: owner, enumerable: true });
          const params = ((bindings.s.types["Envelope"].needsContext || bindings.s.types["Handle"].needsContext || bindings.item.needsContext) ? environment!.import(owner, (owner) => conversion.importCarriedUnchecked<S, Item>(raw, (input: unknown): S["Envelope"] => (bindings.s.types["Envelope"].import(owner, input)) as S["Envelope"], (input: unknown): S["Handle"] => (bindings.s.types["Handle"].import(owner, input)) as S["Handle"], (input: unknown): Item => (bindings.item.import(owner, input)) as Item)) : conversion.importCarriedUnchecked<S, Item>(raw, (input: unknown): S["Envelope"] => (bindings.s.types["Envelope"].import(owner, input)) as S["Envelope"], (input: unknown): S["Handle"] => (bindings.s.types["Handle"].import(owner, input)) as S["Handle"], (input: unknown): Item => (bindings.item.import(owner, input)) as Item));
          const result = await target.methods.relay(params as Protocol.Carried<S, Item>, ownedContext);
          return (() => { const converted = result; validateWire({"apply":"Option","with":{"T":"Envelope"}}, converted, '$', slots); return converted; })();
        },
      }));
    } catch (error) { for (const remove of detach.reverse()) remove(); throw error; }
    return () => { for (const remove of detach.reverse()) remove(); };
  }
  function proxyClient(wire: Wire): Protocol.Client<S, Item> {
    return {
      methods: {
      },
      events: {
        async changed(data, context) {
          const options = { context, meta: context?.outgoingMeta, observer, propagator, family: "proof" };
          validateWire("probe.Payload", data, '$', slots);
          emitWire(wire, ["changed"], data, options);
        },
        async partAdded(data, context) {
          const options = { context, meta: context?.outgoingMeta, observer, propagator, family: "proof" };
          validateWire("RichPart", data, '$', slots);
          emitWire(wire, ["part.added"], data, options);
        },
      },
    };
  }
  function validateClient(implementation: Protocol.Client<S, Item>): void {
    if (!implementation?.methods || !implementation.events) throw new Error('model methods and events are required');
    if (!hasModelHandler(implementation.events, "changed")) throw new Error("event handler for changed is required");
    if (!hasModelHandler(implementation.events, "partAdded")) throw new Error("event handler for part.added is required");
  }
  function bindClient(wire: Wire, implementation: () => Protocol.Client<S, Item>): () => void {
    const detach: Array<() => void> = [];
    try {
      detach.push(registerWire(wire, ["changed"], {
        observer, family: "proof",
        event: async (raw, context) => {
          const target = implementation();
          try { validateWire("probe.Payload", raw, '$', slots); } catch (error) { wire.close(); throw error; }
          await target.events.changed(raw as probe.Payload, context);
        },
      }));
      detach.push(registerWire(wire, ["part.added"], {
        observer, family: "proof",
        event: async (raw, context) => {
          const target = implementation();
          try { validateWire("RichPart", raw, '$', slots); } catch (error) { wire.close(); throw error; }
          await target.events.partAdded(raw as Protocol.RichPart, context);
        },
      }));
    } catch (error) { for (const remove of detach.reverse()) remove(); throw error; }
    return () => { for (const remove of detach.reverse()) remove(); };
  }
  return { options, identity, proxyServer, proxyClient, validateServer, validateClient, bindServer, bindClient };
}
/** Exposes one model session at a new local wire origin. */
export function toWire<S extends AnyFamily = AnyFamily, Item = unknown>(model: Protocol.ClientModel<S, Item>, context: AdapterContext, s: FamilyBinding<S, "Envelope" | "Handle">, item: ValueAdapter<Item>): Wire {
  const adapter = makeAdapter<S, Item>(context, s, item);
  const [access, binding] = wirePair({ ...adapter.options, families: { ...adapter.options.families, [encodePath(["echo"])]: "proof", [encodePath(["no_args"])]: "proof", [encodePath(["classify"])]: "proof", [encodePath(["classify_rich"])]: "proof", [encodePath(["parts"])]: "proof", [encodePath(["relay"])]: "proof", [encodePath(["changed"])]: "proof", [encodePath(["part.added"])]: "proof" } });
  try {
    registerWire(binding, [IDENTITY_METHOD], { request: identityHandler(adapter.identity) });
    const implementation = model(adapter.proxyServer(binding));
    adapter.validateClient(implementation);
    adapter.bindClient(binding, () => implementation);
    return access;
  } catch (error) { access.close(); throw error; }
}
/** Registers receivers before reading begins; complete checks identity before returning the one-use model factory. Close detaches this interpretation without closing its carrier. */
export function prepareFromWire<S extends AnyFamily = AnyFamily, Item = unknown>(wire: Wire, context: AdapterContext, s: FamilyBinding<S, "Envelope" | "Handle">, item: ValueAdapter<Item>): { complete(options?: WireCallOptions): Promise<Protocol.ClientModel<S, Item>>; close(): void } {
  const adapter = makeAdapter<S, Item>(context, s, item);
  const gate = prepareIdentity(wire, adapter.identity, adapter.options);
  let implementation: Protocol.Server<S, Item> | undefined;
  let completed = false, bound = false, closed = false;
  const detach: Array<() => void> = [];
  const cleanup = () => { if (closed) return; closed = true; implementation = undefined; gate.close(); for (const remove of detach.reverse()) remove(); };
  try {
    detach.push(adapter.bindServer(gate.wire, () => implementation!));
  } catch (error) { cleanup(); throw error; }
  return {
    async complete(options?: WireCallOptions): Promise<Protocol.ClientModel<S, Item>> {
      if (closed) throw new DuplexError('scope_closed', 'model interpretation is closed');
      if (completed) throw new DuplexError('already_bound', 'model interpretation already completed');
      completed = true;
      try { await gate.check(options); } catch (error) { cleanup(); throw error; }
      return remote => {
        if (closed) throw new DuplexError('scope_closed', 'model interpretation is closed');
        if (bound) throw new DuplexError('already_bound', 'model already bound');
        bound = true;
        try {
          adapter.validateServer(remote);
          implementation = remote;
          gate.ready();
          return adapter.proxyClient(gate.wire);
        } catch (error) { cleanup(); throw error; }
      };
    },
    close: cleanup,
  };
}
/** Checks and interprets a wire as the same model factory; the resulting session binds once. */
export async function fromWire<S extends AnyFamily = AnyFamily, Item = unknown>(wire: Wire, context: AdapterContext, s: FamilyBinding<S, "Envelope" | "Handle">, item: ValueAdapter<Item>): Promise<Protocol.ClientModel<S, Item>> {
  const preparation = prepareFromWire<S, Item>(wire, context, s, item);
  try { return await preparation.complete(); } catch (error) { preparation.close(); throw error; }
}
export const errors = { denied: "denied" } as const;
export type ErrorCode = (typeof errors)[keyof typeof errors];
