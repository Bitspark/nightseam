# pair

<!-- Generated by nightseam from api/contracts/pair/; do not edit. -->

A family of the `nightseam.duplex/1` profile, declared in `model.json`, `protocol.json`.
It imports `probe`.
The tiers it declares bring the built-in family `duplex`, imported implicitly.

## Parameters

The family is instantiated by filling these parameters:

- **S**: a family with the `session` tier, drawn at `Envelope`, `Handle`.
- **T**: a family with the `session` tier, drawn at `Envelope`.

## Types

### Both

A record, generic in `S.Envelope`, `S.Handle`, `T.Envelope`.

| Field | Type | Presence | Constraints | Description |
|---|---|---|---|---|
| `frame` | `Frame` | required | — |  |
| `echoes` | array of `Echo` | required | — |  |

For example:

```json
{
  "frame": {
    "message": "‹S.Envelope›",
    "back": "‹S.Handle›"
  },
  "echoes": [
    {
      "heard": "‹T.Envelope›"
    }
  ]
}
```

Used by `relay` (result).

### Echo

A record, generic in `T.Envelope`.

| Field | Type | Presence | Constraints | Description |
|---|---|---|---|---|
| `heard` | `T.Envelope` | required | — |  |

For example:

```json
{
  "heard": "‹T.Envelope›"
}
```

Used by `Both.echoes`, `echoed` (data).

### Frame

A record, generic in `S.Envelope`, `S.Handle`.

| Field | Type | Presence | Constraints | Description |
|---|---|---|---|---|
| `message` | `S.Envelope` | required | — |  |
| `back` | `S.Handle` | required | — |  |

For example:

```json
{
  "message": "‹S.Envelope›",
  "back": "‹S.Handle›"
}
```

Used by `Both.frame`.

### Named

A record.

| Field | Type | Presence | Constraints | Description |
|---|---|---|---|---|
| `held` | `probe.Envelope` | required | — |  |

For example:

```json
{
  "held": {
    "version": 0,
    "kind": "‹kind›",
    "id": "‹id›",
    "method": "‹method›",
    "params": {},
    "result": {},
    "error": {},
    "event": "‹event›",
    "data": {},
    "traceparent": "‹traceparent›",
    "tracestate": "‹tracestate›",
    "meta": {
      "‹key›": "‹meta›"
    }
  }
}
```

Used by `named` (request, result).

## Carried types

These are this family's own types, declared in the same language by a built-in family Nightseam declares of itself and imported by the tier that brings it, with no `imports` line. A declaration of this family may not declare one and names it by the built-in that declares it — `duplex.Envelope`.

### Envelope

A record, carried from the built-in `duplex` family. One message of the nightseam.duplex/1 profile: the members the peer acts on, and nothing else.

| Field | Type | Presence | Constraints | Description |
|---|---|---|---|---|
| `version` | `integer` | required | — | The profile's version, 1. |
| `kind` | `string` | required | — | request, response, event or cancel. |
| `id` | `string` | optional | — | What correlates a response or a cancel with its request. |
| `method` | `string` | optional | — | The method a request names. |
| `params` | `json` | optional | — | A request's parameters. |
| `result` | `json` | optional | — | A response's result. |
| `error` | `json` | optional | — | A response's error. |
| `event` | `string` | optional | — | The event an event frame names. |
| `data` | `json` | optional | — | An event's data. |
| `traceparent` | `string` | optional | — | The W3C Trace Context of the frame. |
| `tracestate` | `string` | optional | — | The vendor state of that trace. |
| `meta` | map of `string` | optional | — | What a request or an event carries about the call, delivered to the handler beside the payload. |

### Handle

A record, carried from the built-in `duplex` family. A reference to a channel on the connection that carries the message holding it.

| Field | Type | Presence | Constraints | Description |
|---|---|---|---|---|
| `channel` | `integer` | required | — | The channel's id on that connection. |

## Server side

The server implements these methods and emits these events.

| Method | Request | Result | Errors | Description |
|---|---|---|---|---|
| `named` | `Named` | `Named` | — |  |
| `relay` | `T.Envelope` | `Both` | — |  |

| Event | Data | Description |
|---|---|---|
| `echoed` | `Echo` |  |

### `named` on the wire

The client sends:

```json
{
  "version": 1,
  "kind": "request",
  "id": "c:1",
  "method": "named",
  "params": {
    "held": {
      "version": 0,
      "kind": "‹kind›",
      "id": "‹id›",
      "method": "‹method›",
      "params": {},
      "result": {},
      "error": {},
      "event": "‹event›",
      "data": {},
      "traceparent": "‹traceparent›",
      "tracestate": "‹tracestate›",
      "meta": {
        "‹key›": "‹meta›"
      }
    }
  }
}
```

The server answers:

```json
{
  "version": 1,
  "kind": "response",
  "id": "c:1",
  "result": {
    "held": {
      "version": 0,
      "kind": "‹kind›",
      "id": "‹id›",
      "method": "‹method›",
      "params": {},
      "result": {},
      "error": {},
      "event": "‹event›",
      "data": {},
      "traceparent": "‹traceparent›",
      "tracestate": "‹tracestate›",
      "meta": {
        "‹key›": "‹meta›"
      }
    }
  }
}
```

### `relay` on the wire

The client sends:

```json
{
  "version": 1,
  "kind": "request",
  "id": "c:1",
  "method": "relay",
  "params": "‹T.Envelope›"
}
```

The server answers:

```json
{
  "version": 1,
  "kind": "response",
  "id": "c:1",
  "result": {
    "frame": {
      "message": "‹S.Envelope›",
      "back": "‹S.Handle›"
    },
    "echoes": [
      {
        "heard": "‹T.Envelope›"
      }
    ]
  }
}
```

### `echoed` on the wire

The server emits:

```json
{
  "version": 1,
  "kind": "event",
  "event": "echoed",
  "data": {
    "heard": "‹T.Envelope›"
  }
}
```
