x/model.json#/types/A/fields/0/pattern: ^[[:alpha:]]+$: it is not a Nightseam Unicode regular expression: "[[:" is a POSIX class of RE2, which ECMAScript reads as an ordinary character class and quietly means something else by at byte 1. A pattern is written in Nightseam's own regular-expression language: ECMAScript Unicode syntax without lookaround and without backreferences, matching code points with ECMAScript character classes and line terminators. [invalid_pattern]
x/model.json#/types/A/fields/1/pattern: ^(?i)abc$: it is not a Nightseam Unicode regular expression: unsupported group prefix at byte 2. A pattern is written in Nightseam's own regular-expression language: ECMAScript Unicode syntax without lookaround and without backreferences, matching code points with ECMAScript character classes and line terminators. [invalid_pattern]
x/model.json#/types/A/fields/2/pattern: ^(?=a)ab$: it is not a Nightseam Unicode regular expression: "(?=" is a lookahead of ECMAScript, which the dialect has no lookaround in at byte 1. A pattern is written in Nightseam's own regular-expression language: ECMAScript Unicode syntax without lookaround and without backreferences, matching code points with ECMAScript character classes and line terminators. [invalid_pattern]
x/model.json#/types/A/fields/3/pattern: ^(?<=a)b$: it is not a Nightseam Unicode regular expression: "(?<=" is a lookbehind of ECMAScript, which the dialect has no lookaround in at byte 1. A pattern is written in Nightseam's own regular-expression language: ECMAScript Unicode syntax without lookaround and without backreferences, matching code points with ECMAScript character classes and line terminators. [invalid_pattern]
error: 4 problems
