// Code generated by nightseam. DO NOT EDIT.
import { createValidator, withDeclaration, jsonAdapter, familyTypeAdapter, DuplexError, type AnyFamily, type FamilyBinding, type TypeBinding, type Slots, type TypeExpression, type WireFamily } from "@nightseam/runtime";
export type { AnyFamily, FamilyBinding, TypeBinding, Slots, TypeExpression };
import type * as probe from "@example/probe-client";
import { validateWire as validate_probe } from "@example/probe-client";
import type { ValueAdapter, ValueContext } from "@nightseam/runtime";
import type { WireModelContext } from "@nightseam/runtime";
export interface AttachParams {
  "id": string;
}
/** A channel that speaks S, and the last sequence it saw. */
export interface Attachment<S extends AnyFamily = AnyFamily> {
  "connection": S["Handle"];
  "last": number;
}
/** One message of the nightseam.duplex/1 profile: the members the peer acts on, and nothing else. */
export interface Envelope {
  /** The profile's version, 1. */
  "version": number;
  /** request, response, event or cancel. */
  "kind": string;
  /** What correlates a response or a cancel with its request. */
  "id"?: string;
  /** The method a request names. */
  "method"?: string;
  /** A request's parameters. */
  "params"?: unknown;
  /** A response's result. */
  "result"?: unknown;
  /** A response's error. */
  "error"?: unknown;
  /** The event an event frame names. */
  "event"?: string;
  /** An event's data. */
  "data"?: unknown;
  /** The W3C Trace Context of the frame. */
  "traceparent"?: string;
  /** The vendor state of that trace. */
  "tracestate"?: string;
  /** What a request or an event carries about the call, delivered to the handler beside the payload. */
  "meta"?: Record<string, string>;
}
/** One message of S with its place in the sequence. */
export interface Frame<S extends AnyFamily = AnyFamily> {
  "sequence": number;
  "message": S["Envelope"];
}
export type Frames<S extends AnyFamily = AnyFamily> = Array<Frame<S>>;
/** A reference to a channel on the connection that carries the message holding it. */
export interface Handle {
  /** The channel's id on that connection. */
  "channel": number;
}
/** The family: its name and the wire types a slot of it draws on. */
export interface Family { readonly name: "carrier"; AttachParams: AttachParams; Envelope: Envelope; Handle: Handle }
/** Conversion only: use the value adapter or validate the entire wire value inside the same operation batch. */
/** Writes Attachment using the supplied conversion for each type argument. */
export function exportAttachmentUnchecked<S extends AnyFamily = AnyFamily>(value: Attachment<S>, convert_S_Handle: (value: S["Handle"]) => unknown): unknown {
  const out: Record<string, unknown> = {};
  out["connection"] = convert_S_Handle((value["connection"]) as S["Handle"]);
  out["last"] = value["last"];
  return out;
}
/** Reads Attachment using the supplied conversion for each type argument. */
/** Conversion only: validation belongs to the value adapter or its caller. */
export function importAttachmentUnchecked<S extends AnyFamily = AnyFamily>(raw: unknown, convert_S_Handle: (value: unknown) => S["Handle"]): Attachment<S> {
  const wire = raw as Record<string, unknown>;
  const out: Record<string, unknown> = {};
  out["connection"] = convert_S_Handle(wire["connection"]);
  out["last"] = wire["last"];
  return out as unknown as Attachment<S>;
}
/** Conversion only: use the value adapter or validate the entire wire value inside the same operation batch. */
/** Writes Frame using the supplied conversion for each type argument. */
export function exportFrameUnchecked<S extends AnyFamily = AnyFamily>(value: Frame<S>, convert_S_Envelope: (value: S["Envelope"]) => unknown): unknown {
  const out: Record<string, unknown> = {};
  out["sequence"] = value["sequence"];
  out["message"] = convert_S_Envelope((value["message"]) as S["Envelope"]);
  return out;
}
/** Reads Frame using the supplied conversion for each type argument. */
/** Conversion only: validation belongs to the value adapter or its caller. */
export function importFrameUnchecked<S extends AnyFamily = AnyFamily>(raw: unknown, convert_S_Envelope: (value: unknown) => S["Envelope"]): Frame<S> {
  const wire = raw as Record<string, unknown>;
  const out: Record<string, unknown> = {};
  out["sequence"] = wire["sequence"];
  out["message"] = convert_S_Envelope(wire["message"]);
  return out as unknown as Frame<S>;
}
/** Conversion only: use the value adapter or validate the entire wire value inside the same operation batch. */
/** Writes Frames using the supplied conversion for each type argument. */
export function exportFramesUnchecked<S extends AnyFamily = AnyFamily>(value: Frames<S>, convert_S_Envelope: (value: S["Envelope"]) => unknown): unknown {
  return (value as unknown[]).map((item) => exportFrameUnchecked<S>((item) as Frame<S>, (input: S["Envelope"]): unknown => convert_S_Envelope((input) as S["Envelope"])));
}
/** Reads Frames using the supplied conversion for each type argument. */
/** Conversion only: validation belongs to the value adapter or its caller. */
export function importFramesUnchecked<S extends AnyFamily = AnyFamily>(raw: unknown, convert_S_Envelope: (value: unknown) => S["Envelope"]): Frames<S> {
  return (raw as unknown[]).map((item) => importFrameUnchecked<S>(item, (input: unknown): S["Envelope"] => (convert_S_Envelope(input)) as S["Envelope"]));
}
export function adapterAttachParams(): ValueAdapter<AttachParams> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "AttachParams", validate: validateWire, slots };
  const needsContext = false;
  return { binding, needsContext,
    export(context: unknown, value: AttachParams): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = value;
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): AttachParams {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): AttachParams => {
        const converted = raw as AttachParams;
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterAttachment<S extends AnyFamily = AnyFamily>(slot_S: FamilyBinding<S, "Handle">): ValueAdapter<Attachment<S>> {
  const type_S_Handle = familyTypeAdapter(slot_S, "Handle");
  const slots: Slots = { "S": slot_S };
  const binding: TypeBinding = { type: "Attachment", validate: validateWire, slots };
  const needsContext = type_S_Handle.needsContext;
  return { binding, needsContext,
    export(context: unknown, value: Attachment<S>): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = exportAttachmentUnchecked<S>(value, (value: S["Handle"]) => type_S_Handle.export(owner, value));
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Attachment<S> {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Attachment<S> => {
        const converted = importAttachmentUnchecked<S>(raw, (value: unknown) => type_S_Handle.import(owner, value));
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterFrame<S extends AnyFamily = AnyFamily>(slot_S: FamilyBinding<S, "Envelope">): ValueAdapter<Frame<S>> {
  const type_S_Envelope = familyTypeAdapter(slot_S, "Envelope");
  const slots: Slots = { "S": slot_S };
  const binding: TypeBinding = { type: "Frame", validate: validateWire, slots };
  const needsContext = type_S_Envelope.needsContext;
  return { binding, needsContext,
    export(context: unknown, value: Frame<S>): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = exportFrameUnchecked<S>(value, (value: S["Envelope"]) => type_S_Envelope.export(owner, value));
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Frame<S> {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Frame<S> => {
        const converted = importFrameUnchecked<S>(raw, (value: unknown) => type_S_Envelope.import(owner, value));
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterFrames<S extends AnyFamily = AnyFamily>(slot_S: FamilyBinding<S, "Envelope">): ValueAdapter<Frames<S>> {
  const type_S_Envelope = familyTypeAdapter(slot_S, "Envelope");
  const slots: Slots = { "S": slot_S };
  const binding: TypeBinding = { type: "Frames", validate: validateWire, slots };
  const needsContext = type_S_Envelope.needsContext;
  return { binding, needsContext,
    export(context: unknown, value: Frames<S>): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = exportFramesUnchecked<S>(value, (value: S["Envelope"]) => type_S_Envelope.export(owner, value));
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Frames<S> {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Frames<S> => {
        const converted = importFramesUnchecked<S>(raw, (value: unknown) => type_S_Envelope.import(owner, value));
        return converted;
      };
      return convert(context);
    },
  };
}
export interface ServerMethods<S extends AnyFamily = AnyFamily> {
  attach(params: AttachParams, context?: ValueContext<AttachParams | Attachment<S>, WireModelContext>): Attachment<S> | Promise<Attachment<S>>;
  relay(params: Frame<S>, context?: ValueContext<Frame<S> | probe.Envelope, WireModelContext>): probe.Envelope | Promise<probe.Envelope>;
}
export interface ServerEvents<S extends AnyFamily = AnyFamily> {
}
export interface Server<S extends AnyFamily = AnyFamily> { methods: ServerMethods<S>; events: ServerEvents<S>; }
export interface ClientMethods<S extends AnyFamily = AnyFamily> {
}
export interface ClientEvents<S extends AnyFamily = AnyFamily> {
  frameRelayed(data: Frame<S>, context?: ValueContext<Frame<S>, WireModelContext>): void | Promise<void>;
}
export interface Client<S extends AnyFamily = AnyFamily> { methods: ClientMethods<S>; events: ClientEvents<S>; }
export type ServerModel<S extends AnyFamily = AnyFamily> = (remote: Client<S>) => Server<S>;
export type ClientModel<S extends AnyFamily = AnyFamily> = (remote: Server<S>) => Client<S>;

const contractTypes = {"types":{"AttachParams":{"kind":"record","fields":[{"name":"id","type":"string","required":true}]},"Attachment":{"kind":"record","fields":[{"name":"connection","type":"S.Handle","required":true},{"name":"last","type":"integer","required":true}]},"Envelope":{"kind":"record","fields":[{"name":"version","type":"integer","required":true},{"name":"kind","type":"string","required":true},{"name":"id","type":"string","required":false},{"name":"method","type":"string","required":false},{"name":"params","type":"json","required":false},{"name":"result","type":"json","required":false},{"name":"error","type":"json","required":false},{"name":"event","type":"string","required":false},{"name":"data","type":"json","required":false},{"name":"traceparent","type":"string","required":false},{"name":"tracestate","type":"string","required":false},{"name":"meta","type":{"map":"string"},"required":false}]},"Frame":{"kind":"record","fields":[{"name":"sequence","type":"integer","required":true},{"name":"message","type":"S.Envelope","required":true}]},"Frames":{"kind":"alias","type":{"array":"Frame"}},"Handle":{"kind":"record","fields":[{"name":"channel","type":"integer","required":true}]}},"parameters":[{"name":"S","of":"protocol"}]} as unknown as WireFamily;
/** Canonical language-neutral declaration bytes, the input to wireDigest. */
export const wireDeclaration = "{\"definitions\":{\"carrier\":{\"client\":{\"ref\":\"carrier/$client\"},\"errors\":[],\"kind\":\"family\",\"parameters\":[{\"name\":\"S\",\"of\":\"protocol\"}],\"server\":{\"ref\":\"carrier/$server\"},\"types\":{\"AttachParams\":{\"ref\":\"carrier/AttachParams\"},\"Attachment\":{\"ref\":\"carrier/Attachment\"},\"Frame\":{\"ref\":\"carrier/Frame\"},\"Frames\":{\"array\":{\"ref\":\"carrier/Frame\"}}}},\"carrier/$client\":{\"direction\":\"client\",\"events\":{},\"extends\":[],\"kind\":\"side\",\"methods\":{},\"parameters\":[{\"name\":\"S\",\"of\":\"protocol\"}]},\"carrier/$server\":{\"direction\":\"server\",\"events\":{\"frame.relayed\":{\"ref\":\"carrier/Frame\"}},\"extends\":[],\"kind\":\"side\",\"methods\":{\"attach\":{\"errors\":[],\"request\":{\"ref\":\"carrier/AttachParams\"},\"result\":{\"ref\":\"carrier/Attachment\"}},\"relay\":{\"errors\":[],\"request\":{\"ref\":\"carrier/Frame\"},\"result\":{\"family\":{\"ref\":\"probe\"},\"projection\":\"Envelope\"}}},\"parameters\":[{\"name\":\"S\",\"of\":\"protocol\"}]},\"carrier/AttachParams\":{\"fields\":[{\"name\":\"id\",\"nullable\":false,\"required\":true,\"type\":{\"primitive\":\"string\"},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]},\"carrier/Attachment\":{\"captures\":[{\"name\":\"S\",\"of\":\"protocol\"}],\"fields\":[{\"name\":\"connection\",\"nullable\":false,\"required\":true,\"type\":{\"draw\":{\"parameter\":\"carrier/S\"},\"name\":\"Handle\"},\"unique\":false},{\"name\":\"last\",\"nullable\":false,\"required\":true,\"type\":{\"primitive\":\"integer\"},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]},\"carrier/Frame\":{\"captures\":[{\"name\":\"S\",\"of\":\"protocol\"}],\"fields\":[{\"name\":\"sequence\",\"nullable\":false,\"required\":true,\"type\":{\"primitive\":\"integer\"},\"unique\":false},{\"name\":\"message\",\"nullable\":false,\"required\":true,\"type\":{\"draw\":{\"parameter\":\"carrier/S\"},\"name\":\"Envelope\"},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]},\"probe\":{\"client\":{\"ref\":\"probe/$client\"},\"errors\":[\"denied\",\"not_found\"],\"kind\":\"family\",\"parameters\":[],\"server\":{\"ref\":\"probe/$server\"},\"types\":{\"Base\":{\"ref\":\"probe/Base\"},\"Carried\":{\"ref\":\"probe/Carried\"},\"Counts\":{\"map\":{\"primitive\":\"integer\"}},\"OpenRecord\":{\"ref\":\"probe/OpenRecord\"},\"Payload\":{\"ref\":\"probe/Payload\"},\"Payloads\":{\"array\":{\"ref\":\"probe/Payload\"}},\"Seen\":{\"ref\":\"probe/Seen\"},\"Status\":{\"ref\":\"probe/Status\"}}},\"probe/$client\":{\"direction\":\"client\",\"events\":{\"noticed\":{\"ref\":\"probe/Seen\"}},\"extends\":[],\"kind\":\"side\",\"methods\":{\"reverse\":{\"errors\":[],\"request\":{\"ref\":\"probe/Payload\"},\"result\":{\"ref\":\"probe/Payload\"}}},\"parameters\":[]},\"probe/$server\":{\"direction\":\"server\",\"events\":{\"changed\":{\"ref\":\"probe/Payload\"}},\"extends\":[],\"kind\":\"side\",\"methods\":{\"echo\":{\"errors\":[],\"request\":{\"ref\":\"probe/Payload\"},\"result\":{\"ref\":\"probe/Payload\"}},\"no_args\":{\"errors\":[],\"request\":{\"empty\":true},\"result\":{\"primitive\":\"string\"}},\"seen\":{\"errors\":[],\"request\":{\"ref\":\"probe/Seen\"},\"result\":{\"array\":{\"ref\":\"probe/Payload\"}}}},\"parameters\":[]},\"probe/Base\":{\"fields\":[{\"name\":\"text\",\"nullable\":false,\"required\":true,\"type\":{\"primitive\":\"string\"},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]},\"probe/Carried\":{\"kind\":\"union\",\"parameters\":[],\"tag\":\"kind\",\"value\":\"value\",\"variants\":{\"envelope\":{\"family\":{\"ref\":\"probe\"},\"projection\":\"Envelope\"},\"handle\":{\"family\":{\"ref\":\"probe\"},\"projection\":\"Handle\"},\"none\":{\"empty\":true}}},\"probe/OpenRecord\":{\"fields\":[{\"name\":\"id\",\"nullable\":false,\"required\":true,\"type\":{\"primitive\":\"string\"},\"unique\":false},{\"name\":\"note\",\"nullable\":false,\"required\":false,\"type\":{\"primitive\":\"string\"},\"unique\":false}],\"kind\":\"record\",\"open\":true,\"parameters\":[]},\"probe/Payload\":{\"extends\":[{\"ref\":\"probe/Base\"}],\"fields\":[{\"name\":\"count\",\"nullable\":false,\"required\":true,\"type\":{\"primitive\":\"integer\"},\"unique\":false},{\"name\":\"note\",\"nullable\":true,\"required\":false,\"type\":{\"primitive\":\"string\"},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]},\"probe/Seen\":{\"fields\":[{\"name\":\"at\",\"nullable\":false,\"required\":true,\"type\":{\"primitive\":\"timestamp\"},\"unique\":false},{\"name\":\"status\",\"nullable\":false,\"required\":true,\"type\":{\"ref\":\"probe/Status\"},\"unique\":false},{\"name\":\"extra\",\"nullable\":false,\"required\":false,\"type\":{\"primitive\":\"json\"},\"unique\":false},{\"name\":\"ratio\",\"nullable\":false,\"required\":true,\"type\":{\"primitive\":\"number\"},\"unique\":false},{\"name\":\"ok\",\"nullable\":false,\"required\":true,\"type\":{\"primitive\":\"boolean\"},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]},\"probe/Status\":{\"kind\":\"enum\",\"parameters\":[],\"values\":[\"context.example\",\"done\",\"ready\"]}},\"root\":{\"ref\":\"carrier\"},\"version\":1}";
/** SHA-256 of the canonical full declaration's exact UTF-8 bytes. */
export const wireDigest = "d8d193970574dd32d4b69dea0332aa9292e50f03acd7edcb8373c5b8355d1327";
/** Runtime validation applies equally to calls, replies, reverse calls and events; what fills a slot of a parameter is validated by the binding of the family that fills it. */
export const validateWire = withDeclaration(createValidator(contractTypes, wireDigest, { "probe": validate_probe }), wireDeclaration);
/** This family's declaration and reusable interpretations, with no operation context or owner. */
export const family = { name: "carrier", validate: validateWire, types: { "AttachParams": adapterAttachParams(), "Envelope": jsonAdapter<Envelope>({ type: "Envelope", validate: validateWire }), "Handle": jsonAdapter<Handle>({ type: "Handle", validate: validateWire }) } } as const;
