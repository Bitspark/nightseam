// Code generated by nightseam. DO NOT EDIT.
import { DuplexPeer, DuplexError, type PeerOptions, type CallOptions, type EmitOptions, type RequestContext, type EventContext, type FrameConnection } from "@nightseam/runtime";
import type { Tunnel } from "@nightseam/tunnel";
import { validateWire } from './types.ts';
import type * as Protocol from './types.ts';
export * from './types.ts';
export { DuplexError };
/** Typed event handlers installed before the client reads its first frame. Omitted fields leave events unhandled. */
export interface Events {
  settled?: (data: Protocol.Outcome, context: EventContext) => void | Promise<void>;
}
export interface Handler {
  /** The reverse direction: the server hands the client callables to call. */
  supervise(params: Protocol.Supervise, context: RequestContext): Protocol.Outcome | Promise<Protocol.Outcome>;
}
export interface Caller {
  describe(params: Protocol.Ticket, options?: CallOptions): Promise<string>;
  start(params: Protocol.Start, options?: CallOptions): Promise<Protocol.Job>;
}
/** The public errors of the family: what the code of a DuplexError a call rejects with may be. */
export const errors = { /** The caller is denied. */ denied: "denied", /** The job is no longer there. */ gone: "gone" } as const;
/** One of the family's public error codes. */
export type ErrorCode = (typeof errors)[keyof typeof errors];
export class Client implements Caller {
  readonly peer: DuplexPeer;
  constructor(peer: DuplexPeer, handler: Handler | undefined, events: Events) {
    this.peer = peer;
    if (!handler) throw new Error('reverse-call handler is required');
    peer.handle("supervise", async (params, context) => { try { validateWire("Supervise", params); } catch(error) { throw new DuplexError('invalid_params', String(error)); } const result = await handler.supervise(params as Protocol.Supervise, context); validateWire("Outcome", result); return result; });
    if (events.settled) this.onSettled(events.settled);
  }
  /** Connects to a WebSocket endpoint and speaks the family over it. */
  static async dial(url: string, options: PeerOptions, handler: Handler | undefined, events: Events): Promise<Client> { const peer = new DuplexPeer({ ...options, families: { ...options.families, "describe": "worker", "start": "worker", "supervise": "worker", "settled": "worker" } }); const client = new Client(peer, handler, events); await peer.connect(url); return client; }
  /** Speaks the family over a connection of the seam — a tunnel channel, a pipe, an open socket — as the client side of it. */
  static async attach(connection: FrameConnection, options: PeerOptions, handler: Handler | undefined, events: Events): Promise<Client> { const peer = new DuplexPeer({ ...options, families: { ...options.families, "describe": "worker", "start": "worker", "supervise": "worker", "settled": "worker" } }); const client = new Client(peer, handler, events); await peer.attach(connection); return client; }
  /** Resolves a handle to the channel it names on a tunnel and speaks the family over it. */
  static async open(tunnel: Tunnel, handle: Protocol.Handle, options: PeerOptions, handler: Handler | undefined, events: Events): Promise<Client> { const channel = tunnel.channel(handle.channel); if (!channel) throw new Error('no channel ' + handle.channel + ' on the connection'); return Client.attach(channel, options, handler, events); }
  close(): void { this.peer.close(); }
  /** Ordinary RPC: self-contained data, usable with no live runtime. */
  async describe(params: Protocol.Ticket, options?: CallOptions): Promise<string> { validateWire("Ticket", params); const result = await this.peer.call<string>("describe", params, options); validateWire("string", result); return result; }
  /** Takes a callback and answers a record of callables. */
  async start(params: Protocol.Start, options?: CallOptions): Promise<Protocol.Job> { validateWire("Start", params); const result = await this.peer.call<Protocol.Job>("start", params, options); validateWire("Job", result); return result; }
  onSettled(handler: (data: Protocol.Outcome, context: EventContext) => void | Promise<void>): () => void { return this.peer.onEvent("settled", (data, context) => { try { validateWire("Outcome", data); } catch(error) { this.peer.close(); throw error; } return handler(data as Protocol.Outcome, context); }); }
}
