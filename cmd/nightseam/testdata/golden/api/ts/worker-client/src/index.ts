// Code generated by nightseam. DO NOT EDIT.
import { DuplexError, callWire, emitWire, registerWire, wirePair, type WireModelContext } from "@nightseam/runtime";
import { encodePath, type Wire } from '@nightseam/duplex';
import type { AdapterContext, ValueAdapter, ValueContext } from "@nightseam/runtime";
import type { LiveOwner } from "@nightseam/live";
import { validateWire } from './types.ts';
import type * as Protocol from './types.ts';
import type { AnyFamily, FamilyBinding, Slots } from './types.ts';
import * as conversion from './types.ts';
export * from './types.ts';
export { DuplexError };
export type { AdapterContext };
function makeAdapter(context: AdapterContext) {
  const observer = context.options?.observer;
  const bindings = {  };
  const slots: Slots = {  };
  const environment = context.valueEnvironment;
  if ((true) && !environment) throw new DuplexError('scope_closed', 'model adaptation requires an explicit value environment');
  function hasModelHandler(facet: object, name: string): boolean {
    for (let current = facet; current !== null && current !== Object.prototype; current = Object.getPrototypeOf(current)) {
      if (Object.prototype.hasOwnProperty.call(current, name)) return typeof (facet as Record<string, unknown>)[name] === 'function';
    }
    return false;
  }
  function proxyServer(wire: Wire): Protocol.Server {
    return {
      methods: {
        async describe(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs, meta: context?.outgoingMeta, observer, family: "worker" };
          validateWire("Ticket", params);
          const result = await callWire<string>(wire, ["describe"], params, options);
          validateWire("string", result);
          return result;
        },
        async start(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs, meta: context?.outgoingMeta, observer, family: "worker" };
          const owner = (true || true) ? environment!.select((context as {valueContext?: unknown} | undefined)?.valueContext) : undefined;
          const result = await environment!.publish(owner, owner => environment!.export(owner, (owner) => { const converted = conversion.exportStart(owner as LiveOwner, (params) as Protocol.Start); validateWire("Start", converted); return converted; }), sent => callWire(wire, ["start"], sent, options));
          validateWire("Job", result);
          return environment!.import(owner, (owner) => conversion.importJob(owner as LiveOwner, result));
        },
      },
      events: {
      },
    };
  }
  function bindServer(wire: Wire, implementation: Protocol.Server): void {
    if (!implementation?.methods || !implementation.events) throw new Error('model methods and events are required');
    if (!hasModelHandler(implementation.methods, "describe")) throw new Error("handler for describe is required");
    if (!hasModelHandler(implementation.methods, "start")) throw new Error("handler for start is required");
    const detach: Array<() => void> = [];
    try {
      detach.push(registerWire(wire, ["describe"], {
        observer, family: "worker",
        request: async (raw, context) => {
          try { validateWire("Ticket", raw); } catch (error) { throw new DuplexError('invalid_params', String(error)); }
          const params = raw as Protocol.Ticket;
          const result = await implementation.methods.describe(params as Protocol.Ticket, context);
          return (() => { const converted = result; validateWire("string", converted); return converted; })();
        },
      }));
      detach.push(registerWire(wire, ["start"], {
        observer, family: "worker",
        request: async (raw, context) => {
          try { validateWire("Start", raw); } catch (error) { throw new DuplexError('invalid_params', String(error)); }
          const owner = (true || true) ? environment!.child(undefined) : undefined; const ownedContext = Object.create(context) as WireModelContext & { valueContext: unknown }; if (owner !== undefined) Object.defineProperty(ownedContext, 'valueContext', { value: owner, enumerable: true });
          const params = environment!.import(owner, (owner) => conversion.importStart(owner as LiveOwner, raw));
          const result = await implementation.methods.start(params as Protocol.Start, ownedContext);
          return environment!.export(owner, (owner) => { const converted = conversion.exportJob(owner as LiveOwner, (result) as Protocol.Job); validateWire("Job", converted); return converted; });
        },
      }));
    } catch (error) { for (const remove of detach.reverse()) remove(); throw error; }
  }
  function proxyClient(wire: Wire): Protocol.Client {
    return {
      methods: {
        async supervise(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs, meta: context?.outgoingMeta, observer, family: "worker" };
          const owner = (true || true) ? environment!.select((context as {valueContext?: unknown} | undefined)?.valueContext) : undefined;
          const result = await environment!.publish(owner, owner => environment!.export(owner, (owner) => { const converted = conversion.exportSupervise(owner as LiveOwner, (params) as Protocol.Supervise); validateWire("Supervise", converted); return converted; }), sent => callWire(wire, ["supervise"], sent, options));
          validateWire("Outcome", result);
          return environment!.import(owner, (owner) => conversion.importOutcome(owner as LiveOwner, result));
        },
      },
      events: {
        async settled(data, context) {
          const options = { context, meta: context?.outgoingMeta, observer, family: "worker" };
          const owner = (true) ? environment!.select((context as {valueContext?: unknown} | undefined)?.valueContext) : undefined;
          await environment!.publish(owner, owner => environment!.export(owner, (owner) => { const converted = conversion.exportOutcome(owner as LiveOwner, (data) as Protocol.Outcome); validateWire("Outcome", converted); return converted; }), sent => (async () => { emitWire(wire, ["settled"], sent, options); })());
        },
      },
    };
  }
  function bindClient(wire: Wire, implementation: Protocol.Client): void {
    if (!implementation?.methods || !implementation.events) throw new Error('model methods and events are required');
    if (!hasModelHandler(implementation.methods, "supervise")) throw new Error("handler for supervise is required");
    if (!hasModelHandler(implementation.events, "settled")) throw new Error("event handler for settled is required");
    const detach: Array<() => void> = [];
    try {
      detach.push(registerWire(wire, ["supervise"], {
        observer, family: "worker",
        request: async (raw, context) => {
          try { validateWire("Supervise", raw); } catch (error) { throw new DuplexError('invalid_params', String(error)); }
          const owner = (true || true) ? environment!.child(undefined) : undefined; const ownedContext = Object.create(context) as WireModelContext & { valueContext: unknown }; if (owner !== undefined) Object.defineProperty(ownedContext, 'valueContext', { value: owner, enumerable: true });
          const params = environment!.import(owner, (owner) => conversion.importSupervise(owner as LiveOwner, raw));
          const result = await implementation.methods.supervise(params as Protocol.Supervise, ownedContext);
          return environment!.export(owner, (owner) => { const converted = conversion.exportOutcome(owner as LiveOwner, (result) as Protocol.Outcome); validateWire("Outcome", converted); return converted; });
        },
      }));
      detach.push(registerWire(wire, ["settled"], {
        observer, family: "worker",
        event: async (raw, context) => {
          try { validateWire("Outcome", raw); } catch (error) { wire.close(); throw error; }
          const owner = (true) ? environment!.child(undefined) : undefined; const ownedContext = Object.create(context) as WireModelContext & { valueContext: unknown }; if (owner !== undefined) Object.defineProperty(ownedContext, 'valueContext', { value: owner, enumerable: true });
          await implementation.events.settled(environment!.import(owner, (owner) => conversion.importOutcome(owner as LiveOwner, raw)), ownedContext);
        },
      }));
    } catch (error) { for (const remove of detach.reverse()) remove(); throw error; }
  }
  return { proxyServer, proxyClient, bindServer, bindClient };
}
/** Exposes one model session at a new local wire origin. */
export function toWire(model: Protocol.ClientModel, context: AdapterContext): Wire {
  const adapter = makeAdapter(context);
  const [access, binding] = wirePair({ ...context.options, families: { ...context.options?.families, [encodePath(["describe"])]: "worker", [encodePath(["start"])]: "worker", [encodePath(["supervise"])]: "worker", [encodePath(["settled"])]: "worker" } });
  try { const implementation = model(adapter.proxyServer(binding)); adapter.bindClient(binding, implementation); return access; } catch (error) { access.close(); throw error; }
}
/** Interprets a wire as the same model factory; the resulting session binds once. */
export async function fromWire(wire: Wire, context: AdapterContext): Promise<Protocol.ClientModel> {
  const adapter = makeAdapter(context);
  let bound = false;
  return remote => {
    if (bound) throw new DuplexError('already_bound', 'model already bound');
    bound = true;
    adapter.bindServer(wire, remote);
    return adapter.proxyClient(wire);
  };
}
export const errors = { denied: "denied", gone: "gone" } as const;
export type ErrorCode = (typeof errors)[keyof typeof errors];
