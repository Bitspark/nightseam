// Code generated by nightseam. DO NOT EDIT.
import { DuplexError, callWire, emitWire, registerWire, wirePair, declarationDigest, familyTypeAdapter, jsonAdapter, identityHandler, IDENTITY_METHOD, prepareIdentity, type WireModelContext, type WireCallOptions } from "@nightseam/runtime";
import { encodePath, type Wire } from '@nightseam/duplex';
import type { AdapterContext, ValueAdapter, ValueContext } from "@nightseam/runtime";
import { validateWire } from './types.ts';
import type * as Protocol from './types.ts';
import type { AnyFamily, FamilyBinding, Slots } from './types.ts';
import * as conversion from './types.ts';
import * as live_carrier from "@example/carrier-client";
import type * as carrier from "@example/carrier-client";
import type * as probe from "@example/probe-client";
export * from './types.ts';
export { DuplexError };
export type { AdapterContext };
function makeAdapter<A extends AnyFamily = AnyFamily, B extends AnyFamily = AnyFamily>(context: AdapterContext, a: FamilyBinding<A, "Envelope">, b: FamilyBinding<B, "Envelope">) {
  const options = { ...context.options };
  const observer = options.observer;
  const propagator = options.propagator;
  const requestTimeoutMs = options.requestTimeoutMs;
  const bindings = { a, b };
  const slots: Slots = { "A": a, "B": b };
  familyTypeAdapter(a, "Envelope");
  familyTypeAdapter(b, "Envelope");
  const identity = { path: "album", digest: declarationDigest(validateWire, slots) };
  const environment = context.valueEnvironment;
  if ((a.types["Envelope"].needsContext || b.types["Envelope"].needsContext) && !environment) throw new DuplexError('scope_closed', 'model adaptation requires an explicit value environment');
  function hasModelHandler(facet: object, name: string): boolean {
    for (let current = facet; current !== null && current !== Object.prototype; current = Object.getPrototypeOf(current)) {
      if (Object.prototype.hasOwnProperty.call(current, name)) return typeof (facet as Record<string, unknown>)[name] === 'function';
    }
    return false;
  }
  function proxyServer(wire: Wire): Protocol.Server<A, B> {
    return {
      methods: {
        async look(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs ?? requestTimeoutMs, meta: context?.outgoingMeta, observer, propagator, family: "album" };
          const owner = ((bindings.a.types["Envelope"].needsContext) || (bindings.a.types["Envelope"].needsContext || bindings.b.types["Envelope"].needsContext)) ? environment!.select((context as {valueContext?: unknown} | undefined)?.valueContext) : undefined;
          const result = await ((bindings.a.types["Envelope"].needsContext) ? environment!.publish(owner, owner => ((bindings.a.types["Envelope"].needsContext) ? environment!.export(owner, (owner) => { const converted = conversion.exportMineUnchecked<A>((params) as Protocol.Mine<A>, (input: A["Envelope"]): unknown => bindings.a.types["Envelope"].export(owner, (input) as A["Envelope"])); validateWire("Mine", converted, '$', slots); return converted; }) : (() => { const converted = conversion.exportMineUnchecked<A>((params) as Protocol.Mine<A>, (input: A["Envelope"]): unknown => bindings.a.types["Envelope"].export(owner, (input) as A["Envelope"])); validateWire("Mine", converted, '$', slots); return converted; })()), sent => callWire(wire, ["look"], sent, options)) : callWire(wire, ["look"], ((bindings.a.types["Envelope"].needsContext) ? environment!.export(owner, (owner) => { const converted = conversion.exportMineUnchecked<A>((params) as Protocol.Mine<A>, (input: A["Envelope"]): unknown => bindings.a.types["Envelope"].export(owner, (input) as A["Envelope"])); validateWire("Mine", converted, '$', slots); return converted; }) : (() => { const converted = conversion.exportMineUnchecked<A>((params) as Protocol.Mine<A>, (input: A["Envelope"]): unknown => bindings.a.types["Envelope"].export(owner, (input) as A["Envelope"])); validateWire("Mine", converted, '$', slots); return converted; })()), options));
          validateWire("Both", result, '$', slots);
          return ((bindings.a.types["Envelope"].needsContext || bindings.b.types["Envelope"].needsContext) ? environment!.import(owner, (owner) => conversion.importBothUnchecked<A, B>(result, (input: unknown): A["Envelope"] => (bindings.a.types["Envelope"].import(owner, input)) as A["Envelope"], (input: unknown): B["Envelope"] => (bindings.b.types["Envelope"].import(owner, input)) as B["Envelope"])) : conversion.importBothUnchecked<A, B>(result, (input: unknown): A["Envelope"] => (bindings.a.types["Envelope"].import(owner, input)) as A["Envelope"], (input: unknown): B["Envelope"] => (bindings.b.types["Envelope"].import(owner, input)) as B["Envelope"]));
        },
      },
      events: {
      },
    };
  }
  function validateServer(implementation: Protocol.Server<A, B>): void {
    if (!implementation?.methods || !implementation.events) throw new Error('model methods and events are required');
    if (!hasModelHandler(implementation.methods, "look")) throw new Error("handler for look is required");
  }
  function bindServer(wire: Wire, implementation: () => Protocol.Server<A, B>): () => void {
    const detach: Array<() => void> = [];
    try {
      detach.push(registerWire(wire, ["look"], {
        observer, family: "album",
        request: async (raw, context) => {
          const target = implementation();
          try { validateWire("Mine", raw, '$', slots); } catch (error) { if (error instanceof DuplexError && error.code === 'contract_mismatch') throw error; throw new DuplexError('invalid_params', String(error)); }
          const owner = ((bindings.a.types["Envelope"].needsContext) || (bindings.a.types["Envelope"].needsContext || bindings.b.types["Envelope"].needsContext)) ? environment!.child(undefined) : undefined; const ownedContext = Object.create(context) as ValueContext<Protocol.Mine<A> | Protocol.Both<A, B>, WireModelContext>; if (owner !== undefined) Object.defineProperty(ownedContext, 'valueContext', { value: owner, enumerable: true });
          const params = ((bindings.a.types["Envelope"].needsContext) ? environment!.import(owner, (owner) => conversion.importMineUnchecked<A>(raw, (input: unknown): A["Envelope"] => (bindings.a.types["Envelope"].import(owner, input)) as A["Envelope"])) : conversion.importMineUnchecked<A>(raw, (input: unknown): A["Envelope"] => (bindings.a.types["Envelope"].import(owner, input)) as A["Envelope"]));
          const result = await target.methods.look(params as Protocol.Mine<A>, ownedContext);
          return ((bindings.a.types["Envelope"].needsContext || bindings.b.types["Envelope"].needsContext) ? environment!.export(owner, (owner) => { const converted = conversion.exportBothUnchecked<A, B>((result) as Protocol.Both<A, B>, (input: A["Envelope"]): unknown => bindings.a.types["Envelope"].export(owner, (input) as A["Envelope"]), (input: B["Envelope"]): unknown => bindings.b.types["Envelope"].export(owner, (input) as B["Envelope"])); validateWire("Both", converted, '$', slots); return converted; }) : (() => { const converted = conversion.exportBothUnchecked<A, B>((result) as Protocol.Both<A, B>, (input: A["Envelope"]): unknown => bindings.a.types["Envelope"].export(owner, (input) as A["Envelope"]), (input: B["Envelope"]): unknown => bindings.b.types["Envelope"].export(owner, (input) as B["Envelope"])); validateWire("Both", converted, '$', slots); return converted; })());
        },
      }));
    } catch (error) { for (const remove of detach.reverse()) remove(); throw error; }
    return () => { for (const remove of detach.reverse()) remove(); };
  }
  function proxyClient(wire: Wire): Protocol.Client<A, B> {
    return {
      methods: {
      },
      events: {
      },
    };
  }
  function validateClient(implementation: Protocol.Client<A, B>): void {
    if (!implementation?.methods || !implementation.events) throw new Error('model methods and events are required');
  }
  function bindClient(wire: Wire, implementation: () => Protocol.Client<A, B>): () => void {
    const detach: Array<() => void> = [];
    try {
    } catch (error) { for (const remove of detach.reverse()) remove(); throw error; }
    return () => { for (const remove of detach.reverse()) remove(); };
  }
  return { options, identity, proxyServer, proxyClient, validateServer, validateClient, bindServer, bindClient };
}
/** Exposes one model session at a new local wire origin. */
export function toWire<A extends AnyFamily = AnyFamily, B extends AnyFamily = AnyFamily>(model: Protocol.ClientModel<A, B>, context: AdapterContext, a: FamilyBinding<A, "Envelope">, b: FamilyBinding<B, "Envelope">): Wire {
  const adapter = makeAdapter<A, B>(context, a, b);
  const [access, binding] = wirePair({ ...adapter.options, families: { ...adapter.options.families, [encodePath(["look"])]: "album" } });
  try {
    registerWire(binding, [IDENTITY_METHOD], { request: identityHandler(adapter.identity) });
    const implementation = model(adapter.proxyServer(binding));
    adapter.validateClient(implementation);
    adapter.bindClient(binding, () => implementation);
    return access;
  } catch (error) { access.close(); throw error; }
}
/** Registers receivers before reading begins; complete checks identity before returning the one-use model factory. Close detaches this interpretation without closing its carrier. */
export function prepareFromWire<A extends AnyFamily = AnyFamily, B extends AnyFamily = AnyFamily>(wire: Wire, context: AdapterContext, a: FamilyBinding<A, "Envelope">, b: FamilyBinding<B, "Envelope">): { complete(options?: WireCallOptions): Promise<Protocol.ClientModel<A, B>>; close(): void } {
  const adapter = makeAdapter<A, B>(context, a, b);
  const gate = prepareIdentity(wire, adapter.identity, adapter.options);
  let implementation: Protocol.Server<A, B> | undefined;
  let completed = false, bound = false, closed = false;
  const detach: Array<() => void> = [];
  const cleanup = () => { if (closed) return; closed = true; implementation = undefined; gate.close(); for (const remove of detach.reverse()) remove(); };
  try {
    detach.push(adapter.bindServer(gate.wire, () => implementation!));
  } catch (error) { cleanup(); throw error; }
  return {
    async complete(options?: WireCallOptions): Promise<Protocol.ClientModel<A, B>> {
      if (closed) throw new DuplexError('scope_closed', 'model interpretation is closed');
      if (completed) throw new DuplexError('already_bound', 'model interpretation already completed');
      completed = true;
      try { await gate.check(options); } catch (error) { cleanup(); throw error; }
      return remote => {
        if (closed) throw new DuplexError('scope_closed', 'model interpretation is closed');
        if (bound) throw new DuplexError('already_bound', 'model already bound');
        bound = true;
        try {
          adapter.validateServer(remote);
          implementation = remote;
          gate.ready();
          return adapter.proxyClient(gate.wire);
        } catch (error) { cleanup(); throw error; }
      };
    },
    close: cleanup,
  };
}
/** Checks and interprets a wire as the same model factory; the resulting session binds once. */
export async function fromWire<A extends AnyFamily = AnyFamily, B extends AnyFamily = AnyFamily>(wire: Wire, context: AdapterContext, a: FamilyBinding<A, "Envelope">, b: FamilyBinding<B, "Envelope">): Promise<Protocol.ClientModel<A, B>> {
  const preparation = prepareFromWire<A, B>(wire, context, a, b);
  try { return await preparation.complete(); } catch (error) { preparation.close(); throw error; }
}
