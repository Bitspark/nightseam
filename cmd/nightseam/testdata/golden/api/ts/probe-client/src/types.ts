// Code generated by nightseam. DO NOT EDIT.
import { createValidator, withDeclaration, jsonAdapter, familyTypeAdapter, callableIdentity, DuplexError, type DeclarationIdentity, type AnyFamily, type FamilyBinding, type TypeBinding, type Slots, type TypeExpression, type WireFamily } from "@nightseam/runtime";
export type { AnyFamily, FamilyBinding, TypeBinding, Slots, TypeExpression };
import type { ValueAdapter, ValueContext } from "@nightseam/runtime";
import type { WireModelContext } from "@nightseam/runtime";
/** What every payload carries. */
export interface Base {
  /** The text of the payload. */
  "text": string;
}
export type Carried = { "kind": "envelope"; "value": Envelope } | { "kind": "handle"; "value": Handle } | { "kind": "none" };
export type Counts = Record<string, number>;
/** One message of the nightseam.duplex/1 profile: the members the peer acts on, and nothing else. */
export interface Envelope {
  /** The profile's version, 1. */
  "version": number;
  /** request, response, event or cancel. */
  "kind": string;
  /** What correlates a response or a cancel with its request. */
  "id"?: string;
  /** The method a request names. */
  "method"?: string;
  /** A request's parameters. */
  "params"?: unknown;
  /** A response's result. */
  "result"?: unknown;
  /** A response's error. */
  "error"?: unknown;
  /** The event an event frame names. */
  "event"?: string;
  /** An event's data. */
  "data"?: unknown;
  /** The W3C Trace Context of the frame. */
  "traceparent"?: string;
  /** The vendor state of that trace. */
  "tracestate"?: string;
  /** What a request or an event carries about the call, delivered to the handler beside the payload. */
  "meta"?: Record<string, string>;
}
/** A reference to a channel on the connection that carries the message holding it. */
export interface Handle {
  /** The channel's id on that connection. */
  "channel": number;
}
/** A record that keeps the fields it does not declare. */
export interface OpenRecord {
  "id": string;
  "note"?: string;
  [key: string]: unknown;
}
/** A payload: a base with a count and a note that may be absent or null. */
export interface Payload {
  /** The text of the payload. */
  "text": string;
  "count": number;
  "note"?: string | null;
}
/** Payloads, in order. */
export type Payloads = Array<Payload>;
export interface Seen {
  "at": string;
  "status": Status;
  "extra"?: unknown;
  "ratio": number;
  "ok": boolean;
}
/** Where a probe stands. */
export type Status = "ready" | "done" | "context.example";
/** The family: its name and the wire types a slot of it draws on. */
export interface Family { readonly name: "probe"; Base: Base; Carried: Carried; Counts: Counts; Envelope: Envelope; Handle: Handle; OpenRecord: OpenRecord; Payload: Payload; Payloads: Payloads; Seen: Seen; Status: Status }
export function adapterBase(): ValueAdapter<Base> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Base", validate: validateWire, slots };
  const needsContext = false;
  return { binding, needsContext,
    export(context: unknown, value: Base): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = value;
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Base {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Base => {
        const converted = raw as Base;
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterCarried(): ValueAdapter<Carried> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Carried", validate: validateWire, slots };
  const needsContext = false;
  return { binding, needsContext,
    export(context: unknown, value: Carried): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = value;
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Carried {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Carried => {
        const converted = raw as Carried;
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterCounts(): ValueAdapter<Counts> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Counts", validate: validateWire, slots };
  const needsContext = false;
  return { binding, needsContext,
    export(context: unknown, value: Counts): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = value;
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Counts {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Counts => {
        const converted = raw as Counts;
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterOpenRecord(): ValueAdapter<OpenRecord> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "OpenRecord", validate: validateWire, slots };
  const needsContext = false;
  return { binding, needsContext,
    export(context: unknown, value: OpenRecord): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = value;
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): OpenRecord {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): OpenRecord => {
        const converted = raw as OpenRecord;
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterPayload(): ValueAdapter<Payload> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Payload", validate: validateWire, slots };
  const needsContext = false;
  return { binding, needsContext,
    export(context: unknown, value: Payload): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = value;
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Payload {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Payload => {
        const converted = raw as Payload;
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterPayloads(): ValueAdapter<Payloads> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Payloads", validate: validateWire, slots };
  const needsContext = false;
  return { binding, needsContext,
    export(context: unknown, value: Payloads): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = value;
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Payloads {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Payloads => {
        const converted = raw as Payloads;
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterSeen(): ValueAdapter<Seen> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Seen", validate: validateWire, slots };
  const needsContext = false;
  return { binding, needsContext,
    export(context: unknown, value: Seen): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = value;
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Seen {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Seen => {
        const converted = raw as Seen;
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterStatus(): ValueAdapter<Status> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Status", validate: validateWire, slots };
  const needsContext = false;
  return { binding, needsContext,
    export(context: unknown, value: Status): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = value;
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Status {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Status => {
        const converted = raw as Status;
        return converted;
      };
      return convert(context);
    },
  };
}
export interface ServerMethods {
  echo(params: Payload, context?: WireModelContext): Payload | Promise<Payload>;
  noArgs(params: Record<string, never>, context?: WireModelContext): string | Promise<string>;
  seen(params: Seen, context?: WireModelContext): Payloads | Promise<Payloads>;
}
export interface ServerEvents {
  noticed(data: Seen, context?: WireModelContext): void | Promise<void>;
}
export interface Server { methods: ServerMethods; events: ServerEvents; }
export interface ClientMethods {
  reverse(params: Payload, context?: WireModelContext): Payload | Promise<Payload>;
}
export interface ClientEvents {
  changed(data: Payload, context?: WireModelContext): void | Promise<void>;
}
export interface Client { methods: ClientMethods; events: ClientEvents; }
export type ServerModel = (remote: Client) => Server;
export type ClientModel = (remote: Server) => Client;

const contractTypes = {"types":{"Base":{"kind":"record","fields":[{"name":"text","type":"string","required":true}]},"Carried":{"kind":"union","tag":"kind","variants":{"envelope":"Envelope","handle":"Handle","none":{"empty":true}}},"Counts":{"kind":"alias","type":{"map":"integer"}},"Envelope":{"kind":"record","fields":[{"name":"version","type":"integer","required":true},{"name":"kind","type":"string","required":true},{"name":"id","type":"string","required":false},{"name":"method","type":"string","required":false},{"name":"params","type":"json","required":false},{"name":"result","type":"json","required":false},{"name":"error","type":"json","required":false},{"name":"event","type":"string","required":false},{"name":"data","type":"json","required":false},{"name":"traceparent","type":"string","required":false},{"name":"tracestate","type":"string","required":false},{"name":"meta","type":{"map":"string"},"required":false}]},"Handle":{"kind":"record","fields":[{"name":"channel","type":"integer","required":true}]},"OpenRecord":{"kind":"record","fields":[{"name":"id","type":"string","required":true},{"name":"note","type":"string","required":false}],"open":true},"Payload":{"kind":"record","fields":[{"name":"count","type":"integer","required":true},{"name":"note","type":"string","required":false,"nullable":true}],"extends":["Base"]},"Payloads":{"kind":"alias","type":{"array":"Payload"}},"Seen":{"kind":"record","fields":[{"name":"at","type":"timestamp","required":true},{"name":"status","type":"Status","required":true},{"name":"extra","type":"json","required":false},{"name":"ratio","type":"number","required":true},{"name":"ok","type":"boolean","required":true}]},"Status":{"kind":"enum","values":["ready","done","context.example"]}}} as unknown as WireFamily;
/** Canonical language-neutral declaration bytes, the input to wireDigest. */
export const wireDeclaration = "{\"definitions\":{\"probe\":{\"client\":{\"ref\":\"probe/$client\"},\"errors\":[\"denied\",\"not_found\"],\"kind\":\"family\",\"parameters\":[],\"server\":{\"ref\":\"probe/$server\"},\"types\":{\"Base\":{\"ref\":\"probe/Base\"},\"Carried\":{\"ref\":\"probe/Carried\"},\"Counts\":{\"map\":{\"primitive\":\"integer\"}},\"OpenRecord\":{\"ref\":\"probe/OpenRecord\"},\"Payload\":{\"ref\":\"probe/Payload\"},\"Payloads\":{\"array\":{\"ref\":\"probe/Payload\"}},\"Seen\":{\"ref\":\"probe/Seen\"},\"Status\":{\"ref\":\"probe/Status\"}}},\"probe/$client\":{\"direction\":\"client\",\"events\":{\"noticed\":{\"ref\":\"probe/Seen\"}},\"extends\":[],\"kind\":\"side\",\"methods\":{\"reverse\":{\"errors\":[],\"request\":{\"ref\":\"probe/Payload\"},\"result\":{\"ref\":\"probe/Payload\"}}},\"parameters\":[]},\"probe/$server\":{\"direction\":\"server\",\"events\":{\"changed\":{\"ref\":\"probe/Payload\"}},\"extends\":[],\"kind\":\"side\",\"methods\":{\"echo\":{\"errors\":[],\"request\":{\"ref\":\"probe/Payload\"},\"result\":{\"ref\":\"probe/Payload\"}},\"no_args\":{\"errors\":[],\"request\":{\"empty\":true},\"result\":{\"primitive\":\"string\"}},\"seen\":{\"errors\":[],\"request\":{\"ref\":\"probe/Seen\"},\"result\":{\"array\":{\"ref\":\"probe/Payload\"}}}},\"parameters\":[]},\"probe/Base\":{\"fields\":[{\"name\":\"text\",\"nullable\":false,\"required\":true,\"type\":{\"primitive\":\"string\"},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]},\"probe/Carried\":{\"kind\":\"union\",\"parameters\":[],\"tag\":\"kind\",\"value\":\"value\",\"variants\":{\"envelope\":{\"family\":{\"ref\":\"probe\"},\"projection\":\"Envelope\"},\"handle\":{\"family\":{\"ref\":\"probe\"},\"projection\":\"Handle\"},\"none\":{\"empty\":true}}},\"probe/OpenRecord\":{\"fields\":[{\"name\":\"id\",\"nullable\":false,\"required\":true,\"type\":{\"primitive\":\"string\"},\"unique\":false},{\"name\":\"note\",\"nullable\":false,\"required\":false,\"type\":{\"primitive\":\"string\"},\"unique\":false}],\"kind\":\"record\",\"open\":true,\"parameters\":[]},\"probe/Payload\":{\"extends\":[{\"ref\":\"probe/Base\"}],\"fields\":[{\"name\":\"count\",\"nullable\":false,\"required\":true,\"type\":{\"primitive\":\"integer\"},\"unique\":false},{\"name\":\"note\",\"nullable\":true,\"required\":false,\"type\":{\"primitive\":\"string\"},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]},\"probe/Seen\":{\"fields\":[{\"name\":\"at\",\"nullable\":false,\"required\":true,\"type\":{\"primitive\":\"timestamp\"},\"unique\":false},{\"name\":\"status\",\"nullable\":false,\"required\":true,\"type\":{\"ref\":\"probe/Status\"},\"unique\":false},{\"name\":\"extra\",\"nullable\":false,\"required\":false,\"type\":{\"primitive\":\"json\"},\"unique\":false},{\"name\":\"ratio\",\"nullable\":false,\"required\":true,\"type\":{\"primitive\":\"number\"},\"unique\":false},{\"name\":\"ok\",\"nullable\":false,\"required\":true,\"type\":{\"primitive\":\"boolean\"},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]},\"probe/Status\":{\"kind\":\"enum\",\"parameters\":[],\"values\":[\"context.example\",\"done\",\"ready\"]}},\"root\":{\"ref\":\"probe\"},\"version\":1}";
/** SHA-256 of the canonical full declaration's exact UTF-8 bytes. */
export const wireDigest = "5b0146ce180b930f114d47e93cb14e39986c2d5e20de174d98363ebf3ad51a43";
/** Runtime validation applies equally to calls, replies, reverse calls and events; what fills a slot of a parameter is validated by the binding of the family that fills it. */
export const validateWire = withDeclaration(createValidator(contractTypes, wireDigest, {  }), wireDeclaration);
/** This family's declaration and reusable interpretations, with no operation context or owner. */
export const family = { name: "probe", validate: validateWire, types: { "Base": adapterBase(), "Carried": adapterCarried(), "Counts": adapterCounts(), "Envelope": jsonAdapter<Envelope>({ type: "Envelope", validate: validateWire }), "Handle": jsonAdapter<Handle>({ type: "Handle", validate: validateWire }), "OpenRecord": adapterOpenRecord(), "Payload": adapterPayload(), "Payloads": adapterPayloads(), "Seen": adapterSeen(), "Status": adapterStatus() } } as const;
