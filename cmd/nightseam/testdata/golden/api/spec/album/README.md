# album

<!-- Generated by nightseam from api/contracts/album/; do not edit. -->

A family of the `nightseam.duplex/1` profile, declared in `model.json`, `protocol.json`.
It imports `carrier`, `probe`.

## Parameters

The family is generic in the families bound to these parameters, where its generated code is instantiated:

- **A**, of the `session` role, drawn at `Envelope`.
- **B**, of the `session` role, drawn at `Envelope`.

## Types

### Borrowed

A record, generic in `B.Envelope`. A carrier frame, its S filled by B.

| Field | Type | Presence | Constraints | Description |
|---|---|---|---|---|
| `frame` | `carrier.Frame` with S=B | required | — |  |

### Both

A record, generic in `A.Envelope`, `B.Envelope`.

| Field | Type | Presence | Constraints | Description |
|---|---|---|---|---|
| `mine` | `Mine` | required | — |  |
| `borrowed` | `Borrowed` | required | — |  |
| `fixed` | `Fixed` | required | — |  |
| `params` | `carrier.AttachParams` | required | — |  |

### Fixed

A record. A carrier frame, its S filled by probe.

| Field | Type | Presence | Constraints | Description |
|---|---|---|---|---|
| `frame` | `carrier.Frame` with S=probe | required | — |  |

### Mine

A record, generic in `A.Envelope`. One message of A.

| Field | Type | Presence | Constraints | Description |
|---|---|---|---|---|
| `held` | `A.Envelope` | required | — |  |

Every family with a protocol also carries `Envelope`, one message of the profile, and `Handle`, a reference to a channel that speaks it.

## Server side

The server implements these methods and emits these events.

| Method | Request | Result | Errors | Description |
|---|---|---|---|---|
| `look` | `Mine` | `Both` | — |  |
