// Code generated by nightseam. DO NOT EDIT.
package holdertest

import (
	bytes "bytes"
	context "context"
	json "encoding/json"
	errors "errors"
	adapter "example.test/generated/api/go/holder-binding"
	protocol "example.test/generated/api/go/holder-protocol"
	fmt "fmt"
	duplex "github.com/Bitspark/nightseam/duplex/go"
	runtime "github.com/Bitspark/nightseam/runtime/go"
	reflect "reflect"
	sync "sync"
)

type Presentation func(context.Context, duplex.Wire) (duplex.Wire, func(), error)
type Options struct {
	Context       runtime.AdapterContext
	RemoteContext runtime.AdapterContext
	Presentation  Presentation
	Inputs        map[string]any
	Equal         func(string, any, any) error
}

func once(f func()) func() { var once sync.Once; return func() { once.Do(f) } }

// Local keeps the bounded asynchronous wire created by ToWire.
func Local(_ context.Context, wire duplex.Wire) (duplex.Wire, func(), error) {
	return wire, func() {}, nil
}

// Mounted selects a nonempty origin from a mount without allocating a carrier.
func Mounted(_ context.Context, wire duplex.Wire) (duplex.Wire, func(), error) {
	root := duplex.Mount(map[string]duplex.Wire{"family": wire})
	return duplex.At(root, []string{"family"}), once(func() { _ = root.Close(1000, "") }), nil
}

// Forwarded introduces one local forwarding hop.
func Forwarded(_ context.Context, wire duplex.Wire) (duplex.Wire, func(), error) {
	left, right, err := runtime.NewWirePair(runtime.Options{})
	if err != nil {
		return nil, nil, err
	}
	detach, err := runtime.ForwardWire(right, wire)
	if err != nil {
		_ = left.Close(1000, "")
		return nil, nil, err
	}
	return left, once(func() { detach(); _ = left.Close(1000, "") }), nil
}

// Pipe carries real serialized frames between two prepared peers.
func Pipe(ctx context.Context, wire duplex.Wire) (duplex.Wire, func(), error) {
	left, right := duplex.Pipe(1 << 20)
	var detach func()
	server, err := runtime.NewPeer(ctx, right, runtime.ServerRole, runtime.Options{Prepare: func(peer *runtime.Peer) error {
		var err error
		detach, err = runtime.ForwardWire(peer.Wire(), wire)
		return err
	}})
	if err != nil {
		_ = left.Close(context.Background(), 1000, "")
		_ = right.Close(context.Background(), 1000, "")
		return nil, nil, err
	}
	client, err := runtime.NewPeer(ctx, left, runtime.ClientRole, runtime.Options{})
	if err != nil {
		if detach != nil {
			detach()
		}
		_ = server.Close()
		_ = left.Close(context.Background(), 1000, "")
		return nil, nil, err
	}
	return client.Wire(), once(func() { detach(); _ = client.Close(); _ = server.Close() }), nil
}

func compare(method string, expected, actual any, expectedErr, actualErr error, equal func(string, any, any) error) error {
	if expectedErr != nil || actualErr != nil {
		if expectedErr == nil || actualErr == nil {
			return fmt.Errorf("%s: direct error %v, round-trip error %v", method, expectedErr, actualErr)
		}
		code := func(err error) string {
			var public *runtime.PublicError
			if errors.As(err, &public) {
				return public.Code
			}
			return "internal"
		}
		if code(expectedErr) != code(actualErr) {
			return fmt.Errorf("%s: direct error %v, round-trip error %v", method, expectedErr, actualErr)
		}
		var expectedPublic, actualPublic *runtime.PublicError
		if errors.As(expectedErr, &expectedPublic) && errors.As(actualErr, &actualPublic) {
			return compare(method+".error", expectedPublic, actualPublic, nil, nil, nil)
		}
		return nil
	}
	if equal != nil {
		return equal(method, expected, actual)
	}
	a, err := runtime.MarshalJSON(expected)
	if err != nil {
		return fmt.Errorf("%s: result needs an Equal observer: %w", method, err)
	}
	b, err := runtime.MarshalJSON(actual)
	if err != nil {
		return fmt.Errorf("%s: round-trip result needs an Equal observer: %w", method, err)
	}
	var x, y any
	da, db := json.NewDecoder(bytes.NewReader(a)), json.NewDecoder(bytes.NewReader(b))
	da.UseNumber()
	db.UseNumber()
	if err = da.Decode(&x); err != nil {
		return err
	}
	if err = db.Decode(&y); err != nil {
		return err
	}
	if !reflect.DeepEqual(x, y) {
		return fmt.Errorf("%s: direct and round-trip results differ: %s / %s", method, a, b)
	}
	return nil
}

// Pair presents one fresh model over a frame pipe by default. The returned factory is bound once.
func Pair[SJob runtime.Of[STag], SProgress runtime.Of[STag], STag any](ctx context.Context, model protocol.ServerModel[SJob, SProgress], options Options, adapterSJob runtime.ValueAdapter[SJob], adapterSProgress runtime.ValueAdapter[SProgress]) (protocol.ServerModel[SJob, SProgress], func(), error) {
	wire, err := adapter.ToWire[SJob, SProgress](model, options.Context, adapterSJob, adapterSProgress)
	if err != nil {
		return nil, nil, err
	}
	present := options.Presentation
	if present == nil {
		present = Pipe
	}
	view, detach, err := present(ctx, wire)
	if err != nil {
		if detach != nil {
			detach()
		}
		_ = wire.Close(1000, "")
		return nil, nil, err
	}
	close := once(func() {
		if detach != nil {
			detach()
		}
		_ = wire.Close(1000, "")
	})
	complete, unbind, err := adapter.PrepareFromWire[SJob, SProgress](view, options.RemoteContext, adapterSJob, adapterSProgress)
	if err != nil {
		close()
		return nil, nil, err
	}
	stop := once(func() { unbind(); close() })
	paired, err := complete(ctx)
	if err != nil {
		stop()
		return nil, nil, err
	}
	return paired, stop, nil
}

type candidate struct {
	raw    string
	reason string
}

var examples = map[string]candidate{
	"Choice": {raw: "", reason: "no compatible concrete family is available for S"},
	"Held":   {raw: "", reason: "no compatible concrete family is available for S"},
	"Jobs":   {raw: "", reason: "no compatible concrete family is available for S"},
	"Nested": {raw: "", reason: "no compatible concrete family is available for S"},
}

// Example validates the document's candidate against T; incompatible generic witnesses fail explicitly.
func Example[T any](name string) (T, error) {
	var zero T
	c, ok := examples[name]
	if !ok {
		return zero, fmt.Errorf("unknown example %s", name)
	}
	if c.reason != "" {
		return zero, fmt.Errorf("example %s unavailable: %s", name, c.reason)
	}
	return runtime.JSONAdapter[T]().Import(context.Background(), json.RawMessage(c.raw))
}

// Smoke calls every method and compares two freshly constructed equivalent models.
// Inputs supplies native witnesses by wire method name; Equal supplies live observational equivalence.
type observingMethods[SJob, SProgress any] struct {
	inner protocol.ServerMethods[SJob, SProgress]
	check func(string, any) error
}

func (o observingMethods[SJob, SProgress]) Exchange(ctx context.Context, params protocol.Held[SJob, SProgress]) (protocol.Held[SJob, SProgress], error) {
	if err := o.check("exchange", params); err != nil {
		var zero protocol.Held[SJob, SProgress]
		return zero, err
	}
	return o.inner.Exchange(ctx, params)
}
func Smoke[SJob runtime.Of[STag], SProgress runtime.Of[STag], STag any](ctx context.Context, model protocol.ServerModel[SJob, SProgress], opposite protocol.Client[SJob, SProgress], options Options, adapterSJob runtime.ValueAdapter[SJob], adapterSProgress runtime.ValueAdapter[SProgress]) error {
	if model == nil {
		return fmt.Errorf("model factory is required")
	}
	var mutex sync.Mutex
	inputs := map[string]any{}
	seen := map[string]uint64{}
	var inputError error
	_ = inputError
	check := func(method string, actual any) error {
		mutex.Lock()
		seen[method]++
		expected, tracked := inputs[method]
		delete(inputs, method)
		mutex.Unlock()
		if !tracked {
			return nil
		}
		err := compare(method+".request", expected, actual, nil, nil, options.Equal)
		if err != nil {
			mutex.Lock()
			inputError = err
			mutex.Unlock()
		}
		return err
	}
	observed := func(remote protocol.Client[SJob, SProgress]) (protocol.Server[SJob, SProgress], error) {
		value, err := model(remote)
		if err == nil {
			if value.Methods == nil {
				return value, fmt.Errorf("model methods are required")
			}
			value.Methods = observingMethods[SJob, SProgress]{inner: value.Methods, check: check}
		}
		return value, err
	}
	paired, stop, err := Pair[SJob, SProgress](ctx, observed, options, adapterSJob, adapterSProgress)
	if err != nil {
		return err
	}
	defer stop()
	remote, err := paired(opposite)
	if err != nil {
		return err
	}
	direct, err := model(opposite)
	if err != nil {
		return err
	}
	_ = remote
	_ = direct
	{
		var input protocol.Held[SJob, SProgress]
		if supplied, ok := options.Inputs["exchange"]; ok {
			var valid bool
			input, valid = supplied.(protocol.Held[SJob, SProgress])
			if !valid {
				return fmt.Errorf("input exchange has the wrong native type")
			}
		} else {
			if adapterSJob.NeedsContext || adapterSProgress.NeedsContext {
				return fmt.Errorf("input exchange needs a caller-supplied native value")
			}
			return fmt.Errorf("input exchange unavailable: no compatible concrete family is available for S")
		}
		if options.Equal == nil && (adapterSJob.NeedsContext || adapterSProgress.NeedsContext) {
			return fmt.Errorf("exchange: requires an Equal observer for live values")
		}
		mutex.Lock()
		inputs["exchange"] = input
		before := seen["exchange"]
		mutex.Unlock()
		actual, actualErr := remote.Methods.Exchange(ctx, input)
		mutex.Lock()
		observedError := inputError
		called := seen["exchange"] > before
		mutex.Unlock()
		if observedError != nil {
			return observedError
		}
		if !called {
			return fmt.Errorf("exchange: model was not reached: %v", actualErr)
		}
		expected, expectedErr := direct.Methods.Exchange(ctx, input)
		if err := compare("exchange", expected, actual, expectedErr, actualErr, options.Equal); err != nil {
			return err
		}
	}
	return nil
}
