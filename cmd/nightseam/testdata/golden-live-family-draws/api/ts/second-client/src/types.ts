// Code generated by nightseam. DO NOT EDIT.
import { createValidator, withDeclaration, jsonAdapter, familyTypeAdapter, callableIdentity, DuplexError, type DeclarationIdentity, type AnyFamily, type FamilyBinding, type TypeBinding, type Slots, type TypeExpression, type WireFamily } from "@nightseam/runtime";
export type { AnyFamily, FamilyBinding, TypeBinding, Slots, TypeExpression };
import type { ValueAdapter, ValueContext } from "@nightseam/runtime";
import { LiveOwner } from "@nightseam/live";
import type { WireModelContext } from "@nightseam/runtime";
/** One message of the nightseam.duplex/1 profile: the members the peer acts on, and nothing else. */
export interface Envelope {
  /** The profile's version, 1. */
  "version": number;
  /** request, response, event or cancel. */
  "kind": string;
  /** What correlates a response or a cancel with its request. */
  "id"?: string;
  /** The method a request names. */
  "method"?: string;
  /** A request's parameters. */
  "params"?: unknown;
  /** A response's result. */
  "result"?: unknown;
  /** A response's error. */
  "error"?: unknown;
  /** The event an event frame names. */
  "event"?: string;
  /** An event's data. */
  "data"?: unknown;
  /** The W3C Trace Context of the frame. */
  "traceparent"?: string;
  /** The vendor state of that trace. */
  "tracestate"?: string;
  /** What a request or an event carries about the call, delivered to the handler beside the payload. */
  "meta"?: Record<string, string>;
}
/** A reference to a channel on the connection that carries the message holding it. */
export interface Handle {
  /** The channel's id on that connection. */
  "channel": number;
}
export interface Job {
  "run": Run;
}
export interface Progress {
  "read": Run;
}
/** A value of it is one implementation, called across the seam; each is its own binding, with its own lifetime. */
export type Run = (request: number, options?: { signal?: AbortSignal; owner?: LiveOwner }) => Promise<number>;
/** The family: its name and the wire types a slot of it draws on. */
export interface Family { readonly name: "second"; Envelope: Envelope; Handle: Handle; Job: Job; Progress: Progress; Run: Run }
/** Conversion only: use the value adapter or validate the entire wire value inside the same operation batch. */
/** Writes Job as it travels: each callable in it becomes a binding of the owner, and the reference that names it takes its place. */
export function exportJobUnchecked(owner: LiveOwner, value: Job): unknown {
  return owner.exportValue((owner) => {
    const out: Record<string, unknown> = {};
    out["run"] = exportRun(owner, (value["run"]) as Run);
    return out;
  });
}
/** Reads Job as it arrived: each reference in it becomes a typed proxy of the binding it names, so a handler is given native values. */
/** Conversion only: validation belongs to the value adapter or its caller. */
export function importJobUnchecked(owner: LiveOwner, raw: unknown): Job {
  return owner.importValue((owner) => {
    const wire = raw as Record<string, unknown>;
    const out: Record<string, unknown> = {};
    out["run"] = importRun(owner, wire["run"]);
    return out as unknown as Job;
  });
}
/** Conversion only: use the value adapter or validate the entire wire value inside the same operation batch. */
/** Writes Progress as it travels: each callable in it becomes a binding of the owner, and the reference that names it takes its place. */
export function exportProgressUnchecked(owner: LiveOwner, value: Progress): unknown {
  return owner.exportValue((owner) => {
    const out: Record<string, unknown> = {};
    out["read"] = exportRun(owner, (value["read"]) as Run);
    return out;
  });
}
/** Reads Progress as it arrived: each reference in it becomes a typed proxy of the binding it names, so a handler is given native values. */
/** Conversion only: validation belongs to the value adapter or its caller. */
export function importProgressUnchecked(owner: LiveOwner, raw: unknown): Progress {
  return owner.importValue((owner) => {
    const wire = raw as Record<string, unknown>;
    const out: Record<string, unknown> = {};
    out["read"] = importRun(owner, wire["read"]);
    return out as unknown as Progress;
  });
}
/** The declaration a reference to Run carries. It is nominal: a reference is usable exactly where this callable is expected. */
export const contractRun = "second/Run";
/** Makes a binding of a local Run and answers the reference that names it. */
export function exportRun(owner: LiveOwner, value: Run): unknown {
  if (typeof value !== 'function') throw new TypeError('callable implementation must be a function');
  return owner.exportValue((owner) => {
    const parent = owner;
    const reference = owner.export(contractRun, wireDigest, async (request, options) => {
      const owner = parent.child();
      const context = { ...options, owner };
      validateWire("integer", request);
      const argument = request as number;
      const result = await value(argument, context);
      return (() => { const converted = result; validateWire("integer", converted); return converted; })();
    });
    return reference.toJSON();
  });
}
/** A Run that calls the binding a reference names. */
export function importRun(owner: LiveOwner, raw: unknown): Run {
  const invoke = owner.import(owner.scope.decode(raw), contractRun, wireDigest);
  const scope = owner.scope;
  return async (request: number, options?: { signal?: AbortSignal; owner?: LiveOwner }) => {
    const owner = options?.owner?.scope === scope ? options.owner : scope.owner();
    const result = await invoke((() => { const converted = request; validateWire("integer", converted); return converted; })(), options);
    validateWire("integer", result);
    return result as number;
  };
}
export function adapterJob(): ValueAdapter<Job> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Job", validate: validateWire, slots };
  const needsContext = true;
  return { binding, needsContext,
    export(context: unknown, value: Job): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      const convert = (owner: LiveOwner): unknown => {
        const converted = exportJobUnchecked(owner!, value);
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return context.exportValue(convert);
    },
    import(context: unknown, raw: unknown): Job {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: LiveOwner): Job => {
        const converted = importJobUnchecked(owner!, raw);
        return converted;
      };
      return context.importValue(convert);
    },
  };
}
export function adapterProgress(): ValueAdapter<Progress> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Progress", validate: validateWire, slots };
  const needsContext = true;
  return { binding, needsContext,
    export(context: unknown, value: Progress): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      const convert = (owner: LiveOwner): unknown => {
        const converted = exportProgressUnchecked(owner!, value);
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return context.exportValue(convert);
    },
    import(context: unknown, raw: unknown): Progress {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: LiveOwner): Progress => {
        const converted = importProgressUnchecked(owner!, raw);
        return converted;
      };
      return context.importValue(convert);
    },
  };
}
export function adapterRun(): ValueAdapter<Run> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Run", validate: validateWire, slots };
  const needsContext = true;
  return { binding, needsContext,
    export(context: unknown, value: Run): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      const convert = (owner: LiveOwner): unknown => {
        const converted = exportRun(owner!, value);
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return context.exportValue(convert);
    },
    import(context: unknown, raw: unknown): Run {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      if (!(context instanceof LiveOwner)) throw new DuplexError('scope_closed', 'live conversion requires an owner');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: LiveOwner): Run => {
        const converted = importRun(owner!, raw);
        return converted;
      };
      return context.importValue(convert);
    },
  };
}
export interface ServerMethods {
}
export interface ServerEvents {
}
export interface Server { methods: ServerMethods; events: ServerEvents; }
export interface ClientMethods {
}
export interface ClientEvents {
}
export interface Client { methods: ClientMethods; events: ClientEvents; }
export type ServerModel = (remote: Client) => Server;
export type ClientModel = (remote: Server) => Client;

const contractTypes = {"types":{"Envelope":{"kind":"record","fields":[{"name":"version","type":"integer","required":true},{"name":"kind","type":"string","required":true},{"name":"id","type":"string","required":false},{"name":"method","type":"string","required":false},{"name":"params","type":"json","required":false},{"name":"result","type":"json","required":false},{"name":"error","type":"json","required":false},{"name":"event","type":"string","required":false},{"name":"data","type":"json","required":false},{"name":"traceparent","type":"string","required":false},{"name":"tracestate","type":"string","required":false},{"name":"meta","type":{"map":"string"},"required":false}]},"Handle":{"kind":"record","fields":[{"name":"channel","type":"integer","required":true}]},"Job":{"kind":"record","fields":[{"name":"run","type":"Run","required":true}]},"Progress":{"kind":"record","fields":[{"name":"read","type":"Run","required":true}]},"Run":{"kind":"callable","contract":"second/Run","request":"integer","result":"integer"}}} as unknown as WireFamily;
/** Canonical language-neutral declaration bytes, the input to wireDigest. */
export const wireDeclaration = "{\"definitions\":{\"second\":{\"client\":{\"ref\":\"second/$client\"},\"errors\":[],\"kind\":\"family\",\"parameters\":[],\"server\":{\"ref\":\"second/$server\"},\"types\":{\"Job\":{\"ref\":\"second/Job\"},\"Progress\":{\"ref\":\"second/Progress\"},\"Run\":{\"ref\":\"second/Run\"}}},\"second/$client\":{\"direction\":\"client\",\"events\":{},\"extends\":[],\"kind\":\"side\",\"methods\":{},\"parameters\":[]},\"second/$server\":{\"direction\":\"server\",\"events\":{},\"extends\":[],\"kind\":\"side\",\"methods\":{},\"parameters\":[]},\"second/Job\":{\"fields\":[{\"name\":\"run\",\"nullable\":false,\"required\":true,\"type\":{\"ref\":\"second/Run\"},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]},\"second/Progress\":{\"fields\":[{\"name\":\"read\",\"nullable\":false,\"required\":true,\"type\":{\"ref\":\"second/Run\"},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]},\"second/Run\":{\"errors\":[],\"kind\":\"callable\",\"parameters\":[],\"request\":{\"primitive\":\"integer\"},\"result\":{\"primitive\":\"integer\"}}},\"root\":{\"ref\":\"second\"},\"version\":1}";
/** SHA-256 of the canonical full declaration's exact UTF-8 bytes. */
export const wireDigest = "2a663378ebe35b18492addf499d4bdeea9febf5fa4aad3260d0eef49a1d5040d";
/** Runtime validation applies equally to calls, replies, reverse calls and events; what fills a slot of a parameter is validated by the binding of the family that fills it. */
export const validateWire = withDeclaration(createValidator(contractTypes, wireDigest, {  }), wireDeclaration);
/** This family's declaration and reusable interpretations, with no operation context or owner. */
export const family = { name: "second", validate: validateWire, types: { "Envelope": jsonAdapter<Envelope>({ type: "Envelope", validate: validateWire }), "Handle": jsonAdapter<Handle>({ type: "Handle", validate: validateWire }), "Job": adapterJob(), "Progress": adapterProgress(), "Run": adapterRun() } } as const;
