// Code generated by nightseam. DO NOT EDIT.
import { DuplexError, callWire, emitWire, registerWire, wirePair, declarationDigest, familyTypeAdapter, identityHandler, IDENTITY_METHOD, prepareIdentity, type WireModelContext, type WireCallOptions } from "@nightseam/runtime";
import { encodePath, type Wire } from '@nightseam/duplex';
import type { AdapterContext, ValueAdapter, ValueContext } from "@nightseam/runtime";
import { validateWire } from './types.ts';
import type * as Protocol from './types.ts';
import type { AnyFamily, FamilyBinding, Slots } from './types.ts';
import * as conversion from './types.ts';
export * from './types.ts';
export { DuplexError };
export type { AdapterContext };
function makeAdapter<S extends AnyFamily & { "Job": unknown; "Progress": unknown } = AnyFamily & { "Job": unknown; "Progress": unknown }>(context: AdapterContext, s: FamilyBinding<S, "Job" | "Progress">) {
  const options = { ...context.options };
  const observer = options.observer;
  const propagator = options.propagator;
  const requestTimeoutMs = options.requestTimeoutMs;
  const bindings = { s };
  const slots: Slots = { "S": s };
  familyTypeAdapter(s, "Job");
  familyTypeAdapter(s, "Progress");
  const identity = { path: "holder", digest: declarationDigest(validateWire, slots) };
  const environment = context.valueEnvironment;
  if ((s.types["Job"].needsContext || s.types["Progress"].needsContext) && !environment) throw new DuplexError('scope_closed', 'model adaptation requires an explicit value environment');
  function hasModelHandler(facet: object, name: string): boolean {
    for (let current = facet; current !== null && current !== Object.prototype; current = Object.getPrototypeOf(current)) {
      if (Object.prototype.hasOwnProperty.call(current, name)) return typeof (facet as Record<string, unknown>)[name] === 'function';
    }
    return false;
  }
  function proxyServer(wire: Wire): Protocol.Server<S> {
    return {
      methods: {
        async exchange(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs ?? requestTimeoutMs, meta: context?.outgoingMeta, observer, propagator, family: "holder" };
          const owner = ((bindings.s.types["Job"].needsContext || bindings.s.types["Progress"].needsContext) || (bindings.s.types["Job"].needsContext || bindings.s.types["Progress"].needsContext)) ? environment!.select((context as {valueContext?: unknown} | undefined)?.valueContext) : undefined;
          const result = await ((bindings.s.types["Job"].needsContext || bindings.s.types["Progress"].needsContext) ? environment!.publish(owner, owner => ((bindings.s.types["Job"].needsContext || bindings.s.types["Progress"].needsContext) ? environment!.export(owner, (owner) => { const converted = conversion.exportHeldUnchecked<S>((params) as Protocol.Held<S>, (input: S["Job"]): unknown => bindings.s.types["Job"].export(owner, (input) as S["Job"]), (input: S["Progress"]): unknown => bindings.s.types["Progress"].export(owner, (input) as S["Progress"])); validateWire("Held", converted, '$', slots); return converted; }) : (() => { const converted = conversion.exportHeldUnchecked<S>((params) as Protocol.Held<S>, (input: S["Job"]): unknown => bindings.s.types["Job"].export(owner, (input) as S["Job"]), (input: S["Progress"]): unknown => bindings.s.types["Progress"].export(owner, (input) as S["Progress"])); validateWire("Held", converted, '$', slots); return converted; })()), sent => callWire(wire, ["exchange"], sent, options)) : callWire(wire, ["exchange"], ((bindings.s.types["Job"].needsContext || bindings.s.types["Progress"].needsContext) ? environment!.export(owner, (owner) => { const converted = conversion.exportHeldUnchecked<S>((params) as Protocol.Held<S>, (input: S["Job"]): unknown => bindings.s.types["Job"].export(owner, (input) as S["Job"]), (input: S["Progress"]): unknown => bindings.s.types["Progress"].export(owner, (input) as S["Progress"])); validateWire("Held", converted, '$', slots); return converted; }) : (() => { const converted = conversion.exportHeldUnchecked<S>((params) as Protocol.Held<S>, (input: S["Job"]): unknown => bindings.s.types["Job"].export(owner, (input) as S["Job"]), (input: S["Progress"]): unknown => bindings.s.types["Progress"].export(owner, (input) as S["Progress"])); validateWire("Held", converted, '$', slots); return converted; })()), options));
          validateWire("Held", result, '$', slots);
          return ((bindings.s.types["Job"].needsContext || bindings.s.types["Progress"].needsContext) ? environment!.import(owner, (owner) => conversion.importHeldUnchecked<S>(result, (input: unknown): S["Job"] => (bindings.s.types["Job"].import(owner, input)) as S["Job"], (input: unknown): S["Progress"] => (bindings.s.types["Progress"].import(owner, input)) as S["Progress"])) : conversion.importHeldUnchecked<S>(result, (input: unknown): S["Job"] => (bindings.s.types["Job"].import(owner, input)) as S["Job"], (input: unknown): S["Progress"] => (bindings.s.types["Progress"].import(owner, input)) as S["Progress"]));
        },
      },
      events: {
      },
    };
  }
  function validateServer(implementation: Protocol.Server<S>): void {
    if (!implementation?.methods || !implementation.events) throw new Error('model methods and events are required');
    if (!hasModelHandler(implementation.methods, "exchange")) throw new Error("handler for exchange is required");
  }
  function bindServer(wire: Wire, implementation: () => Protocol.Server<S>): () => void {
    const detach: Array<() => void> = [];
    try {
      detach.push(registerWire(wire, ["exchange"], {
        observer, family: "holder",
        request: async (raw, context) => {
          const target = implementation();
          try { validateWire("Held", raw, '$', slots); } catch (error) { if (error instanceof DuplexError && error.code === 'contract_mismatch') throw error; throw new DuplexError('invalid_params', String(error)); }
          const owner = ((bindings.s.types["Job"].needsContext || bindings.s.types["Progress"].needsContext) || (bindings.s.types["Job"].needsContext || bindings.s.types["Progress"].needsContext)) ? environment!.child(undefined) : undefined; const ownedContext = Object.create(context) as ValueContext<Protocol.Held<S> | Protocol.Held<S>, WireModelContext>; if (owner !== undefined) Object.defineProperty(ownedContext, 'valueContext', { value: owner, enumerable: true });
          const params = ((bindings.s.types["Job"].needsContext || bindings.s.types["Progress"].needsContext) ? environment!.import(owner, (owner) => conversion.importHeldUnchecked<S>(raw, (input: unknown): S["Job"] => (bindings.s.types["Job"].import(owner, input)) as S["Job"], (input: unknown): S["Progress"] => (bindings.s.types["Progress"].import(owner, input)) as S["Progress"])) : conversion.importHeldUnchecked<S>(raw, (input: unknown): S["Job"] => (bindings.s.types["Job"].import(owner, input)) as S["Job"], (input: unknown): S["Progress"] => (bindings.s.types["Progress"].import(owner, input)) as S["Progress"]));
          const result = await target.methods.exchange(params as Protocol.Held<S>, ownedContext);
          return ((bindings.s.types["Job"].needsContext || bindings.s.types["Progress"].needsContext) ? environment!.export(owner, (owner) => { const converted = conversion.exportHeldUnchecked<S>((result) as Protocol.Held<S>, (input: S["Job"]): unknown => bindings.s.types["Job"].export(owner, (input) as S["Job"]), (input: S["Progress"]): unknown => bindings.s.types["Progress"].export(owner, (input) as S["Progress"])); validateWire("Held", converted, '$', slots); return converted; }) : (() => { const converted = conversion.exportHeldUnchecked<S>((result) as Protocol.Held<S>, (input: S["Job"]): unknown => bindings.s.types["Job"].export(owner, (input) as S["Job"]), (input: S["Progress"]): unknown => bindings.s.types["Progress"].export(owner, (input) as S["Progress"])); validateWire("Held", converted, '$', slots); return converted; })());
        },
      }));
    } catch (error) { for (const remove of detach.reverse()) remove(); throw error; }
    return () => { for (const remove of detach.reverse()) remove(); };
  }
  function proxyClient(wire: Wire): Protocol.Client<S> {
    return {
      methods: {
      },
      events: {
      },
    };
  }
  function validateClient(implementation: Protocol.Client<S>): void {
    if (!implementation?.methods || !implementation.events) throw new Error('model methods and events are required');
  }
  function bindClient(wire: Wire, implementation: () => Protocol.Client<S>): () => void {
    const detach: Array<() => void> = [];
    try {
    } catch (error) { for (const remove of detach.reverse()) remove(); throw error; }
    return () => { for (const remove of detach.reverse()) remove(); };
  }
  return { options, identity, proxyServer, proxyClient, validateServer, validateClient, bindServer, bindClient };
}
/** Exposes one model session at a new local wire origin. */
export function toWire<S extends AnyFamily & { "Job": unknown; "Progress": unknown } = AnyFamily & { "Job": unknown; "Progress": unknown }>(model: Protocol.ClientModel<S>, context: AdapterContext, s: FamilyBinding<S, "Job" | "Progress">): Wire {
  const adapter = makeAdapter<S>(context, s);
  const [access, binding] = wirePair({ ...adapter.options, families: { ...adapter.options.families, [encodePath(["exchange"])]: "holder" } });
  try {
    registerWire(binding, [IDENTITY_METHOD], { request: identityHandler(adapter.identity) });
    const implementation = model(adapter.proxyServer(binding));
    adapter.validateClient(implementation);
    adapter.bindClient(binding, () => implementation);
    return access;
  } catch (error) { access.close(); throw error; }
}
/** Registers receivers before reading begins; complete checks identity before returning the one-use model factory. Close detaches this interpretation without closing its carrier. */
export function prepareFromWire<S extends AnyFamily & { "Job": unknown; "Progress": unknown } = AnyFamily & { "Job": unknown; "Progress": unknown }>(wire: Wire, context: AdapterContext, s: FamilyBinding<S, "Job" | "Progress">): { complete(options?: WireCallOptions): Promise<Protocol.ClientModel<S>>; close(): void } {
  const adapter = makeAdapter<S>(context, s);
  const gate = prepareIdentity(wire, adapter.identity, adapter.options);
  let implementation: Protocol.Server<S> | undefined;
  let completed = false, bound = false, closed = false;
  const detach: Array<() => void> = [];
  const cleanup = () => { if (closed) return; closed = true; implementation = undefined; gate.close(); for (const remove of detach.reverse()) remove(); };
  try {
    detach.push(adapter.bindServer(gate.wire, () => implementation!));
  } catch (error) { cleanup(); throw error; }
  return {
    async complete(options?: WireCallOptions): Promise<Protocol.ClientModel<S>> {
      if (closed) throw new DuplexError('scope_closed', 'model interpretation is closed');
      if (completed) throw new DuplexError('already_bound', 'model interpretation already completed');
      completed = true;
      try { await gate.check(options); } catch (error) { cleanup(); throw error; }
      return remote => {
        if (closed) throw new DuplexError('scope_closed', 'model interpretation is closed');
        if (bound) throw new DuplexError('already_bound', 'model already bound');
        bound = true;
        try {
          adapter.validateServer(remote);
          implementation = remote;
          gate.ready();
          return adapter.proxyClient(gate.wire);
        } catch (error) { cleanup(); throw error; }
      };
    },
    close: cleanup,
  };
}
/** Checks and interprets a wire as the same model factory; the resulting session binds once. */
export async function fromWire<S extends AnyFamily & { "Job": unknown; "Progress": unknown } = AnyFamily & { "Job": unknown; "Progress": unknown }>(wire: Wire, context: AdapterContext, s: FamilyBinding<S, "Job" | "Progress">): Promise<Protocol.ClientModel<S>> {
  const preparation = prepareFromWire<S>(wire, context, s);
  try { return await preparation.complete(); } catch (error) { preparation.close(); throw error; }
}
import { record as recordWire, type WireLog, type RecordOptions, type RecordedWire } from '@nightseam/duplex';
import { checkIdentity } from "@nightseam/runtime";
/** The closed union of this side's outgoing native event payloads. */
export type RecordedEvent<S extends AnyFamily & { "Job": unknown; "Progress": unknown } = AnyFamily & { "Job": unknown; "Progress": unknown }> = never;
export interface Recorder<S extends AnyFamily & { "Job": unknown; "Progress": unknown } = AnyFamily & { "Job": unknown; "Progress": unknown }> extends RecordedWire { append(event: RecordedEvent<S>, context?: WireModelContext): Promise<void>; }
/** Checks a prepared origin before typed append; failed setup leaves its carrier usable. */
export async function record<S extends AnyFamily & { "Job": unknown; "Progress": unknown } = AnyFamily & { "Job": unknown; "Progress": unknown }>(target: Wire, log: WireLog, options: RecordOptions, context: AdapterContext, s: FamilyBinding<S, "Job" | "Progress">, setup?: WireCallOptions): Promise<Recorder<S>> {
  const adapter = makeAdapter<S>(context, s);
  const preparation = prepareIdentity(target, adapter.identity, adapter.options);
  try {
    await preparation.check(setup); preparation.ready();
    const wire = await recordWire(preparation.wire, log, { ...options, onClose(error) { preparation.close(); options.onClose?.(error); } }, setup?.signal);
    const events = adapter.proxyServer(wire).events;
    return {
      ...wire,
      async append(event, context) {
        throw new Error('This side declares no outgoing events.');
      },
      async follow(after, subscriber, signal) {
        await checkIdentity((method, params, options) => callWire(subscriber, [method], params, { ...options, observer: adapter.options.observer, propagator: adapter.options.propagator }), adapter.identity, { signal, timeoutMs: adapter.options.requestTimeoutMs });
        return wire.follow(after, subscriber, signal);
      },
    };
  } catch (error) { preparation.close(); throw error; }
}
