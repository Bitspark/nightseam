// Code generated by nightseam. DO NOT EDIT.
import { createValidator, withDeclaration, jsonAdapter, familyTypeAdapter, callableIdentity, DuplexError, type DeclarationIdentity, type AnyFamily, type FamilyBinding, type TypeBinding, type Slots, type TypeExpression, type WireFamily } from "@nightseam/runtime";
export type { AnyFamily, FamilyBinding, TypeBinding, Slots, TypeExpression };
import type { ValueAdapter, ValueContext } from "@nightseam/runtime";
import type { WireModelContext } from "@nightseam/runtime";
export type Choice<S extends AnyFamily & { "Job": unknown } = AnyFamily & { "Job": unknown }> = { "kind": "job"; "value": S["Job"] } | { "kind": "none" };
/** One message of the nightseam.duplex/1 profile: the members the peer acts on, and nothing else. */
export interface Envelope {
  /** The profile's version, 1. */
  "version": number;
  /** request, response, event or cancel. */
  "kind": string;
  /** What correlates a response or a cancel with its request. */
  "id"?: string;
  /** The method a request names. */
  "method"?: string;
  /** A request's parameters. */
  "params"?: unknown;
  /** A response's result. */
  "result"?: unknown;
  /** A response's error. */
  "error"?: unknown;
  /** The event an event frame names. */
  "event"?: string;
  /** An event's data. */
  "data"?: unknown;
  /** The W3C Trace Context of the frame. */
  "traceparent"?: string;
  /** The vendor state of that trace. */
  "tracestate"?: string;
  /** What a request or an event carries about the call, delivered to the handler beside the payload. */
  "meta"?: Record<string, string>;
}
/** A reference to a channel on the connection that carries the message holding it. */
export interface Handle {
  /** The channel's id on that connection. */
  "channel": number;
}
export interface Held<S extends AnyFamily & { "Job": unknown; "Progress": unknown } = AnyFamily & { "Job": unknown; "Progress": unknown }> {
  "job": S["Job"];
  "progress": S["Progress"];
  "nested": Nested<S>;
}
export type Jobs<S extends AnyFamily & { "Job": unknown } = AnyFamily & { "Job": unknown }> = Array<S["Job"]>;
export interface Nested<S extends AnyFamily & { "Job": unknown; "Progress": unknown } = AnyFamily & { "Job": unknown; "Progress": unknown }> {
  "jobs": Jobs<S>;
  "progress": Record<string, S["Progress"] | null>;
  "choice": Choice<S>;
  "absent"?: S["Job"];
}
/** The family: its name and the wire types a slot of it draws on. */
export interface Family { readonly name: "holder"; Envelope: Envelope; Handle: Handle }
/** Conversion only: use the value adapter or validate the entire wire value inside the same operation batch. */
/** Writes Choice using the supplied conversion for each type argument. */
export function exportChoiceUnchecked<S extends AnyFamily & { "Job": unknown } = AnyFamily & { "Job": unknown }>(value: Choice<S>, convert_S_Job: (value: S["Job"]) => unknown): unknown {
  const held = value as Record<string, unknown>;
  switch (held["kind"]) {
    case "job": return { "kind": "job", "value": convert_S_Job((held["value"]) as S["Job"]) };
    case "none": return { "kind": "none" };
  }
  throw new Error("Choice: unknown variant " + String(held["kind"]));
}
/** Reads Choice using the supplied conversion for each type argument. */
/** Conversion only: validation belongs to the value adapter or its caller. */
export function importChoiceUnchecked<S extends AnyFamily & { "Job": unknown } = AnyFamily & { "Job": unknown }>(raw: unknown, convert_S_Job: (value: unknown) => S["Job"]): Choice<S> {
  const held = raw as Record<string, unknown>;
  switch (held["kind"]) {
    case "job": return { "kind": "job", "value": convert_S_Job(held["value"]) } as unknown as Choice<S>;
    case "none": return { "kind": "none" } as unknown as Choice<S>;
  }
  throw new Error("Choice: unknown variant " + String(held["kind"]));
}
/** Conversion only: use the value adapter or validate the entire wire value inside the same operation batch. */
/** Writes Held using the supplied conversion for each type argument. */
export function exportHeldUnchecked<S extends AnyFamily & { "Job": unknown; "Progress": unknown } = AnyFamily & { "Job": unknown; "Progress": unknown }>(value: Held<S>, convert_S_Job: (value: S["Job"]) => unknown, convert_S_Progress: (value: S["Progress"]) => unknown): unknown {
  const out: Record<string, unknown> = {};
  out["job"] = convert_S_Job((value["job"]) as S["Job"]);
  out["progress"] = convert_S_Progress((value["progress"]) as S["Progress"]);
  out["nested"] = exportNestedUnchecked<S>((value["nested"]) as Nested<S>, (input: S["Job"]): unknown => convert_S_Job((input) as S["Job"]), (input: S["Progress"]): unknown => convert_S_Progress((input) as S["Progress"]));
  return out;
}
/** Reads Held using the supplied conversion for each type argument. */
/** Conversion only: validation belongs to the value adapter or its caller. */
export function importHeldUnchecked<S extends AnyFamily & { "Job": unknown; "Progress": unknown } = AnyFamily & { "Job": unknown; "Progress": unknown }>(raw: unknown, convert_S_Job: (value: unknown) => S["Job"], convert_S_Progress: (value: unknown) => S["Progress"]): Held<S> {
  const wire = raw as Record<string, unknown>;
  const out: Record<string, unknown> = {};
  out["job"] = convert_S_Job(wire["job"]);
  out["progress"] = convert_S_Progress(wire["progress"]);
  out["nested"] = importNestedUnchecked<S>(wire["nested"], (input: unknown): S["Job"] => (convert_S_Job(input)) as S["Job"], (input: unknown): S["Progress"] => (convert_S_Progress(input)) as S["Progress"]);
  return out as unknown as Held<S>;
}
/** Conversion only: use the value adapter or validate the entire wire value inside the same operation batch. */
/** Writes Jobs using the supplied conversion for each type argument. */
export function exportJobsUnchecked<S extends AnyFamily & { "Job": unknown } = AnyFamily & { "Job": unknown }>(value: Jobs<S>, convert_S_Job: (value: S["Job"]) => unknown): unknown {
  return (value as unknown[]).map((item) => convert_S_Job((item) as S["Job"]));
}
/** Reads Jobs using the supplied conversion for each type argument. */
/** Conversion only: validation belongs to the value adapter or its caller. */
export function importJobsUnchecked<S extends AnyFamily & { "Job": unknown } = AnyFamily & { "Job": unknown }>(raw: unknown, convert_S_Job: (value: unknown) => S["Job"]): Jobs<S> {
  return (raw as unknown[]).map((item) => convert_S_Job(item));
}
/** Conversion only: use the value adapter or validate the entire wire value inside the same operation batch. */
/** Writes Nested using the supplied conversion for each type argument. */
export function exportNestedUnchecked<S extends AnyFamily & { "Job": unknown; "Progress": unknown } = AnyFamily & { "Job": unknown; "Progress": unknown }>(value: Nested<S>, convert_S_Job: (value: S["Job"]) => unknown, convert_S_Progress: (value: S["Progress"]) => unknown): unknown {
  const out: Record<string, unknown> = {};
  out["jobs"] = exportJobsUnchecked<S>((value["jobs"]) as Jobs<S>, (input: S["Job"]): unknown => convert_S_Job((input) as S["Job"]));
  out["progress"] = Object.fromEntries(Object.entries(value["progress"] as Record<string, unknown>).map(([key, item]) => [key, (item === null ? null : convert_S_Progress((item) as S["Progress"]))]));
  out["choice"] = exportChoiceUnchecked<S>((value["choice"]) as Choice<S>, (input: S["Job"]): unknown => convert_S_Job((input) as S["Job"]));
  if (value["absent"] !== undefined) out["absent"] = convert_S_Job((value["absent"]) as S["Job"]);
  return out;
}
/** Reads Nested using the supplied conversion for each type argument. */
/** Conversion only: validation belongs to the value adapter or its caller. */
export function importNestedUnchecked<S extends AnyFamily & { "Job": unknown; "Progress": unknown } = AnyFamily & { "Job": unknown; "Progress": unknown }>(raw: unknown, convert_S_Job: (value: unknown) => S["Job"], convert_S_Progress: (value: unknown) => S["Progress"]): Nested<S> {
  const wire = raw as Record<string, unknown>;
  const out: Record<string, unknown> = {};
  out["jobs"] = importJobsUnchecked<S>(wire["jobs"], (input: unknown): S["Job"] => (convert_S_Job(input)) as S["Job"]);
  out["progress"] = Object.fromEntries(Object.entries(wire["progress"] as Record<string, unknown>).map(([key, item]) => [key, (item === null ? null : convert_S_Progress(item))]));
  out["choice"] = importChoiceUnchecked<S>(wire["choice"], (input: unknown): S["Job"] => (convert_S_Job(input)) as S["Job"]);
  if (wire["absent"] !== undefined) out["absent"] = convert_S_Job(wire["absent"]);
  return out as unknown as Nested<S>;
}
export function adapterChoice<S extends AnyFamily & { "Job": unknown } = AnyFamily & { "Job": unknown }>(slot_S: FamilyBinding<S, "Job">): ValueAdapter<Choice<S>> {
  const type_S_Job = familyTypeAdapter(slot_S, "Job");
  const slots: Slots = { "S": slot_S };
  const binding: TypeBinding = { type: "Choice", validate: validateWire, slots };
  const needsContext = type_S_Job.needsContext;
  return { binding, needsContext,
    export(context: unknown, value: Choice<S>): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = exportChoiceUnchecked<S>(value, (value: S["Job"]) => type_S_Job.export(owner, value));
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Choice<S> {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Choice<S> => {
        const converted = importChoiceUnchecked<S>(raw, (value: unknown) => type_S_Job.import(owner, value));
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterHeld<S extends AnyFamily & { "Job": unknown; "Progress": unknown } = AnyFamily & { "Job": unknown; "Progress": unknown }>(slot_S: FamilyBinding<S, "Job" | "Progress">): ValueAdapter<Held<S>> {
  const type_S_Job = familyTypeAdapter(slot_S, "Job");
  const type_S_Progress = familyTypeAdapter(slot_S, "Progress");
  const slots: Slots = { "S": slot_S };
  const binding: TypeBinding = { type: "Held", validate: validateWire, slots };
  const needsContext = type_S_Job.needsContext || type_S_Progress.needsContext;
  return { binding, needsContext,
    export(context: unknown, value: Held<S>): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = exportHeldUnchecked<S>(value, (value: S["Job"]) => type_S_Job.export(owner, value), (value: S["Progress"]) => type_S_Progress.export(owner, value));
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Held<S> {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Held<S> => {
        const converted = importHeldUnchecked<S>(raw, (value: unknown) => type_S_Job.import(owner, value), (value: unknown) => type_S_Progress.import(owner, value));
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterJobs<S extends AnyFamily & { "Job": unknown } = AnyFamily & { "Job": unknown }>(slot_S: FamilyBinding<S, "Job">): ValueAdapter<Jobs<S>> {
  const type_S_Job = familyTypeAdapter(slot_S, "Job");
  const slots: Slots = { "S": slot_S };
  const binding: TypeBinding = { type: "Jobs", validate: validateWire, slots };
  const needsContext = type_S_Job.needsContext;
  return { binding, needsContext,
    export(context: unknown, value: Jobs<S>): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = exportJobsUnchecked<S>(value, (value: S["Job"]) => type_S_Job.export(owner, value));
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Jobs<S> {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Jobs<S> => {
        const converted = importJobsUnchecked<S>(raw, (value: unknown) => type_S_Job.import(owner, value));
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterNested<S extends AnyFamily & { "Job": unknown; "Progress": unknown } = AnyFamily & { "Job": unknown; "Progress": unknown }>(slot_S: FamilyBinding<S, "Job" | "Progress">): ValueAdapter<Nested<S>> {
  const type_S_Job = familyTypeAdapter(slot_S, "Job");
  const type_S_Progress = familyTypeAdapter(slot_S, "Progress");
  const slots: Slots = { "S": slot_S };
  const binding: TypeBinding = { type: "Nested", validate: validateWire, slots };
  const needsContext = type_S_Job.needsContext || type_S_Progress.needsContext;
  return { binding, needsContext,
    export(context: unknown, value: Nested<S>): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = exportNestedUnchecked<S>(value, (value: S["Job"]) => type_S_Job.export(owner, value), (value: S["Progress"]) => type_S_Progress.export(owner, value));
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Nested<S> {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Nested<S> => {
        const converted = importNestedUnchecked<S>(raw, (value: unknown) => type_S_Job.import(owner, value), (value: unknown) => type_S_Progress.import(owner, value));
        return converted;
      };
      return convert(context);
    },
  };
}
export interface ServerMethods<S extends AnyFamily & { "Job": unknown; "Progress": unknown } = AnyFamily & { "Job": unknown; "Progress": unknown }> {
  exchange(params: Held<S>, context?: ValueContext<Held<S> | Held<S>, WireModelContext>): Held<S> | Promise<Held<S>>;
}
export interface ServerEvents<S extends AnyFamily & { "Job": unknown; "Progress": unknown } = AnyFamily & { "Job": unknown; "Progress": unknown }> {
}
export interface Server<S extends AnyFamily & { "Job": unknown; "Progress": unknown } = AnyFamily & { "Job": unknown; "Progress": unknown }> { methods: ServerMethods<S>; events: ServerEvents<S>; }
export interface ClientMethods<S extends AnyFamily & { "Job": unknown; "Progress": unknown } = AnyFamily & { "Job": unknown; "Progress": unknown }> {
}
export interface ClientEvents<S extends AnyFamily & { "Job": unknown; "Progress": unknown } = AnyFamily & { "Job": unknown; "Progress": unknown }> {
}
export interface Client<S extends AnyFamily & { "Job": unknown; "Progress": unknown } = AnyFamily & { "Job": unknown; "Progress": unknown }> { methods: ClientMethods<S>; events: ClientEvents<S>; }
export type ServerModel<S extends AnyFamily & { "Job": unknown; "Progress": unknown } = AnyFamily & { "Job": unknown; "Progress": unknown }> = (remote: Client<S>) => Server<S>;
export type ClientModel<S extends AnyFamily & { "Job": unknown; "Progress": unknown } = AnyFamily & { "Job": unknown; "Progress": unknown }> = (remote: Server<S>) => Client<S>;

const contractTypes = {"types":{"Choice":{"kind":"union","tag":"kind","variants":{"job":"S.Job","none":{"empty":true}}},"Envelope":{"kind":"record","fields":[{"name":"version","type":"integer","required":true},{"name":"kind","type":"string","required":true},{"name":"id","type":"string","required":false},{"name":"method","type":"string","required":false},{"name":"params","type":"json","required":false},{"name":"result","type":"json","required":false},{"name":"error","type":"json","required":false},{"name":"event","type":"string","required":false},{"name":"data","type":"json","required":false},{"name":"traceparent","type":"string","required":false},{"name":"tracestate","type":"string","required":false},{"name":"meta","type":{"map":"string"},"required":false}]},"Handle":{"kind":"record","fields":[{"name":"channel","type":"integer","required":true}]},"Held":{"kind":"record","fields":[{"name":"job","type":"S.Job","required":true},{"name":"progress","type":"S.Progress","required":true},{"name":"nested","type":"Nested","required":true}]},"Jobs":{"kind":"alias","type":{"array":"S.Job"}},"Nested":{"kind":"record","fields":[{"name":"jobs","type":"Jobs","required":true},{"name":"progress","type":{"map":{"nullable":"S.Progress"}},"required":true},{"name":"choice","type":"Choice","required":true},{"name":"absent","type":"S.Job","required":false}]}},"parameters":[{"name":"S","of":"live"}]} as unknown as WireFamily;
/** Canonical language-neutral declaration bytes, the input to wireDigest. */
export const wireDeclaration = "{\"definitions\":{\"holder\":{\"client\":{\"ref\":\"holder/$client\"},\"errors\":[],\"kind\":\"family\",\"parameters\":[{\"name\":\"S\",\"of\":\"live\"}],\"server\":{\"ref\":\"holder/$server\"},\"types\":{\"Choice\":{\"ref\":\"holder/Choice\"},\"Held\":{\"ref\":\"holder/Held\"},\"Jobs\":{\"array\":{\"draw\":{\"parameter\":\"holder/S\"},\"name\":\"Job\"}},\"Nested\":{\"ref\":\"holder/Nested\"}}},\"holder/$client\":{\"direction\":\"client\",\"events\":{},\"extends\":[],\"kind\":\"side\",\"methods\":{},\"parameters\":[{\"name\":\"S\",\"of\":\"live\"}]},\"holder/$server\":{\"direction\":\"server\",\"events\":{},\"extends\":[],\"kind\":\"side\",\"methods\":{\"exchange\":{\"errors\":[],\"request\":{\"ref\":\"holder/Held\"},\"result\":{\"ref\":\"holder/Held\"}}},\"parameters\":[{\"name\":\"S\",\"of\":\"live\"}]},\"holder/Choice\":{\"captures\":[{\"name\":\"S\",\"of\":\"live\"}],\"kind\":\"union\",\"parameters\":[],\"tag\":\"kind\",\"value\":\"value\",\"variants\":{\"job\":{\"draw\":{\"parameter\":\"holder/S\"},\"name\":\"Job\"},\"none\":{\"empty\":true}}},\"holder/Held\":{\"captures\":[{\"name\":\"S\",\"of\":\"live\"}],\"fields\":[{\"name\":\"job\",\"nullable\":false,\"required\":true,\"type\":{\"draw\":{\"parameter\":\"holder/S\"},\"name\":\"Job\"},\"unique\":false},{\"name\":\"progress\",\"nullable\":false,\"required\":true,\"type\":{\"draw\":{\"parameter\":\"holder/S\"},\"name\":\"Progress\"},\"unique\":false},{\"name\":\"nested\",\"nullable\":false,\"required\":true,\"type\":{\"ref\":\"holder/Nested\"},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]},\"holder/Nested\":{\"captures\":[{\"name\":\"S\",\"of\":\"live\"}],\"fields\":[{\"name\":\"jobs\",\"nullable\":false,\"required\":true,\"type\":{\"array\":{\"draw\":{\"parameter\":\"holder/S\"},\"name\":\"Job\"}},\"unique\":false},{\"name\":\"progress\",\"nullable\":false,\"required\":true,\"type\":{\"map\":{\"nullable\":{\"draw\":{\"parameter\":\"holder/S\"},\"name\":\"Progress\"}}},\"unique\":false},{\"name\":\"choice\",\"nullable\":false,\"required\":true,\"type\":{\"ref\":\"holder/Choice\"},\"unique\":false},{\"name\":\"absent\",\"nullable\":false,\"required\":false,\"type\":{\"draw\":{\"parameter\":\"holder/S\"},\"name\":\"Job\"},\"unique\":false}],\"kind\":\"record\",\"open\":false,\"parameters\":[]}},\"root\":{\"ref\":\"holder\"},\"version\":1}";
/** SHA-256 of the canonical full declaration's exact UTF-8 bytes. */
export const wireDigest = "a6b69fd64213a81ca04b1378d89dd8336a10dde92fafa0b08c5cbe4c0fd6c262";
/** Runtime validation applies equally to calls, replies, reverse calls and events; what fills a slot of a parameter is validated by the binding of the family that fills it. */
export const validateWire = withDeclaration(createValidator(contractTypes, wireDigest, {  }), wireDeclaration);
/** This family's declaration and reusable interpretations, with no operation context or owner. */
export const family = { name: "holder", validate: validateWire, types: { "Envelope": jsonAdapter<Envelope>({ type: "Envelope", validate: validateWire }), "Handle": jsonAdapter<Handle>({ type: "Handle", validate: validateWire }) } } as const;
