// Code generated by nightseam. DO NOT EDIT.
import { createValidator, DuplexError, type AnyFamily, type FamilyBinding, type TypeBinding, type Slots, type TypeExpression, type WireFamily } from "@nightseam/runtime";
export type { AnyFamily, FamilyBinding, TypeBinding, Slots, TypeExpression };
import type * as probe from "@example/probe-client";
import { validateWire as validate_probe } from "@example/probe-client";
import type { ValueAdapter, ValueContext } from "@nightseam/runtime";
import type { WireModelContext } from "@nightseam/runtime";
/** One message of S with a page of Item beside it. */
export interface Carried<S extends AnyFamily = AnyFamily, Item = unknown> {
  "message": S["Envelope"];
  /** A handle that may be absent from the value rather than from the member. */
  "back": S["Handle"] | null;
  "page": Page<Item>;
}
/** One message of the nightseam.duplex/1 profile: the members the peer acts on, and nothing else. */
export interface Envelope {
  /** The profile's version, 1. */
  "version": number;
  /** request, response, event or cancel. */
  "kind": string;
  /** What correlates a response or a cancel with its request. */
  "id"?: string;
  /** The method a request names. */
  "method"?: string;
  /** A request's parameters. */
  "params"?: unknown;
  /** A response's result. */
  "result"?: unknown;
  /** A response's error. */
  "error"?: unknown;
  /** The event an event frame names. */
  "event"?: string;
  /** An event's data. */
  "data"?: unknown;
  /** The W3C Trace Context of the frame. */
  "traceparent"?: string;
  /** The vendor state of that trace. */
  "tracestate"?: string;
  /** What a request or an event carries about the call, delivered to the handler beside the payload. */
  "meta"?: Record<string, string>;
}
/** A reference to a channel on the connection that carries the message holding it. */
export interface Handle {
  /** The channel's id on that connection. */
  "channel": number;
}
/** A value, or nothing: a generic union, declared in the language rather than built in. */
export type Option<T = unknown> = { "kind": "none"; "value": OptionNone } | { "kind": "some"; "value": T };
export type OptionNone = Record<string, never>;
/** A page of anything: a type parameter filled by a type expression. */
export interface Page<T = unknown> {
  "items": Array<T>;
  "next"?: string | null;
}
/** One part of a message: a record with a literal field, a shape written inline, and a scalar payload. */
export type Part = { "type": "count"; "value": number } | { "type": "image"; "value": PartImage } | { "type": "text"; "value": TextPart };
export interface PartImage {
  "url": string;
  "alt"?: string | null;
}
/** A page of parts: a generic type of this family, filled here. */
export type Parts = Page<Part>;
export interface PartsRequest {
  "after"?: string | null;
}
/** What came of a call: two type parameters on one declaration. */
export type Result<T = unknown, E = unknown> = { "kind": "err"; "value": E } | { "kind": "ok"; "value": T };
/** Part widened: an extending union adds variants, and a value of the base validates against it. */
export type RichPart = { "type": "count"; "value": number } | { "type": "image"; "value": PartImage } | { "type": "text"; "value": TextPart } | { "type": "table"; "value": RichPartTable };
export interface RichPartTable {
  /** A collection of values that may be null. */
  "rows": Array<string | null>;
}
/** A part with its own literal type field, retained inside the union payload. */
export interface TextPart {
  /** A literal field of the payload; the union has its own discriminator. */
  "type": "text";
  "body": string;
}
/** The family: its name and the wire types a slot of it draws on. */
export interface Family { readonly name: "proof"; Envelope: Envelope; Handle: Handle; OptionNone: OptionNone; Part: Part; PartImage: PartImage; Parts: Parts; PartsRequest: PartsRequest; RichPart: RichPart; RichPartTable: RichPartTable; TextPart: TextPart }
/** Writes Carried using the supplied conversion for each type argument. */
export function exportCarried<S extends AnyFamily = AnyFamily, Item = unknown>(value: Carried<S, Item>, convert_S_Envelope: (value: S["Envelope"]) => unknown, convert_S_Handle: (value: S["Handle"]) => unknown, convert_Item_: (value: Item) => unknown): unknown {
  const out: Record<string, unknown> = {};
  out["message"] = convert_S_Envelope((value["message"]) as S["Envelope"]);
  out["back"] = (value["back"] === null ? null : convert_S_Handle((value["back"]) as S["Handle"]));
  out["page"] = exportPage<Item>((value["page"]) as Page<Item>, (input: Item): unknown => convert_Item_((input) as Item));
  return out;
}
/** Reads Carried using the supplied conversion for each type argument. */
export function importCarried<S extends AnyFamily = AnyFamily, Item = unknown>(raw: unknown, convert_S_Envelope: (value: unknown) => S["Envelope"], convert_S_Handle: (value: unknown) => S["Handle"], convert_Item_: (value: unknown) => Item): Carried<S, Item> {
  const wire = raw as Record<string, unknown>;
  const out: Record<string, unknown> = {};
  out["message"] = convert_S_Envelope(wire["message"]);
  out["back"] = (wire["back"] === null ? null : convert_S_Handle(wire["back"]));
  out["page"] = importPage<Item>(wire["page"], (input: unknown): Item => (convert_Item_(input)) as Item);
  return out as unknown as Carried<S, Item>;
}
/** Writes Option using the supplied conversion for each type argument. */
export function exportOption<T = unknown>(value: Option<T>, convert_T_: (value: T) => unknown): unknown {
  const held = value as Record<string, unknown>;
  switch (held["kind"]) {
    case "none": return { "kind": "none", "value": held["value"] };
    case "some": return { "kind": "some", "value": convert_T_((held["value"]) as T) };
  }
  throw new Error("Option: unknown variant " + String(held["kind"]));
}
/** Reads Option using the supplied conversion for each type argument. */
export function importOption<T = unknown>(raw: unknown, convert_T_: (value: unknown) => T): Option<T> {
  const held = raw as Record<string, unknown>;
  switch (held["kind"]) {
    case "none": return { "kind": "none", "value": held["value"] } as unknown as Option<T>;
    case "some": return { "kind": "some", "value": convert_T_(held["value"]) } as unknown as Option<T>;
  }
  throw new Error("Option: unknown variant " + String(held["kind"]));
}
/** Writes Page using the supplied conversion for each type argument. */
export function exportPage<T = unknown>(value: Page<T>, convert_T_: (value: T) => unknown): unknown {
  const out: Record<string, unknown> = {};
  out["items"] = (value["items"] as unknown[]).map((item) => convert_T_((item) as T));
  if (value["next"] !== undefined) out["next"] = value["next"];
  return out;
}
/** Reads Page using the supplied conversion for each type argument. */
export function importPage<T = unknown>(raw: unknown, convert_T_: (value: unknown) => T): Page<T> {
  const wire = raw as Record<string, unknown>;
  const out: Record<string, unknown> = {};
  out["items"] = (wire["items"] as unknown[]).map((item) => convert_T_(item));
  if (wire["next"] !== undefined) out["next"] = wire["next"];
  return out as unknown as Page<T>;
}
/** Writes Result using the supplied conversion for each type argument. */
export function exportResult<T = unknown, E = unknown>(value: Result<T, E>, convert_T_: (value: T) => unknown, convert_E_: (value: E) => unknown): unknown {
  const held = value as Record<string, unknown>;
  switch (held["kind"]) {
    case "err": return { "kind": "err", "value": convert_E_((held["value"]) as E) };
    case "ok": return { "kind": "ok", "value": convert_T_((held["value"]) as T) };
  }
  throw new Error("Result: unknown variant " + String(held["kind"]));
}
/** Reads Result using the supplied conversion for each type argument. */
export function importResult<T = unknown, E = unknown>(raw: unknown, convert_T_: (value: unknown) => T, convert_E_: (value: unknown) => E): Result<T, E> {
  const held = raw as Record<string, unknown>;
  switch (held["kind"]) {
    case "err": return { "kind": "err", "value": convert_E_(held["value"]) } as unknown as Result<T, E>;
    case "ok": return { "kind": "ok", "value": convert_T_(held["value"]) } as unknown as Result<T, E>;
  }
  throw new Error("Result: unknown variant " + String(held["kind"]));
}
export function adapterCarried<S extends AnyFamily = AnyFamily, Item = unknown>(slot_S: FamilyBinding<S>, slot_Item: ValueAdapter<Item>): ValueAdapter<Carried<S, Item>> {
  const slots: Slots = { "S": slot_S, "Item": slot_Item.binding };
  const binding: TypeBinding = { type: "Carried", validate: validateWire, slots };
  const needsContext = slot_Item.needsContext;
  return { binding, needsContext,
    export(context: unknown, value: Carried<S, Item>): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = exportCarried<S, Item>(value, (value: S["Envelope"]) => value, (value: S["Handle"]) => value, (value: Item) => slot_Item.export(owner, value));
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Carried<S, Item> {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Carried<S, Item> => {
        const converted = importCarried<S, Item>(raw, (value: unknown) => value as S["Envelope"], (value: unknown) => value as S["Handle"], (value: unknown) => slot_Item.import(owner, value));
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterOption<T = unknown>(slot_T: ValueAdapter<T>): ValueAdapter<Option<T>> {
  const slots: Slots = { "T": slot_T.binding };
  const binding: TypeBinding = { type: "Option", validate: validateWire, slots };
  const needsContext = slot_T.needsContext;
  return { binding, needsContext,
    export(context: unknown, value: Option<T>): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = exportOption<T>(value, (value: T) => slot_T.export(owner, value));
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Option<T> {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Option<T> => {
        const converted = importOption<T>(raw, (value: unknown) => slot_T.import(owner, value));
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterOptionNone(): ValueAdapter<OptionNone> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "OptionNone", validate: validateWire, slots };
  const needsContext = false;
  return { binding, needsContext,
    export(context: unknown, value: OptionNone): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = value;
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): OptionNone {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): OptionNone => {
        const converted = raw as OptionNone;
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterPage<T = unknown>(slot_T: ValueAdapter<T>): ValueAdapter<Page<T>> {
  const slots: Slots = { "T": slot_T.binding };
  const binding: TypeBinding = { type: "Page", validate: validateWire, slots };
  const needsContext = slot_T.needsContext;
  return { binding, needsContext,
    export(context: unknown, value: Page<T>): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = exportPage<T>(value, (value: T) => slot_T.export(owner, value));
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Page<T> {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Page<T> => {
        const converted = importPage<T>(raw, (value: unknown) => slot_T.import(owner, value));
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterPart(): ValueAdapter<Part> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Part", validate: validateWire, slots };
  const needsContext = false;
  return { binding, needsContext,
    export(context: unknown, value: Part): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = value;
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Part {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Part => {
        const converted = raw as Part;
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterPartImage(): ValueAdapter<PartImage> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "PartImage", validate: validateWire, slots };
  const needsContext = false;
  return { binding, needsContext,
    export(context: unknown, value: PartImage): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = value;
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): PartImage {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): PartImage => {
        const converted = raw as PartImage;
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterParts(): ValueAdapter<Parts> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "Parts", validate: validateWire, slots };
  const needsContext = false;
  return { binding, needsContext,
    export(context: unknown, value: Parts): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = value;
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Parts {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Parts => {
        const converted = raw as Parts;
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterPartsRequest(): ValueAdapter<PartsRequest> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "PartsRequest", validate: validateWire, slots };
  const needsContext = false;
  return { binding, needsContext,
    export(context: unknown, value: PartsRequest): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = value;
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): PartsRequest {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): PartsRequest => {
        const converted = raw as PartsRequest;
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterResult<T = unknown, E = unknown>(slot_T: ValueAdapter<T>, slot_E: ValueAdapter<E>): ValueAdapter<Result<T, E>> {
  const slots: Slots = { "T": slot_T.binding, "E": slot_E.binding };
  const binding: TypeBinding = { type: "Result", validate: validateWire, slots };
  const needsContext = slot_T.needsContext || slot_E.needsContext;
  return { binding, needsContext,
    export(context: unknown, value: Result<T, E>): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = exportResult<T, E>(value, (value: T) => slot_T.export(owner, value), (value: E) => slot_E.export(owner, value));
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): Result<T, E> {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): Result<T, E> => {
        const converted = importResult<T, E>(raw, (value: unknown) => slot_T.import(owner, value), (value: unknown) => slot_E.import(owner, value));
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterRichPart(): ValueAdapter<RichPart> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "RichPart", validate: validateWire, slots };
  const needsContext = false;
  return { binding, needsContext,
    export(context: unknown, value: RichPart): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = value;
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): RichPart {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): RichPart => {
        const converted = raw as RichPart;
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterRichPartTable(): ValueAdapter<RichPartTable> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "RichPartTable", validate: validateWire, slots };
  const needsContext = false;
  return { binding, needsContext,
    export(context: unknown, value: RichPartTable): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = value;
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): RichPartTable {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): RichPartTable => {
        const converted = raw as RichPartTable;
        return converted;
      };
      return convert(context);
    },
  };
}
export function adapterTextPart(): ValueAdapter<TextPart> {
  const slots: Slots = {  };
  const binding: TypeBinding = { type: "TextPart", validate: validateWire, slots };
  const needsContext = false;
  return { binding, needsContext,
    export(context: unknown, value: TextPart): unknown {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      const convert = (owner: unknown): unknown => {
        const converted = value;
        binding.validate(binding.type, converted, '$', slots);
        return converted;
      };
      return convert(context);
    },
    import(context: unknown, raw: unknown): TextPart {
      if (needsContext && context === undefined) throw new DuplexError('scope_closed', 'conversion requires a value context');
      binding.validate(binding.type, raw, '$', slots);
      const convert = (owner: unknown): TextPart => {
        const converted = raw as TextPart;
        return converted;
      };
      return convert(context);
    },
  };
}
export interface ServerMethods<S extends AnyFamily = AnyFamily, Item = unknown> {
  echo(params: probe.Payload, context?: WireModelContext): probe.Payload | Promise<probe.Payload>;
  noArgs(params: Record<string, never>, context?: WireModelContext): string | Promise<string>;
  classify(params: Part, context?: WireModelContext): string | Promise<string>;
  classifyRich(params: RichPart, context?: WireModelContext): string | Promise<string>;
  parts(params: PartsRequest, context?: WireModelContext): Result<Parts, string> | Promise<Result<Parts, string>>;
  relay(params: Carried<S, Item>, context?: ValueContext<Carried<S, Item> | Option<Envelope>, WireModelContext>): Option<Envelope> | Promise<Option<Envelope>>;
}
export interface ServerEvents<S extends AnyFamily = AnyFamily, Item = unknown> {
}
export interface Server<S extends AnyFamily = AnyFamily, Item = unknown> { methods: ServerMethods<S, Item>; events: ServerEvents<S, Item>; }
export interface ClientMethods<S extends AnyFamily = AnyFamily, Item = unknown> {
}
export interface ClientEvents<S extends AnyFamily = AnyFamily, Item = unknown> {
  changed(data: probe.Payload, context?: WireModelContext): void | Promise<void>;
  partAdded(data: RichPart, context?: WireModelContext): void | Promise<void>;
}
export interface Client<S extends AnyFamily = AnyFamily, Item = unknown> { methods: ClientMethods<S, Item>; events: ClientEvents<S, Item>; }
export type ServerModel<S extends AnyFamily = AnyFamily, Item = unknown> = (remote: Client<S, Item>) => Server<S, Item>;
export type ClientModel<S extends AnyFamily = AnyFamily, Item = unknown> = (remote: Server<S, Item>) => Client<S, Item>;

const contractTypes = {"types":{"Carried":{"kind":"record","fields":[{"name":"message","type":"S.Envelope","required":true},{"name":"back","type":{"nullable":"S.Handle"},"required":true},{"name":"page","type":{"apply":"Page","with":{"T":"Item"}},"required":true}]},"Envelope":{"kind":"record","fields":[{"name":"version","type":"integer","required":true},{"name":"kind","type":"string","required":true},{"name":"id","type":"string","required":false},{"name":"method","type":"string","required":false},{"name":"params","type":"json","required":false},{"name":"result","type":"json","required":false},{"name":"error","type":"json","required":false},{"name":"event","type":"string","required":false},{"name":"data","type":"json","required":false},{"name":"traceparent","type":"string","required":false},{"name":"tracestate","type":"string","required":false},{"name":"meta","type":{"map":"string"},"required":false}]},"Handle":{"kind":"record","fields":[{"name":"channel","type":"integer","required":true}]},"Option":{"kind":"union","parameters":[{"name":"T"}],"tag":"kind","variants":{"none":{"kind":"record"},"some":"T"}},"Page":{"kind":"record","fields":[{"name":"items","type":{"array":"T"},"required":true},{"name":"next","type":{"nullable":"string"},"required":false}],"parameters":[{"name":"T"}]},"Part":{"kind":"union","tag":"type","variants":{"count":"integer","image":{"kind":"record","fields":[{"name":"url","type":"string","required":true,"pattern":"^https://[A-Za-z0-9.-]+/[^ ]*$"},{"name":"alt","type":{"nullable":"string"},"required":false}]},"text":"TextPart"}},"Parts":{"kind":"alias","type":{"apply":"Page","with":{"T":"Part"}}},"Result":{"kind":"union","parameters":[{"name":"T"},{"name":"E"}],"tag":"kind","variants":{"err":"E","ok":"T"}},"RichPart":{"kind":"union","extends":["Part"],"tag":"type","variants":{"table":{"kind":"record","fields":[{"name":"rows","type":{"array":{"nullable":"string"}},"required":true}]}}},"TextPart":{"kind":"record","fields":[{"name":"type","type":{"literal":"text"},"required":true},{"name":"body","type":"string","required":true,"length":{"min":1}}]}},"parameters":[{"name":"S","of":"protocol"},{"name":"Item"}]} as unknown as WireFamily;
/** The SHA-256 digest of the family's exact wire description. */
export const wireDigest = "c707012387e56828117d67518f558250b1abc502f15721c3d55a820f717645f1";
/** Runtime validation applies equally to calls, replies, reverse calls and events; what fills a slot of a parameter is validated by the binding of the family that fills it. */
export const validateWire = createValidator(contractTypes, wireDigest, { "probe": validate_probe });
/** This family bound: its name and its validator, to fill a family slot in another family's client. */
export const family = { name: "proof", validate: validateWire } as const;
