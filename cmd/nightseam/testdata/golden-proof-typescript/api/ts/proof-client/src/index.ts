// Code generated by nightseam. DO NOT EDIT.
import { DuplexError, callWire, emitWire, registerWire, wirePair, type WireModelContext } from "@nightseam/runtime";
import { encodePath, type Wire } from '@nightseam/duplex';
import type { AdapterContext, ValueAdapter, ValueContext } from "@nightseam/runtime";
import { validateWire } from './types.ts';
import type * as Protocol from './types.ts';
import type { AnyFamily, FamilyBinding, Slots } from './types.ts';
import * as conversion from './types.ts';
import type * as probe from "@example/probe-client";
export * from './types.ts';
export { DuplexError };
export type { AdapterContext };
function makeAdapter<S extends AnyFamily = AnyFamily, Item = unknown>(context: AdapterContext, s: FamilyBinding<S>, item: ValueAdapter<Item>) {
  const observer = context.options?.observer;
  const propagator = context.options?.propagator;
  const requestTimeoutMs = context.options?.requestTimeoutMs;
  const bindings = { s, item };
  const slots: Slots = { "S": s, "Item": item.binding };
  const environment = context.valueEnvironment;
  if ((item.needsContext) && !environment) throw new DuplexError('scope_closed', 'model adaptation requires an explicit value environment');
  function hasModelHandler(facet: object, name: string): boolean {
    for (let current = facet; current !== null && current !== Object.prototype; current = Object.getPrototypeOf(current)) {
      if (Object.prototype.hasOwnProperty.call(current, name)) return typeof (facet as Record<string, unknown>)[name] === 'function';
    }
    return false;
  }
  function proxyServer(wire: Wire): Protocol.Server<S, Item> {
    return {
      methods: {
        async echo(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs ?? requestTimeoutMs, meta: context?.outgoingMeta, observer, propagator, family: "proof" };
          validateWire("probe.Payload", params, '$', slots);
          const result = await callWire<probe.Payload>(wire, ["echo"], params, options);
          validateWire("probe.Payload", result, '$', slots);
          return result;
        },
        async noArgs(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs ?? requestTimeoutMs, meta: context?.outgoingMeta, observer, propagator, family: "proof" };
          validateWire({ empty: true }, params, '$', slots);
          const result = await callWire<string>(wire, ["no_args"], params, options);
          validateWire("string", result, '$', slots);
          return result;
        },
        async classify(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs ?? requestTimeoutMs, meta: context?.outgoingMeta, observer, propagator, family: "proof" };
          validateWire("Part", params, '$', slots);
          const result = await callWire<string>(wire, ["classify"], params, options);
          validateWire("string", result, '$', slots);
          return result;
        },
        async classifyRich(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs ?? requestTimeoutMs, meta: context?.outgoingMeta, observer, propagator, family: "proof" };
          validateWire("RichPart", params, '$', slots);
          const result = await callWire<string>(wire, ["classify_rich"], params, options);
          validateWire("string", result, '$', slots);
          return result;
        },
        async parts(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs ?? requestTimeoutMs, meta: context?.outgoingMeta, observer, propagator, family: "proof" };
          validateWire({"kind":"record","fields":[{"name":"after","type":{"nullable":"string"},"required":false}]}, params, '$', slots);
          const result = await callWire<Protocol.Result<Protocol.Parts, string>>(wire, ["parts"], params, options);
          validateWire({"apply":"Result","with":{"E":"string","T":"Parts"}}, result, '$', slots);
          return result;
        },
        async relay(params, context) {
          const options = { context, signal: context?.signal, timeoutMs: context?.timeoutMs ?? requestTimeoutMs, meta: context?.outgoingMeta, observer, propagator, family: "proof" };
          const owner = ((bindings.item.needsContext)) ? environment!.select((context as {valueContext?: unknown} | undefined)?.valueContext) : undefined;
          const result = await ((bindings.item.needsContext) ? environment!.publish(owner, owner => ((bindings.item.needsContext) ? environment!.export(owner, (owner) => { const converted = conversion.exportCarried<S, Item>((params) as Protocol.Carried<S, Item>, (input: S["Envelope"]): unknown => input, (input: S["Handle"]): unknown => input, (input: Item): unknown => bindings.item.export(owner, (input) as Item)); validateWire("Carried", converted, '$', slots); return converted; }) : (() => { const converted = conversion.exportCarried<S, Item>((params) as Protocol.Carried<S, Item>, (input: S["Envelope"]): unknown => input, (input: S["Handle"]): unknown => input, (input: Item): unknown => bindings.item.export(owner, (input) as Item)); validateWire("Carried", converted, '$', slots); return converted; })()), sent => callWire(wire, ["relay"], sent, options)) : callWire(wire, ["relay"], ((bindings.item.needsContext) ? environment!.export(owner, (owner) => { const converted = conversion.exportCarried<S, Item>((params) as Protocol.Carried<S, Item>, (input: S["Envelope"]): unknown => input, (input: S["Handle"]): unknown => input, (input: Item): unknown => bindings.item.export(owner, (input) as Item)); validateWire("Carried", converted, '$', slots); return converted; }) : (() => { const converted = conversion.exportCarried<S, Item>((params) as Protocol.Carried<S, Item>, (input: S["Envelope"]): unknown => input, (input: S["Handle"]): unknown => input, (input: Item): unknown => bindings.item.export(owner, (input) as Item)); validateWire("Carried", converted, '$', slots); return converted; })()), options));
          validateWire({"apply":"Option","with":{"T":"Envelope"}}, result, '$', slots);
          return result as Protocol.Option<Protocol.Envelope>;
        },
      },
      events: {
      },
    };
  }
  function bindServer(wire: Wire, implementation: Protocol.Server<S, Item>): void {
    if (!implementation?.methods || !implementation.events) throw new Error('model methods and events are required');
    if (!hasModelHandler(implementation.methods, "echo")) throw new Error("handler for echo is required");
    if (!hasModelHandler(implementation.methods, "noArgs")) throw new Error("handler for no_args is required");
    if (!hasModelHandler(implementation.methods, "classify")) throw new Error("handler for classify is required");
    if (!hasModelHandler(implementation.methods, "classifyRich")) throw new Error("handler for classify_rich is required");
    if (!hasModelHandler(implementation.methods, "parts")) throw new Error("handler for parts is required");
    if (!hasModelHandler(implementation.methods, "relay")) throw new Error("handler for relay is required");
    const detach: Array<() => void> = [];
    try {
      detach.push(registerWire(wire, ["echo"], {
        observer, family: "proof",
        request: async (raw, context) => {
          try { validateWire("probe.Payload", raw, '$', slots); } catch (error) { throw new DuplexError('invalid_params', String(error)); }
          const params = raw as probe.Payload;
          const result = await implementation.methods.echo(params as probe.Payload, context);
          return (() => { const converted = result; validateWire("probe.Payload", converted, '$', slots); return converted; })();
        },
      }));
      detach.push(registerWire(wire, ["no_args"], {
        observer, family: "proof",
        request: async (raw, context) => {
          try { validateWire({ empty: true }, raw, '$', slots); } catch (error) { throw new DuplexError('invalid_params', String(error)); }
          const params = raw;
          const result = await implementation.methods.noArgs(params as Record<string, never>, context);
          return (() => { const converted = result; validateWire("string", converted, '$', slots); return converted; })();
        },
      }));
      detach.push(registerWire(wire, ["classify"], {
        observer, family: "proof",
        request: async (raw, context) => {
          try { validateWire("Part", raw, '$', slots); } catch (error) { throw new DuplexError('invalid_params', String(error)); }
          const params = raw as Protocol.Part;
          const result = await implementation.methods.classify(params as Protocol.Part, context);
          return (() => { const converted = result; validateWire("string", converted, '$', slots); return converted; })();
        },
      }));
      detach.push(registerWire(wire, ["classify_rich"], {
        observer, family: "proof",
        request: async (raw, context) => {
          try { validateWire("RichPart", raw, '$', slots); } catch (error) { throw new DuplexError('invalid_params', String(error)); }
          const params = raw as Protocol.RichPart;
          const result = await implementation.methods.classifyRich(params as Protocol.RichPart, context);
          return (() => { const converted = result; validateWire("string", converted, '$', slots); return converted; })();
        },
      }));
      detach.push(registerWire(wire, ["parts"], {
        observer, family: "proof",
        request: async (raw, context) => {
          try { validateWire({"kind":"record","fields":[{"name":"after","type":{"nullable":"string"},"required":false}]}, raw, '$', slots); } catch (error) { throw new DuplexError('invalid_params', String(error)); }
          const params = raw as Protocol.PartsRequest;
          const result = await implementation.methods.parts(params as Protocol.PartsRequest, context);
          return (() => { const converted = result; validateWire({"apply":"Result","with":{"E":"string","T":"Parts"}}, converted, '$', slots); return converted; })();
        },
      }));
      detach.push(registerWire(wire, ["relay"], {
        observer, family: "proof",
        request: async (raw, context) => {
          try { validateWire("Carried", raw, '$', slots); } catch (error) { throw new DuplexError('invalid_params', String(error)); }
          const owner = ((bindings.item.needsContext)) ? environment!.child(undefined) : undefined; const ownedContext = Object.create(context) as ValueContext<Protocol.Carried<S, Item> | Protocol.Option<Protocol.Envelope>, WireModelContext>; if (owner !== undefined) Object.defineProperty(ownedContext, 'valueContext', { value: owner, enumerable: true });
          const params = ((bindings.item.needsContext) ? environment!.import(owner, (owner) => conversion.importCarried<S, Item>(raw, (input: unknown): S["Envelope"] => (input) as S["Envelope"], (input: unknown): S["Handle"] => (input) as S["Handle"], (input: unknown): Item => (bindings.item.import(owner, input)) as Item)) : conversion.importCarried<S, Item>(raw, (input: unknown): S["Envelope"] => (input) as S["Envelope"], (input: unknown): S["Handle"] => (input) as S["Handle"], (input: unknown): Item => (bindings.item.import(owner, input)) as Item));
          const result = await implementation.methods.relay(params as Protocol.Carried<S, Item>, ownedContext);
          return (() => { const converted = result; validateWire({"apply":"Option","with":{"T":"Envelope"}}, converted, '$', slots); return converted; })();
        },
      }));
    } catch (error) { for (const remove of detach.reverse()) remove(); throw error; }
  }
  function proxyClient(wire: Wire): Protocol.Client<S, Item> {
    return {
      methods: {
      },
      events: {
        async changed(data, context) {
          const options = { context, meta: context?.outgoingMeta, observer, propagator, family: "proof" };
          validateWire("probe.Payload", data, '$', slots);
          emitWire(wire, ["changed"], data, options);
        },
        async partAdded(data, context) {
          const options = { context, meta: context?.outgoingMeta, observer, propagator, family: "proof" };
          validateWire("RichPart", data, '$', slots);
          emitWire(wire, ["part.added"], data, options);
        },
      },
    };
  }
  function bindClient(wire: Wire, implementation: Protocol.Client<S, Item>): void {
    if (!implementation?.methods || !implementation.events) throw new Error('model methods and events are required');
    if (!hasModelHandler(implementation.events, "changed")) throw new Error("event handler for changed is required");
    if (!hasModelHandler(implementation.events, "partAdded")) throw new Error("event handler for part.added is required");
    const detach: Array<() => void> = [];
    try {
      detach.push(registerWire(wire, ["changed"], {
        observer, family: "proof",
        event: async (raw, context) => {
          try { validateWire("probe.Payload", raw, '$', slots); } catch (error) { wire.close(); throw error; }
          await implementation.events.changed(raw as probe.Payload, context);
        },
      }));
      detach.push(registerWire(wire, ["part.added"], {
        observer, family: "proof",
        event: async (raw, context) => {
          try { validateWire("RichPart", raw, '$', slots); } catch (error) { wire.close(); throw error; }
          await implementation.events.partAdded(raw as Protocol.RichPart, context);
        },
      }));
    } catch (error) { for (const remove of detach.reverse()) remove(); throw error; }
  }
  return { proxyServer, proxyClient, bindServer, bindClient };
}
/** Exposes one model session at a new local wire origin. */
export function toWire<S extends AnyFamily = AnyFamily, Item = unknown>(model: Protocol.ClientModel<S, Item>, context: AdapterContext, s: FamilyBinding<S>, item: ValueAdapter<Item>): Wire {
  const adapter = makeAdapter<S, Item>(context, s, item);
  const [access, binding] = wirePair({ ...context.options, families: { ...context.options?.families, [encodePath(["echo"])]: "proof", [encodePath(["no_args"])]: "proof", [encodePath(["classify"])]: "proof", [encodePath(["classify_rich"])]: "proof", [encodePath(["parts"])]: "proof", [encodePath(["relay"])]: "proof", [encodePath(["changed"])]: "proof", [encodePath(["part.added"])]: "proof" } });
  try { const implementation = model(adapter.proxyServer(binding)); adapter.bindClient(binding, implementation); return access; } catch (error) { access.close(); throw error; }
}
/** Interprets a wire as the same model factory; the resulting session binds once. */
export async function fromWire<S extends AnyFamily = AnyFamily, Item = unknown>(wire: Wire, context: AdapterContext, s: FamilyBinding<S>, item: ValueAdapter<Item>): Promise<Protocol.ClientModel<S, Item>> {
  const adapter = makeAdapter<S, Item>(context, s, item);
  let bound = false;
  return remote => {
    if (bound) throw new DuplexError('already_bound', 'model already bound');
    bound = true;
    adapter.bindServer(wire, remote);
    return adapter.proxyClient(wire);
  };
}
export const errors = { denied: "denied" } as const;
export type ErrorCode = (typeof errors)[keyof typeof errors];
