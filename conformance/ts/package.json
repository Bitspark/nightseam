{
  "name": "@nightseam/conformance",
  "version": "0.0.0",
  "private": true,
  "description": "The TypeScript testee of Nightseam's conformance suite: the runtime and tunnel under the runner's control over conformance/DRIVER.md. Never published.",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "@bitspark/bitwire": "0.2.0",
    "@nightseam/duplex": "workspace:*",
    "@nightseam/live": "workspace:*",
    "@nightseam/runtime": "workspace:*",
    "@nightseam/tunnel": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.6.1",
    "@types/ws": "^8.5.13",
    "typescript": "^7.0.2",
    "ws": "^8.18.0"
  },
  "scripts": {
    "check": "tsc -p tsconfig.check.json"
  }
}
