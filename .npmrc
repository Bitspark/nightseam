# Archon's packages are on the public npm registry; a developer's own
# scoped configuration — GitHub Packages for @bitspark, say — must not
# steer the lockfile elsewhere, since CI installs anonymously.
@bitspark:registry=https://registry.npmjs.org
