# identity

<!-- Generated by nightseam from nightseam:identity/; do not edit. -->

A family of the `nightseam.duplex/1` profile, declared in `nightseam:identity/model.json`, `nightseam:identity/protocol.json`.
The tiers it declares bring the built-in family `duplex`, imported implicitly.

## Types

### Declaration

A record. The declaration a generated adapter interprets at one wire origin. Identity is independent of the carrier and of authentication.

| Field | Type | Presence | Constraints | Description |
|---|---|---|---|---|
| `path` | `string` | required | — | The nominal declaration path. |
| `digest` | `string` | optional | — | The generated SHA-256 declaration digest, exactly 64 lowercase hexadecimal characters. Absence makes no revision claim; an empty present string is invalid. |

## Carried types

These are this family's own types, declared in the same language by a built-in family Nightseam declares of itself and imported by the tier that brings it, with no `imports` line. A declaration of this family may not declare one and names it by the built-in that declares it — `duplex.Envelope`.

### Envelope

A record, carried from the built-in `duplex` family. One message of the nightseam.duplex/1 profile: the members the peer acts on, and nothing else.

| Field | Type | Presence | Constraints | Description |
|---|---|---|---|---|
| `version` | `integer` | required | — | The profile's version, 1. |
| `kind` | `string` | required | — | request, response, event or cancel. |
| `id` | `string` | optional | — | What correlates a response or a cancel with its request. |
| `method` | `string` | optional | — | The method a request names. |
| `params` | `json` | optional | — | A request's parameters. |
| `result` | `json` | optional | — | A response's result. |
| `error` | `json` | optional | — | A response's error. |
| `event` | `string` | optional | — | The event an event frame names. |
| `data` | `json` | optional | — | An event's data. |
| `traceparent` | `string` | optional | — | The W3C Trace Context of the frame. |
| `tracestate` | `string` | optional | — | The vendor state of that trace. |
| `meta` | map of `string` | optional | — | What a request or an event carries about the call, delivered to the handler beside the payload. |

### Handle

A record, carried from the built-in `duplex` family. A reference to a channel on the connection that carries the message holding it.

| Field | Type | Presence | Constraints | Description |
|---|---|---|---|---|
| `channel` | `integer` | required | — | The channel's id on that connection. |

## Server side

The server implements these methods and emits these events.

| Method | Request | Result | Errors | Description |
|---|---|---|---|---|
| `identity.check` | `Declaration` | `Declaration` | `contract_invalid`, `contract_mismatch` | Compares the caller's expected declaration identity before model interpretation and answers with the serving identity. |

### `identity.check` on the wire

The client sends:

```json
{
  "version": 1,
  "kind": "request",
  "id": "c:1",
  "method": "identity.check",
  "params": {
    "path": "‹path›",
    "digest": "‹digest›"
  }
}
```

The server answers:

```json
{
  "version": 1,
  "kind": "response",
  "id": "c:1",
  "result": {
    "path": "‹path›",
    "digest": "‹digest›"
  }
}
```

Or refuses with `contract_invalid`:

```json
{
  "version": 1,
  "kind": "response",
  "id": "c:1",
  "error": {
    "code": "contract_invalid",
    "message": "The declaration identity is malformed."
  }
}
```

Or refuses with `contract_mismatch`:

```json
{
  "version": 1,
  "kind": "response",
  "id": "c:1",
  "error": {
    "code": "contract_mismatch",
    "message": "The declared path or specified digest differs from the expected identity."
  }
}
```

## Client side

The client implements these methods, which the server calls, and emits these events.

| Method | Request | Result | Errors | Description |
|---|---|---|---|---|
| `identity.check` | `Declaration` | `Declaration` | `contract_invalid`, `contract_mismatch` | Compares the caller's expected declaration identity before reverse model interpretation and answers with the serving identity. |

### `identity.check` on the wire

The server sends:

```json
{
  "version": 1,
  "kind": "request",
  "id": "s:1",
  "method": "identity.check",
  "params": {
    "path": "‹path›",
    "digest": "‹digest›"
  }
}
```

The client answers:

```json
{
  "version": 1,
  "kind": "response",
  "id": "s:1",
  "result": {
    "path": "‹path›",
    "digest": "‹digest›"
  }
}
```

Or refuses with `contract_invalid`:

```json
{
  "version": 1,
  "kind": "response",
  "id": "s:1",
  "error": {
    "code": "contract_invalid",
    "message": "The declaration identity is malformed."
  }
}
```

Or refuses with `contract_mismatch`:

```json
{
  "version": 1,
  "kind": "response",
  "id": "s:1",
  "error": {
    "code": "contract_mismatch",
    "message": "The declared path or specified digest differs from the expected identity."
  }
}
```

## Errors

The public errors of the family, by code: what a call may fail with.

| Code | Description |
|---|---|
| `contract_invalid` | The declaration identity is malformed. |
| `contract_mismatch` | The declared path or specified digest differs from the expected identity. |
